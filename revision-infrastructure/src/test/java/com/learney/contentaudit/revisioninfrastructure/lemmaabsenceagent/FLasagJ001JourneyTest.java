package com.learney.contentaudit.revisioninfrastructure.lemmaabsenceagent;

import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.anyMap;
import static org.mockito.ArgumentMatchers.anyString;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

import com.learney.contentaudit.auditdomain.CefrLevel;
import com.learney.contentaudit.refinerdomain.LemmaAbsenceCorrectionContext;
import com.learney.contentaudit.refinerdomain.LengthDirection;
import com.learney.contentaudit.refinerdomain.SuggestedLemmaQuerySessionFactory;
import com.learney.contentaudit.revisiondomain.lemmaabsence.LemmaAbsenceGeneratorResponse;
import com.learney.contentaudit.revisioninfrastructure.lagen.LagenConfig;
import com.sentinel.agents.framework.state.RunState;
import com.sentinel.agents.framework.state.RunStatus;
import java.util.Collections;
import javax.annotation.processing.Generated;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.MethodOrderer;
import org.junit.jupiter.api.Order;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.TestMethodOrder;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel — journey test stubs"
)
@Tag("F-LASAG")
@Tag("F-LASAG-J001")
@TestMethodOrder(MethodOrderer.OrderAnnotation.class)
public class FLasagJ001JourneyTest {

    @Test
    @Order(1)
    @Tag("path-1")
    @DisplayName("path-1: Se prepara un CorrectionContext válid... → Se invoca el puerto QuizCandidateGene... → El candidato pasa el gate estructural... → Se devuelve exactamente un QuizCandid... [todos los evals bloqueantes (#1, #2, #3, #5) dan veredicto positivo] → success")
    public void path1_todosLosEvalsBloqueantes1235DanVeredictoPositivo_success() {
        // -----------------------------------------------------------------------
        // Step: preparar_contexto — se prepara un CorrectionContext válido de
        // FEAT-RCLA para un diagnóstico LEMMA_ABSENCE con suggestedWords a refinar
        // -----------------------------------------------------------------------
        LemmaAbsenceCorrectionContext context = new LemmaAbsenceCorrectionContext(
                "task-j001",
                "She goes to school every day.",
                "Ella va a la escuela todos los días.",
                "Daily Routines",
                "Fill in the blank with the correct verb.",
                "Everyday Activities",
                CefrLevel.A1,
                Collections.emptyList(),
                Collections.emptyList(),
                "She [[goes]] to school every day.",
                12,
                10,
                15,
                0,
                LengthDirection.KEEP_SAME,
                null, null, Collections.emptyList(), "quiz-node-001"
        );

        LagenConfig config = new LagenConfig(
                "test-provider", "test-model", "http://localhost:1234/v1",
                null, null, null, null, 5, 3
        );

        // -----------------------------------------------------------------------
        // Step: generar — el agente ejecuta su react corto consultando la tool de
        // suggested-lemmas (gate F-LASAG-R001, F-LASAG-R002).
        // El mock launcher devuelve COMPLETED: simulación de que el react terminó
        // con éxito y el candidato pasa el gate estructural de FEAT-LAPS
        // (gate F-LASAG-R007 queda fuera del límite del sistema: conducta del harness).
        // -----------------------------------------------------------------------
        RunState completedState = RunState.empty().withRunStatus(RunStatus.COMPLETED);
        AgentRuntimeLauncher launcher = mock(AgentRuntimeLauncher.class);
        when(launcher.launch(anyString(), anyMap(), any(), anyMap())).thenReturn(completedState);

        // -----------------------------------------------------------------------
        // Step: emitir — el parser extrae el QuizCandidate {quizSentence, translation}
        // del estado de run terminado; gate F-LASAG-R003.
        // -----------------------------------------------------------------------
        LemmaAbsenceGeneratorResponse expectedCandidate = new LemmaAbsenceGeneratorResponse(
                "She [[walks]] to school every day.",
                "Ella camina a la escuela todos los días.");
        AgentCandidateParser parser = mock(AgentCandidateParser.class);
        when(parser.parse(any(), anyString(), anyString())).thenReturn(expectedCandidate);

        AgentRuntimeErrorClassifier classifier = mock(AgentRuntimeErrorClassifier.class);
        SuggestedLemmaQuerySessionFactory sessionFactory = mock(SuggestedLemmaQuerySessionFactory.class);

        LemmaAbsenceAgentGenerator generator = new LemmaAbsenceAgentGenerator(
                config, launcher, sessionFactory, classifier, parser,
                null,   // ChatModel — null es seguro; el fake launcher nunca lo invoca
                "lemma-absence-llm"
        );

        // Act
        LemmaAbsenceGeneratorResponse result = generator.generate(context);

        // -----------------------------------------------------------------------
        // Assert — outcome success del journey J001:
        // se devuelve exactamente un QuizCandidate con quizSentence y translation no nulos
        // (F-LASAG-R003)
        // -----------------------------------------------------------------------
        assertNotNull(result,
                "J001 success: generate() debe devolver un QuizCandidate no nulo (F-LASAG-R003)");
        assertNotNull(result.getQuizSentence(),
                "J001 success: quizSentence debe estar presente en el QuizCandidate (F-LASAG-R003)");
        assertNotNull(result.getTranslation(),
                "J001 success: translation debe estar presente en el QuizCandidate (F-LASAG-R003)");
    }
}
