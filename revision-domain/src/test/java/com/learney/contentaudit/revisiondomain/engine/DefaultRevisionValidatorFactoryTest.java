package com.learney.contentaudit.revisiondomain.engine;

import static org.junit.jupiter.api.Assertions.assertInstanceOf;

import com.learney.contentaudit.revisiondomain.ApprovalMode;
import com.learney.contentaudit.revisiondomain.RevisionValidator;
import com.learney.contentaudit.revisiondomain.RevisionValidatorFactory;
import javax.annotation.processing.Generated;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
public class DefaultRevisionValidatorFactoryTest {
    @Test
    @DisplayName("Given ApprovalMode.AUTO, when create is called, then it returns an AutoApproveValidator instance (exactly one validator active for the invocation)")
    @Tag("FEAT-REVAPR")
    @Tag("F-REVAPR-R006")
    public void givenApprovalModeAUTOWhenCreateIsCalledThenItReturnsAnAutoApproveValidatorInstanceExactlyOneValidatorActiveForTheInvocation(
            ) {
        // Arrange
        RevisionValidatorFactory factory = new DefaultRevisionValidatorFactory();

        // Act
        RevisionValidator result = factory.create(ApprovalMode.AUTO);

        // Assert — exactly one validator active: the AutoApproveValidator (F-REVAPR-R006)
        assertInstanceOf(AutoApproveValidator.class, result);
    }

    @Test
    @DisplayName("Given ApprovalMode.HUMAN, when create is called, then it returns a HumanApprovalValidator instance (exactly one validator active for the invocation)")
    @Tag("FEAT-REVAPR")
    @Tag("F-REVAPR-R006")
    public void givenApprovalModeHUMANWhenCreateIsCalledThenItReturnsAHumanApprovalValidatorInstanceExactlyOneValidatorActiveForTheInvocation(
            ) {
        // Arrange
        RevisionValidatorFactory factory = new DefaultRevisionValidatorFactory();

        // Act
        RevisionValidator result = factory.create(ApprovalMode.HUMAN);

        // Assert — exactly one validator active: the HumanApprovalValidator (F-REVAPR-R006)
        assertInstanceOf(HumanApprovalValidator.class, result);
    }
}
