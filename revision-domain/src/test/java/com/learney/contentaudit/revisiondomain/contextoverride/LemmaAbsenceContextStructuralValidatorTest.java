package com.learney.contentaudit.revisiondomain.contextoverride;

import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.learney.contentaudit.revisiondomain.OverrideRejectedException;
import javax.annotation.processing.Generated;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertThrows;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
public class LemmaAbsenceContextStructuralValidatorTest {

    private LemmaAbsenceContextStructuralValidator sut;
    private ObjectMapper objectMapper;

    @BeforeEach
    void setUp() {
        objectMapper = new ObjectMapper();
        sut = new LemmaAbsenceContextStructuralValidator(objectMapper);
    }

    @Test
    @DisplayName("should throw OverrideRejectedException when the LEMMA_ABSENCE payload misses a required structural field or has an invalid value")
    @Tag("FEAT-REVCTX")
    @Tag("F-REVCTX-R003")
    public void shouldThrowOverrideRejectedExceptionWhenTheLEMMAABSENCEPayloadMissesARequiredStructuralFieldOrHasAnInvalidValue(
            ) {
        // R003: validez estructural — required fields must be present and of correct type.
        // A LEMMA_ABSENCE context requires at minimum: taskId, sentence, translation,
        // knowledgeTitle, cefrLevel, misplacedLemmas, suggestedLemmas.
        // Providing a payload that omits required fields must throw OverrideRejectedException.
        String incompleteJson = """
                {
                  "sentence": "She runs every day.",
                  "translation": "Ella corre todos los dias."
                }
                """;
        // Missing taskId, cefrLevel, misplacedLemmas, suggestedLemmas, etc.
        assertThrows(OverrideRejectedException.class, () -> {
            JsonNode payload = objectMapper.readTree(incompleteJson);
            sut.validateAndBuild(payload, "quiz-001");
        }, "OverrideRejectedException must be thrown when required LEMMA_ABSENCE fields are missing");
    }
}
