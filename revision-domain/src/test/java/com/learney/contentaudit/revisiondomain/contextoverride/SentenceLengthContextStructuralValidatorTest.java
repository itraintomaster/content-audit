package com.learney.contentaudit.revisiondomain.contextoverride;

import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.learney.contentaudit.revisiondomain.OverrideRejectedException;
import javax.annotation.processing.Generated;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertThrows;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
public class SentenceLengthContextStructuralValidatorTest {

    private SentenceLengthContextStructuralValidator sut;
    private ObjectMapper objectMapper;

    @BeforeEach
    void setUp() {
        objectMapper = new ObjectMapper();
        sut = new SentenceLengthContextStructuralValidator(objectMapper);
    }

    @Test
    @DisplayName("should throw OverrideRejectedException when the SENTENCE_LENGTH payload misses a required structural field or has an out-of-domain value")
    @Tag("FEAT-REVCTX")
    @Tag("F-REVCTX-R003")
    public void shouldThrowOverrideRejectedExceptionWhenTheSENTENCELENGTHPayloadMissesARequiredStructuralFieldOrHasAnOutofdomainValue(
            ) {
        // R003: validez estructural — required fields must be present with correct types and
        // values within domain. SENTENCE_LENGTH requires: taskId, sentence, tokenCount (integer),
        // targetMin, targetMax, delta. An out-of-domain value (e.g. tokenCount as a string)
        // or a missing required field must throw OverrideRejectedException.
        String invalidJson = """
                {
                  "sentence": "She runs every day.",
                  "translation": "Ella corre todos los dias.",
                  "tokenCount": "not-a-number"
                }
                """;
        // Missing taskId, cefrLevel; tokenCount is not a valid integer — out-of-domain.
        assertThrows(OverrideRejectedException.class, () -> {
            JsonNode payload = objectMapper.readTree(invalidJson);
            sut.validateAndBuild(payload, "quiz-002");
        }, "OverrideRejectedException must be thrown when SENTENCE_LENGTH required fields are missing or have out-of-domain values");
    }
}
