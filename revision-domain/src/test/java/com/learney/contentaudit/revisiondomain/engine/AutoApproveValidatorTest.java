package com.learney.contentaudit.revisiondomain.engine;

import com.learney.contentaudit.auditdomain.AuditTarget;
import com.learney.contentaudit.refinerdomain.DiagnosisKind;
import com.learney.contentaudit.revisiondomain.RevisionProposal;
import com.learney.contentaudit.revisiondomain.RevisionValidatorResult;
import com.learney.contentaudit.revisiondomain.RevisionVerdict;
import java.time.Instant;
import java.util.Optional;
import javax.annotation.processing.Generated;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
public class AutoApproveValidatorTest {

    private RevisionProposal anyProposal() {
        RevisionProposal proposal = new RevisionProposal();
        proposal.setProposalId("task-001-20260418T000000Z");
        proposal.setTaskId("task-001");
        proposal.setPlanId("plan-001");
        proposal.setSourceAuditId("audit-001");
        proposal.setDiagnosisKind(DiagnosisKind.LEMMA_ABSENCE);
        proposal.setNodeTarget(AuditTarget.QUIZ);
        proposal.setNodeId("quiz-42");
        proposal.setElementBefore(null);
        proposal.setElementAfter(null);
        proposal.setRationale("bypass: identity revision");
        proposal.setReviserKind("bypass");
        proposal.setCreatedAt(Instant.parse("2026-04-18T00:00:00Z"));
        return proposal;
    }

    @Test
    @DisplayName("Given any RevisionProposal, when validate is called, then it returns a RevisionValidatorResult with verdict APPROVED")
    @Tag("FEAT-REVBYP")
    @Tag("F-REVBYP-R007")
    public void givenAnyRevisionProposalWhenValidateIsCalledThenItReturnsARevisionValidatorResultWithVerdictAPPROVED(
            ) {
        // Arrange
        AutoApproveValidator validator = new AutoApproveValidator();
        RevisionProposal proposal = anyProposal();

        // Act
        RevisionValidatorResult result = validator.validate(proposal);

        // Assert — F-REVBYP-R007: the bypass validator always returns APPROVED
        assertEquals(RevisionVerdict.APPROVED, result.verdict());
    }

    @Test
    @DisplayName("Given an approved result, when rejectionReason is queried, then it returns Optional.empty")
    @Tag("FEAT-REVBYP")
    @Tag("F-REVBYP-R006")
    public void givenAnApprovedResultWhenRejectionReasonIsQueriedThenItReturnsOptionalempty() {
        // Arrange — F-REVBYP-R006: an APPROVED verdict carries no rejection reason
        AutoApproveValidator validator = new AutoApproveValidator();
        RevisionProposal proposal = anyProposal();

        // Act
        RevisionValidatorResult result = validator.validate(proposal);

        // Assert
        assertEquals(Optional.empty(), result.rejectionReason());
    }
}
