package com.learney.contentaudit.revisiondomain.engine;

import com.learney.contentaudit.auditdomain.AuditTarget;
import com.learney.contentaudit.refinerdomain.DiagnosisKind;
import com.learney.contentaudit.refinerdomain.RefinementTask;
import com.learney.contentaudit.refinerdomain.RefinementTaskStatus;
import com.learney.contentaudit.revisiondomain.CourseElementSnapshot;
import com.learney.contentaudit.revisiondomain.RevisionProposal;
import javax.annotation.processing.Generated;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
public class IdentityReviserTest {

    private IdentityReviser sut;

    @BeforeEach
    void setUp() {
        sut = new IdentityReviser();
    }

    /** Builds a minimal RefinementTask for the given DiagnosisKind. */
    private RefinementTask buildTask(DiagnosisKind kind) {
        return new RefinementTask(
                "task-bypass-1",
                AuditTarget.QUIZ,
                "quiz-001",
                "Test quiz node",
                kind,
                1,
                RefinementTaskStatus.PENDING);
    }

    /** Builds a minimal CourseElementSnapshot representing a QUIZ node. */
    private CourseElementSnapshot buildSnapshot() {
        return new CourseElementSnapshot(AuditTarget.QUIZ, "quiz-001", null);
    }

    @Test
    @DisplayName("Given a CorrectionContext, when propose is called, then it returns a RevisionProposal with all R001 fields populated")
    @Tag("FEAT-REVBYP")
    @Tag("F-REVBYP-R001")
    public void givenACorrectionContextWhenProposeIsCalledThenItReturnsARevisionProposalWithAllR001FieldsPopulated(
            ) {
        // Arrange — a task with SENTENCE_LENGTH diagnosis and a before snapshot
        RefinementTask task = buildTask(DiagnosisKind.SENTENCE_LENGTH);
        CourseElementSnapshot before = buildSnapshot();

        // Act
        RevisionProposal proposal = sut.propose(task, null, before);

        // Assert — R001: every field of RevisionProposal must be non-null (createdAt included)
        Assertions.assertNotNull(proposal, "propose() must return a non-null RevisionProposal");
        Assertions.assertNotNull(proposal.getProposalId(), "proposalId must be populated (R001)");
        Assertions.assertNotNull(proposal.getTaskId(), "taskId must be populated (R001)");
        Assertions.assertNotNull(proposal.getPlanId(), "planId must be populated (R001)");
        Assertions.assertNotNull(proposal.getSourceAuditId(), "sourceAuditId must be populated (R001)");
        Assertions.assertNotNull(proposal.getDiagnosisKind(), "diagnosisKind must be populated (R001)");
        Assertions.assertNotNull(proposal.getNodeTarget(), "nodeTarget must be populated (R001)");
        Assertions.assertNotNull(proposal.getNodeId(), "nodeId must be populated (R001)");
        Assertions.assertNotNull(proposal.getElementBefore(), "elementBefore must be populated (R001)");
        Assertions.assertNotNull(proposal.getElementAfter(), "elementAfter must be populated (R001)");
        Assertions.assertNotNull(proposal.getRationale(), "rationale must be populated (R001)");
        Assertions.assertNotNull(proposal.getReviserKind(), "reviserKind must be populated (R001)");
        Assertions.assertNotNull(proposal.getCreatedAt(), "createdAt must be populated (R001)");
    }

    @Test
    @DisplayName("Given any before snapshot, when propose is called, then elementAfter equals elementBefore")
    @Tag("FEAT-REVBYP")
    @Tag("F-REVBYP-R002")
    public void givenAnyBeforeSnapshotWhenProposeIsCalledThenElementAfterEqualsElementBefore() {
        // Arrange — bypass identity: the after snapshot must equal the before snapshot
        RefinementTask task = buildTask(DiagnosisKind.LEMMA_ABSENCE);
        CourseElementSnapshot before = buildSnapshot();

        // Act
        RevisionProposal proposal = sut.propose(task, null, before);

        // Assert — R002: elementAfter must be semantically equivalent to elementBefore
        Assertions.assertEquals(
                before,
                proposal.getElementAfter(),
                "elementAfter must equal elementBefore in the bypass (identity) reviser (R002)");
    }

    @Test
    @DisplayName("Given any DiagnosisKind, when handles is called, then it returns true")
    @Tag("FEAT-REVBYP")
    @Tag("F-REVBYP-R004")
    public void givenAnyDiagnosisKindWhenHandlesIsCalledThenItReturnsTrue() {
        // R004: the bypass Reviser must accept every DiagnosisKind (universal catch-all)
        for (DiagnosisKind kind : DiagnosisKind.values()) {
            Assertions.assertTrue(
                    sut.handles(kind),
                    "IdentityReviser must handle any DiagnosisKind, failed for: " + kind);
        }
    }

    @Test
    @DisplayName("Given a proposal generated by IdentityReviser, then rationale equals 'bypass: identity revision' and reviserKind equals 'bypass'")
    @Tag("FEAT-REVBYP")
    @Tag("F-REVBYP-R004")
    public void givenAProposalGeneratedByIdentityReviserThenRationaleEqualsBypassIdentityRevisionAndReviserKindEqualsBypass(
            ) {
        // Arrange
        RefinementTask task = buildTask(DiagnosisKind.SENTENCE_LENGTH);
        CourseElementSnapshot before = buildSnapshot();

        // Act
        RevisionProposal proposal = sut.propose(task, null, before);

        // Assert — R004: constant rationale and reviserKind for the bypass
        Assertions.assertEquals(
                "bypass: identity revision",
                proposal.getRationale(),
                "rationale must be 'bypass: identity revision' (R004)");
        Assertions.assertEquals(
                "bypass",
                proposal.getReviserKind(),
                "reviserKind must be 'bypass' (R004)");
    }
}
