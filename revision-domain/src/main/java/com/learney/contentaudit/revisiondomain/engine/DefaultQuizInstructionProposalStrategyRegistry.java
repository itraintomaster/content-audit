package com.learney.contentaudit.revisiondomain.engine;

import com.learney.contentaudit.revisiondomain.QuizInstructionProposalStrategy;
import com.learney.contentaudit.revisiondomain.QuizInstructionProposalStrategyRegistry;
import com.learney.contentaudit.revisiondomain.StrategyId;
import java.util.List;
import java.util.Optional;
import javax.annotation.processing.Generated;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
public final class DefaultQuizInstructionProposalStrategyRegistry implements QuizInstructionProposalStrategyRegistry {
    private final QuizInstructionProposalStrategyRegistryConfig config;

    public DefaultQuizInstructionProposalStrategyRegistry(
            QuizInstructionProposalStrategyRegistryConfig config) {
        this.config = config;
    }

    @Override
    public Optional<QuizInstructionProposalStrategy> active() {
        String activeName = config.getActiveStrategyName();
        if (activeName == null || activeName.isBlank()) {
            return Optional.empty();
        }
        List<QuizInstructionProposalStrategy> strategies = config.getStrategies();
        if (strategies == null) {
            return Optional.empty();
        }
        return strategies.stream()
                .filter(s -> activeName.equals(s.id().getName()))
                .findFirst();
    }

    @Override
    public Optional<QuizInstructionProposalStrategy> byName(String name) {
        if (name == null) {
            return Optional.empty();
        }
        List<QuizInstructionProposalStrategy> strategies = config.getStrategies();
        if (strategies == null) {
            return Optional.empty();
        }
        return strategies.stream()
                .filter(s -> name.equals(s.id().getName()))
                .findFirst();
    }

    @Override
    public List<StrategyId> listAll() {
        List<QuizInstructionProposalStrategy> strategies = config.getStrategies();
        if (strategies == null) {
            return List.of();
        }
        return strategies.stream()
                .map(QuizInstructionProposalStrategy::id)
                .toList();
    }
}
