package com.learney.contentaudit.revisiondomain.engine;

import com.learney.contentaudit.revisiondomain.KnowledgeTitleProposalStrategy;
import com.learney.contentaudit.revisiondomain.KnowledgeTitleProposalStrategyRegistry;
import com.learney.contentaudit.revisiondomain.StrategyId;
import java.util.List;
import java.util.Optional;
import javax.annotation.processing.Generated;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
public final class DefaultKnowledgeTitleProposalStrategyRegistry implements KnowledgeTitleProposalStrategyRegistry {
    private final KnowledgeTitleProposalStrategyRegistryConfig config;

    public DefaultKnowledgeTitleProposalStrategyRegistry(
            KnowledgeTitleProposalStrategyRegistryConfig config) {
        this.config = config;
    }

    @Override
    public Optional<KnowledgeTitleProposalStrategy> active() {
        String activeName = config.getActiveName();
        if (activeName == null || activeName.isBlank()) {
            return Optional.empty();
        }
        List<KnowledgeTitleProposalStrategy> strategies = config.getRegistered();
        if (strategies == null) {
            return Optional.empty();
        }
        return strategies.stream()
                .filter(s -> activeName.equals(s.id().getName()))
                .findFirst();
    }

    @Override
    public Optional<KnowledgeTitleProposalStrategy> byName(String name) {
        if (name == null) {
            return Optional.empty();
        }
        List<KnowledgeTitleProposalStrategy> strategies = config.getRegistered();
        if (strategies == null) {
            return Optional.empty();
        }
        return strategies.stream()
                .filter(s -> name.equals(s.id().getName()))
                .findFirst();
    }

    @Override
    public List<StrategyId> listAll() {
        List<KnowledgeTitleProposalStrategy> strategies = config.getRegistered();
        if (strategies == null) {
            return List.of();
        }
        return strategies.stream()
                .map(KnowledgeTitleProposalStrategy::id)
                .toList();
    }
}
