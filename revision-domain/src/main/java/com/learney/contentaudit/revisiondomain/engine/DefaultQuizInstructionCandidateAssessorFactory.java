package com.learney.contentaudit.revisiondomain.engine;
import com.learney.contentaudit.revisiondomain.quizinstruction.QuizInstructionCandidateAssessorConfig;

import com.learney.contentaudit.revisiondomain.CourseElementLocator;
import com.learney.contentaudit.revisiondomain.quizinstruction.QuizInstructionCandidateAssessor;
import com.learney.contentaudit.revisiondomain.quizinstruction.QuizInstructionCandidateAssessorFactory;
import javax.annotation.processing.Generated;

/**
 * F-QICOR-R015. Mirrors {@link DefaultRevisionEngineFactory}'s own handling of {@code
 * elementLocator}: {@link QuizInstructionCandidateAssessorConfig#getElementLocator()} is
 * nullable, and in the normal composition-root wiring it IS null -- {@link
 * DefaultCourseElementLocator} is package-private on purpose (the composition root never
 * constructs one directly), so this factory builds the default instance itself, from
 * inside the same package, exactly as {@code DefaultRevisionEngineFactory.create()} does
 * for {@link com.learney.contentaudit.revisiondomain.RevisionEngineConfig}.
 */
@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
public class DefaultQuizInstructionCandidateAssessorFactory implements QuizInstructionCandidateAssessorFactory {
    @Override
    public QuizInstructionCandidateAssessor create(QuizInstructionCandidateAssessorConfig config) {
        CourseElementLocator elementLocator = config.getElementLocator() != null
                ? config.getElementLocator()
                : new DefaultCourseElementLocator();
        return new DefaultQuizInstructionCandidateAssessor(
                config.getAssessor(),
                config.getDeriver(),
                config.getRefinementPlanStore(),
                config.getAuditReportStore(),
                config.getContextResolver(),
                config.getCourseRepository(),
                elementLocator);
    }
}
