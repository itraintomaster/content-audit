package com.learney.contentaudit.revisiondomain.impactpreview;

import java.util.Objects;
import javax.annotation.processing.Generated;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
public class ScoreDelta {
    private double before;

    private double after;

    private double difference;

    public ScoreDelta() {
    }

    public ScoreDelta(double before, double after, double difference) {
        this.before = before;
        this.after = after;
        this.difference = difference;
    }

    public double getBefore() {
        return this.before;
    }

    public void setBefore(double before) {
        this.before = before;
    }

    public double getAfter() {
        return this.after;
    }

    public void setAfter(double after) {
        this.after = after;
    }

    public double getDifference() {
        return this.difference;
    }

    public void setDifference(double difference) {
        this.difference = difference;
    }

    /** Record-style accessor alias for tests (F-PIPRE). */
    public double before() { return this.before; }
    /** Record-style accessor alias for tests (F-PIPRE). */
    public double after() { return this.after; }
    /** Record-style accessor alias for tests (F-PIPRE). */
    public double difference() { return this.difference; }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        ScoreDelta that = (ScoreDelta) o;
        return Objects.equals(this.before, that.before)
                    && Objects.equals(this.after, that.after)
                    && Objects.equals(this.difference, that.difference);
    }

    @Override
    public int hashCode() {
        return Objects.hash(before, after, difference);
    }
}
