package com.learney.contentaudit.revisiondomain.preservationengine;

import com.learney.contentaudit.auditdomain.AuditTarget;
import com.learney.contentaudit.refinerdomain.DiagnosisKind;
import com.learney.contentaudit.revisiondomain.preservation.CorrectionScope;
import com.learney.contentaudit.revisiondomain.preservation.ScopedField;
import java.util.Set;
import javax.annotation.processing.Generated;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
class DefaultCorrectionScope implements CorrectionScope {

    /**
     * F-LAPS-R013: a lexical correction of a quiz may change the plain sentence, the
     * translation, the composition of the statement (which parts exist and in what order)
     * and, within that composition, the fixed text of TEXT parts and the accepted answers
     * of CLOZE parts. Everything else — including the quiz title (F-RPRES-R004 / F-LAPS-R014),
     * the form type, incidence and presentation labels, and the empty text of a blank — stays
     * out of scope and therefore preserved by default (F-RPRES-R001).
     */
    private static final Set<ScopedField> LEMMA_ABSENCE_QUIZ_SCOPE = Set.of(
            new ScopedField("sentences", null),
            new ScopedField("translation", null),
            new ScopedField("form.sentenceParts[]", null),
            new ScopedField("form.sentenceParts[].text", "TEXT"),
            new ScopedField("form.sentenceParts[].options", "CLOZE")
    );

    /**
     * A knowledge label correction changes the knowledge's label and instructions, and — as
     * the sole exception to F-RPRES-R003 — the title of every quiz belonging to that
     * knowledge, kept aligned to the corrected label (F-RPRES-R004).
     */
    private static final Set<ScopedField> KNOWLEDGE_TITLE_LENGTH_KNOWLEDGE_SCOPE = Set.of(
            new ScopedField("label", null),
            new ScopedField("instructions", null),
            new ScopedField("quizTemplates[].title", null)
    );

    @Override
    public Set<ScopedField> changeableFields(DiagnosisKind kind, AuditTarget target) {
        if (kind == DiagnosisKind.LEMMA_ABSENCE && target == AuditTarget.QUIZ) {
            return LEMMA_ABSENCE_QUIZ_SCOPE;
        }
        if (kind == DiagnosisKind.KNOWLEDGE_TITLE_LENGTH && target == AuditTarget.KNOWLEDGE) {
            return KNOWLEDGE_TITLE_LENGTH_KNOWLEDGE_SCOPE;
        }
        // F-RPRES-R001: a DiagnosisKind/AuditTarget pair with no declared scope has an empty
        // complement — every attribute of the element stays preserved by default.
        return Set.of();
    }
}
