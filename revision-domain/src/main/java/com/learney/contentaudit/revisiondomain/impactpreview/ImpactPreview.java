package com.learney.contentaudit.revisiondomain.impactpreview;

import java.time.Instant;
import java.util.List;
import java.util.Objects;
import javax.annotation.processing.Generated;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
public class ImpactPreview {
    private String proposalId;

    private Instant computedAt;

    private ImpactPreviewAvailability availability;

    private ImpactPreviewUnavailability unavailability;

    private List<LevelImpact> levelImpacts;

    public ImpactPreview() {
    }

    public ImpactPreview(String proposalId, Instant computedAt,
            ImpactPreviewAvailability availability, ImpactPreviewUnavailability unavailability,
            List<LevelImpact> levelImpacts) {
        this.proposalId = proposalId;
        this.computedAt = computedAt;
        this.availability = availability;
        this.unavailability = unavailability;
        this.levelImpacts = levelImpacts;
    }

    public String getProposalId() {
        return this.proposalId;
    }

    public void setProposalId(String proposalId) {
        this.proposalId = proposalId;
    }

    public Instant getComputedAt() {
        return this.computedAt;
    }

    public void setComputedAt(Instant computedAt) {
        this.computedAt = computedAt;
    }

    public ImpactPreviewAvailability getAvailability() {
        return this.availability;
    }

    public void setAvailability(ImpactPreviewAvailability availability) {
        this.availability = availability;
    }

    public ImpactPreviewUnavailability getUnavailability() {
        return this.unavailability;
    }

    public void setUnavailability(ImpactPreviewUnavailability unavailability) {
        this.unavailability = unavailability;
    }

    public List<LevelImpact> getLevelImpacts() {
        return this.levelImpacts;
    }

    public void setLevelImpacts(List<LevelImpact> levelImpacts) {
        this.levelImpacts = levelImpacts;
    }

    /** Record-style accessor alias for tests (F-PIPRE). */
    public String proposalId() { return this.proposalId; }
    /** Record-style accessor alias for tests (F-PIPRE). */
    public ImpactPreviewAvailability availability() { return this.availability; }
    /** Record-style accessor alias for tests (F-PIPRE). */
    public ImpactPreviewUnavailability unavailability() { return this.unavailability; }
    /** Record-style accessor alias for tests (F-PIPRE). */
    public java.util.List<LevelImpact> levelImpacts() { return this.levelImpacts; }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        ImpactPreview that = (ImpactPreview) o;
        return Objects.equals(this.proposalId, that.proposalId)
                    && Objects.equals(this.computedAt, that.computedAt)
                    && Objects.equals(this.availability, that.availability)
                    && Objects.equals(this.unavailability, that.unavailability)
                    && Objects.equals(this.levelImpacts, that.levelImpacts);
    }

    @Override
    public int hashCode() {
        return Objects.hash(proposalId, computedAt, availability, unavailability, levelImpacts);
    }
}
