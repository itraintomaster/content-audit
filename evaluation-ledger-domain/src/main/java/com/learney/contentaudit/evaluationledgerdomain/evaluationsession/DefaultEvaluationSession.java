package com.learney.contentaudit.evaluationledgerdomain.evaluationsession;

import com.learney.contentaudit.evaluationledgerdomain.ContentFingerprinter;
import com.learney.contentaudit.evaluationledgerdomain.EvaluationBudget;
import com.learney.contentaudit.evaluationledgerdomain.EvaluationCoverage;
import com.learney.contentaudit.evaluationledgerdomain.EvaluationEmitted;
import com.learney.contentaudit.evaluationledgerdomain.EvaluationFailureKind;
import com.learney.contentaudit.evaluationledgerdomain.EvaluationKey;
import com.learney.contentaudit.evaluationledgerdomain.EvaluationLedger;
import com.learney.contentaudit.evaluationledgerdomain.EvaluationNotEmitted;
import com.learney.contentaudit.evaluationledgerdomain.EvaluationOutcome;
import com.learney.contentaudit.evaluationledgerdomain.EvaluationRecord;
import com.learney.contentaudit.evaluationledgerdomain.EvaluationResolution;
import com.learney.contentaudit.evaluationledgerdomain.EvaluationResolutionKind;
import com.learney.contentaudit.evaluationledgerdomain.EvaluationSession;
import com.learney.contentaudit.evaluationledgerdomain.EvaluationSubject;
import com.learney.contentaudit.evaluationledgerdomain.Evaluator;
import java.time.Instant;
import java.util.Map;
import java.util.Optional;
import javax.annotation.processing.Generated;

/**
 * Coordinates one analysis run over this evaluator: for each content, consult
 * the current registered result first (F-EVCOST-R003); if none applies,
 * evaluate within the run's new-evaluation budget (F-EVCOST-R003,
 * F-EVCOST-R006) and record the outcome immediately, before moving on to the
 * next content (F-EVCOST-R004).
 *
 * <p>The fingerprint is derived from the very same content {@link Map} that is
 * then handed to the {@link Evaluator} — never a second, independently built
 * representation — so that F-EVCOST-R002 ("exactly what the evaluator
 * receives, nothing more") holds by construction.
 *
 * <p>A failure attributable to the service ({@link
 * EvaluationFailureKind#EVALUATOR_UNAVAILABLE}) makes this session stop
 * consulting the evaluator for the rest of the run: every subsequent content
 * that needs a new evaluation is reported {@link
 * EvaluationResolutionKind#PENDING} without ever reaching {@link
 * Evaluator#evaluate(EvaluationSubject)} again (F-EVCOST-R009). A failure
 * attributable to a single content ({@link
 * EvaluationFailureKind#OUTPUT_UNUSABLE}) does not: the run keeps consulting
 * the evaluator for the remaining content.
 */
@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
class DefaultEvaluationSession implements EvaluationSession {
    private final EvaluationLedger ledger;

    private final ContentFingerprinter fingerprinter;

    private final Evaluator evaluator;

    private final EvaluationBudget budget;

    private int newEvaluationsUsed = 0;

    private boolean evaluatorUnavailable = false;

    private int reached = 0;

    private int evaluatedInRun = 0;

    private int reused = 0;

    private int pending = 0;

    private int failed = 0;

    public DefaultEvaluationSession(EvaluationLedger ledger, ContentFingerprinter fingerprinter,
            Evaluator evaluator, EvaluationBudget budget) {
        this.ledger = ledger;
        this.fingerprinter = fingerprinter;
        this.evaluator = evaluator;
        this.budget = budget;
    }

    @Override
    public EvaluationResolution resolve(EvaluationSubject subject) {
        return resolveInternal(subject, false);
    }

    @Override
    public EvaluationResolution resolveForced(EvaluationSubject subject) {
        return resolveInternal(subject, true);
    }

    @Override
    public EvaluationCoverage coverage() {
        int withResult = evaluatedInRun + reused;
        return new EvaluationCoverage(reached, withResult, evaluatedInRun, reused, pending, failed);
    }

    @Override
    public String evaluatorVersion() {
        return evaluator.evaluatorVersion();
    }

    private EvaluationResolution resolveInternal(EvaluationSubject subject, boolean forceReevaluation) {
        reached++;

        // R002: the fingerprint is derived from exactly the map that will be
        // handed to the evaluator below — the very same reference, not a
        // second representation built independently.
        Map<String, String> content = subject.getContent();
        String contentFingerprint = fingerprinter.fingerprint(content);
        EvaluationKey key = new EvaluationKey(
                evaluator.evaluatorId(), evaluator.evaluatorVersion(), contentFingerprint);

        // R003 + R006: the current, vigent result (same evaluator, same
        // version, same fingerprint) is consulted first. R008: it is only
        // bypassed when re-evaluation was explicitly requested.
        if (!forceReevaluation) {
            Optional<EvaluationRecord> current = ledger.find(key);
            if (current.isPresent()) {
                reused++;
                return new EvaluationResolution(EvaluationResolutionKind.REUSED, current.get().getPayload());
            }
        }

        // R009: once a service failure was observed, this session stops
        // consulting the evaluator for the rest of the run; everything that
        // still needs a new evaluation is left pending.
        if (evaluatorUnavailable) {
            pending++;
            return new EvaluationResolution(EvaluationResolutionKind.PENDING, null);
        }

        // R003: the budget bounds only NEW evaluations; reuse above never
        // consumes it.
        if (newEvaluationsUsed >= budget.getMaxNewEvaluations()) {
            pending++;
            return new EvaluationResolution(EvaluationResolutionKind.PENDING, null);
        }
        newEvaluationsUsed++;

        EvaluationOutcome outcome = evaluator.evaluate(subject);

        if (outcome instanceof EvaluationEmitted emitted) {
            // R004: recorded immediately, before resolving the next content.
            // R005: only what the evaluator emitted is registered.
            EvaluationRecord record = new EvaluationRecord(
                    key, emitted.getPayload(), subject.getSubjectRef(), Instant.now());
            ledger.append(record);
            evaluatedInRun++;
            return new EvaluationResolution(EvaluationResolutionKind.EVALUATED, emitted.getPayload());
        }

        if (outcome instanceof EvaluationNotEmitted notEmitted) {
            // R005: a transient failure is never registered; the content
            // stays pending for a future run.
            failed++;
            if (notEmitted.getKind() == EvaluationFailureKind.EVALUATOR_UNAVAILABLE) {
                // R009: the failure is attributable to the service — stop
                // consulting it for the remainder of this run.
                evaluatorUnavailable = true;
            }
            return new EvaluationResolution(EvaluationResolutionKind.FAILED, null);
        }

        throw new IllegalStateException(
                "Unknown EvaluationOutcome implementation: " + outcome.getClass().getName());
    }
}
