# Progress

2026-04-19 — analyst — initial draft of FEAT-CLIRV (kubectl-style restructure of content-audit CLI). Encodes pre-agreed verbs (get/delete/prune + domain verbs analyze/plan/revise/config), 4 resources (audits/plans/tasks/analyzers), explicit removal of refiner/analyzer groups (no shims), workdir override rule for sandbox E2E. Five journeys + four open doubts.
2026-04-19 — analyst — all four open doubts resolved by user decisions and encoded as concrete rules. Touched R002 (no cascade + no delete task), R017 (--workdir flag + CONTENT_AUDIT_HOME env var with explicit precedence), R020 (clarified that stats survives the dissolution of the analyzer group), and added new R021 (stats as a top-level domain verb 'stats analyzer <name> [course]'). Updated verb list, command surface, "what is removed", and "out of scope" sections accordingly. Pruned ASSUMPTIONS #6 (analyzer stats removal) and replaced the open-questions section with a resolution summary. Re-validated: OK.
2026-04-19 — architect — architectural patch and tech spec landed. architectural_patch.yaml + TECH_SPEC.md + sentinel-baseline.yaml present in requirement folder. Validation: 16 adds / 1 modify / 16 deletes / 0 conflicts; 9 tech-spec fences all subset of patch. Ready for the user to register FEAT-CLIRV in sentinel.yaml's features: list and apply the patch via Architect Studio (or `sentinel patch apply`). After apply, tests can be designed by @qa-tester and impl by the dev agent.
2026-04-19 — analyst — surgical revision of R015 + ASSUMPTION #4 to match architect's revised ReviseCommand(taskId, planId) signature. Default = latest plan (mirrors R012); --plan <id> disambiguates; three failure messages spelled out. ASSUMPTION #4 collapsed into a resolution statement. J001 untouched. Validation: OK.

2026-04-19 — developer — FEAT-CLIRV implementation complete. All 8 new commands implemented (DefaultWorkdirResolver, GetCmd, DeleteCmd, PruneCmd, PlanCmd, ReviseCmd, ConfigAnalyzerCmd, StatsAnalyzerCmd). Main.java rewritten with flat command tree. Stale Refiner* files deleted. StoreHelper added for filesystem-level delete/list ops. mvn compile + test pass (BUILD SUCCESS, 0 failures).
2026-04-19 — test-writer — All 5 journey test files implemented (J001-J005). Fixture course created at audit-cli/src/test/resources/fixtures/tiny-course/english-course/ (1 milestone, 1 topic, 1 knowledge, 2 quizzes). All 10 path tests compile and run: 9 pass, 1 skipped (@Disabled J005 path-3). Developer's real .content-audit/ not polluted (did not exist before or after tests).
2026-04-19 — analyst — extended R008 to add `--target` / `-t` and `--diagnosis` / `-d` filter flags (preserved from the deleted `refiner next` command but accidentally dropped). Updated REQUIREMENT.md table, added a combined-filter example, and added two error templates. Mirrored the change in sentinel.yaml's F-CLIRV-R008 description + errorMessage. Rule count unchanged (FEAT-CLIRV still 21/5; F-CLIRV-R008 occurrences in sentinel.yaml still 8). Validation: OK.
  why: kubectl-style migration was meant to rename, not lose, existing filters; surfacing them as additional flags on `get tasks` keeps the prior operator workflow intact under the new grammar.
2026-04-19 — developer — implemented --target/-t and --diagnosis/-d filter flags in GetCmd.java. Added @Option fields, 6-arg GetTasksFilter constructor call in call(), and two filter steps in handleTasks() (before sort/limit). All 5 CLIRV journey tests pass; all 6 smoke tests pass (exit 0 on valid, exit 2 on invalid enum). GetTasksFilter @Generated contract untouched.
2026-04-19 — test-writer — implemented all 6 DefaultWorkdirResolverTest stubs (R017 x5, R018 x1). Results: 5 pass, 1 @Disabled (env-mutation test — no SystemStubs dep; covered by FClirvJ005JourneyTest). No AssertJ in module — used JUnit 5 Assertions. WorkdirResolver.resolve() throws InvalidWorkdirException (RuntimeException) — no checked exception in interface signature.
2026-04-19 — test-writer — implemented all 20 GetCmdTest stubs. Results: 20 pass, 0 failures, 0 errors, 0 skipped. See fix-log for the formatName injection pattern needed to avoid NPE when constructing GetCmd directly.
2026-04-19 — test-writer — implemented all 10 DeleteCmdTest stubs (R002 x6, R010 x1, R005 x1, R006 x1, R019 x1) and all 8 PruneCmdTest stubs (R003 x5, R005 x1, R006 x1, R011 x1). Results: 10 + 8 = 18 tests, 18 pass, 0 failures, 0 skips. Both use @TempDir + setBaseDir for filesystem-level operations; R010 and pure-validation tests use mocked RefinementPlanStore.
2026-04-19 — test-writer — implemented ConfigAnalyzerCmdTest (2 tests, R016), StatsAnalyzerCmdTest (4 tests, R021), ContentAuditCmdTest (2 tests, R020). Results: 7 pass, 1 @Disabled (StatsAnalyzerCmd env-var fallback — no SystemStubs dep). LENIENT strictness on StatsAnalyzerCmdTest because impl uses listAnalyzers() not getAnalyzerConfig() for the registry check.
2026-04-19 — test-writer — implemented PlanCmdTest (5 tests: 4 R014 + 1 R004) and ReviseCmdTest (6 tests: all R015). Results: 11/11 pass, 0 failures, 0 skipped. See fix-log for the @BeforeEach reflection pattern used to initialize picocli @Option fields that @InjectMocks leaves null (formatName in PlanCmd; coursePath in ReviseCmd).
2026-04-19 — developer — restored correctionContext enrichment lost by FEAT-CLIRV. Architectural patch added correctionContextResolver to GetCmd.requiresInject; sentinel generate regenerated imports; developer added field + constructor param + all formatting helpers. Main.java updated to pass the dep. GetCmdTest.java setUp updated to pass 4th arg (mock CorrectionContextResolver). All 99 tests pass. All 3 smoke tests pass (SENTENCE_LENGTH and LEMMA_ABSENCE show correctionContext, COCA_BUCKETS does not).
