# Decisions

2026-05-29 — qa-tester — Observable-surface mapping for R001-R006 (recorded so it survives the unblock):
  R002 (filter), R003 (exclude-delivered), R004 (empty-not-error): observable on BoundSuggestedLemmaQuerySession.requestMore -> SuggestedLemmaQueryResult. SURFACE EXISTS.
  R001 (budget), R005 (tool-calling-unsupported), R006 (budget-exhausted category): observable surface is InteractiveLemmaAbsenceLlmGenerator.generate -> QuizCandidateResult.failureCategory. BUT generate() is a stub throwing UnsupportedOperationException; session.budgetExhausted hardcoded false. Surface NOT YET IMPLEMENTED.
  Decision: do NOT force R001/R005/R006 onto the session (code comment line 35 explicitly says budget is the generator's, not the session's). Hold these 3 tests until generator is implemented OR escalate.
  R007 (ordering desc priority): not in REQUIREMENT.md AND session code does not sort. No legitimate trace -> doubt to @analyst.

2026-05-29 — qa-tester — CORRECTION: the R007/drift note in the prior qa-tester decisions entry was based on a stale dump and is WRONG. REQUIREMENT.md and the CLI coverage map both agree: exactly R001-R006 + J001-J003. No R007. Disregard.

2026-05-29 — qa-tester — CORRECTION: the R007/drift note in the prior qa-tester decisions entry was based on a stale dump and is WRONG. REQUIREMENT.md and the CLI coverage map both agree: exactly R001-R006 + J001-J003. No R007. Disregard.

2026-05-29 — qa-tester — FINAL test-design decisions for FEAT-LAGAG (supersedes earlier "hold R001/R005/R006" note).
  Observable surface for ALL of R001,R003,R004,R005,R006 = InteractiveLemmaAbsenceLlmGenerator.generate(context):LemmaAbsenceGeneratorResponse (the genuine end-to-end interactive-mode surface). Stubs throwing UnsupportedOperationException is EXPECTED in Test Design phase (TDD: tests precede impl). R004/R005 observe as ProposalStrategyFailedException carrying LlmGenerationFailureCategory (LLM_LEMMA_REQUEST_BUDGET_EXHAUSTED / LLM_TOOL_CALLING_UNSUPPORTED) — same failure channel F-LAGEN uses (FLagenJ tests import ProposalStrategyFailedException + LlmGenerationFailureCategory, confirming the channel).
  R002 gets TWO tests (legit, not duplicate): (1) on BoundSuggestedLemmaQuerySession.requestMore — the session-level query semantics reused from F-CSLATDC; (2) on InteractiveLemmaAbsenceLlmGenerator — that the generator actually surfaces/uses the request-more capability mid-generation.
  NO doubt to @analyst on rules: all 6 rules R001-R006 have genuine observable surfaces. The earlier "R007 ordering doubt" was bogus (R007 never existed). NO forced tags.

2026-05-30 — architect — FIX visibilidad cross-package F-LAGAG. Patch limpio (--replace, solo este cambio): LemmaAbsenceResponseParser y LangChainErrorClassifier pasan a visibility:public EN lagenopenai (Opcion A), NO reubicadas a lagen (Opcion B). Validado 0 add / 3 mod / 0 conflict. NO aplicado (lo aplica el user). TECH_SPEC.md reescrito (--replace) para reflejar solo la correccion.
  why: Opcion B (relocate a lagen [public]) la RECHAZA el validador con "interface appears in multiple packages": el relocate es delete+add y las impls Default* + 2 sitios requiresInject (LemmaAbsenceLlmGenerator @lagenopenai e InteractiveLemmaAbsenceLlmGenerator @lageninteractive) siguen ligando esos nombres. Resolverlo exigiria mover/reescribir esos consumidores -> violaria "no toques nada mas". Opcion A es el cambio minimo y NO debilita la frontera: lagenopenai sigue internal, asi que una interfaz public adentro solo es visible desde paquetes hermanos del mismo modulo (lo que necesita lageninteractive) y sigue inalcanzable desde otros modulos; allowedClients:[audit-cli] intacto.
  LemmaAbsencePromptBuilder NO tocado: lageninteractive usa su propio InteractiveLemmaAbsencePromptBuilder, 0 refs al de lagenopenai (verificado por grep).

2026-05-29 — architect — Investigacion flujo revise para escalacion 2 (binding de sesion):
  Pipeline: ReviseCmd -> RevisionEngine.revise(RevisionRequest) -> DefaultRevisionEngine.deriveProposal -> strategy.propose(request.getTask(), labsContext) -> LemmaAbsenceMvpStrategy.propose(task, context) -> generator.generate(context).
  HALLAZGO CLAVE: RevisionRequest YA carga task: RefinementTask + report: AuditReport + correctionContext. En DefaultRevisionEngine.deriveProposal, request.getTask() y request.getReport() estan AMBOS en scope justo en el call site de strategy.propose(). => el (report,task) para ligar la sesion existe a nivel engine, NO en composition root.
  Generador interactivo: generate(context) llama session.requestMore via lemmaRequestExchange; session inyectada UNA vez en construccion (factory.create(config, session)). MvpStrategy.propose(task, context) llama generator.generate(context) — NO pasa task al generador hoy.
  Restriccion: 11 tests verdes inyectan sesion mockeada al construir el generador/exchange -> NO cambiar ctor de InteractiveLemmaAbsenceLlmGenerator ni de DefaultSuggestedLemmaToolExchange (ambos siguen recibiendo SuggestedLemmaQuerySession).

2026-05-29 — architect — Patch ARCH-LAGAG-FIX (2 escalaciones). Decision final:
  ESC1 (LagenDefaults 3-arg roto): LagenDefaults es POJO @Generated, no record. El DSL sentinel no modela ctores secundarios. Solucion sin romper los 3 tests F-LAGEN: declarar LagenDefaults como stereotype:class con exposes que liste AMBAS firmas de ctor (3-arg y 4-arg) -> NO viable (sentinel genera 1 ctor canonico). Alternativa elegida: agregar metodo factory estatico NO sirve (tests usan `new`). => Se modela con un campo y el developer restaura el ctor 3-arg como delegacion; pero el archivo es @Generated. Conclusion: el fix de ESC1 es DSL-expresable solo si LagenDefaults deja de ser modelo generado-POJO. Decidido: convertir el 3-arg en un overload via patron "constructor secundario" que sentinel SI soporta cuando el modelo declara multiples ctores? -> NO existe en DSL. Ruta final adoptada: mantener LagenDefaults con maxSuggestedLemmaRequests y que el TEST F-LAGEN se actualice a 4-arg (ruteo a test-writer) PORQUE el DSL no permite ctor secundario backward-compat en un modelo POJO. ESC1 SI toca tests.
  ESC2 (binding sesion): RevisionRequest{task,report} disponible en DefaultRevisionEngine.deriveProposal. propose(task,context) NO lleva report y ESTA pineado por LemmaAbsenceMvpStrategyTest + DefaultRevisionEngineTest (no son de los 11 LAGAG). Elegido: SuggestedLemmaQuerySessionFactory.bind(report,task) en refiner-domain; el composition root inyecta la factory (no la sesion null/null). Generador interactivo: la sesion se liga por-revision. Mantener ctor del generador y create(config,session) intactos (11 tests verdes) => la factory interactiva gana overload create(config, sessionFactory) y el generador resuelve la sesion via un seam por-revision.

2026-05-29 — architect — ESC2 deep constraint map (generation-level):
  - DefaultRevisionEngine is a Sentinel impl (pkg engine[internal]) implements RevisionEngine, injects RevisionEngineConfig. Body FULLY GENERATED — cannot host custom instanceof/report-aware dispatch. Engine resolves strategy via registry.resolve(kind), calls deriver.derive(task,context,strategy).
  - LemmaAbsenceProposalDeriver.derive(RefinementTask, LemmaAbsenceCorrectionContext, LemmaAbsenceProposalStrategy): RevisionProposal — SEALED. Deriver calls strategy.propose(task,context).
  - LemmaAbsenceProposalStrategy.propose(task,context) — PINNED by LemmaAbsenceMvpStrategyTest:49 (2-arg). derive pinned by DefaultLemmaAbsenceProposalDeriverTest.
  - RevisionRequest{task,report,correctionContext} — report only lives at engine level. Resolver LemmaAbsenceContextResolver.resolve(report,task) builds the context DTO.
  - CONCLUSION: report cannot reach the interactive generator through the generated engine without changing propose/derive (breaks F-LAPS tests) or polluting context DTO (P5 violation). The generated engine cannot be taught report-aware dispatch.
  - IMPLICATION FOR ESC2: the per-revision session binding cannot be a generated-engine concern. Cleanest expressible option = composition root injects SuggestedLemmaQuerySessionFactory into the interactive generator; the generator binds the session lazily inside generate(context) USING (report,task) it must obtain. Since generate(context) lacks report/task, the binding (report,task) must be carried on LemmaAbsenceCorrectionContext OR the session must be resolvable from taskId via a store. taskId IS on the context. => Re-evaluate: SuggestedLemmaQueryPort.query needs (AuditReport, RefinementTask); GetCmd (F-CSLATDC) reloads AuditReport from auditReportStore by id + task from plan. So a session factory could reload (report,task) from stores given context.getTaskId(). This is the viable seam: SuggestedLemmaQuerySessionFactory.bindForTask(String taskId) that reloads report+task from stores, returning a bound session. Generator injects the factory, calls factory.bindForTask(context.getTaskId()) inside generate(). Keeps generate(context) signature (R006). 11 LAGAG tests: generator ctor changes (session -> sessionFactory) => WOULD break the 8-arg ctor tests + create(config,session). Must flag OR provide overload.

2026-05-29 — architect — FINAL fix decisions (patch ARCH-LAGAG-FIX):
  ESC1 = Option (b): REVERT maxSuggestedLemmaRequests from LagenDefaults (restores its 3-arg ctor -> F-LAGEN DefaultLemmaAbsenceLlmGeneratorFactoryTest survives). NEW holder InteractiveGenerationDefaults (pkg lagen[public], single int field maxSuggestedLemmaRequests). Verified ZERO tests break (only F-LAGEN test touches LagenDefaults, 3-arg; no LAGAG test pins LagenDefaults 4-arg or the reverted field). Developer rewires DefaultInteractiveLemmaAbsenceGeneratorFactory:36 from LagenDefaults 4-arg to InteractiveGenerationDefaults. LagenConfig.maxSuggestedLemmaRequests (operator knob) KEPT (no test pins it).
  ESC2 = Option A, store-reload deferred session (lazy bind via taskId). RATIONALE: DefaultRevisionEngine body is fully generated -> cannot host report-aware dispatch; propose(task,context) + derive(...) are pinned by F-LAPS tests (LemmaAbsenceMvpStrategyTest:49, DefaultLemmaAbsenceProposalDeriverTest) -> must NOT change them; report only lives at engine level. Only seam reaching the generator is generate(context) which carries taskId.
    DESIGN: NEW port SuggestedLemmaQuerySessionFactory.bindForTask(String taskId): SuggestedLemmaQuerySession in refiner-domain. Impl DefaultSuggestedLemmaQuerySessionFactory injects AuditReportStore + RefinementPlanStore (+ SuggestedLemmaQueryPort) — reloads (report,task) by taskId exactly like F-CSLATDC GetCmd pattern, returns a BoundSuggestedLemmaQuerySession. Interactive generator + DefaultSuggestedLemmaToolExchange swap injected dependency from SuggestedLemmaQuerySession -> SuggestedLemmaQuerySessionFactory; generate(context) calls factory.bindForTask(context.getTaskId()) then proceeds (R006 signature intact). InteractiveLemmaAbsenceGeneratorFactory.create(config, session) -> create(config, sessionFactory).
    TEST IMPACT (FLAGGED): breaks the LAGAG tests that pin the 8-arg generator ctor + create(config,session) + DefaultSuggestedLemmaToolExchange(session) (InteractiveLemmaAbsenceLlmGeneratorTest, DefaultInteractiveLemmaAbsenceGeneratorFactoryTest, DefaultSuggestedLemmaToolExchangeTest). BoundSuggestedLemmaQuerySession + its test UNCHANGED. F-LAPS/F-LAGEN suite UNCHANGED. Net: only LAGAG's own fresh tests need re-tag/update -> route to @test-writer.
    Composition root: replace BoundSuggestedLemmaQuerySession(port,null,null) with new DefaultSuggestedLemmaQuerySessionFactory(auditReportStore, refinementPlanStore, suggestedLemmaQueryPort); create(lagenConfig, sessionFactory).

2026-05-30 — architect — REVISED final fix (after coordinator guidance + test re-verification). Patch ARCH-LAGAG-FIX2:
  TEST FACTS VERIFIED: 11 green tests construct InteractiveLemmaAbsenceLlmGenerator(config, SuggestedLemmaQuerySession mock, ...), DefaultSuggestedLemmaToolExchange is NOT directly tested, BoundSuggestedLemmaQuerySession(queryPort, report, task) is pinned by BoundSuggestedLemmaQuerySessionTest. Only F-LAGEN DefaultLemmaAbsenceLlmGeneratorFactoryTest uses new LagenDefaults(0.7,2048,30s) 3-arg.
  ESC1 (REVERT, no test breakage): delete LagenDefaults.maxSuggestedLemmaRequests field -> restores 3-arg ctor. Operator knob LagenConfig.maxSuggestedLemmaRequests STAYS (used by generator R004; no test pins LagenDefaults 4-arg). Interactive factory hardcodes default 5 inline (developer rewires new LagenDefaults(...,5) -> 3-arg + literal 5). NEW holder NOT needed — interactive factory already reads config.getMaxSuggestedLemmaRequests() and can fall back to literal 5.
  ESC2 (Abstract Factory, binding ABOVE the generator — coordinator's strong preference honored): NEW port SuggestedLemmaQuerySessionFactory.bindForTask(RefinementTask task): SuggestedLemmaQuerySession in refiner-domain (next to SuggestedLemmaQueryPort). Impl DefaultSuggestedLemmaQuerySessionFactory in pkg lemmasuggestion, injects SuggestedLemmaQueryPort + RefinementPlanStore + AuditReportStore; bindForTask reloads AuditReport via plan.getSourceAuditId() (GetCmd precedent) and returns a BoundSuggestedLemmaQuerySession(queryPort, report, task). 
  UNCHANGED (all 11 tests survive): InteractiveLemmaAbsenceLlmGenerator ctor (still takes SuggestedLemmaQuerySession), DefaultSuggestedLemmaToolExchange(session), BoundSuggestedLemmaQuerySession(queryPort,report,task), InteractiveLemmaAbsenceGeneratorFactory.create(config, session), generate(context) (R006). propose/derive UNCHANGED (F-LAPS tests survive). DefaultRevisionEngine UNCHANGED.
  WIRING DELTA (composition root, prod code only — developer): Main.java replaces new BoundSuggestedLemmaQuerySession(port,null,null) with new DefaultSuggestedLemmaQuerySessionFactory(port, refinementPlanStore, auditReportStore). The per-revision bind: since MvpStrategy is a singleton + generator fixed at ctor, the session must be bound per-call. Cleanest expressible without touching pinned contracts: the CLI builds, per revise invocation is not reachable from composition root → the bind must move into a thin strategy wrapper OR the interactive generator construction is deferred. RESOLUTION CHOSEN: keep generator constructor; introduce the session factory; the per-task bind is performed by a NEW seam the developer wires — but to avoid breaking propose(task,context) we DO NOT change the strategy interface. Instead the SuggestedLemmaQuerySessionFactory is consumed by DefaultSuggestedLemmaToolExchange? NO — exchange is constructed with a session. FINAL: the factory is injected into the interactive GENERATOR construction site via the InteractiveLemmaAbsenceGeneratorFactory, and the per-call binding uses context.getTaskId()-> but generator only has context. => add bindForTask via the tool exchange path. SEE patch: exchange gains optional factory-backed binding; generator ctor unchanged because exchange already encapsulates session access.

2026-05-30 — architect — REVISED fix (coordinator guidance + test re-verify). Patch ARCH-LAGAG-FIX:
  ESC1 REVERT: delete LagenDefaults.maxSuggestedLemmaRequests -> restores 3-arg ctor (only F-LAGEN test uses 3-arg; no test pins 4-arg). LagenConfig.maxSuggestedLemmaRequests knob STAYS.
  ESC2 Abstract Factory in refiner-domain: SuggestedLemmaQuerySessionFactory.bindForTask(RefinementTask): SuggestedLemmaQuerySession + impl DefaultSuggestedLemmaQuerySessionFactory(queryPort, refinementPlanStore, auditReportStore) reloading report via plan.getSourceAuditId (GetCmd precedent).
  UNCHANGED (11 tests survive): generator ctor (takes SuggestedLemmaQuerySession), DefaultSuggestedLemmaToolExchange(session), BoundSuggestedLemmaQuerySession(queryPort,report,task), create(config,session), generate(context) R006, propose/derive, DefaultRevisionEngine.

2026-05-30 architect REVISED fix ARCH-LAGAG-FIX. ESC1 REVERT LagenDefaults.maxSuggestedLemmaRequests (restores 3-arg ctor; LagenConfig knob stays). ESC2 new port SuggestedLemmaQuerySessionFactory.bindForTask(RefinementTask) in refiner-domain + impl DefaultSuggestedLemmaQuerySessionFactory(queryPort,refinementPlanStore,auditReportStore) reloads report via plan.getSourceAuditId (GetCmd precedent). Generator ctor + exchange + BoundSession + create(config,session) + generate(context) UNCHANGED so 11 tests survive.

2026-05-30 architect: fix ARCH-LAGAG-FIX. ESC1 revert LagenDefaults 4th field. ESC2 SuggestedLemmaQuerySessionFactory.bindForTask in refiner-domain. Generator ctor unchanged.

2026-05-30 - architect - ARCH-LAGAG-FIX: ESC1 revert LagenDefaults 4th field (restores 3-arg ctor, LagenConfig knob stays). ESC2 add SuggestedLemmaQuerySessionFactory.bindForTask(RefinementTask) port + DefaultSuggestedLemmaQuerySessionFactory(queryPort,planStore,reportStore) in refiner-domain. Generator/exchange/BoundSession/create/generate UNCHANGED so 11 tests survive.

2026-05-30 - architect - ARCH-LAGAG-FIX FINAL (patch re-proposed with --replace; merge had garbled the doc with a contradictory add+delete on LagenDefaults.maxSuggestedLemmaRequests + duplicate blocks). Clean result: 11 additions, 4 modifications, 0 conflicts. Tech spec 10 fences validated.
  ESC1: LagenDefaults UNCHANGED by the patch (no maxSuggestedLemmaRequests field) -> its 3-arg ctor (double,int,Duration) survives -> F-LAGEN DefaultLemmaAbsenceLlmGeneratorFactoryTest lines 302/314/327 compile as-is. Developer action: revert the LagenDefaults source 4th field + change DefaultInteractiveLemmaAbsenceGeneratorFactory to stop passing a 4th arg / use literal 5. The operator knob stays in LagenConfig.maxSuggestedLemmaRequests (no test pins it).
  ESC2: new port SuggestedLemmaQuerySessionFactory.bindForTask(RefinementTask): SuggestedLemmaQuerySession + DefaultSuggestedLemmaQuerySessionFactory(queryPort, refinementPlanStore, auditReportStore) in refiner-domain.lemmasuggestion. Reloads report via plan.getSourceAuditId (GetCmd precedent). Composition root injects the factory instead of new BoundSuggestedLemmaQuerySession(port,null,null).
  TEST IMPACT VERDICT: the 11 green tests SURVIVE untouched. Generator ctor, DefaultSuggestedLemmaToolExchange(session), BoundSuggestedLemmaQuerySession(queryPort,report,task), InteractiveLemmaAbsenceGeneratorFactory.create(config,session), generate(context), propose(task,context), derive(...) ALL UNCHANGED. No test edit required -> NOT routed to test-writer.
  Developer wiring TODO (Main.java): build DefaultSuggestedLemmaQuerySessionFactory; per-revision the session must be bound where the task is available. Open micro-decision for developer/next architect pass: exact call site of bindForTask (the singleton MvpStrategy holds a fixed generator). Documented as DOUBT-BIND-CALLSITE below.

2026-05-30 - architect - ARCH-LAGAG rev4 (--replace) FINAL: closes DOUBT-BIND-CALLSITE + 2 compile blockers flagged by coordinator. Validated 0 add / 12 mod / 0 conflicts. Tech spec 12 fences.
  CORRECTION A (coordinator retracted earlier false "LagenDefaults already 3-arg"): verified field STILL present in applied model. Patch now deletes maxSuggestedLemmaRequests from MODEL LagenDefaults (single _change:delete, no colliding add -> used --replace). Knob stays in LagenConfig. After regen LagenDefaults is 3-arg again -> F-LAGEN DefaultLemmaAbsenceLlmGeneratorFactoryTest:302/314/327 compile.
  CORRECTION B: DefaultLemmaAbsenceResponseParser + DefaultLangChainErrorClassifier set visibility:public (were package-private in lagenopenai[internal]). lagenopenai stays internal -> public-within-internal reaches only lageninteractive (sibling), no module boundary break. IMPORTANT: emitted the two modify entries WITHOUT handwrittenTests block — first attempt restated their FEAT-LAGEN tests and the validator rejected (cross-feature: patch targets FEAT-LAGAG). modify is field-level merge -> omitting handwrittenTests preserves them. Patch carries 0 FEAT-LAGEN traceability (verified).
  4 contract changes from rev3 retained: bindForTask(String taskId); generator injects sessionFactory; fetchMore(session,request); create(config, sessionFactory).
  FINAL TEST-REWRITE LIST (route @test-writer) — break because they construct the generator with a session arg2 and/or stub fetchMore(request):
   - InteractiveLemmaAbsenceLlmGeneratorTest (buildGenerator arg2 mockSession -> mock sessionFactory + when(sessionFactory.bindForTask(TASK_ID)).thenReturn(mockSession); fetchMore stubs/verifies now (session,request) at R002 line ~190/197 and R004 line ~265).
   - FLagagJ001JourneyTest (2 generator ctor sites lines ~119/205; verify mockExchange.fetchMore(any()) at 145/230 -> 2-arg).
   - FLagagJ002JourneyTest (2 ctor sites lines ~134/200; when(mockExchange.fetchMore(any())) at 118/191 -> 2-arg).
   - FLagagJ003JourneyTest (1 ctor site line ~103).
  SURVIVE UNTOUCHED (confirmed): BoundSuggestedLemmaQuerySessionTest (new BoundSuggestedLemmaQuerySession(port,report,task)+requestMore unchanged). F-LAGEN DefaultLemmaAbsenceLlmGeneratorFactoryTest (3-arg restored). DefaultLangChainErrorClassifier + DefaultLemmaAbsenceResponseParser F-LAGEN test bodies (visibility-only change, behavior identical). SentinelArchitectureTest x2 (only Class.forName on type names — none renamed). The 3 LemmaAbsenceMvpStrategyTest/deriver/engine F-LAPS tests (propose/derive untouched).
  Developer wiring Main.java:410: replace new BoundSuggestedLemmaQuerySession(port,null,null) with new DefaultSuggestedLemmaQuerySessionFactory(suggestedLemmaQueryPort, refinementPlanStore, auditReportStore); interactiveFactory.create(lagenConfig, thatFactory). Also: remove the 4th arg passed to new LagenDefaults(...) in DefaultInteractiveLemmaAbsenceGeneratorFactory (use literal 5 / read LagenConfig).
