# Progress — FEAT-EVCOST

2026-07-29 — analyst — REQUIREMENT.md creado: 8 reglas, 3 journeys de flujo (J001 reuso/invalidacion, J002 interrupcion y falla transitoria, J003 independencia de los informes y re-evaluacion) y 3 doubts abiertos. `requirement validate` [OK]; `feature list` = FEAT-EVCOST 8 reglas / 3 journeys.
  why: capacidad general extraida del primer consumidor (FEAT-QINST, requirements/2026-07-29.02_validacion-de-consigna-de-quiz) por pedido explicito del usuario: el patron se repite con cualquier evaluador costoso basado en juicio.

2026-07-29 — analyst — Proximo paso: @architect. Las dudas abiertas (presupuesto general, retencion de historia, concurrencia) no bloquean el diseño; la unica observable a corto plazo es DOUBT-CONCURRENCIA (con la opcion A, dos corridas en paralelo pueden dejar el registro inutilizable).

2026-07-30 — qa-tester — Cobertura propuesta (architectural_patch.yaml en esta carpeta, SIN aplicar): 19 handwrittenTests que cubren las 8 reglas + placement de las 3 journeys (J001 evaluation-ledger-domain/.evaluationsession, J002 evaluation-ledger-infrastructure, J003 audit-cli/com.learney.contentaudit.journeys). `patch propose` valido: 6 modifications, 0 conflicts. Proximo paso: el usuario aplica, se desbloquea `sentinel generate`, y pasa a @test-writer.
  why: el placement de las 3 journeys era lo que bloqueaba `sentinel generate` ("has flow but no testModule").

2026-07-30 — analyst — FEAT-EVCOST pasa a 9 reglas / 3 journeys. Agregada R009 (distincion falla-del-contenido vs falla-del-servicio, con corte de consultas ante la segunda y sin abortar el analisis); J002 gana un cuarto outcome/terminal (falla_del_servicio); Alcance y Referencias actualizados. `requirement validate` [OK]; `feature list` = 9/3.
  why: cierra el hueco (a) que @qa-tester escalo por no tener regla que lo respalde. Proximos pasos: (1) `sentinel feature sync` porque cambio una journey, (2) @qa-tester agrega el handwrittenTest de R009 y revisa el testModule de J002 (la rama nueva se observa en la sesion, no en el store append-line).

2026-07-30 — qa-tester — Patch RE-PROPUESTO con --replace tras F-EVCOST-R009 (feature pasa a 9 reglas) y el 4to path de J002: 21 handwrittenTests (las 9 reglas cubiertas) + el mismo placement de journeys. Sigue SIN aplicar. `patch propose` valido: 6 modifications, 0 conflicts.

2026-07-31 — test-writer — Implementados los 19 handwrittenTests/paths de evaluation-ledger-domain (Sha256ContentFingerprinterTest 2, DefaultEvaluationSessionTest 13, FEvcostJ001JourneyTest 4 paths). `JAVA_HOME=openjdk-25 mvn -o test -pl evaluation-ledger-domain`: 21 tests corridos (19 + 2 de SentinelArchitectureTest), todos rojos con `UnsupportedOperationException: Not implemented yet` de la produccion (Sha256ContentFingerprinter/DefaultEvaluationSession) — esperado en TDD, cero errores de compilacion. Sin escalaciones.
  why: DefaultEvaluationSession/Factory se cablearon directo (constructor por orden de requiresInject via `describeComponent`: ledger, fingerprinter, evaluator, policy para la sesion; ledger, fingerprinter para la factory) sin leer su codigo; los tests usan una fake InMemoryLedger (duplicada en ambos archivos de test, nested classes no se comparten entre clases top-level) para modelar "corrida siguiente" como una segunda instancia de sesion sobre el mismo estado.

2026-07-31 — test-writer — 10 stubs de evaluation-ledger-infrastructure implementados y verdes en compilacion (rojos en ejecucion por UnsupportedOperationException, esperado): FileSystemEvaluationLedgerTest (6, R004 x3 + R006 + R007 + R008) y FEvcostJ002JourneyTest (4 paths). `mvn test-compile` y `mvn test` con JAVA_HOME=openjdk-25 confirmados: 10/10 fallan exactamente en el seam de produccion sin implementar (FileSystemEvaluationLedger.append/find, DefaultEvaluationSessionFactory.open). Sin escalaciones.
  why: R004 mitad dificil se probo truncando bytes reales del archivo (ver fix-log); J002 path-4 nuevo se distingue de path-2 por longitud de invokedSubjectRefs (se corta vs sigue) y por PENDING (nunca consultado) vs FAILED (consultado, sin veredicto) en EvaluationResolutionKind.

2026-07-31 — test-writer — Implementados los 2 paths de `FEvcostJ003JourneyTest` (audit-cli, R007+R008): usa `FileSystemAuditReportStore` + `FileSystemEvaluationLedger` + `Sha256ContentFingerprinter` reales sobre el MISMO `@TempDir`, con `Evaluator` mockeado (unico limite externo). path-1 (no pidio re-evaluar): guarda un informe + un veredicto, borra fisicamente `.content-audit/audits/` (mismo patron que el test homologo de FileSystemEvaluationLedgerTest para R007), abre una sesion nueva y verifica REUSED + evaluator nunca consultado. path-2 (pidio re-evaluar): mismo setup, pero `EvaluationRunPolicy(..., reevaluate=true, null)` en la segunda sesion; verifica EVALUATED + evaluator SI consultado + `ledger.history(...)` contiene AMBOS payloads (antes y despues). Compila; ambos quedan rojos por `UnsupportedOperationException` en `DefaultEvaluationSessionFactory.open` (produccion de evaluation-ledger-domain sin implementar aun) — TDD esperado. `mvn test -pl audit-cli`: 360 tests (352 baseline verdes + 8 nuevos de esta tanda), sin regresiones.
  why: "eliminar informes" no tiene metodo en AuditReportStore (solo save/load/loadLatest/list) — se modela como borrado fisico del directorio `.content-audit/audits/`, igual que ya lo hizo el test homologo a nivel infraestructura; reusar ese patron evita inventar un mecanismo de borrado que el contrato no expone.

2026-07-31 — developer — Produccion de `evaluation-ledger-domain` IMPLEMENTADA: `Sha256ContentFingerprinter`, `DefaultEvaluationSession`, `DefaultEvaluationSessionFactory`. `JAVA_HOME=openjdk-25 mvn -o test -pl evaluation-ledger-domain`: 21/21 verde (19 handwritten+journey + 2 SentinelArchitectureTest), sin tocar ningun test. `mvn -o install -DskipTests -Dexec.skip=true` completo (16 modulos) verde. Sin escalaciones.
  why: el requerimiento y los tests ya cubrian cada borde; no hizo falta reinterpretar nada.

2026-07-31 — developer — `FileSystemEvaluationLedger` (evaluation-ledger-infrastructure) IMPLEMENTADO: un archivo `.jsonl` por evaluador bajo `.content-audit/evaluations/`, una linea JSON por entrada (Jackson plano, sin pretty-print, escapa saltos de linea reales asi que un registro nunca ocupa mas de una linea fisica), append via `FileChannel` con `StandardOpenOption.APPEND` (atomico por escritura en filesystems locales) y lectura permisiva byte a byte que descarta cualquier linea que no parsea como JSON completo. `JAVA_HOME=openjdk-25 mvn -o test -pl evaluation-ledger-infrastructure`: 12/12 verde a la primera (6 FileSystemEvaluationLedgerTest + 4 paths FEvcostJ002JourneyTest + 2 SentinelArchitectureTest), sin tocar ningun test. `mvn -o install -DskipTests -Dexec.skip=true` (16 modulos) verde.
  why: los tests ya dictaban el contrato completo (truncar bytes reales, 2 threads con CyclicBarrier); no hizo falta reinterpretar nada.

2026-07-31 — developer — Al correr `mvn -o test -pl audit-cli` (no skip) aparecio 1 falla real en `FEvcostJ003JourneyTest.path2` (re-evaluacion explicita): `secondRun.resolve(subject)` con `EvaluationRunPolicy(10, true, null)` devuelve REUSED en vez de EVALUATED. Causa: `DefaultEvaluationSession.resolveInternal` (evaluation-ledger-domain) nunca lee `policy.isReevaluate()` ni `policy.getReevaluationScope()` — solo el parametro interno `forceReevaluation` (que unicamente `resolveForced()` pone en true) decide el bypass del reuso. El test J003 (ya escrito, no mio) asume que `resolve()` con `policy.reevaluate=true` fuerza la re-evaluacion. Bug de evaluation-ledger-domain, NO de evaluation-ledger-infrastructure — fuera de mi alcance de modulo, no lo toque. Los otros 6 errores del mismo run (AnalyzeCmdTest, QuizInstructionDetailedFormatterTest) son de FEAT-QINST (no implementada aun), ajenos y preexistentes.
  why: dejar constancia para que el proximo developer/architect de evaluation-ledger-domain sepa exactamente donde esta el hueco antes de tocar ese modulo.

2026-07-30 — developer — ARCH-002 (migracion del runtime de grafos, patch separado en esta carpeta segun TECH_SPEC) IMPLEMENTADA. `DefaultAgentGraphRunner` (agent-runtime-infrastructure/graphexecution) absorbe el algoritmo de los dos lanzadores borrados con la cadena de resolucion de 3 pasos (config -> inputs["project_root"] -> CWD). `SharedRuntimeAgentLauncher` y `SharedRuntimeKnowledgeTitleLauncher` delegan en `AgentGraphRunner`; las dos factories (`DefaultLemmaAbsenceAgentGeneratorFactory`, `DefaultKnowledgeTitleAgentGeneratorFactory`) construyen un `AgentGraphRunner` compartido via `DefaultAgentGraphRunnerFactory().create(new AgentGraphRunnerConfig())` (ambos campos null -> cadena de resolucion activa) y apuntan a los lanzadores nuevos. `mvn -o install -DskipTests -Dexec.skip=true -pl agent-runtime-infrastructure,revision-infrastructure -am` compila verde. Ningun requiresInject ni firma de generator cambio; ningun test tocado.
  why: no habia red de tests que ejercitara la implementacion (los handwrittenTests de FEAT-LASAG/FEAT-KTLR mockean el lanzador); el codigo de referencia de git (commit 06068094) mostro que lemma-absence resuelve relativo al CWD y nunca puebla project_root, y knowledge-title resuelve relativo a project_root — confirmado leyendo KnowledgeTitleAgentGenerator.java:93, que SIEMPRE pone project_root en el input map. Con config vacia en ambas factories, cada consumidor cae exactamente en el escalon de la cadena que tenia antes.

2026-07-31 — developer — Primer consumidor real de FEAT-EVCOST cerrado: `quiz-instruction-infrastructure` (el adaptador `Evaluator` de FEAT-QINST que corre el agente `quiz-instruction-validator` sobre `AgentGraphRunner`) implementado y verde, 13/13 (detalle en la memoria de FEAT-QINST). Confirma en produccion real (no solo en los mocks de `DefaultEvaluationSessionTest`) las dos reglas que este consumidor le pedia a EVCOST: R002 (huella = contenido juzgado, `subject.getContent()` viaja verbatim al grafo, nunca se toca) y R009 (falla-del-contenido vs falla-del-servicio: `EVALUATOR_UNAVAILABLE` cuando el juez/runtime se cae, `OUTPUT_UNUSABLE` cuando el propio juez declara su salida invalida -- ninguna de las dos corta el analisis, ambas dejan el ejercicio pendiente para la corrida siguiente via `EvaluationSession`).
  why: cierra el circuito end-to-end del patron que motivo separar EVCOST de QINST; el patron queda validado por un segundo caso real (no solo el diseño) antes de que aparezca el tercer evaluador costoso que EVCOST anticipa.

2026-07-31 — developer — Fix quirurgico en `evaluation-ledger-domain`: `DefaultEvaluationSessionFactory.open(policy, evaluator)` ahora lanza `IllegalArgumentException` si `policy == null`, ANTES de construir la sesion. Motivo: `resolveInternal` hacia NPE sobre `policy.getMaxNewEvaluations()` con policy null y sin reuso posible; como `DefaultAuditRunner` envuelve cada analizador en `catch (RuntimeException)` (F-QINST-R007, no debe abortar la auditoria por una falla puntual), la NPE quedaba tragada y el sintoma visible era "0 evaluaciones" indistinguible de "nada para evaluar". No se toco `resolveInternal` ni se sustituyo ningun default (esa decision ya vive en `DefaultQuizInstructionAnalyzerFactory`, audit-domain, verde). No se toco el gap de `policy.isReevaluate()`/`reevaluationScope()` que @architect esta evaluando en paralelo en el mismo modulo — quedan claramente separados. `mvn -o test -pl evaluation-ledger-domain` (JAVA_HOME=openjdk-25): 21/21 verde, ningun test tocado. `mvn -o install -DskipTests -Dexec.skip=true -pl evaluation-ledger-domain`: BUILD SUCCESS. Sin escalaciones.
  why: fail-fast en el punto de apertura (open) en vez de en el fondo de resolveInternal, para que ningun catch por-nodo pueda tragarla y el mensaje señale a quien abrio mal la sesion.

2026-07-31 — analyst — Cerrado el pendiente (c) de @architect: F-EVCOST-R008 aclara que el alcance de la re-evaluacion lo acota el consumidor y que la capacidad re-evalua el contenido puntual pedido y ningun otro. Aclaracion, no cambio de decision. `requirement validate` = [OK]; `feature status FEAT-EVCOST` = OK, no hace falta `feature sync`.
  why: siguiente paso: @qa-tester evalua si el criterio (b) ampliado (el resto vigente se reutiliza en la misma corrida) necesita un handwrittenTest propio sobre la sesion.

2026-08-01 — developer — Cerrando audit-cli (ultimo modulo de FEAT-QINST/FEAT-EVCOST, ver memoria de FEAT-QINST para el detalle completo): confirmado que `FEvcostJ003JourneyTest.path2` sigue rojo por el mismo bug ya documentado el 2026-07-31 (`policy.isReevaluate()` no lo lee `DefaultEvaluationSession.resolveInternal`, solo `resolveForced()` fuerza). NO se toco (es exactamente el "hueco 3" que @architect esta evaluando en paralelo, mencionado explicitamente como fuera de alcance en el pedido). 359/360 verde en audit-cli; el unico rojo es este.
  why: dejar constancia de que el estado no cambio, para que quien resuelva el hueco 3 sepa que este es el (unico) test que se destraba con el fix.

2026-08-01 — developer — Patch de arquitectura aplicado (hueco 3 resuelto): `EvaluationRunPolicy` migro a `audit-domain` y desaparecio de este modulo; nace `EvaluationBudget` (solo `maxNewEvaluations`) como parametro de `EvaluationSessionFactory.open(...)`, y `EvaluationSession` gana `evaluatorVersion(): String`. Adapte produccion: `DefaultEvaluationSession`/`DefaultEvaluationSessionFactory` pasan de `policy: EvaluationRunPolicy` a `budget: EvaluationBudget` (campo + constructor + `newEvaluationsUsed >= budget.getMaxNewEvaluations()`); `evaluatorVersion()` delega en `evaluator.evaluatorVersion()` (misma fuente que ya arma cada `EvaluationKey`, sin duplicar version). Se mantuvo intacta la validacion fail-fast en `open()` (ahora `budget == null`) y no se toco `resolveForced` ni el corte por servicio caido (R009). `mvn -o compile -Dexec.skip=true -pl evaluation-ledger-domain`: BUILD SUCCESS. `mvn -o install -DskipTests -Dexec.skip=true -pl evaluation-ledger-domain`: falla solo en test-compile (DefaultEvaluationSessionTest/FEvcostJ001JourneyTest referencian el `EvaluationRunPolicy` viejo) — esperado, no se toco ningun test. Sin escalaciones.
  why: el hueco quedaba en que la sesion recibia campos (`reevaluate`/`reevaluationScope`) que nunca leia (el corte fino de reevaluacion ya vivia en `resolveForced`, decidido por el consumidor); separar en `EvaluationBudget` deja la sesion sin campos muertos.

2026-08-01 — test-writer — Churn mecanico post-patch (hueco 3) en 5 de los 7 modulos afectados, todos verdes y sin tocar ninguna aserción/fixture/@Tag: `evaluation-ledger-domain` (`DefaultEvaluationSessionTest` 13 sitios + `FEvcostJ001JourneyTest` 5 sitios: `new EvaluationRunPolicy(n,false,null)` → `new EvaluationBudget(n)`; 21/21), `evaluation-ledger-infrastructure` (`FEvcostJ002JourneyTest`: helper `openSession`/`generousPolicy` retipado a `EvaluationBudget`; 12/12), `audit-domain` (solo el import `EvaluationRunPolicy` re-apuntado a `auditdomain` en `QuizInstructionAnalyzerTest`/`FQinstJ001JourneyTest`/`FQinstJ002JourneyTest` -- el tipo no cambio de forma, solo de paquete, asi que los helpers `normalPolicy`/`reevaluatePolicy` no se tocaron; `DefaultQuizInstructionAnalyzerFactoryTest` con el `ArgumentCaptor` retipado a `EvaluationBudget`; 409/409), `audit-application` (`DefaultAuditRunnerTest`: 7x `evaluatorId()`→`analyzerName()` + import `EvaluationRunPolicy` removido porque ya llegaba por el wildcard `auditdomain.*`; **BLOQUEADO** para correr standalone porque `FQinstJ003JourneyTest` en el mismo modulo no compila, ver mas abajo), `quiz-instruction-infrastructure` (`DefaultQuizInstructionJudgeVersionResolverTest`: constructor gano `AgentDefinitionLocator`, mockeado con `locate(anyString())` devolviendo `AgentDefinitionFound(Path.of(".sentinel/agents/"+agentName))` -- preserva el mismo digest sobre los mismos directorios reales que el test ya asumia; `DefaultQuizInstructionJudgeFactoryTest`: `create(...)` gano el 3er parametro, mockeado sin stubear -- el path bajo prueba nunca lo toca; 13/13).
  why: ver fix-log para el detalle de por que se mockeo el locator apuntando a las carpetas reales de `.sentinel/agents/` en vez de un directorio ficticio.

2026-08-01 — test-writer — `AnalyzeCmdTest` (audit-cli) tambien resuelto: import `EvaluationRunPolicy` re-apuntado a `auditdomain` + 6x `getEvaluationPolicies()`→`getAnalyzerPolicies()`. Confirmado por `mvn test-compile -pl audit-cli` (sin `-am`, usando el jar de audit-application instalado con tests skippeados) que los UNICOS errores restantes del modulo son los 5 sitios de `FEvcostJ003JourneyTest.java` (el archivo que no se toca) -- `AnalyzeCmdTest` en si compila limpio.

2026-08-01 — test-writer — BLOQUEO reportado, sin tocar los 2 archivos (regla explicita del usuario): `FQinstJ003JourneyTest` (audit-application) y `FEvcostJ003JourneyTest` (audit-cli) no son churn mecanico, requieren re-diseño real, y su falta de compilacion tumba TODO el test-compile de sus modulos (no solo sus propios 4+2 paths) -- confirmado corriendo `mvn test-compile` en ambos. Detalle: `FQinstJ003JourneyTest` construye una `ContentAnalyzer` fake que llama `sessionFactory.open(policy, evaluator)` con `policy` de tipo `EvaluationRunPolicy` (para leer `policy.isReevaluate()` y elegir `resolve()` vs `resolveForced()`) -- convertir eso a `EvaluationBudget` en el sitio de `open()` es mecanico y análogo a lo que ya hace `DefaultQuizInstructionAnalyzerFactory` en produccion, PERO no lo hice porque el usuario pidio explicitamente no tocar este archivo. `FEvcostJ003JourneyTest.path2` es mas grave: pasa `new EvaluationRunPolicy(10, true, null)` a `open()` y llama `session.resolve(subject)` (no `resolveForced`) esperando que la reevaluacion se dispare sola -- exactamente el bug ya documentado el 2026-07-31 (`resolveInternal` nunca lee `isReevaluate()`); con `EvaluationBudget` ese campo ya NI EXISTE en el tipo que recibe `open()`, asi que el test tal como esta ya no tiene traduccion mecanica posible: alguien tiene que decidir si ese path pasa a usar `resolveForced()` explicito (cambiando el diseño del fixture) o si migra a otro mecanismo. Reportado al usuario, en espera de instrucciones antes de tocar cualquiera de los dos.
  why: distinguir claramente "esto compilaria con el mismo patron de churn" (FQinstJ003) de "esto ya no tiene traduccion 1:1 posible con el nuevo contrato" (FEvcostJ003 path2), para que quien retome sepa que el segundo no es solo mas trabajo sino una decision de diseño real.

2026-08-01 — qa-tester — Propuesto 1 handwrittenTest para la aclaracion de F-EVCOST-R008 en DefaultEvaluationSession: el contenido VECINO con resultado vigente se sigue reutilizando en la misma corrida en la que se fuerza la re-evaluacion de otro (patch propio en requirements/2026-07-29.03.../architectural_patch.yaml, SIN aplicar; separado del de FEAT-QINST porque el validador rechaza reglas de otra carpeta en un patch requirement-scoped).
  why: el test existente de R008 solo cubre "se re-evalua solo con pedido explicito"; la mitad "y ningun otro" (resolveForced(A) no arrastra a B) no estaba cubierta. OJO al aplicar: DefaultEvaluationSessionTest ya tiene @Tag("F-EVCOST-R008"), asi que cae en el bug del generador que no materializa stubs nuevos en clases que ya tenian ese tag.

2026-08-01 — test-writer — RE-DISEÑADO `FEvcostJ003JourneyTest.path2` (el bloqueo del 2026-08-01 anterior, "ya no tiene traduccion mecanica posible"): en vez de `open(new EvaluationRunPolicy(10, true, null))` + `resolve(subject)` esperando que la re-evaluacion se dispare sola, ahora abre con `EvaluationBudget(10)` (sin flag de reevaluar -- ya no existe en el tipo) y llama `secondRun.resolveForced(subject)` directo, siguiendo la decision de arquitectura ya tomada (`decisions.md` 2026-07-31 "Hueco 3"): la capacidad general no decide re-evaluar, lo decide el consumidor, contenido por contenido. El resto del path (verificar EVALUATED con el payload nuevo, verificar que `evaluator.evaluate()` SI se invoco, y que `ledger.history(...)` contiene AMBOS payloads -- el de antes y el de la re-evaluacion) quedo intacto: eso es exactamente lo que F-EVCOST-R008 exige y lo que el path afirma en su `@DisplayName`. path-1 (mismo archivo) solo necesito el cambio mecanico `EvaluationRunPolicy(10,false,null)` → `EvaluationBudget(10)`, sin tocar su `resolve()`. `mvn -o test -pl audit-cli` (JAVA_HOME=openjdk-25): 360/360 verde (352 baseline + 8 nuevos de FEAT-QINST ya verdes + los 2 de este archivo). Ningun `@Tag`/`@DisplayName`/nombre de metodo tocado. Sin escalaciones -- el path sigue verificando F-EVCOST-R007+R008 tal cual los tagea.
  why: ver decisions.md 2026-07-31 (architect, "Hueco 3") -- `reevaluationScope` es vocabulario del consumidor (un id de knowledge) y de un `EvaluationSubject` la sesion solo conoce `subjectRef` (id de ejercicio): no podia aplicar ese alcance aunque el campo hubiera sobrevivido en `EvaluationBudget`. `resolveForced()` es la unica superficie del contrato (`EvaluationSession`) que expresa "el consumidor decide re-evaluar esto puntual", y ya la usa el otro journey (FQinstJ003, FEAT-QINST) para el mismo proposito.
