# Progress — FEAT-EVCOST

2026-07-29 — analyst — REQUIREMENT.md creado: 8 reglas, 3 journeys de flujo (J001 reuso/invalidacion, J002 interrupcion y falla transitoria, J003 independencia de los informes y re-evaluacion) y 3 doubts abiertos. `requirement validate` [OK]; `feature list` = FEAT-EVCOST 8 reglas / 3 journeys.
  why: capacidad general extraida del primer consumidor (FEAT-QINST, requirements/2026-07-29.02_validacion-de-consigna-de-quiz) por pedido explicito del usuario: el patron se repite con cualquier evaluador costoso basado en juicio.

2026-07-29 — analyst — Proximo paso: @architect. Las dudas abiertas (presupuesto general, retencion de historia, concurrencia) no bloquean el diseño; la unica observable a corto plazo es DOUBT-CONCURRENCIA (con la opcion A, dos corridas en paralelo pueden dejar el registro inutilizable).

2026-07-30 — qa-tester — Cobertura propuesta (architectural_patch.yaml en esta carpeta, SIN aplicar): 19 handwrittenTests que cubren las 8 reglas + placement de las 3 journeys (J001 evaluation-ledger-domain/.evaluationsession, J002 evaluation-ledger-infrastructure, J003 audit-cli/com.learney.contentaudit.journeys). `patch propose` valido: 6 modifications, 0 conflicts. Proximo paso: el usuario aplica, se desbloquea `sentinel generate`, y pasa a @test-writer.
  why: el placement de las 3 journeys era lo que bloqueaba `sentinel generate` ("has flow but no testModule").

2026-07-30 — analyst — FEAT-EVCOST pasa a 9 reglas / 3 journeys. Agregada R009 (distincion falla-del-contenido vs falla-del-servicio, con corte de consultas ante la segunda y sin abortar el analisis); J002 gana un cuarto outcome/terminal (falla_del_servicio); Alcance y Referencias actualizados. `requirement validate` [OK]; `feature list` = 9/3.
  why: cierra el hueco (a) que @qa-tester escalo por no tener regla que lo respalde. Proximos pasos: (1) `sentinel feature sync` porque cambio una journey, (2) @qa-tester agrega el handwrittenTest de R009 y revisa el testModule de J002 (la rama nueva se observa en la sesion, no en el store append-line).

2026-07-30 — qa-tester — Patch RE-PROPUESTO con --replace tras F-EVCOST-R009 (feature pasa a 9 reglas) y el 4to path de J002: 21 handwrittenTests (las 9 reglas cubiertas) + el mismo placement de journeys. Sigue SIN aplicar. `patch propose` valido: 6 modifications, 0 conflicts.

2026-07-31 — test-writer — Implementados los 19 handwrittenTests/paths de evaluation-ledger-domain (Sha256ContentFingerprinterTest 2, DefaultEvaluationSessionTest 13, FEvcostJ001JourneyTest 4 paths). `JAVA_HOME=openjdk-25 mvn -o test -pl evaluation-ledger-domain`: 21 tests corridos (19 + 2 de SentinelArchitectureTest), todos rojos con `UnsupportedOperationException: Not implemented yet` de la produccion (Sha256ContentFingerprinter/DefaultEvaluationSession) — esperado en TDD, cero errores de compilacion. Sin escalaciones.
  why: DefaultEvaluationSession/Factory se cablearon directo (constructor por orden de requiresInject via `describeComponent`: ledger, fingerprinter, evaluator, policy para la sesion; ledger, fingerprinter para la factory) sin leer su codigo; los tests usan una fake InMemoryLedger (duplicada en ambos archivos de test, nested classes no se comparten entre clases top-level) para modelar "corrida siguiente" como una segunda instancia de sesion sobre el mismo estado.

2026-07-31 — test-writer — 10 stubs de evaluation-ledger-infrastructure implementados y verdes en compilacion (rojos en ejecucion por UnsupportedOperationException, esperado): FileSystemEvaluationLedgerTest (6, R004 x3 + R006 + R007 + R008) y FEvcostJ002JourneyTest (4 paths). `mvn test-compile` y `mvn test` con JAVA_HOME=openjdk-25 confirmados: 10/10 fallan exactamente en el seam de produccion sin implementar (FileSystemEvaluationLedger.append/find, DefaultEvaluationSessionFactory.open). Sin escalaciones.
  why: R004 mitad dificil se probo truncando bytes reales del archivo (ver fix-log); J002 path-4 nuevo se distingue de path-2 por longitud de invokedSubjectRefs (se corta vs sigue) y por PENDING (nunca consultado) vs FAILED (consultado, sin veredicto) en EvaluationResolutionKind.

2026-07-31 — test-writer — Implementados los 2 paths de `FEvcostJ003JourneyTest` (audit-cli, R007+R008): usa `FileSystemAuditReportStore` + `FileSystemEvaluationLedger` + `Sha256ContentFingerprinter` reales sobre el MISMO `@TempDir`, con `Evaluator` mockeado (unico limite externo). path-1 (no pidio re-evaluar): guarda un informe + un veredicto, borra fisicamente `.content-audit/audits/` (mismo patron que el test homologo de FileSystemEvaluationLedgerTest para R007), abre una sesion nueva y verifica REUSED + evaluator nunca consultado. path-2 (pidio re-evaluar): mismo setup, pero `EvaluationRunPolicy(..., reevaluate=true, null)` en la segunda sesion; verifica EVALUATED + evaluator SI consultado + `ledger.history(...)` contiene AMBOS payloads (antes y despues). Compila; ambos quedan rojos por `UnsupportedOperationException` en `DefaultEvaluationSessionFactory.open` (produccion de evaluation-ledger-domain sin implementar aun) — TDD esperado. `mvn test -pl audit-cli`: 360 tests (352 baseline verdes + 8 nuevos de esta tanda), sin regresiones.
  why: "eliminar informes" no tiene metodo en AuditReportStore (solo save/load/loadLatest/list) — se modela como borrado fisico del directorio `.content-audit/audits/`, igual que ya lo hizo el test homologo a nivel infraestructura; reusar ese patron evita inventar un mecanismo de borrado que el contrato no expone.

2026-07-30 — developer — ARCH-002 (migracion del runtime de grafos, patch separado en esta carpeta segun TECH_SPEC) IMPLEMENTADA. `DefaultAgentGraphRunner` (agent-runtime-infrastructure/graphexecution) absorbe el algoritmo de los dos lanzadores borrados con la cadena de resolucion de 3 pasos (config -> inputs["project_root"] -> CWD). `SharedRuntimeAgentLauncher` y `SharedRuntimeKnowledgeTitleLauncher` delegan en `AgentGraphRunner`; las dos factories (`DefaultLemmaAbsenceAgentGeneratorFactory`, `DefaultKnowledgeTitleAgentGeneratorFactory`) construyen un `AgentGraphRunner` compartido via `DefaultAgentGraphRunnerFactory().create(new AgentGraphRunnerConfig())` (ambos campos null -> cadena de resolucion activa) y apuntan a los lanzadores nuevos. `mvn -o install -DskipTests -Dexec.skip=true -pl agent-runtime-infrastructure,revision-infrastructure -am` compila verde. Ningun requiresInject ni firma de generator cambio; ningun test tocado.
  why: no habia red de tests que ejercitara la implementacion (los handwrittenTests de FEAT-LASAG/FEAT-KTLR mockean el lanzador); el codigo de referencia de git (commit 06068094) mostro que lemma-absence resuelve relativo al CWD y nunca puebla project_root, y knowledge-title resuelve relativo a project_root — confirmado leyendo KnowledgeTitleAgentGenerator.java:93, que SIEMPRE pone project_root en el input map. Con config vacia en ambas factories, cada consumidor cae exactamente en el escalon de la cadena que tenia antes.
