# Fix log

2026-05-06 - qa-tester - sentinel patch apply silenció el placement de las 8 journeys (testModule/testPackage). handwrittenTests sí aterrizaron correctamente; las 18 reglas tienen sus 44 tests. Solo los journey gates F-CDIFF-J001..J008 quedaron sin testModule/testPackage en sentinel.yaml, y por eso sentinel generate no produjo las clases FCdiffJ00*JourneyTest.java.
  why: el bug `2026-04-20-01-silent-drop-journey-test-placement.md` (resolved 2026-04-20) reporta que el merge de apply era correcto y el bug real estaba en validate. Con la fix vigente, un re-propose journeys-only cuenta correctamente "8 modifications" — antes hubiera dicho "0". El fix es: re-proponer un patch focalizado SOLO sobre journeys placement y volver a aplicar. El usuario re-aplica, sentinel generate produce los 8 stubs, y el flujo sigue.

2026-05-06 - qa-tester - Patch journeys-only re-propuesto en architectural_patch.yaml (8 modifications, 0 conflicts). Pendiente: que el usuario corra `sentinel patch apply` y `sentinel generate` para que aterricen los stubs `FCdiffJ001JourneyTest.java`..`FCdiffJ008JourneyTest.java` (3 en audit-cli/.../commands, 5 en revision-domain/.../engine).

2026-05-06 - test-writer - FileSystemActiveAnalysisSelectionStoreTest: usa el patron user.dir + storeFor() como todos los otros FileSystem*StoreTest del modulo. El quinto test (no-destructividad) crea archivos centinela en audits/ y revisions/ y verifica que lastModified no cambia tras write/clear.
  why: patron ya validado en FileSystemRevisionArtifactStoreTest y FileSystemImpactPreviewStoreTest.

2026-05-06 - test-writer - FCdiffJ006JourneyTest: verify(formatter, times(2)) necesario porque el formatter se llama una vez para B y otra para A en el mismo metodo de test; un verify simple contaria exactamente 1 invocacion y fallaria.
  why: Mockito verify() default = exactly-1; dos pasos en el mismo test method requieren times(N).

2026-05-06 - test-writer - DefaultConsolidatedViewBuilderTest: quizNode() helper llama mock(AuditableEntity.class) con when(ent.getId()). AuditNode requiere entidad con getId() para que el builder identifique nodos por ID al construir NodeImpacts.
  why: El builder necesita mapear nodos del AuditReport con propuestas por nodeId; sin getId() el match falla.

2026-05-06 - test-writer - FCdiffJ004JourneyTest path-2: courseMapper/auditEngine requieren lenient stubbing (any()) porque el test sin propuestas llama auditEngine para calcular el consolidated "sin cambios". Alternativa viable: stub explícito con el mismo AuditableCourse.

2026-05-06 - developer - FEAT-CDIFF v3 production code implemented (7 files). DefaultFieldExclusionRegistry: global seeds for 5 roles (OPAQUE_PERSISTENCE_ID: _id/oid/$oid; STRUCTURAL_REFERENCE: parentId/childId/courseId/milestoneId/topicId/knowledgeId/quizId; TIMESTAMP: createdAt/updatedAt/lastModifiedAt/timestamp; INTERNAL_ORDER: index/position; NON_SEMANTIC: empty). DefaultListIdentityRegistry: seeds for AbsentLemma (lemmaAndPos.lemma+pos), MisplacedLemma (lemmaAndPos.lemma+pos), BucketResult (bandName), BucketSummary (bandName), QuarterResult (index), ProgressionAssessment (bandName), ImprovementDirective (type+bandName+levelName). RecursiveNodeFieldDiffer: walks entity/scores/diagnoses subtrees via reflection; keyed-list diff uses natural key; positional-list diff uses [i]. DefaultNodeFieldDifferFactory: create() = new RecursiveNodeFieldDiffer(new DefaultFieldExclusionRegistry(), new DefaultListIdentityRegistry()). DefaultConsolidatedViewBuilder: removed collectStatisticImpacts/buildStatisticImpacts entirely; uses nodeFieldDiffer.diff(origNode, consNode, pendNode) per touched nodeId; indexAuditNodes() helper walks tree to build id→AuditNode map; pendingProposalIds now a Map<String,List<String>>. DefaultConsolidatedViewBuilderFactory: added config.getNodeFieldDiffer() as 9th arg. DefaultConsolidatedViewFormatter: removed statisticImpacts section; emits fieldChanges as map(path → {original,consolidated,pendingProjection}); acceptedProposalIds+pendingProposalIds pluralized. Main.java: added DefaultNodeFieldDifferFactory import + nodeFieldDiffer instantiation + 9th arg to ConsolidatedViewBuilderConfig. revision-domain + audit-cli compile clean. sentinel verify PASS.
  why: v2 referenced deleted StatisticImpact class and used v2 NodeImpact constructor.

2026-05-06 - developer - FEAT-CDIFF v3 production code implemented (sentinel patch propose merge sobre architectural_patch ARCH-CDIFF-005). 6 deletes + 33 nuevos en DefaultConsolidatedViewBuilder, 1 delete + 5 nuevos en DefaultConsolidatedViewFormatter, 12 nuevos en RecursiveNodeFieldDiffer, 5 nuevos en DefaultFieldExclusionRegistry, 5 nuevos en DefaultListIdentityRegistry, 1 nuevo en DefaultNodeFieldDifferFactory. Validado: 0 additions, 11 modifications, 1 deletions, 0 conflicts. Todas las reglas vigentes cubiertas (R001..R007, R009..R013, R015..R017, R019..R023) por al menos un test directo, sin transitive coverage. R018 (propuestas estructurales) NO cubierta porque el modelo actual no tiene carrier para propuestas estructurales (todas son sustituciones sobre nodeId estable); la regla es vacuamente verdadera y testearla requeriria construir un escenario imposible — escalado como doubt al usuario.
  why: refactor v3 (forma uniforme field → tripleta + descubrimiento dinamico). Las cinco invariantes de R007 son las "famous five"; cada una se observa con un test independiente sobre el builder (R007.1 fidelidad de original, R007.2 consolidated del runAudit, R007.3 pendingProjection del runAudit, R007.4 stacking, R007.5 padre proviene del motor). R022 invariantes 1/2 + R020 cinco categorias se prueban dos veces: una sobre RecursiveNodeFieldDiffer (el motor honra las reglas) y otra sobre los registros por separado (los seeds existen).

