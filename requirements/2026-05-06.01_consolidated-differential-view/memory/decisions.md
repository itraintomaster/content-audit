# Decisions

2026-05-06 - analyst - Hoja con aceptada+pendiente: la pendiente se compara contra el vigente (=aceptada), no contra el baseline original. Maximo dos valores por campo.
  why: usuario lo pidio explicitamente; cualquier otra opcion introduce un valor "fantasma" del baseline que ya no es recuperable porque la aceptada ya esta materializada en el curso.

2026-05-06 - analyst - Deltas de estadisticas son puntos absolutos sobre la escala del dominio, no porcentajes relativos. La UI puede presentarlos como `+5%` por convencion, pero matematicamente son diferencias absolutas.
  why: usuario lo aclaro explicitamente; evita el caso ambiguo de "+5%" interpretado como "cinco por ciento mas que el original" (relativo) vs. "cinco puntos porcentuales mas" (absoluto).

2026-05-06 - analyst - Padre se calcula re-agregando subarbol con la estrategia del analizador, no sumando deltas de hijos. Aplica explicitamente a casos no lineales como COCA_BUCKETS (acumulacion de conteos).
  why: FEAT-COCA usa estrategia de agregacion polimorfica (F-COCA-R029); sumar deltas individuales daria resultados incorrectos para analizadores no-lineales.

2026-05-06 - analyst - La vista consolidada NO es un AuditReport nuevo. La generacion de la vista nunca emite auditId.
  why: paralelo a F-PIPRE-R002 generalizado al curso entero. Preserva la separacion entre historia oficial (AuditReports escritos por el motor) y vista derivada (lo que la UI ve combinando esa historia con decisiones del operador).

2026-05-06 - analyst - Refactor del REQUIREMENT.md: pasa de "vista" a "contrato de datos del CLI". Reglas describen los campos canonicos (consolidated, pendingProjection, acceptedDelta, pendingDelta, acceptedProposalIds, pendingProposalId, pendingApplicability, consolidatedAvailability) y su semantica. Lo de "como lo ve el operador" queda como contexto, no como reglas.
  why: pedido explicito del usuario via team-lead. content-audit es CLI/backend; la UI es un consumidor externo. Las reglas tienen que hablar de datos emitidos, no de presentacion. Se incorpora R016 explicito que supersede F-PIPRE-R011/DOUBT-BATCH-PREVIEW como pidio architect.

2026-05-06 - analyst - DOUBT-FIELD-IDENTITY se marca RESOLVED con Opcion C (granularidad coarse a nodo entero). El consumidor que necesite diff fino lo computa comparando consolidated vs pendingProjection.
  why: feedback de architect; CourseElementSnapshot hoy solo carga quiz: QuizTemplateEntity para QUIZ y los demas niveles tienen snapshot vacio. Distinguir campo dentro del nodo es ingenieria especulativa sin caso de uso real.

2026-05-06 - analyst - DOUBT-PENDING-CONFLICT se marca RESOLVED con Opcion A (orden por createdAt; choque cae en R012 NOT_APPLICABLE).
  why: feedback de architect; F-REVAPR-R010 ya cubre el caso comun y R012 ya soporta exactamente la semantica de "elementBefore no coincide con vigente".

2026-05-06 - architect - DOUBT-FIELD-IDENTITY resuelto a Opcion C (granularidad coarse a nodo entero) acordado con analyst.
  why: hoy todas las propuestas son QUIZ-target via FEAT-LAPS; CourseElementSnapshot solo carga quiz para QUIZ. Sin propuestas multi-campo reales, distinguir "campo dentro del nodo" requeriria tocar RevisionProposal (alto blast radius sobre FEAT-REVBYP/REVAPR/PIPRE/LAPS) sin caso de uso real. NodeImpact reusa CourseElementSnapshot existente; el consumidor que necesite diff fino compara consolidated vs pendingProjection.

2026-05-06 - architect - DOUBT-PENDING-CONFLICT resuelto a Opcion A (orden createdAt + R012 catch).
  why: F-REVAPR-R010 cubre el caso comun (no acumular pendientes sobre la misma tarea). Para "dos pendientes sobre el mismo nodo desde tareas distintas", aplicar en orden createdAt y dejar que la segunda caiga en R012 (elementBefore != consolidated -> NonApplicablePending con ELEMENT_BEFORE_MISMATCH) reusa la maquinaria de R012 sin agregar mecanismo nuevo.

2026-05-06 - architect - DOUBT-MATERIALIZATION resuelto a Opcion A (on-demand sin cache) para MVP.
  why: dos invocaciones de AuditEngine.runAudit por request (consolidated + pendingProjection) en cursos de ~11.500 quizzes puede ser caro pero todavia no medimos. Agregar cache especulativo seria over-engineering. La signatura narrow del puerto (build(coursePath): ConsolidatedView) deja libre meter cache detras sin tocar consumidores en una iteracion futura.

2026-05-06 - architect - DOUBT-ACTIVE-PERSISTENCE resuelto a Opcion A (archivo dedicado .content-audit/active-analysis.json).
  why: simetria con FileSystemAuditReportStore / FileSystemRefinementPlanStore (todos viven en .content-audit/). Un solo escritor, atomic-replace, idempotencia (write con mismo par = no-op) implementada en el adapter. Centraliza las garantias de F-CDIFF-R002 en un solo lugar testeable.

2026-05-06 - architect - DOUBT-UNAVAILABILITY-TAXONOMY: reusa el patron categoria-enum + detalle-string ya elegido para FEAT-PIPRE.
  why: consistencia entre features. ConsolidatedViewUnavailabilityReason tiene 5 valores (NO_ACTIVE_ANALYSIS / ACTIVE_PLAN_UNAVAILABLE / INCONSISTENT_PROPOSAL / REAGGREGATION_FAILED / OTHER) mas detalle textual. Mismo shape que ImpactPreviewUnavailability + ImpactPreviewUnavailabilityReason de FEAT-PIPRE.

2026-05-06 - architect - ActiveAnalysisSelectionStore vive en audit-domain (no revision-domain).
  why: la seleccion de "que AuditReport esta vigente" es un concepto del bounded context de auditoria, no de revision. El consolidador es solo uno de sus consumidores; futuros (CLI 'get audit --active', otros visualizadores) lo leen del mismo store. revision-domain ya depende de audit-domain, asi que no hay friccion para que el consolidador lo consuma.

2026-05-06 - architect - Package consolidatedview en revision-domain (no modulo nuevo).
  why: la feature consume RevisionArtifact + AuditReport + CourseEntity, exactamente las dependsOn que revision-domain ya tiene. Crear un modulo "consolidation-domain" duplicaria el grafo sin ganar encapsulacion. El package es public; la implementacion (DefaultConsolidatedViewBuilder + factory) vive en el package interno engine. Patron Public Port + Hidden Adapter intra-modulo, identico al de impactpreview.

2026-05-06 - architect - NodeImpact reusa CourseElementSnapshot existente; snapshots solo poblados para QUIZ.
  why: para padres (KNOWLEDGE/TOPIC/MILESTONE/COURSE) consolidated y pendingProjection son null porque CourseElementSnapshot solo lleva quiz. El impacto de los padres se expresa via la lista StatisticImpact que comparten (nodeTarget, nodeId). Esta separacion entre "snapshot del nodo hoja" y "metricas del padre" simplifica la salida y respeta el modelo de datos existente.

2026-05-06 - architect - StatisticImpact con Double boxed para nullables (pendingProjection, pendingDelta).
  why: distingue "no hay pendiente que toque la estadistica" (null) de "delta cero" (0.0). Una estadistica solo tocada por aceptadas tiene pendingProjection=null y pendingDelta=null; una solo tocada por pendientes tiene acceptedDelta=0.0 pero pendingDelta!=null.

2026-05-06 - architect - DefaultConsolidatedViewBuilder NUNCA llama AuditReportStore.save.
  why: F-CDIFF-R015 prohibe que la salida emita un AuditReport oficial. AuditReportStore es inyectado solo para .load(); el contrato del puerto en el dominio sigue siendo el mismo, pero el builder NO usa save. La regla es testeable directamente en DefaultConsolidatedViewBuilderTest verificando que mockedAuditReportStore.save() nunca se invoca en cualquier path.

2026-05-06 - architect - GetConsolidatedCommand y SetActiveAnalysisCommand son verbs sealed dedicados.
  why: el shape de la respuesta (ConsolidatedView con NodeImpacts/StatisticImpacts/NonApplicablePendings) es estructuralmente distinto del retorno simple de GetCommand. Meterlo dentro de GetCommand acoplaria dos contratos en una signatura. Un verb sealed dedicado por command sigue el patron de los demas (AnalyzeCommand, PlanCommand, etc.).

2026-05-06 - architect - ConsolidatedViewFormatter / DefaultConsolidatedViewFormatter visibility: public a nivel Java.
  why: viven en audit-cli.formatting (package internal a nivel modulo) pero los consumidores reales son DefaultGetConsolidatedCommand (package hermano commands) y la composition root (Main). Sin visibility: public a nivel Java son package-private y no se importan desde commands. Patron drift-corregido en ARCH-PIPRE-VIS para ImpactPreviewFormatter / DefaultImpactPreviewFormatter; aplica igual aqui.

2026-05-06 - architect - Refinamiento texto: revisionArtifactStore.description en DefaultConsolidatedViewBuilder hace explicito que REJECTED se descarta (F-CDIFF-R005).
  why: Nota minor de analyst en review. El particionado por verdict ya estaba implicito en el algoritmo, pero el contrato del puerto debia decirlo en el description para que qa-tester pueda taggear directamente el comportamiento de R005 sobre la inyeccion. Cambio no-arquitectural; no toca fences del TECH_SPEC.

2026-05-06 - architect - Patch ARCH-CDIFF-002 propuesto: agregar 7 requiresInject faltantes a DefaultConsolidatedViewBuilder (activeAnalysisSelectionStore, auditReportStore, refinementPlanStore, courseRepository, courseElementLocator, courseMapper, auditEngine).
  why: omision en ARCH-CDIFF-001 detectada por @developer durante la implementacion. ConsolidatedViewBuilderConfig ya expone los 8 campos pero el builder solo declaraba revisionArtifactStore. Patron de DefaultImpactPreviewComputer (cada dep como campo individual, factory unpackea el config). Pendiente: usuario aplica el patch y sentinel generate regenera el constructor de DefaultConsolidatedViewBuilder con las 8 inyecciones. TECH_SPEC.md reescrito con --replace porque ARCH-CDIFF-001 ya esta aplicado y los fences viejos quedaron fuera del active patch (narrative original archivado en git en cc66adb).

2026-05-06 - architect - Patch ARCH-CDIFF-003 propuesto: agregar requiresInject baseDir(Path) a los 4 FileSystem* stores de audit-infrastructure (AuditReport, RefinementPlan, RevisionArtifact, ImpactPreview) y verdict(RevisionVerdict)+rejectionReason(Optional<String>) a DefaultRevisionValidatorResult de revision-domain.engine.
  why: el scaffold cc66adb regenero estos 5 archivos como @Generated y borro los constructores hand-written que existian (descubrimiento que invalida la guidance dada en la escalation previa de developer sobre FileSystemActiveAnalysisSelectionStore). Los campos privados final sobrevivieron pero sin constructor; F1/F2 impide a developer editar @Generated. La fix correcta es modelarlo en sentinel.yaml para que el generador emita el constructor. ObjectMapper queda como detalle interno (cada store tiene su createObjectMapper() privado) — no es puerto inyectable. ARCH-CDIFF-002 fue aplicado por el usuario entre ARCH-CDIFF-002 y ARCH-CDIFF-003, por eso TECH_SPEC.md se reescribio con --replace para cubrir solo el scope de 003.

2026-05-06 - architect - ARCH-CDIFF-003 ampliado: agregar requiresInject formatters(Map<String, ReportFormatter>) a DefaultFormatterRegistry de audit-cli.formatting.
  why: misma clase de breakage que los 5 elementos previos. Archivo @Generated con campo final declarado y constructor barrido por el regen cc66adb. Map<String, X> ya es sintaxis valida en sentinel.yaml (DispatchingDetailedFormatter usa Map<String, DetailedFormatter>). Main.java:261 instancia con un solo argumento Map. Mergeado con la proposal existente; total ahora 9 modifications en el patch.

2026-05-06 - architect - ARCH-CDIFF-003 cierra el batch: agregar requiresInject whitespaceNormalizer(WhitespaceNormalizer) a PlainSentenceDeriver de course-domain.quizsentenceengine.
  why: ultimo @Generated roto detectado por sweep de developer. Patch ahora cubre 7 implementaciones rotas por scaffold cc66adb (4 stores + DefaultRevisionValidatorResult + DefaultFormatterRegistry + PlainSentenceDeriver). Total: 0 additions, 11 modifications, 0 conflicts.

2026-05-06 - architect - Patch ARCH-CDIFF-004 (vs sentinel.yaml line 3224-3235): cleanup de DefaultAuditRunner.requiresInject. Borrar contentAudit:ContentAudit (system name no es tipo) y courseMapper:CourseMapper (el cuerpo nunca lo usa). Agregar allAnalyzers:List<ContentAnalyzer> y scoreAggregator:ScoreAggregator (el cuerpo SI los usa y los call sites Main.java:254 + DefaultAuditRunnerTest:37 ya pasan los 5 argumentos esperados).
  why: bug pre-existente (drift entre sentinel.yaml y codigo) descubierto al re-correr sentinel generate post-ARCH-CDIFF-003. La firma constructor que el generador emitia (7 args, con ContentAudit y CourseMapper basura) no matcheaba lo que Main.java y el test ya invocan (5 args). Resuelto en una sola pasada: 2 deletes + 2 adds dentro de requiresInject. ARCH-CDIFF-001/002/003 ya estan aplicados; TECH_SPEC.md reescrito con --replace.

2026-05-06 - analyst - Removidos F-CDIFF-R014 (regla "baseline nuevo == consolidated anterior") y F-CDIFF-J007 (journey de equivalencia post-stack). Numeracion conservada con hueco en R014 para preservar traceability de los tests existentes (R015..R018 mantienen sus IDs y @Tag). Actualizadas las cross-references en R003.detail.2, Contexto, Alcance y References (FEAT-REVAPR) y en Assumption #2 para reformular la equivalencia post-stack como propiedad emergente del sistema y no como contrato del consolidador.
  why: el consolidador recibe AuditReports prefabricados, no corre auditEngine; no puede enforced ni testear directamente la equivalencia entre baseline nuevo y consolidated anterior. La propiedad es emergente del sistema completo (auditEngine deterministico + materializacion de aceptadas via FEAT-REVAPR R011). El test J007 mockeaba dos vistas con valores iguales por construccion y verificaba 0.85==0.85, una tautologia sin valor de regresion. Lo que el consolidador SI garantiza (vista limpia cuando el plan no tiene propuestas) ya esta cubierto por R007/R009/R003 en combinacion. La verificacion end-to-end de la equivalencia pertenece a un nivel de testing que combina FEAT-REVAPR + auditEngine + esta feature, fuera del scope del consolidador.

2026-05-06 - architect - El patch DSL architectural NO soporta deleciones de reglas o journeys.
  why: confirmado empiricamente. Un patch con bloque `features:` declarando `_change: delete` sobre un journey valida con "0 deletions" — el validador silenciosamente ignora todo lo que este fuera de `modules:`. La superficie del patch DSL es: modules + interfaces + implementations + models + packages + dependencies. Reglas y journeys viven en sentinel.yaml.features y se derivan de REQUIREMENT.md (analyst-owned). Para borrar R014/J007 de sentinel.yaml hay que regenerar desde REQUIREMENT.md (probablemente via `sentinel generate`) o edicion manual del usuario sobre lineas 17970-17991 (R014) y 18257-18286 (J007). El architectural_patch.yaml quedo con un no-op de prueba — NO aplicar. Mensaje a analyst con el diagnostico completo enviado.

2026-05-06 - analyst - Refactor v3 del REQUIREMENT.md a forma uniforme `field → (original, consolidated, pendingProjection)` por nodo afectado. Contrato anterior (snapshot crudo + lista paralela de estadisticas con cuatro escalares) descartaba los diagnosticos tipados (tokenCount proyectado, listas de AbsentLemma, distribucion COCA proyectada) que el motor calcula sobre el arbol con pendientes. Nueva forma uniforme: cada nodo afectado expone un mapa de fields cambiados con tres fotos cada uno; tres clases de fields (diagnosticos tipados / contenido crudo whitelist / estadisticas escalares por dimension); deltas no son parte del contrato (computables por el consumidor). Mapa: R006 reescrita (mapa por nodo), R007 reescrita (significado fijo de cada foto), R008 reescrita (padre usa la misma forma con re-agregacion por estrategia), R009 reescrita (estadistica = caso particular de field), R010 reescrita (deltas fuera del contrato). Reglas nuevas: R019 (tres clases de fields), R020 (whitelist por tipo de nodo del contenido crudo), R021 (paridad pares (nivel,dimension), heredera de la R011 anterior), R022 (identidad de listas con clave natural: AbsentLemma=(lemma,pos), MisplacedLemma=(lemma,pos), BucketResult=bandName, QuarterResult=index, ProgressionAssessment=bandName, ImprovementDirective=(type,bandName,levelName), BucketSummary=bandName). R011 anterior renombrada a R021. R011 nueva: trazabilidad de propuestas (acceptedProposalIds + pendingProposalIds) por nodo, ya no por field como sub-entries de R007. R012/R013/R015/R016/R017/R018 conservan IDs y semantica; solo redaccion ajustada para no referenciar la lista paralela de estadisticas. DOUBT-FIELD-IDENTITY queda cerrada con Opcion C (las tres clases). Numeracion: R001..R013 + R015..R018 vigentes (hueco historico en R014 conservado), + R019..R022 nuevas. Journeys: J001..J006 + J008 narrativas reescritas para describir el mapa y la tripleta; gates actualizados a las nuevas reglas; J004 reorientada a "el contrato emite la tripleta y deja los deltas al consumidor". Cero journeys nuevos. Validacion sentinel-core OK.
  why: gap detectado por el usuario al consumir la salida actual del CLI: no se ve, p.ej., el tokenCount del estado pendiente de una oracion. La forma uniforme tambien hace el contrato extensible: cuando un analizador nuevo agrega un diagnostico tipado, sus componentes empiezan a aparecer automaticamente como fields del mapa sin reescribir el contrato. Las cross-references desde R007 (anterior, "trazabilidad de propuestas") quedan migradas a R011 nueva.

2026-05-06 - analyst - F-CDIFF-R007 reescrita de "tabla descriptiva de las tres fotos" a "cuatro invariantes verificables" (Opcion A: regla unica con invariantes enumeradas en el cuerpo, glosario movido a <details>). Invariantes: (1) fidelidad de original respecto al baseline (no se recomputa, redondea ni deriva), (2) determinacion de consolidated solo con aceptadas, (3) determinacion de pendingProjection con aceptadas+pendientes aplicables, (4) orden de stacking (pendientes sobre consolidated, no sobre original; observable directamente en "aceptada+pendiente sobre el mismo nodeId"). Cada invariante es testeable construyendo el escenario y verificando igualdad/inecuacion sobre la tripleta emitida. Gates de J001/J002 a R007 conservados; J002 ya describe explicitamente la invariante 4 en emitir_mapa_fields. Validacion sentinel-core OK.
  why: feedback del usuario: la R007 anterior era documentacion (tabla Foto/Significado), no regla. Memoria del proyecto exige que toda regla tenga test explicito; una tabla de definiciones no es testeable. Opcion A elegida sobre B (split en R007a..R007d) para preservar legibilidad del contrato; las invariantes 1-3 son simetricas y un solo journey las cubre con un gate; la invariante 4 es la unica con observacion realmente independiente y queda como item enumerado dentro de la regla unica.

2026-05-06 - analyst - Refactor v4: Grupo D pasa de tres clases enumeradas a descubrimiento dinamico + exclusiones por rol funcional + anidamiento por path estable. Pedido del usuario: "exponer todo tipo de valor que haya cambiado y de hecho hacerlo dinamico... que si encuentra que un field ha cambiado, agregue ese cambio sin tener que crear un requerimiento nuevo... ademas, debera tener alguna forma de exponer los objetos nested". Cambios en reglas: R019 reescrita (recorrido recursivo, hoja escalar con cambio = field, aparicion automatica de cosas nuevas), R020 reescrita (5 categorias de exclusion por rol funcional: identificadores opacos / referencias estructurales / timestamps / ordenes internos / catch-all "no semantico"; en lugar de whitelist por tipo de nodo), R021 redefinida en el mismo ID como "agnosticismo dimensional" (una pendiente de dimension X mueve hojas de Y; ambas aparecen). Regla nueva R023 (anidamiento: hojas profundas con clave de path estable, agrupadas en una sola entrada con tripleta, mismas claves entre nodos comparables). R022 reescrita: invariante testeable "lista con identidad declarada → diff por elemento; lista sin identidad → diff posicional"; las identidades concretas (AbsentLemma=(lemma,pos), etc.) pasaron de cuerpo de regla a ejemplos en <details>.
  why: la version anterior (tres clases enumeradas + whitelist por tipo de nodo) requeria mantenimiento del requerimiento por feature (analizador nuevo / campo nuevo en quiz / etc.). El descubrimiento dinamico delega "que field aparece" al recorrido recursivo del modelo y al motor de auditoria; el contrato solo obliga "cualquier hoja con cambio aparece" + "ningun campo cuyo rol funcional es ruido aparece". Trade-off aceptado por el usuario: el consumidor interpreta hojas crudas (token count, percentage, listas con identidad). El cliente lee mas pero el CLI escala automaticamente con cualquier nuevo analizador.

2026-05-06 - analyst - Auditoria de testeabilidad sobre todas las reglas R001..R022. Cambios para que todas declaren invariantes observables, no descripciones: R001 (raiz expone activeAuditId+activePlanId), R002 (no destructivo + idempotente como invariantes sobre el filesystem), R003 (separa la afirmacion testeable propia de FEAT-CDIFF: sin decisiones nuevas → lista de afectados vacia; deja la equivalencia post-stack como propiedad emergente del sistema en <details>), R004 (recuperacion de pendientes dormidas al apuntar par activo), R005 (no contribuye a fotos + no aparece en trazabilidad), R006 (tres invariantes: solo fields con cambio / forma uniforme / nodo sin cambios no aparece), R008 (re-agregacion por estrategia, no suma de hijos + forma uniforme con hojas), R009 (no hay seccion paralela de estadisticas afectadas), R011 (cuatro invariantes de cobertura accepted/pending + orden + por nodo no por field), R012 (tres invariantes: marcado / no contribucion a pendingProjection / resto intacto), R015 (tres invariantes sobre filesystem: no AuditReport / no plan-propuesta-preview / no curso), R016 (combinacion en un documento + preservacion de previews por-propuesta), R017 (ninguna foto contiene efecto de planes ajenos + identidad observable), R018 (propuesta estructural no contribuye + no aparece en pendingApplicability). DOUBT-FIELD-IDENTITY actualizado con Opcion D (descubrimiento dinamico) en lugar de Opcion C (tres clases enumeradas).
  why: feedback del usuario "revisa bien el tema de las reglas que todas queden declamadas de forma testeable". Memoria del proyecto "Every rule needs an explicit test; transitive coverage does not count". R007 ya servia de modelo (cuatro invariantes en el cuerpo, glosario en <details>). Aplicado mismo patron a las demas: cuerpo principal tiene afirmaciones observables; tablas, glosarios y casos derivados van a <details>.

2026-05-06 - analyst - F-CDIFF-R008 ELIMINADA del REQUIREMENT.md. La regla anterior ("Las fotos de un padre se obtienen re-agregando con la estrategia del analizador") describia comportamiento del motor de auditoria, no del contrato de salida de FEAT-CDIFF. FEAT-CDIFF no re-agrega: corre el motor sobre el curso consolidado y sobre el curso con-pendiente, pide al motor el AuditReport, y LEE el valor que el motor dejo en el AuditNode correspondiente. Como se agrega (promedio simple, acumulacion por banda, etc.) es alcance del motor de auditoria y de las features de cada analizador (F-COCA-R029, FEAT-DSLEN, etc.). R007 ya cubria el caso con sus invariantes 2 y 3 ("la foto consolidated/pendingProjection es lo que el motor produce sobre el arbol respectivo"); promovi la implicacion explicita a invariante 5 de R007 ("Las tres fotos provienen del motor, no de FEAT-CDIFF": un test puede afirmar que la foto del padre es identica al valor que el AuditReport correspondiente expone). Anchor F-CDIFF-R008 ya no existe; numeracion conservada con hueco junto a R014. Journeys actualizados: J001 (combinar_y_emitir, gate sin R008), J003 (narrativa y gates reescritos para citar R006/R007/R019/R021/R022 sin R008), J005 (incluir_en_proyeccion, gate sin R008). References ajustadas: FEAT-COURSE ahora cita R006; FEAT-COCA cita R007.5 (no R008). Validacion sentinel-core OK.
  why: feedback del usuario explicito: "no tiene sentido esta regla. Define como calcular agregaciones. Las agregaciones se resuelven cuando se hace el analisis con la version consolidada del curso (y con la de los pendings). Es decir: lo resuelve la auditoria y no se necesita definir nada aqui, solo copiar los valores en el lugar correspondiente". Mantener R008 violaba separacion de concerns y heredaria a FEAT-CDIFF tests que en realidad cubren features ajenas (FEAT-COCA, FEAT-DSLEN). El "test del bug clasico de deltas que se suman" pertenece a FEAT-COCA, no a FEAT-CDIFF.

2026-05-06 - architect - ARCH-CDIFF-005 propuesto y validado: refactor v3 a forma uniforme field → tripleta. Cambios principales: (a) NodeImpact pierde consolidated/pendingProjection (CourseElementSnapshot) y pendingProposalId (singular); gana fieldChanges:Map<String,FieldChange> y pendingProposalIds:List<String>. (b) StatisticImpact (modelo) y ConsolidatedView.statisticImpacts (campo) ELIMINADOS — los scores son fields del mapa. (c) Nuevos modelos publicos en consolidatedview: FieldChange (tripleta Object/Object/Object), FieldPath (record con raw:String, dot-separator + [k=v] / [i]), FieldExclusionRole (enum 5 valores R020). (d) Nuevo puerto publico NodeFieldDiffer en consolidatedview con diff(AuditNode, AuditNode, AuditNode) → Map<String, FieldChange>. (e) Nuevo package internal fielddiff con: RecursiveNodeFieldDiffer (impl package-private, reflection sobre records); DefaultFieldExclusionRegistry y DefaultListIdentityRegistry (impl package-private de los dos registros centrales que materializan R020 y R022 sin tocar records existentes); DefaultNodeFieldDifferFactory (unica clase publica del package). (f) DefaultConsolidatedViewBuilder: agregar requiresInject nodeFieldDiffer:NodeFieldDiffer; borrar 19 handwrittenTests v2 que asumian StatisticImpact / consolidated:CourseElementSnapshot / pendingProposalId singular. (g) ConsolidatedViewBuilderConfig: agregar nodeFieldDiffer:NodeFieldDiffer (la factory lo unpackea). (h) DefaultConsolidatedViewFormatter: borrar 3 handwrittenTests v2 que asumian StatisticImpact (los de R001/R012/R013 sobreviven). Patch validado: 5 additions, 7 modifications, 1 deletion, 0 conflicts. TECH_SPEC.md regenerado (11 fences, todas subset del patch).
  why: refactor REQUIREMENT.md v3 (descubrimiento dinamico R019 + exclusiones por rol funcional R020 + path estable R023 + identidad de listas R022). Decisiones arquitecturales: R020 mecanismo = registro central (no anotaciones — alto blast radius sobre records de audit-domain/course-domain — y no convencion de nombre — fragil). R022 mecanismo = mismo registro central separado (DefaultListIdentityRegistry). R023 path schema = string con dot-separator + [k=v] (lexicografico) para identidad declarada + [i] para posicional, encerrado en record FieldPath para que el separador viva en un solo lugar. FieldChange.original/consolidated/pendingProjection son Object porque la hoja escalar es polimorfica (primitivo boxed / String / enum / null) — Java sin union types y un FieldChange<T> generico no compatibilizaria los cuatro casos sin perder semantica. Pendiente: aplicacion del patch por el usuario y posterior re-design de tests por @qa-tester.
