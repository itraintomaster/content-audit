code: FEAT-REVCTX
modules:
  - name: revision-domain
    packages:
      - name: contextoverride
        implementations:
          - name: DefaultCorrectionContextOverrideParser
            handwrittenTests:
              - name: "should expose a static withDefaultValidators factory that wires both LEMMA_ABSENCE and SENTENCE_LENGTH validators so the audit-cli composition root can construct the parser without breaking the package-private encapsulation of the per-kind validators"
                traceability:
                  feature: FEAT-REVCTX
                  rule: F-REVCTX-R002
