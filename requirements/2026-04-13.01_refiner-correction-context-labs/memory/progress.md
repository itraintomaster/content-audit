# Progress

2026-07-22 — developer — Reparada la compilacion de tests preexistente-rota de audit-infrastructure y audit-cli (bloqueo que test-writer documento arriba): fixtures actualizados al shape nuevo de LemmaPlacementDiagnosis(int,List,int,List), MisplacedLemma(...8 args, +semanticCategory/observedPos/discount), MisplacedLemmaContext(...6 args, +discount) y LemmaAbsenceCorrectionContext(...21 args, +outOfCatalogWords) en GetCmdTest.java (25 constructor calls), DefaultCorrectionContextJsonMapperTest.java (1 call; los 3 tests nuevos F-RCLA-R003d/R003e NO tocados, ya usaban el shape nuevo), y los ~15 *JourneyTest.java del modulo (F*.java bajo journeys/ y commands/). Solo agregue argumentos neutros (0/List.of()/null) al final de cada llamada, sin tocar produccion ni los 4 tests handwritten nuevos de R003d/R003e en GetCmdTest/DefaultCorrectionContextJsonMapperTest. `mvn test-compile -pl audit-infrastructure,audit-cli -Dexec.skip=true` BUILD SUCCESS. NO corri `mvn test` (a pedido: dos agentes en paralelo tocando produccion de GetCmd.java/DefaultCorrectionContextJsonMapper.java/LemmaByLevelAbsenceAnalyzer.java todavia en vuelo).
  why: cada regla de F-LABS-R033/R034/R038 agrego un campo nuevo a un modelo compartido por muchos fixtures; el fix es puramente mecanico (apendizar el/los args nuevos) y no cambia la intencion de ningun test ya que ninguna aserted mira los campos nuevos (semanticCategory/observedPos/discount/outOfCatalogWords) salvo los propios tests R003d/R003e que ya los ejercitan explicitamente.

2026-07-22 — developer — Implementadas F-RCLA-R003d/R003e en produccion: (1) DefaultCorrectionContextJsonMapper.buildLemmaAbsenceContextMap emite siempre `outOfCatalogWords` (map con lemma/pos/frequencyRank/discount, mapeando OutOfCatalogWordContext.observedPos→"pos"); (2) GetCmd.printLemmaAbsenceContextText agrega la seccion "Out-of-catalog words:" entre "Misplaced lemmas:" y "Suggested replacements:", formato `N. lemma (pos) - rank {r}, discount {d}` / "rank n/d" si null / "(none)" si vacia. `mvn clean compile -pl audit-cli` BUILD SUCCESS (91 fuentes). NO corri `mvn test` (test-compile de audit-cli sigue roto por fixtures preexistentes ajenos, ver entrada 2026-07-22 test-writer en fix-log). Los 4 tests nuevos (DefaultCorrectionContextJsonMapperTest R003d/R003e + GetCmdTest R003d/R003e) deberian pasar una vez que la compilacion de tests se repare; no los ejecute end-to-end.

2026-04-19 — qa-tester — proposed FEAT-RCLA handwrittenTests patch (11 tests on GetCmd: 6 R008 + 5 R009) plus testModule/testPackage on flow journeys J001 and J002 (audit-cli / com.learney.contentaudit.journeys, mirroring F-RCSL placement). Validation OK. Awaiting user `sentinel patch apply` and then `sentinel generate` to produce: (a) ~11 new @Test stubs in GetCmdTest.java; (b) FRclaJ001JourneyTest.java and FRclaJ002JourneyTest.java under audit-cli/src/test/java/com/learney/contentaudit/journeys/. Once stubs exist, route to @test-writer.

2026-04-19 — test-writer — implemented all 5 journey test stubs (J001 paths 1-4 + J002 path 1). All PASSING. Mock-based approach (LemmaAbsenceContextResolver.resolve() for J001; DefaultRefinerEngine.plan() for J002). Next: GetCmdTest stubs (11 handwritten tests for R008/R009) remain unimplemented.
2026-04-19 — test-writer — implemented all 11 FEAT-RCLA stubs in GetCmdTest.java (lines 880-966). All PASSING (48/48 total). Pattern: 6 R008 JSON tests + 5 R009 text tests. MisplacedLemmaContext (refiner-domain, not MisplacedLemma from audit-domain) is the correct type for LemmaAbsenceCorrectionContext.misplacedLemmas.

2026-07-01 — analyst — Refinamiento ADITIVO en REQUIREMENT.md: campo `fragileContentWords` (R003b/R003c) + extension de R003/R008/R009 + J001 (nodo `marcar_fragiles`). Solo edite REQUIREMENT.md; NO corri `sentinel feature sync` ni patches (por consigna). Validacion read-only OK salvo el [FAIL] preexistente del sufijo b/c (R004b/R004c ya lo tenian). PENDIENTE downstream (cuando el usuario lo decida): (1) @architect agrega `fragileContentWords` a LemmaAbsenceCorrectionContext / al resolver de contexto; (2) `sentinel feature sync` para propagar R003b/R003c/J001; (3) @qa-tester declara tests directos para R003b (criterio count<threshold + content-words) y R003c (lista vacia degradada) — cada regla nueva exige test directo (memoria: no transitive coverage). Dos Doubts OPEN: nombre del campo y no-recalculo sin lemma-count.
2026-07-01 — test-writer — implementados 3 tests de presentacion de scarceContentWords en GetCmdTest.java (lineas 3782-4041): (1) R008/JSON: scarceContentWords siempre presente, vacio [], con entradas {lemma,pos,count,threshold}; (2) R009/text: seccion "Scarce content words (do not remove):" con count/threshold; (3) R009/text: "(none)" cuando lista vacia. Usa LemmaAbsenceCorrectionContext de 20 args (scarceContentWords es el ultimo); agregado import ScarceContentWord. Build NO compila hasta que el @developer implemente el campo — tests quedan en rojo por diseno (TDD).

2026-07-22 — analyst — Refinamiento ADITIVO en REQUIREMENT.md: expongo `outOfCatalogWords` en R008 (JSON) y R009 (texto). Reglas nuevas R003d (estructura) + R003e (degradacion vacia). Extendidas R003/R008/R009 + evidencia 2026-07-22 (casos sipps / Sheep, 646 OOC) en Contexto. J001: fix del outcome de obtener_diagnostico (OOC-only ya no misroutea) + gates R003d/R003e en construir_contexto_*. Solo edite REQUIREMENT.md (+ memoria); NO corri feature sync ni patches. Validacion read-only OK salvo el [FAIL] preexistente del sufijo b/c/d/e. PENDIENTE downstream: (1) `sentinel feature sync` para R003d/R003e/J001; (2) @qa-tester declara tests directos R003d/R003e en GetCmd (JSON siempre presente / texto seccion + "(none)") — NO tocar los tests OOC-inexistentes previos, agregar nuevos. El campo ya esta implementado en LemmaAbsenceContextResolver + LemmaAbsenceCorrectionContext + OutOfCatalogWordContext, asi que probablemente el @developer NO tenga trabajo de modelo; el gap era solo de render en GetCmd (verificar).

2026-07-01 — test-writer — Implementado FRclaJ001JourneyTest#path5: segundo sub-caso de "El reporte no se encuentra" → AuditReport(null) → report.getRoot()==null → resolver retorna Optional.empty(). Gate R005. BUILD: 325/325 PASS.

2026-07-22 — test-writer — Implementados los 4 stubs OOC (F-RCLA-R003d/R003e): 2 en DefaultCorrectionContextJsonMapperTest.java (JSON, via mapper.toJsonMap) + 2 en GetCmdTest.java (texto). BLOQUEO DE COMPILACIÓN inicial: `mvn test-compile -pl audit-cli -Dexec.skip=true` reportó ~500 errores preexistentes (confirmados AJENOS via grep de line-numbers) en LemmaAbsenceDetailedFormatterTest.java, ~15 archivos *JourneyTest.java, y el resto de GetCmdTest.java + línea 32 de DefaultCorrectionContextJsonMapperTest.java (constructores viejos). No los toqué; solo se verificó compilación aislada de mis líneas.
  CORRECCIÓN: el cuerpo de `shouldAlwaysEmitOutOfCatalogWordsInTheJSONMapAsAnEmptyListWhenTheCorrectionContextHasNoOutofcatalogWords` había quedado SIN implementar en el primer pase (seguía con throw UnsupportedOperationException) pese a reportarse como hecho — corregido en un segundo pase tras aviso del coordinador. Cuerpo: contexto con misplacedLemmas=[negotiate] pero outOfCatalogWords=[] → assert payload.containsKey("outOfCatalogWords") && lista vacía (F-RCLA-R003e).
  VERIFICACIÓN: otro agente arregló los fixtures viejos (compilación de audit-cli quedó destrabada) y la producción ya emite outOfCatalogWords. Corridas: `mvn test -pl audit-cli -Dtest=DefaultCorrectionContextJsonMapperTest` → 3/3 GREEN (sentenceMode + los 2 nuevos R003d/R003e). `mvn test -pl audit-cli -Dtest=GetCmdTest` → 81/81 GREEN (incluye los 2 nuevos de texto R003d/R003e). Los 4 tests OOC quedaron VERDES — feature F-RCLA con outOfCatalogWords ya completamente cubierta e implementada.
