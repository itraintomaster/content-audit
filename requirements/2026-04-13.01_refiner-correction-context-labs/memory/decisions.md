# Decisions

2026-04-19 — analyst — Re-homed rule references from `refiner next` to `get tasks` / `get task <id>` after FEAT-CLIRV deleted the old command. Behavioral objective unchanged.
  why: FEAT-CLIRV (kubectl-style restructure) removed the `refiner next` surface; R007/R008/R009 and J001 still cited it literally. Rule and journey objectives are unchanged — only the command-surface citations were swapped to `get task` (single-task lookup) and `get tasks --status pending --sort priority --limit 1` (next-pending equivalent). J002 was untouched (its actions never literally cited `refiner plan`/`refiner next`). Historical-context paragraph in "El problema actual" still mentions `refiner plan` to explain the prior state; that's allowed.

2026-07-01 — analyst — Cambio ADITIVO: campo `fragileContentWords` en el correctionContext de LEMMA_ABSENCE. Nuevas reglas F-RCLA-R003b (estructura+criterio: content-words de la oracion actual con count<threshold de lemma-count; entradas lemma/pos/count/threshold) y F-RCLA-R003c (degradacion elegante: lista vacia si no hubo lemma-count / sin fragiles, espeja R004c). Extendidas R003(tabla+parrafo fuentes), R008(JSON, campo siempre presente), R009(texto, seccion "Fragile content words (do not remove)"). J001 actualizado: nuevo nodo de decision `marcar_fragiles` (gate R003b/R003c) → construir_contexto_completo | construir_contexto_con_fragiles_vacio; el camino sin-sugerencias tambien gatea R003b/R003c. Dos Doubts OPEN: nombre del campo y no-recalculo ad hoc sin lemma-count.
  why: al reescribir para reemplazar vocab fuera de nivel, el LLM a veces quitaba content-words escasas (p.ej. "butter") y bajaba la cobertura del curso; el campo le avisa cuales NO quitar. Misma señal/umbral que F-SLEM (lemma-count). R003b/R003c marcadas ASSUMPTION (falta confirmacion del usuario). El validator marca [FAIL] en R003b/R003c por el sufijo b/c igual que en R004b/R004c preexistentes: patron tolerado del proyecto, no lo introdujo este cambio.

2026-04-19 — qa-tester — Re-declared 11 handwrittenTests on GetCmd traced to F-RCLA R008/R009 (lost when FEAT-CLIRV deleted RefinerNextCmd) plus declared testModule=audit-cli, testPackage=com.learney.contentaudit.journeys on flow journeys J001 and J002 (previously unset, would have fallen back to module-root placement). R008: 6 tests (JSON shape including the diagnosis-gate test); R009: 5 tests (text format including the misplaced-lemmas list). Patch validates (1 mod / 0 conflicts). Note: original deleted test "should resolve correction context when task diagnosis is LEMMA_ABSENCE" was tagged R007 in the deleted file, but R007 is already covered by DispatchingCorrectionContextResolver tests at the resolver layer — re-tagging to R008 (per user matrix) keeps the trace at the GetCmd JSON-shape level where the behavior is now observable.
  why: F-RCLA dropped from IMPLEMENTED to IMPLEMENTING because R008/R009 had no impl-level traces after the test class deletion; J001/J002 had no testModule/testPackage at all, so generated journey stubs would have landed at audit-cli root — explicit declaration matches F-RCSL's pattern.

2026-07-22 — analyst — Cambio ADITIVO: exponer `outOfCatalogWords` en el correctionContext de LEMMA_ABSENCE (JSON R008 + texto R009). El campo YA existe en el modelo/resolver (siempre poblado, [] nunca null, desde la iteracion fuera-de-catalogo de F-LABS; resolver linea ~189), pero ni JSON ni texto lo renderizaban → tareas motivadas SOLO por palabra fuera de catalogo salian como tarjetas "mudas" (sin lema mal ubicado ni seccion OOC). Evidencia 2026-07-22: "He sipps a lot of tea." (typo OOC 'sipps', discount 0.3) y "Sheep run..." (OOC 'Sheep', rank 4450); 646 ocurrencias OOC en el audit activo. Reglas NUEVAS: F-RCLA-R003d (estructura de cada palabra OOC: lemma/pos/frequencyRank nullable/discount) y F-RCLA-R003e (degradacion: lista siempre presente, [] cuando no hay; espeja R003c/R004c). Extendidas R003 (fila outOfCatalogWords + parrafo fuentes), R008 (JSON campo siempre presente + 2do ejemplo OOC-only), R009 (texto seccion "Out-of-catalog words:" tras "Misplaced lemmas:", "(none)" si vacia + 2do ejemplo). J001: obtener_diagnostico corregido (el exito ya no exige >=1 MisplacedLemma; basta diagnostico presente con lema mal ubicado O palabra OOC — antes misroute-aba las tareas OOC-only); gates R003d/R003e en los 3 nodos construir_contexto_*. Marcadas VALIDATED (evidencia + pedido explicito del usuario).
  why: R006 exige LemmaPlacementDiagnosis presente pero NO exige misplacedLemmas no-vacia; las OOC viven en el mismo diagnosis (placementDiagnosis.getOutOfCatalogWords()), asi que el contexto YA se construia para OOC-only — solo faltaba renderizar. El [FAIL] del validator en R003b/c/d/e es el sufijo b/c/d/e, patron tolerado preexistente (ver entrada 2026-07-01), no lo introdujo este cambio. Pendiente downstream (cuando el usuario decida): `sentinel feature sync` para propagar R003d/R003e/J001 + @qa-tester declara tests directos R003d (forma {lemma,pos,frequencyRank,discount} en JSON/texto) y R003e (lista vacia / "(none)") — no transitive coverage.

2026-07-01 — architect — Patch propuesto (SIN aplicar) para R003b/R003c: modelo nuevo `ScarceContentWord` (record en refiner-domain: lemma:String, pos:String, count:Integer, threshold:Integer) + campo `scarceContentWords: List<ScarceContentWord>` en `LemmaAbsenceCorrectionContext`. Validado: 1 add / 2 mod / 0 conflicts. Cambio 100% aditivo: NO toca `CorrectionContext` ni el `CorrectionContextOverrideParser` (revision-domain) a nivel de contrato.
  why: analogo exacto a misplacedLemmas/suggestedLemmas. count/threshold son Integer (no int) para alinear con SuggestedLemma.lemmaCount/lemmaCountThreshold de F-SLEM, aunque R003b garantiza no-null. OJO nombre: la entrada del 2026-07-01 del analyst en este archivo dice `fragileContentWords`/"Fragile content words"; la REQUIREMENT.md viva y la instruccion del usuario dicen `scarceContentWords`/`ScarceContentWord` — se uso el nombre vivo/decidido por el usuario. Si algun stub viejo quedo con `fragile*`, es del @developer re-homearlo.
