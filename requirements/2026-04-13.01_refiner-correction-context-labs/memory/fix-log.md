# Fix Log

2026-07-22 — developer — fixtures viejos de LemmaAbsenceCorrectionContext/MisplacedLemmaContext solo necesitaban argumentos apendizados al final (outOfCatalogWords=List.of(), discount=null); ningun reordenamiento porque los campos nuevos son siempre los ultimos del constructor.
  why: el generador de Sentinel agrega campos nuevos al final del constructor por convencion en este proyecto, asi que el fix mecanico es siempre "agregar N args al final", nunca reinsertar en medio.

2026-07-22 — developer — outOfCatalogWords render usa `w.getObservedPos()` (no `w.getPos()`) para la key JSON "pos" y el "(pos)" de texto.
  why: OutOfCatalogWordContext modela el campo como `observedPos` (no `pos`), a diferencia de MisplacedLemmaContext/ScarceContentWord que usan `pos`; confundirlo rompe la compilacion.

2026-04-19 — test-writer — used setter-based AuditNode construction (not constructor-based) for J001 journey tests.
  why: LemmaAbsenceContextResolverTest already uses setters; the constructor in FRcslJ001JourneyTest only works if parent refs are separately wired, and the constructor-based approach is less flexible. Setter approach is the canonical pattern in this codebase.

2026-04-19 — test-writer — J001 path-2 used APPEARS_TOO_EARLY-only milestone diagnosis to trigger empty suggestedLemmas.
  why: R004b excludes APPEARS_TOO_EARLY; an empty absent-lemma list or a null diagnosis also works, but APPEARS_TOO_EARLY exercises the filtering logic more precisely.

2026-04-19 — test-writer — J002 ran with -Dsurefire.failIfNoSpecifiedTests=false to avoid failure in upstream modules.
  why: -pl audit-cli -am applies the test pattern to all modules; upstream modules (audit-domain) have no matching tests.

2026-07-01 — qa-tester — Cobertura directa scarceContentWords (Fase 2). R003b (2 tests) + R003c (2 tests) en LemmaAbsenceContextResolver (es quien estampa scarceContentWords, ver desc del campo en sentinel.yaml): inclusión count<threshold / exclusión count>=threshold + forma {lemma,pos,count,threshold}; lista vacía en los 3 casos de degradación (sin lemma-count / sin palabras bajo umbral / sin content-words). R008 (1 test) + R009 (2 tests) en GetCmd (superficie observable JSON/texto): scarceContentWords siempre presente en JSON ([] si vacía); sección "Scarce content words (do not remove)" en texto y "(none)" si vacía. Los ~11 tests R008/R009 previos NO tocan scarceContentWords → se agregan nuevos, no _change:modify (cada uno cubre un campo distinto; contaminar los viejos sería re-scoping deshonesto).
  why: memoria feedback_no_transitive_coverage — cada regla nueva exige test directo; el render de scarceContentWords no está cubierto por los tests de misplacedLemmas/suggestedLemmas.

2026-07-22 — qa-tester — Cobertura directa outOfCatalogWords (R003d/R003e). 4 tests, todos taggeados a los tags VIRGENES R003d/R003e (no R008/R009) para esquivar el bug hasMatchingTaggedTest del generador y porque R003d/R003e se observan en la salida:
  - DefaultCorrectionContextJsonMapper (commands, superficie JSON real donde toJsonMap arma el map; hoy NO serializa OOC): R003d (cada entry con lemma/pos/frequencyRank null/discount, caso sipps) + R003e (siempre presente, [] cuando vacio).
  - GetCmd (commands, superficie texto): R003d (render "lemma (pos) - rank..., discount...", "rank n/d" si null) + R003e (seccion presente con "(none)").
  why: R008/R009 ya tienen tests directos en GetCmd (cubiertos), pero NINGUNO toca OOC; R003d/R003e son reglas nuevas que EXIGEN test directo (no transitive coverage). Tags virgenes en ambas superficies → stubs se generaran sin el bug. Patch handwrittenTests-only (0 add / 2 mod). J001/J002 son flow y ya tienen testModule/testPackage → no se tocan. OJO: R003d/R003e aun NO estan en features de sentinel.yaml (analyst no corrio feature sync); la validacion paso via --requirement-folder contra REQUIREMENT.md. El JSON de OOC todavia no se implementa en el mapper → @developer debe agregarlo; hasta entonces los tests quedaran rojos por diseno (TDD).

2026-07-22 — test-writer — implementados los 4 stubs OOC (R003d/R003e): 2 en DefaultCorrectionContextJsonMapperTest (mapper.toJsonMap, verifica Map anidado {lemma,pos,frequencyRank,discount}) + 2 en GetCmdTest (texto: "Out-of-catalog words:" con "lemma (pos) - rank {r}, discount {d}" / "rank n/d" / "(none)"). Fixture "sipps" (frequencyRank null) + "Sheep" (rank 4450) para cubrir ambas ramas de R003d en un mismo test. LemmaAbsenceCorrectionContext ahora tiene 21 args (outOfCatalogWords es el ultimo, tras scarceContentWords); usado ese orden completo en los 4 tests nuevos. MisplacedLemmaContext ahora tiene 6 args (gano `discount:Double` nullable, F-LABS-R038) — usado el nuevo constructor con discount=null en el fixture R003e.
  why: `mvn test-compile -pl audit-cli` confirma que NINGUN error nuevo cae en las lineas de mis 4 tests; los ~500 errores restantes son preexistentes (constructores viejos de LemmaPlacementDiagnosis/MisplacedLemma en 15 archivos de journeys + LemmaAbsenceDetailedFormatterTest, y las llamadas viejas de 5-arg MisplacedLemmaContext/20-arg LemmaAbsenceCorrectionContext en el resto de GetCmdTest y en la linea 32 preexistente de DefaultCorrectionContextJsonMapperTest) — ninguno de mi autoria, no los toque (fuera de alcance, @developer/otra sesion). audit-cli NO compila end-to-end por ese motivo; mis 4 tests estan bien formados pero no se pudieron ejecutar con `mvn test` hasta que esos ~500 errores preexistentes se resuelvan.
