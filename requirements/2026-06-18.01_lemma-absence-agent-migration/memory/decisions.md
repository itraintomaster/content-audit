# decisions

2026-07-22 — analyst — Nuevo eval determinista bloqueante de no-regresión léxica = criterio #7 DENTRO de R007 (NO regla nueva); NO journeys nuevos, NO feature nueva.
  Evidencia: prueba live run 20260722T214253-b4c83c4a, tarea OOC-only (quiz A1 "My brother remitted money to his family.", past-simple-spelling-modifications). Mecánica nueva OK (curación+edición mínima, 7/7 evals bloqueantes viejos verdes) pero candidato final "My brother transferred money to his family." — `transfer` es B1/B2 → reemplazó fuera-de-catálogo por mal-ubicada, que el próximo audit re-flaggea. Causa raíz: cuando ninguna palabra curada colocaciona, el prompt permite "una del nivel que encaje" pero el modelo no VERIFICA nivel y NINGÚN eval corría el análisis léxico sobre el candidato.
  DECISIÓN DE UBICACIÓN: criterio #7 en R007, NO regla nueva. Precedente decisivo: #3 (longitud) también es determinista, reutiliza el cómputo real del audit vía CLI (`content-audit tokens`) y vive como ítem DENTRO de R007; #6 se agregó in-place en junio. "Single concept, multiple invariants" → R007 es la regla unificada del catálogo de evals; #7 comparte la maquinaria retry/best-effort (R008/R009) → pertenece al catálogo. Clasificación harness (VALIDATED), sin superficie JUnit, igual que #3/#6; NINGÚN handwrittenTest nuevo (qa-tester no afectado).
  Invariante #7: flags(candidato) ⊆ flags(original) sobre conjunto de LEMAS flaggeados (mal-ubicada-para-el-nivel ∪ fuera-de-catálogo). Los flags preexistentes que la edición no tocó NO bloquean (residuo inevitable no vuelve la tarea infalible); solo bloquea un lema flaggeado en el candidato ausente en el original. Al reprobar, el contexto de fallo del reintento nombra la palabra infractora con su nivel ("transfer es B1, el quiz es A1") → alimenta carry-forward (R010, sin cambios).
  DEPENDENCIA (consulta léxica del análisis): #7 debe usar el MISMO análisis léxico del audit (mismo tokenizador+catálogo+reglas de nivel), NO una réplica. Descripto funcionalmente en el Detail de R007: consulta determinista entrada (quizSentence, nivel) → salida lista de flags {lema, nivel esperado, tipo}. Análogo léxico de `content-audit tokens`. Forma concreta del comando y su eventual regla propia = decisión arquitecto/equipo → DOUBT-EVAL-LEXICO-CLI (nueva, ABIERTA; Opción A regla propia en la feature de `tokens` / Opción B detalle interno del harness).
  Cambios: R007 blockquote (+#7), R007 Detail (bullet determinismo #3+#7, pista de validación, bullet dependencia consulta léxica, "Conducta esperada (harness) — criterio #7" con ejemplo remitted→transferred vs remitted→sent), R002 Detail (fase evals corre también consulta léxica), R012 Detail (#3 y #7 fuera de la cota de "única consulta"), J001 `when` (#1,#2,#3,#5,#6 → +#7), Scope (bullet #7 en alcance + bullet forma-del-comando fuera de alcance/delegado), Context (subsección Evidencia prueba live). Glosario: "Catálogo de evals" extendido con no-regresión léxica; términos nuevos "Eval de no-regresión léxica"/LexicalNonRegressionEval (aliases no-regresión léxica, eval #7) y "Consulta léxica del análisis"/LexicalAnalysisQuery.
  `requirement validate` → [OK] (solo WARN benignos: alias-collision del token 'eval' + link-suggestions, mismo tipo que #6/Pragmatismo). `glossary validate` → PASSED. `feature sync --dry-run` → "in sync (8 journeys), No changes needed" (sin delta estructural; cuerpo de reglas/journeys vive solo en REQUIREMENT.md). NO corrí sync (no-op) ni toqué sentinel.yaml.
  why: cerrar el hueco que la prueba live expuso — el agente reemplazaba una palabra fuera de catálogo por una mal ubicada porque ningún eval verificaba el nivel léxico del candidato con el criterio real del audit.

2026-07-22 — analyst — DOUBT-EVAL-LEXICO-CLI RESUELTA por el usuario → Opción A.
  La consulta léxica del análisis (oración+nivel → flags {lema, nivel esperado, tipo}) va como CAPACIDAD PROPIA DEL CLI, con contrato observable y regla propia, en una FEATURE NUEVA DEDICADA (precedente F-CSLATDC). OTRO analyst la crea en paralelo — NO la creo yo.
  Cambios en FEAT-LASAG: doubt marcada [x] Opción A + bloque **Answer** (2026-07-22); bullet de dependencia del criterio #7 en R007 Detail reformulado — cita la consulta como "capacidad propia del CLI ... especificada en una feature dedicada aparte", #7 solo la consume como dependencia (sin nombrar arquitectura). NADA más tocado (Scope "fuera de alcance" sigue válido: la spec de la consulta queda fuera de ESTE requirement).
  `requirement validate` → [OK].
  why: instrucción del coordinador tras resolución del usuario.

2026-07-22 — analyst — Reglas nuevas R013/R014/R015 (palabras fuera de catálogo en generación); NO journeys nuevos, NO feature nueva.
  Evidencia: plan 2026-07-22T05-26-12, 308/1420 tareas LEMMA_ABSENCE son fuera-de-catálogo-only; el adaptador Java buildInputs (LemmaAbsenceAgentGenerator) formatea misplacedLemmas (o "none") + suggestedWords pero NO transmite outOfCatalogWords → el modelo ve "misplaced: none" y revisa a ciegas. El contexto ya expone outOfCatalogWords (F-RCLA-R003d/R003e, hecho hoy).
  R013 (AUTO_VALIDATED): los insumos de generación incluyen las palabras fuera de catálogo formateadas (lema + indicación de rango; "none" si vacío). Superficie observable = el ítem transmitido a la generación (análogo al de misplacedLemmas, que ya es Java-observable). Handwritten test pendiente @qa-tester.
  R014 (VALIDATED, harness): edición mínima toma como objetivo la UNIÓN misplaced ∪ fuera-de-catálogo; guía OOC = (1) typo evidente de palabra del nivel → ortografía correcta si es del nivel; (2) palabra genuina avanzada/rara → equivalente del nivel (pref. suggested/curated cuando colocacionan), mismas restricciones de colocación/objeto que misplaced. Sin superficie JUnit (conducta del nodo generate.md); clasificada como R007-R010.
  R015 (AUTO_VALIDATED, mitad observable): contexto con misplaced vacío + OOC no vacío = generación válida (emite 1 QuizCandidate, no cortocircuita); mitad harness = corrección dirigida por OOC. Handwritten test pendiente @qa-tester.
  Decisión NO-journey: la OOC-only tiene terminal idéntico a J001 (emite candidato); su invariante observable la cubre R015 vía handwrittenTest. Añadir journey duplicaría cobertura y forzaría sync+placement. Set de journeys intacto (J001-J007, J009). feature status FEAT-LASAG = OK (rules no requieren sync).
  requirement validate → [OK]; glossary validate → PASSED (solo WARN preexistentes de alias/links).
  why: pedido del usuario — cerrar el hueco de las 308 tareas OOC-only que el prompt del agente revisaba a ciegas.

2026-06-24 — analyst — Extensión de R007 con criterio #6 (reutilización de la respuesta finita); NO regla nueva, NO feature nueva.
  Catálogo de R007 (blockquote): agregado `> 6.` — bloqueante (cuando aplica): si la respuesta del hueco del quiz original es de un conjunto cerrado/finito (to be, contabilidad, existenciales, artículos, demostrativos, auxiliares, cuantificadores cerrados) Y el ejercicio admite seguir usándola, el candidato debe conservar la misma respuesta (misma categoría); N/A (positivo, no bloquea) si la respuesta del original es de vocabulario abierto o reutilizarla no daría ejercicio válido/natural.
  Detail de R007: "juicios cualitativos #1,#2,#4,#5" → "#1,#2,#4,#5,#6"; "Pista de validación" incluye #6 (caso aplica-y-bloquea + caso N/A); nueva línea "Conducta esperada (harness) — criterio #6" con ejemplo sadness uncountable→singular countable, matiz N/A, y matiz preferencia-al-agotar-reintentos vía R009 (best-effort: deja de ser gate duro, pasa a preferencia en elección del mejor candidato).
  Journey J001 (step `evaluar`, gate [F-LASAG-R007]): when "todos los evals bloqueantes (#1, #2, #3, #5)" → "(#1, #2, #3, #5, #6)". El set de journeys/reglas NO cambia (sigue 8 journeys / 12 reglas, mismos IDs/placements).
  Clasificación: #6 es harness-validated igual que el resto de R007 (R007 sigue VALIDATED, sin superficie JUnit). No toqué journeys ni handwrittenTests.
  Glosario (domain-glossary.yaml): definición de "Catálogo de evals de calidad" extendida con #6; agregado término "Reutilización de la respuesta finita"/FiniteAnswerReuseEval (patrón análogo a LengthWithinRangeEval/PragmatismEval; aliases eval #6, reutilización de la respuesta finita, reutilización de la respuesta, eval de reutilización — evité el alias bare `eval`).
  `requirement validate` → [OK] (solo WARN preexistentes de alias canónicos + 1 WARN nuevo benigno por el término #6, mismo tipo que Pragmatismo/Veredicto bloqueante). `feature sync --dry-run` → "in sync (8 journeys), No changes needed" (sin delta estructural; el texto de cuerpos de regla y de `when` vive solo en REQUIREMENT.md, no en sentinel.yaml — verificado por grep). NO corrí sync real (no-op) ni hand-edité sentinel.yaml (single-writer @architect).
  `sentinel verify` → FAILED por 2 drifts AJENAS (.sentinel/agents/analyst-interactive-v2/agent.yaml + nodes/grill.md, ambas untracked y preexistentes, sin relación con FEAT-LASAG/REQUIREMENT.md). Ningún archivo de FEAT-LASAG aparece en el drift.
  why: pedido del usuario — bloquear cuando la respuesta cerrada del original es reutilizable, pero al forzar la emisión obligatoria al agotar intentos sólo pesar como preferencia (no gate duro).

2026-06-23 — analyst — Corrección funcional del catálogo de evals R007 (ítems #1 y #3); el resto de R007 y todos los journeys/handwrittenTests intactos.
  #1: de "quizSentence es una oración con sentido" → "el quizSentence es correcto y bien formado PARA EL MISMO TIPO/MOLDE del quiz original". No todos los quizzes son oraciones (transformación, par palabra→forma, construcción por cues); el candidato debe respetar el tipo del original, no convertirlo siempre a oración.
  #3: de "no-regresión de length (length(candidate) ≥ length(original))" → "longitud del candidato DENTRO DEL RANGO ESPERADO (target range) del nivel"; sigue bloqueante y determinista, mismo analyzer de length del audit; ya NO compara contra el original (da igual más corto o más largo). #2/#4/#5 sin cambios.
  Detail de R007 actualizado en coherencia (baseline pre-corrección → rango esperado del nivel; "candidato más corto bloqueado" → "fuera de rango bloqueado"; agregado el caso "#1 bloquea por tipo de ejercicio").
  Glosario compartido (requirements/domain-glossary.yaml) actualizado en coherencia: término "Eval de no-regresión de longitud"/LengthNonRegressionEval → "Eval de longitud dentro de rango"/LengthWithinRangeEval (alias "eval #3" conservado; removidos "no-regresión de length"/"no-regresión de longitud", sin referencias activas restantes); "Catálogo de evals" y "Analizador de longitud" reescritos. Verificado 0 refs colgantes al término viejo fuera de baseline/applied-patches.
  `requirement validate` → [OK]; `feature sync --feature FEAT-LASAG --dry-run` → "in sync (8 journeys), No changes needed" (la edición no toca el set de journeys).
  why: el texto del catálogo quedó desalineado con el comportamiento corregido del agente (los quizzes no siempre son oraciones; lo que importa es el target range del nivel, no ser ≥ que el original).

2026-06-19 — architect — Runtime seam de sentinel-agent descubierto (incógnito central de F-LASAG).
  Punto de entrada de producción: `com.sentinel.agents.framework.runner.AgentRun.run(Node rootAgent, Map<String,String> inputs, ExecutionContext ctx): RunState`.
  Carga del grafo declarativo: `AgentRegistry.discover(startDir)` + `registry.load(nameOrAbbrev): Node` (o `AgentLoader.loadFromDirectory(.sentinel/agents/<name>)`).
  ExecutionContext (record) lleva: ChatModel (dev.langchain4j.model.chat.ChatModel), Map<String,Object> tools, gates, scripts, workDir, dispatcher (NodeRunners).
  Resultado: RunState.runStatus() == COMPLETED|HALTED; HALTED+HaltReason.ERROR es la falla; artifacts() = Map<String,ArtifactPointer>.
  Tool in-process: ExecutionContext.tools es Map<String,Object> de POJOs @Tool de langchain4j (builtins como ReadFileTool son objetos Java planos). NO obliga a subprocess: un módulo Java puede inyectar su propio tool object. -> la única tool de F-LASAG (suggested-lemmas) se expone como POJO @Tool in-process.
  why: el requirement declara el seam de runtime como la duda central; queda resuelto leyendo sentinel-agents.

2026-06-19 — architect — La tool NO es GetCommand.get (devuelve Integer/exit-code y vive en audit-cli, composition root).
  La semántica F-CSLATDC ya está como puerto reutilizable en refiner-domain.lemmasuggestion: SuggestedLemmaQueryPort / SuggestedLemmaQuerySession / SuggestedLemmaQuerySessionFactory (los usa hoy el backend interactivo lageninteractive). El tool object del agente delega ahí, sin duplicar F-CSLATDC ni invocar el verbo CLI.
  why: R001/R002 hablan de la semántica de la consulta read-only, no del verbo CLI literal; reusar el puerto evita acoplar revision-infrastructure a audit-cli.

2026-06-19 — architect — DOUBT-TRAZABILIDAD-AGENTE queda ABIERTA (no se resuelve en este patch, por pedido explícito).

2026-06-19 — analyst — Clasificación de Validation para reglas sin superficie JUnit (R007-R010).
  El DSL com.sentinel.dsl.ValidationStatus admite SOLO {VALIDATED, AUTO_VALIDATED, ASSUMPTION} (verificado por javap sobre el jar). No existe un valor para "validado por el harness de evals del propio agente / fuera del suite JUnit".
  Decisión: R007-R010 → VALIDATED, con nota explícita en metadata "(validada por el harness de evals del agente, no por el suite JUnit)". Razón: AUTO_VALIDATED implica cobertura por el suite JUnit, que aquí no existe (qa-tester ya rehusó taggearlas); VALIDATED = confirmación humana del usuario sobre la conducta del harness, es el valor más fiel disponible. El "por qué no AUTO_VALIDATED" queda en el Detail de cada regla.
  why: pedido explícito de usar la clasificación correcta del DSL y dejar explícito el porqué cuando no hay un valor específico.

2026-06-19 — analyst — `sentinel feature sync` es aditivo-only; NO poda journeys huérfanos.
  Sync agrega journeys nuevos a features[].journeys[] pero deja los que ya no están en REQUIREMENT.md con el aviso "will be pruned at next generate". La poda real (y por tanto la eliminación de los 9 testPackage refs colgantes a lagenopenai/lageninteractive) ocurre en `sentinel generate`, que regenera código/contratos/tests — fuera del lane del analyst. Escalado a @architect.
  why: el task esperaba que el sync eliminara los refs; el comportamiento real del CLI lo difiere a generate.

2026-06-19 — analyst — Formato de journeys discoverable: 1 heading `### Journey[...]` + 1 fence yaml single-entry por journey.
  El bloque combinado (9 journeys en un único fence `journeys:`) que tenía F-LASAG era leído como 0 journeys por el parser de `feature sync`. El formato estándar del proyecto (ver FEAT-CDIFF, FEAT-LAGEN) es un fence por journey bajo su heading. Reescrito; los 9 se registraron.
  why: regla operativa para futuros REQUIREMENT.md con múltiples flow-journeys en este proyecto.

2026-06-19 — analyst — J008 degradado a prosa (inconsistencia con R009; bloqueaba `sentinel generate`).
  F-LASAG-J008 ("eval no-regresión de length bloquea un candidato más corto") tenía único terminal result:failure, pero R009 (best-effort) garantiza que un veredicto de eval NUNCA propaga ProposalStrategyFailedException al consumidor — al agotar maxEvalRetries se emite el mejor candidato (success). El failure de J008 es inalcanzable en generate(); cualquier journey-test sería cobertura falsa. Removido del REQUIREMENT.md; escenario ya cubierto por R007 (eval #3) + nota agregada en su Detail. IDs restantes SIN renumerar: J001-J007, J009 estables (J009 NO pasa a J008) para no romper placement/traceability del patch QA.
  `sentinel feature sync` (aditivo-only) NO podó J008 de sentinel.yaml: lo flaggea "will be pruned at next generate". sentinel.yaml sigue con 9 (incl. J008) hasta que @architect corra `sentinel generate`. NO hand-edité sentinel.yaml (journeys derivados + single-writer @architect).
  why: pedido del coordinador; J008 contradecía R009 y rompía generate.

2026-06-19 — architect — Patch + TECH_SPEC entregados (no aplicados).
  Diseño: paquete interno lemmaabsenceagent reemplaza lagenopenai+lageninteractive. Seam público único LemmaAbsenceAgentGeneratorFactory. Adaptador LemmaAbsenceAgentGenerator implementa el puerto existente sin cambiar firma.
  Colaboradores package-private: AgentRuntimeLauncher (aísla AgentRun/AgentRegistry/ExecutionContext, devuelve RunState), SuggestedLemmaAgentTool (delega en SuggestedLemmaQuerySession, F-CSLATDC), AgentRuntimeErrorClassifier (RunState/Throwable -> LlmGenerationFailureCategory -> ProposalStrategyFailedException con categoría), AgentCandidateParser (RunState exitoso -> LemmaAbsenceGeneratorResponse; reemplaza al LemmaAbsenceResponseParser borrado con lagenopenai).
  why: el parser de respuesta vivía en lagenopenai (borrado); el adaptador del agente necesita su propio parser dentro del nuevo paquete — fix en la 2da ronda de propose (merge).
  TECH_SPEC.md: 9 fences, español.

2026-06-19 — architect — Limpieza de 2 orphans post-aplicación de F-LASAG (patch propuesto, NO aplicado).
  Orphan 1: pattern Factory de InteractiveLemmaAbsenceGeneratorFactory quedó colgando tras borrar su interface/impl/paquete.
  Orphan 2: descripción del paquete lagen seguía apuntando a 'lagenopenai' (borrado) → corregida a 'lemmaabsenceagent'.
  HALLAZGO (limitación Sentinel, verificada en código): el bloque `patterns` NO honra `_change: delete` por elemento. PatchMerger:112 y PatchApplier:151 hacen reemplazo TOTAL: `!incoming.patterns().isEmpty() ? incoming.patterns() : existing.patterns()`. Por eso el orphan se creó en el patch original (la lista entrante traía orphan-con-delete + nuevo, y el delete fue ignorado al reemplazar la lista verbatim).
  why: para retirar un pattern hay que enviar la lista COMPLETA de patterns deseados (sin el orphan), no un `_change: delete`. Usé --replace para arrancar limpio.

2026-06-19 — architect — Fix de conflicto langchain4j (blocker de compilación de revision-infrastructure).
  Causa raíz (verificada con mvn dependency:tree + jars en .m2): langchain4j-openai y langchain4j-core declarados en 0.36.2; el código nuevo de lemmaabsenceagent usa dev.langchain4j.model.chat.ChatModel (1.x), que llega transitivo desde sentinel-agents (langchain4j 1.13.0). Maven "nearest wins" → 0.36.2 directo gana; 0.36.x no tiene ChatModel (era ChatLanguageModel). No compila.
  Blast radius (verificado): único uso de langchain4j en producción = los 3 archivos nuevos de lemmaabsenceagent, todos con ChatModel. CERO usos de ChatLanguageModel/OpenAiChatModel/message-types 0.36.x en main (el código viejo lagenopenai/lageninteractive fue borrado). Deps 0.36.2 stale.
  Versión elegida: 1.13.0 para ambos aliases — matchea EXACTAMENTE lo que pulle sentinel-agents (sentinel-agents/pom.xml langchain4j.version=1.13.0; tree muestra langchain4j:1.13.0). Todos los jars 1.13.0 están en .m2. Elegir 1.13.0 evita re-introducir conflicto.
  Nota Maven Central Solr: el índice select solo muestra hasta 1.0.0 para ambos artefactos, pero el tree resuelto y los jars en .m2 prueban que 1.13.0 existe y resuelve (BUILD SUCCESS). El tree es ground truth, no el índice Solr.
  Cambios de provides 0.36→1.13: ChatLanguageModel (dev.langchain4j.model.chat) DESAPARECE en 1.13.0 → se reemplaza por ChatModel (mismo paquete). El resto (OpenAiChatModel+builder dev.langchain4j.model.openai; ChatMessage/SystemMessage/UserMessage/AiMessage dev.langchain4j.data.message; Response dev.langchain4j.model.output) conserva paquete.
