# Progress

2026-04-29 — analyst — REQUIREMENT.md drafted from scratch. Captures the LLM-backed adapter that replaces the lemma-absence-mvp stub. Locked decisions from user (LangChain4j in-process, Gemma via LM Studio default, single-shot, granular failure categories, behavioral-only prompt, strategy renamed to `lemma-absence-llm`). Open doubts: adapter module placement, exact strategy name. Validation pending.
  why: starting point for architecture phase; do not re-litigate scope choices already aligned upstream.

2026-04-30 — analyst — REQUIREMENT.md fully rewritten after user rejected the previous version for architectural bias. Removed all references to ports/adapters/composition root/Main.java/env vars/module names/library names ("LangChain4j", "ChatLanguageModel", "abstraccion de cliente"). Reframed every rule as observable behavior of `revise task <id>` from the operator's standpoint. Dropped doubts that were architectural (DOUBT-ADAPTER-MODULE-PLACEMENT, DOUBT-CLIENT-LIBRARY, DOUBT-CONFIG-SURFACE, DOUBT-CATEGORY-PROPAGATION, old DOUBT-STUB-RETENTION); kept DOUBT-PROVIDER-ID-FORMAT (functional) and reframed the canned-mode question as DOUBT-CANNED-MODE-AVAILABILITY without mentioning wiring. Architectural decisions still live in decisions.md; they are not lost, just out of REQUIREMENT scope.
  why: the REQUIREMENT is the WHAT, not the HOW; the architect must still own placement/library/config-surface decisions in TECH_SPEC.

2026-04-30 — analyst — REQUIREMENT.md reformatted to scan-first layout. Same 11 rules / 6 journeys / 2 doubts / 6 assumptions, no semantic change. Added TL;DR (Que/Por que), per-rule anchors + blockquote summaries with `<details>` for tables/justifications, inline markdown-link citations to F-LAPS / F-REVAPR / F-QSENT / F-RCLAQS rules, dedicated `## Alcance` (absorbing the old "Limitaciones de alcance" block + assumptions as a sub-section), and `## Referencias` annotated list at the bottom. Validation: [OK].
  why: previous draft predates the scan-first format; rewrite keeps semantics intact while making the document readable in the first two sections.

2026-04-30 — architect — Architectural patch + TECH_SPEC.md landed for FEAT-LAGEN. New module `revision-infrastructure` (LangChain4j adapter, allowedClients=[audit-cli]), `revision-domain.strategy` promoted to public + extended with LlmGenerationFailureCategory + CannedLemmaAbsenceQuizCandidateGenerator, audit-cli.bootstrap extended with LAGEN env-var resolvers. Validation: 11 fences, all subset of patch. Next step: user adds `langchain4j-openai` alias + provides[] block to sentinel.yaml dependencies, then `sentinel generate` and onward to test-writer / developer.
  why: handoff to user before implementation can start.

2026-04-30 — architect — Patch regenerated to be self-contained. Reported Sentinel Bug 01 (patches cannot declare `dependencies:`) was fixed in sentinel-core, so the patch now carries `langchain4j-openai` + `langchain4j-core` aliases inside `dependencies:`. Type list verified empirically by downloading the JARs (`mvn dependency:get`) and inspecting them with `jar tf` + `javap` — no longer guessed. API flavor A (legacy `generate(List<ChatMessage>)`) confirmed; provides[] adjusted accordingly (drops ChatRequest/ChatResponse, adds ChatMessage+Response). TECH_SPEC.md updated to record both the alias split and the API-flavor decision. Patch revalidated: 10/2/0/0, no architectural surface change. Manual `sentinel.yaml` edit no longer needed before `sentinel generate`.
  why: closes the manual side-channel from the prior session and removes guessed type names from the dependency declaration.

2026-04-30 — qa-tester — First-pass test design for FEAT-LAGEN: 58 handwrittenTests across 6 implementations (LemmaAbsenceMvpStrategy + 5 in lagenopenai) tagged to existing rules R001..R011, plus testModule/testPackage on the 6 flow-based journeys (revision-infrastructure / com.learney.contentaudit.revisioninfrastructure.lagenopenai for package-private visibility to all collaborators). Patch validated 0/14/0/0. Two escalations queued for the user to forward: (a) @analyst — three new rules needed for canned generator + lagen mode selector + config atomicity (CannedLemmaAbsenceQuizCandidateGenerator, DefaultLagenModeResolver, DefaultLagenConfigResolver have no rule constraining them today). (b) @architect — three contract closures needed for classifier mapping table, providerIdFor format, and factory numeric defaults (test names asume them but cannot pin them tight). Both messages written to escalations/. Patch follow-up pending both confirmations.
  why: rule-driven testing requires every traceability tag to point at an existing rule; underspecified contracts force generic test names that the developer can interpret either way. Hand off to peers, do not force "closest rule" tagging.

2026-04-30 — analyst — Respondida la escalation de @qa-tester: REQUIREMENT.md tiene ahora 14 reglas (R001..R014). Nuevas: F-LAGEN-R012 (canned idempotente), F-LAGEN-R013 (seleccion de modo, default dinamico), F-LAGEN-R014 (validacion atomica de configuracion al startup). DOUBT-CANNED-MODE-AVAILABILITY cerrada (Opcion B). Validacion: [OK]. Sin nuevas asunciones. Mapping para que @qa-tester re-emita el patch: CannedLemmaAbsenceQuizCandidateGenerator -> R012, DefaultLagenModeResolver -> R013 + R014 (modo no reconocido), DefaultLagenConfigResolver -> R014 (atomicidad) y opcionalmente R008 (cobertura de las perillas declaradas).
  why: handoff de vuelta al QA; las reglas estan listas para tagear.

2026-04-30 — architect — Cerre 3 contratos under-specified flaggeados por @qa-tester (escalations/2026-04-30_qa-to-architect.md). Patch correctivo: 2 additions (LagenDefaults record, InvalidProviderIdException) + 16 modifications + 0 deletions + 0 conflicts. Re-emitidas via patch correctivo todas las piezas top-level que el merge habia borrado: audit-cli completo (LagenMode, LagenModeResolver, LagenConfigResolver, 2 exceptions), enum LlmGenerationFailureCategory, atributos top-level de revision-infrastructure (description, dependsOn, allowedClients, uses). TECH_SPEC.md actualizado a 14 fences (+2: defaults + classifier mapping; el contract de providerId va inline como modelo InvalidProviderIdException dentro del fence Definir LagenConfig). Tambien re-emitidos los handwrittenTests del factory (3 nuevos: preserves case, preserves whitespace, +2 raises InvalidProviderIdException), del classifier (10 reemplazando los 3 viejos genericos: 3 unreachable + 2 timeout + 2 auth + 1 root cause regex + 1 LLM_OTHER + 1 never null), y removidos via _change: delete los viejos genericos que QA queria refinar.
  why: handoff de vuelta a @qa-tester con contratos pinneados, nombres de tests ya prepoblados en el patch (QA puede ahora simplemente borrar viejos restantes y usar los nuevos), y los tipos LagenDefaults/InvalidProviderIdException disponibles para asertar.
