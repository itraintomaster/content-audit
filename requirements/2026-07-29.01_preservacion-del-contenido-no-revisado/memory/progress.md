# Progress — FEAT-RPRES

2026-07-29 — analyst — REQUIREMENT.md creado con 3 reglas (R001 preservacion por complemento, R002 vacio!=ausente, R003 radio de impacto), 2 journeys de flujo (J001 R001+R002, J002 R003) y 4 doubts abiertos. `requirement validate` [OK].
  why: dano medido en db/ el 2026-07-29 — 2.658 quizzes revisados sin tipo de formulario/etiqueta/nombre y con peso 0,0; 2.690 partes de enunciado sin su texto vacio; 8.829 quizzes no revisados intactos. El dano nace al construir el elemento revisado, no en el camino de persistencia.

2026-07-29 — analyst — Cerrado con las resoluciones del usuario: 5 reglas (R004 propagacion del titulo, R005 reparacion) y 3 journeys (J003 titulo). 3 doubts RESOLVED, DOUBT-ALCANCE-DECLARADO OPEN para @architect. `requirement validate` [OK]; `feature list` = FEAT-RPRES 5 reglas / 3 journeys.
  why: ver decisions.md. Se edito tambien requirements/2026-04-20.02_lemma-absence-proposal-strategy (R013 y R014); FEAT-LAPS sigue en 19 reglas / 5 journeys, ningun id cambio.

2026-07-29 — analyst — Proximo paso: `sentinel feature sync` (las rules bajo features[] de sentinel.yaml son derivadas, no parcheables) y despues @architect: (a) resolver DOUBT-ALCANCE-DECLARADO, (b) evaluar si la preservacion y la reparacion necesitan contrato nuevo o son comportamiento interno del paso de sustitucion + escritura.

2026-07-29 — analyst — [obsoleto, dejado como historia] Proximo paso: resolver con el usuario los 4 doubts (ALCANCE-DECLARADO, TITULO-DENORMALIZADO, REPARACION, LAPS-ENUM). Solo REPARACION puede agregar una regla; los otros tres afectan alcance/trazabilidad, no la formulacion de R001-R003. Despues: @architect evalua si hace falta contrato nuevo o es comportamiento interno del paso de sustitucion + escritura.

2026-07-29 — qa-tester — Diseno de cobertura FEAT-RPRES: 32 handwrittenTests propuestos en 2 patches (24 en el folder de RPRES, 8 en el de 2026-04-20.02_lemma-absence-proposal-strategy porque el validador rechaza trazabilidad cross-feature en un patch requirement-scoped).
  why: el alcance lexico de parseOnto/DefaultCorrectionScope/deriver esta constrenido literalmente por F-LAPS-R013/R014 reformuladas, no por R001; anclarlo a RPRES habria sido tag forzado.

2026-07-29 — qa-tester — Journeys J001/J002/J003 ya tienen testModule=audit-cli / testPackage=com.learney.contentaudit.journeys y stubs en disco; no hace falta patch de placement.

2026-07-29 — qa-tester — Proximo paso: (a) @architect responde la escalacion de granularidad de form.sentenceParts, (b) usuario aplica los 2 patches, (c) sentinel generate, (d) @test-writer.

2026-07-29 — analyst — Cerrados los 2 doubts de qa-tester: nueva regla F-RPRES-R006 (partes nuevas) y reformulacion de F-KTLR-R007. `requirement validate` [OK] en RPRES y KTLR; `feature list` = FEAT-RPRES 6 reglas / 3 journeys, FEAT-KTLR 11 / 2 (sin cambios de conteo).
  why: ver decisions.md. Dos impactos para @qa-tester: J001 gana un path, y el test de F-KTLR-R007 en DefaultCourseElementLocatorTest necesita mover su testigo de staleness del titulo al contenido.

2026-07-29 — qa-tester — Segunda ronda: 3 handwrittenTests para F-RPRES-R006 (la unica regla que quedaba en 0) propuestos en requirements/2026-07-29.01_.../architectural_patch.yaml. Patch tests-only, anidado en modules[course-domain].packages[quizsentenceengine] — el guard --as=qa-tester lo rechaza por el anidado, lo aplica el usuario.
  why: R006 no agrega contratos (decision del arquitecto): parseOnto(quizSentence, base) ya recibe el FormEntity original entero.

2026-07-29 — qa-tester — Proximo paso: (a) usuario aplica el patch y verifica los 3 nombres en sentinel.yaml, (b) sentinel generate, (c) @test-writer implementa los 3 stubs + corrige los cuerpos de DefaultLemmaAbsenceProposalDeriverTest:117 (title -> sentences) y DefaultCourseElementLocatorTest ~231 (borrar la asercion de title; el testigo de contenido ya esta).

2026-07-29 — analyst — F-RPRES-R006 reformulada (2 invariantes numerados + acotacion al estado que declara la clase), journey J001 nodo partes_nuevas_completas realineado, bullet nuevo en Fuera de alcance, FEAT-QSENT agregada a Referencias, DOUBT-PARTE-NUEVA-COMPLETA RESOLVED. `requirement validate` [OK]; `feature status FEAT-RPRES` = OK (ningun id cambio, no hace falta sync).
  why: ver decisions.md. Sin impacto en paths de J001 ni en los 3 handwrittenTests ya aplicados (siguen validos: los 2 de homogeneidad caen bajo el invariante 1, el del hueco sin hermana bajo el invariante 2, cuyo enunciado ahora coincide literalmente con lo que el test assertea).

2026-07-29 — analyst — Proximo paso para @qa-tester: agregar el 4to test de R006 —parte de TEXTO FIJO sin hermana de su clase— asserteando texto con valor propio Y respuestas ausentes (no pobladas). Es la unica combinacion de R006 sin cobertura y la que originaba la ambiguedad.

2026-07-29 — qa-tester — Tercera ronda: 4to handwrittenTest de F-RPRES-R006 (parte de TEXTO FIJO sin hermana de su clase) propuesto en requirements/2026-07-29.01_.../architectural_patch.yaml. Patch tests-only sobre DefaultQuizSentenceConverter; anidado en packages[quizsentenceengine], asi que lo aplica el usuario sin `--as=qa-tester` (mismo procedimiento que la ronda anterior). Los 3 tests ya aplicados quedan intactos.
  why: unica combinacion de R006 sin cobertura tras la Opcion B de DOUBT-PARTE-NUEVA-COMPLETA; es donde divergian las dos lecturas de la regla.

2026-07-29 — qa-tester — Proximo paso: (a) usuario aplica el patch y verifica el nombre en sentinel.yaml, (b) sentinel generate, (c) @test-writer implementa los 4 stubs de R006.

2026-07-30 — test-writer — Implementados los 3 stubs de F-RPRES-R006 que SI llegaron a generarse en DefaultQuizSentenceConverterTest (course-domain). Los 3 pasan por parseOnto(quizSentence, base): homogeneidad de hueco (compara texto de la CLOZE agregada vs la que ya existia), homogeneidad de texto fijo (options en el mismo estado ausente entre las partes viejas y la nueva), e invariante 2 del hueco sin hermana (base sin ningun CLOZE, texto vacio presente + respuestas pobladas en la parte agregada). Los 3 compilan y fallan hoy con UnsupportedOperationException porque DefaultQuizSentenceConverter.parseOnto aun no esta implementado — esperado en TDD.
  why: mecanismo (de que parte se copia) queda fuera de la asercion a proposito, per decision del architect de que R006 no fija mecanismo.

2026-07-30 — test-writer — HALLAZGO: el 4to test de R006 (parte de TEXTO FIJO sin hermana) declarado en sentinel.yaml:3654 NO tiene stub en el .java pese a `sentinel generate` corrido dos veces (reporta "Skipping test stubs (up to date)" para ese archivo). Es un gap de generacion, no de mi alcance de implementacion (no puedo crear metodos de test nuevos). Los otros 3 R006 + los 4 de FEAT-LAPS (7 en total, el pedido original) SI se generaron y quedaron implementados.
  why: comparado sentinel.yaml (4 entradas F-RPRES-R006) contra el .java (3 metodos @Tag F-RPRES-R006) linea por linea; sentinel-report.yaml marca las 4 como implemented:true, lo cual es inconsistente con el archivo real — posible bug del generador o del report, a investigar por quien mantiene el CLI de Sentinel.

2026-07-30 — test-writer — Proximo paso: (a) investigar por que `sentinel generate` no crea el stub del 4to test de R006 (posible bug de dedup en el generador), (b) una vez generado, implementar ese stub simetrico al de "hueco sin hermana" pero para TEXTO FIJO (base sin ninguna parte TEXT, quizSentence agrega una; assertear texto con valor propio Y options ausente/null), (c) @developer implementa parseOnto en DefaultQuizSentenceConverter para que estos 7 tests (y F-LAPS-R013/R014) pasen en verde.

2026-07-30 — test-writer — Implementados los 4 stubs de DefaultProposalDecisionServiceTest (R001 abort por preservationCheck con violaciones -> PRESERVATION_VIOLATED sin side-effects; R004 align invocado en rama KNOWLEDGE con el resultado de replace() y el resultado de align() persistido; R004 reciproco: alignQuizTitles NUNCA se invoca en rama QUIZ; R003 el pipeline replace->align se encadena desde el curso recien cargado del repository, nunca reconstruido desde el snapshot congelado). buildService() paso a inyectar tambien preservationCheck (mock) porque el constructor real ya lo exige segun sentinel.yaml/describeComponent — confirmado con `mvn compile` (el unico error de main es el ya documentado: Factory sin el 5to arg, mas DefaultCorrectionScope con return type viejo Set<String>).
  why: DefaultProposalDecisionService.requiresInject ya trae preservationCheck (PreservationCheck) como 5to parametro; los tests viejos no lo pasaban y no hubieran compilado contra el contrato actual.

2026-07-30 — test-writer — Implementados los 3 stubs de DefaultCourseElementLocatorTest (R004 alignQuizTitles setea el titulo de TODAS las quizzes del knowledge al label vigente; R004 deja todo atributo del quiz salvo el titulo intacto; R003 alignQuizTitles no toca quizzes de otro knowledge). Usan CourseElementLocator.alignQuizTitles(course, knowledgeId) real (sin mocks), estilo setter identico al resto del archivo.

2026-07-30 — test-writer — Aplicadas las 2 correcciones de cuerpo pendientes: (a) DefaultCourseElementLocatorTest F-KTLR-R007 — borrada la asercion de title (resultQuiz.getTitle()=="v2 corregido"), queda solo el testigo de contenido (sentences); confirmado contra el enunciado actual de F-KTLR-R007 (nota explicita: "el testigo correcto es el contenido"). (b) DefaultLemmaAbsenceProposalDeriverTest:117 (test F-LAPS-R012) — cambiado afterQuiz.getTitle() por afterQuiz.getSentences().get(0), porque F-LAPS-R014 reformulada preserva el titulo (no es alcance lexico) y la oracion plana vive en QuizTemplateEntity.sentences.
  why: ver decisions.md de ambos requirements (2026-07-06.01 y 2026-04-20.02).

2026-07-30 — test-writer — Sin escalaciones. Proximo paso: @developer implementa PreservationCheck/CorrectionScope real (Set<ScopedField>), el 5to arg del constructor en DefaultProposalDecisionServiceFactory, CourseElementLocator.alignQuizTitles, y el guard de preservacion dentro de DefaultProposalDecisionService.approve().

2026-07-30 — test-writer — Implementados los 6 stubs de DefaultPreservationRepairTest (R005 x5, R002 x1). Sin escalaciones: las 6 trazas se sostienen contra el requirement tal cual estan tagueadas.
  why: el arbol NO compila (a proposito — DefaultCorrectionScope.java y DefaultProposalDecisionServiceFactory.java son produccion pendiente de FEAT-RPRES, ajena a este archivo); no se toco ningun archivo de produccion. Detalle de los 6 tests y fixtures en el resumen de la sesion.

2026-07-30 — test-writer — Proximo paso: @developer implementa DefaultPreservationRepair (+ actualiza DefaultCorrectionScope al nuevo Set&lt;ScopedField&gt; y DefaultProposalDecisionServiceFactory al nuevo parametro PreservationCheck) para que estos 6 tests + el resto del arbol compilen y pasen.

2026-07-30 — test-writer — Implementados los 9 stubs de DefaultPreservationCheckTest (revision-domain.preservationengine): R001 x2, R002 x3, R004 x2, R003 x2. Mockea CorrectionScope (Mockito, ya disponible en el pom); construye CourseEntity/KnowledgeEntity/.../QuizTemplateEntity con setters (no-arg + set*) y FormEntity/SentencePartEntity/ScopedField/PreservationViolation/CourseElementSnapshot con su constructor de todos los campos (confirmado por convencion ya usada en DefaultCourseElementLocatorTest y DefaultQuizSentenceConverterTest). No se toco produccion.
  why: compilar el modulo completo sigue fallando por DefaultCorrectionScope.java (retorna Set<String> en vez de Set<ScopedField>) y DefaultProposalDecisionServiceFactory.java (constructor viejo sin PreservationCheck) — ambos son stubs de @developer a proposito, ajenos a este archivo de test.
2026-07-30 — test-writer — Ningun test escalado: los 9 verifican exactamente la regla de su @Tag (R001 preservacion por complemento, R002 los 3 estados, R003 radio de impacto incluida la ganancia de un knowledge, R004 ambas mitades — bloqueo del titulo en correccion lexica y no-violacion en la alineacion por etiqueta).

2026-07-30 — test-writer — Implementados los 7 stubs restantes en audit-cli: FRpresJ001JourneyTest (3 paths), FRpresJ002JourneyTest (2 paths), FRpresJ003JourneyTest (2 paths) y RepairCmdTest (1 test). Ningun test escalado.
  why/diseno: CourseElementLocator y las impls de preservationengine (DefaultCorrectionScope/Check/Repair) son package-private en revision-domain sin factory publica para CourseElementLocator ni para QuizSentenceConverter's collaborators (QuizSentenceSerializer/Parser/PlainSentenceDeriver tambien package-private) — asi que las 3 journeys NO pueden invocar `replace`/`alignQuizTitles`/`parseOnto` real desde audit-cli. Patron adoptado: construir a mano el arbol "after" (representando el efecto ya verificado por unit tests en revision-domain/course-domain), persistir+recargar con `FileSystemCourseRepository` real (CourseValidator no-op, ya que la validez estructural F-COURSE es ajena a esta feature) y gatear con `PreservationCheck` real via `new DefaultPreservationFactory().createCheck()` (unica clase publica del paquete preservationengine). J001 usa DiagnosisKind.LEMMA_ABSENCE (scope confirmado en DefaultCorrectionScopeTest: sentences/translation/sentenceParts[]/text[TEXT]/options[CLOZE]) para los paths 1 y 2; path 3 (R006) no tiene gate de PreservationCheck (decision de architect: R006 es completitud de contenido nuevo, no preservacion) asi que se assertea homogeneidad directamente sobre el FormEntity recargado. J002 arma 2 knowledges (A con 2 quizzes, B con 1) para distinguir "mismo knowledge" vs "otros knowledges". J003 usa KNOWLEDGE_TITLE_LENGTH (path-1, titulos_alineados) y LEMMA_ABSENCE (path-2, titulo_intacto). RepairCmdTest sigue el patron mockeado estandar (no journey): mock de PreservationRepair+CourseRepository, ArgumentCaptor sobre save(), y re-stub de load() para simular "recargar desde la fuente de datos" tras la reparacion.
  compilacion: no se pudo correr `mvn compile` end-to-end (revision-domain sigue sin compilar por los 2 stubs de @developer ya documentados arriba); course-domain + course-infrastructure SI compilan solos y se uso ese build para confirmar setters/constructores de CourseEntity/QuizTemplateEntity/FormEntity/SentencePartEntity y el constructor de FileSystemCourseRepository. El resto de las firmas (PreservationFactory/Check/Repair, RepairReport, CourseElementSnapshot, ScopedField, PreservationViolation, RepairCmd) se verificaron via `describeComponent` + grep de las declaraciones de clase (visibilidad, constructor) en el .java, sin leer cuerpos de metodo.
  proximo paso: @developer implementa CorrectionScope/PreservationCheck/PreservationRepair/PreservationFactory(constructor de Factory ya es public, no requiere cambio) + RepairCmd + el 5to arg de DefaultProposalDecisionServiceFactory para que todo el arbol compile y estos 7 tests corran.

2026-07-30 — developer — Implementado `DefaultQuizSentenceConverter.parseOnto(quizSentence, base)` en course-domain. `mvn -o test -pl course-domain`: 50/50 verde (43 en DefaultQuizSentenceConverterTest, incluye los 9 tests de F-LAPS-R013/R014, F-RPRES-R006 x4 y el invariante de F-QSENT-R023). Sin escalaciones.
  why: revision-domain (otro developer en paralelo) sigue sin compilar por trabajo ajeno; course-domain se verifico aislado con `mvn -o test -pl course-domain`.

2026-07-30 — developer — Implementados los 4 contratos de produccion pendientes en revision-domain (`preservationengine` + `engine`), sin tocar ningun test ni archivo `@Generated` de contrato (interfaces/modelos). `mvn -o test -pl revision-domain -am` = 218/218 verde, incluye los 5 archivos objetivo: DefaultCorrectionScopeTest 4/4, DefaultPreservationCheckTest 9/9, DefaultPreservationRepairTest 6/6, DefaultProposalDecisionServiceTest 18/18, DefaultCourseElementLocatorTest 7/7. Ningun test preexistente (FEAT-LASAG/FEAT-KTLR/FEAT-PIPRE/FEAT-CDIFF/etc.) se rompio.
  que: DefaultCorrectionScope (firma corregida a Set<ScopedField>, tabla fija LEMMA_ABSENCE+QUIZ y KNOWLEDGE_TITLE_LENGTH+KNOWLEDGE, resto vacio); nuevo helper package-private `CourseElementFieldDiff` (comparador por complemento compartido entre check y repair, walkea los leaves de QuizTemplateEntity/KnowledgeEntity/FormEntity/SentencePartEntity); DefaultPreservationCheck.verify (walkea todo el curso, detecta ganancia/perdida de knowledges y quizzes, aplica el scope solo al elemento revisado + excepcion de `quizTemplates[].title` cuando el knowledge revisado lo declara); DefaultPreservationRepair (busca el artifact APPROVED mas antiguo por nodeId+QUIZ, difea intacto-vs-actual con el mismo comparador, restaura via setters in-place solo en modo repair — inspect nunca muta — y marca unrepairable via heuristica de ABSENT en los 4 campos de form cuando no hay contraparte); DefaultPreservationFactory (crea Check/Repair con `new DefaultCorrectionScope()`); DefaultProposalDecisionServiceFactory (5to arg = `new DefaultPreservationFactory().createCheck()`); DefaultProposalDecisionService.approve (encadena replace -> alignQuizTitles (solo rama KNOWLEDGE) -> preservationCheck.verify(courseAntes, courseDespues, elementAfter, diagnosisKind) -> aborta con PRESERVATION_VIOLATED sin persistir nada si hay violaciones); DefaultCourseElementLocator.alignQuizTitles (reconstruye el knowledge target seteando el title de cada quiz al label vigente, resto de la jerarquia intacta).
  why: ver decisions.md y fix-log.md para el detalle de diseno (por que el comparador es compartido, por que el heuristico de degradacion en Repair, por que se encadena desde el curso recien cargado).
  sin escalaciones.

2026-07-30 — developer — Implementado `RepairCmd` (audit-cli.commands) + wiring en `Main`. Ultima pieza pendiente de FEAT-RPRES: `mvn -o test -pl audit-cli` = 352/352 verde (RepairCmdTest 1/1, FRpresJ001/J002/J003 3+2+2, SentinelArchitectureTest 5/5). Sin escalaciones.
  que: RepairCmd sigue el patron de DeleteCmd/ApproveCmd — `@Parameters` resource (solo acepta "course") + coursePath (arity 0..1, via CoursePathResolver) y `@Option --dry-run`. dryRun=true llama `preservationRepair.inspect(course)` y NO persiste; dryRun=false llama `.repair(course)` y hace `courseRepository.save(report.getCourse(), path)`. Main construye `preservationRepair` via `new DefaultPreservationFactory().createRepair(revisionArtifactStore)` (constructor injection real, ninguna dependencia con `new` directo salvo la factory misma, que es el patron ya usado para las demas factories del proyecto) y registra el subcomando "repair".
  why: PreservationFactory.createRepair(RevisionArtifactStore) es la unica costura publica del motor (preservationengine es package-private); revisionArtifactStore ya estaba wireado en Main desde antes (Step 6) para el resto de los comandos.
  nota JDK25/JaCoCo: al correr la suite completa via reactor (-am) el build se corta en `revision-infrastructure` (ya documentado, preexistente). Workaround usado para verificar audit-cli en aislamiento: `mvn -o install -DskipTests -Dexec.skip=true` (instala toda la reactor sin correr tests) seguido de `mvn -o test -pl audit-cli -Dexec.skip=true` (sin -am, usa los jars ya instalados). Ademas se observaron stack traces de JaCoCo/Mockito ("Unsupported class file major version 68/69") impresos como ruido en varios tests preexistentes de audit-cli (no solo revision-infrastructure) que NO hacen fallar ningun test (352/352 verde, BUILD SUCCESS) — Mockito parece seguir funcionando sin instrumentacion cuando JaCoCo no puede instrumentar. Vale la pena que alguien evalue subir jacoco-maven-plugin o bajar el JDK usado para correr Maven si esto empieza a doler mas.
  FEAT-RPRES queda con las 3 implementaciones (course-domain, revision-domain, audit-cli) completas y verdes.

2026-07-30 — developer — Fix quirurgico post-hoc en DefaultLemmaAbsenceProposalDeriver (revision-domain.engine): dejaba de escribir la oracion plana en `title` (ademas de `sentences`), pisando el titulo del quiz revisado. Bloqueaba en la practica todo `approve()` de una correccion lexica (rechazado por PRESERVATION_VIOLATED via preservationCheck.verify, F-RPRES-R004/F-LAPS-R014). Ver fix-log para el detalle; `mvn -o test -pl revision-domain` sigue en 218/218, ningun test tocado.
  proximo paso: ninguno pendiente conocido para este bug puntual; el resto de FEAT-RPRES ya estaba completo (ver entradas de developer arriba).

2026-07-30 — test-writer — Implementado el stub de composicion DefaultProposalDecisionServiceTest#shouldApplyTheApprovalOf...RealLemmaabsenceDeriver (F-RPRES-R001): arma la cadena real deriver->approve()->PreservationCheck real (solo mockea artifactStore/courseRepository/elementLocator/refinementPlanStore; el service se construye con `new DefaultPreservationFactory().createCheck()` en vez del mock de clase). ROJO, y asi debe quedar: `mvn -o test -pl revision-domain` = 220/220 con exactamente esta 1 falla (outcome=PRESERVATION_VIOLATED, esperado APPROVED_APPLIED).
  HALLAZGO: el fix quirurgico de la entrada anterior solo corrigio `title`. Diagnostico con una clase main de un solo uso (borrada, no committeada) confirmo que DefaultLemmaAbsenceProposalDeriver.derive() sigue llamando a `converter.parse(candidate.quizSentence)` en vez de `converter.parseOnto(candidate.quizSentence, before.quiz.form)` — pese a que parseOnto YA esta implementado en course-domain (ver entrada developer 2026-07-30 "parseOnto... 50/50 verde"). Consecuencia real: form.kind/form.incidence/form.label/form.name y el texto vacio de la parte CLOZE quedan ABSENT en vez de copiarse de elementBefore — exactamente el mismo vector de dano de la tabla de F-RPRES-R001 (2.658 quizzes), solo que en la mitad del pipeline que el fix quirurgico no toco. Con el scope real (DefaultCorrectionScope, no el hardcodeado en tests) esto dispara 5 PreservationViolation reales, y approve() devuelve PRESERVATION_VIOLATED para TODA correccion lexica via el deriver real, no solo en el caso donde el titulo se pisaba.
  no ajustado: no se toco produccion ni se debilito la asercion (regla F3 del test-writer). El test queda documentando el gap.
  proximo paso: @developer cambia DefaultLemmaAbsenceProposalDeriver.derive() para llamar `quizSentenceConverter.parseOnto(candidate.quizSentence, before.getQuiz().getForm())` en vez de `parse(...)`, para que el form conserve kind/incidence/label/name y el texto vacio de sus partes desde elementBefore (F-LAPS-R014 reformulada). Tras ese fix este test (y presumiblemente cualquier aprobacion real de LEMMA_ABSENCE) debe pasar a APPROVED_APPLIED sin cambiar la asercion.

2026-07-30 — developer — CIERRE de la causa raiz identificada arriba: DefaultLemmaAbsenceProposalDeriver linea ~40 cambiada de `parse(candidate.getQuizSentence())` a `parseOnto(candidate.getQuizSentence(), baseForm)`, con `baseForm = beforeQuiz.getForm() != null ? beforeQuiz.getForm() : new FormEntity()` (fallback para el caso sin form previo — no se agrego rama de error nueva, se sigue usando el mismo manejo de excepciones existente). El test de composicion de la entrada anterior (F-RPRES-R001, `shouldApplyTheApprovalOf...RealLemmaabsenceDeriver...`) paso de ROJO (PRESERVATION_VIOLATED) a VERDE (APPROVED_APPLIED) sin tocar su asercion. Verificado: revision-domain 220/220, audit-cli 352/352 (via install -DskipTests + test -pl sin -am), course-domain 50/50. Ningun test preexistente tocado ni roto.
  why: cierra el mismo vector de dano que afecto a 2.658 quizzes (form.kind/incidence/label/name + texto vacio de CLOZE fabricados desde cero por `parse()`), ahora en la unica llamada de produccion que quedaba sin usar `parseOnto`. FEAT-RPRES queda sin deuda conocida sobre este punto.
