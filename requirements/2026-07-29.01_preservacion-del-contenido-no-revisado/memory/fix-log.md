# Fix Log — FEAT-RPRES

2026-07-30 — test-writer — DefaultPreservationRepairTest: RevisionArtifactStore y CorrectionScope se mockean con `Mockito.mock(...)` plano (sin `@ExtendWith(MockitoExtension.class)`), stubbeando solo `artifactStore.list()` (no hay metodo listByNodeId; `list()` es el unico que da acceso a artifacts de cualquier plan, necesario para un walk de curso completo) y `scope.changeableFields(LEMMA_ABSENCE, QUIZ)`.
  why: evita UnnecessaryStubbingException de MockitoExtension en modo estricto mientras el mecanismo interno de DefaultPreservationRepair no existe todavia (TDD); sigue el patron ya usado en LemmaAbsenceMvpStrategyTest/RecursiveNodeFieldDifferTest de este mismo modulo.

2026-07-30 — test-writer — Fixture de restauracion usa la tabla concreta de F-RPRES-R001 (form.kind/incidence/label/name) como los 4 atributos degradados, no form.sentenceParts[].text.
  why: el propio decisions.md del arquitecto documenta que el runtime check NO cubre la degradacion del text de una parte CLOZE (limitacion de granularidad del scope declarado), asi que anclar el fixture de PreservationRepair a esos 4 campos es lo unico verificable sin asumir un mecanismo de deteccion no especificado por el contrato.

2026-07-30 — test-writer — Test del huerfano (R002) usa CERO artifacts en el store, no un artifact con elementBefore ya degradado.
  why: "ninguno huerfano" en decisions.md usa "huerfano" exactamente para el caso de cero artifacts por nodeId; es la lectura literal de RepairReport.unrepairable ("Ids de elementos degradados sin contraparte integra disponible en los artifacts de revision").

2026-07-30 — test-writer — 3 de los 4 stubs de F-RPRES-R006 en DefaultQuizSentenceConverterTest (course-domain) implementados y compilando; fallan hoy con UnsupportedOperationException (parseOnto sin implementar en produccion, esperado en TDD).
  why: cada test usa converter.parseOnto(quizSentence, base) con fixtures donde el quizSentence agrega partes que el base no tenia, para poder observar homogeneidad (invariante 1) o el estado "sin hermana" (invariante 2) sin depender del mecanismo interno de copia.

2026-07-30 — test-writer — FRpresJ001/J002/J003JourneyTest y RepairCmdTest (audit-cli) no pueden usar `new DefaultCourseElementLocator()` ni `new DefaultQuizSentenceConverter(new QuizSentenceSerializer(), ...)`: ambas clases (y sus collaborators QuizSentenceSerializer/QuizSentenceParser/PlainSentenceDeriver) son package-private, y no hay factory publica para CourseElementLocator (solo para PreservationCheck/PreservationRepair via PreservationFactory).
  why: confirmado con `grep "^class\|^public class"` sobre los .java reales antes de escribir el fixture — evita el intento fallido de instanciarlas desde otro paquete. Patron adoptado: construir el arbol "after" a mano (representa el efecto ya cubierto por las unit tests de esas clases en su propio modulo) y usar solo los puertos publicos: CourseRepository (real, FileSystemCourseRepository con CourseValidator no-op) y PreservationCheck (real, via `new DefaultPreservationFactory().createCheck()`, unica clase public de preservationengine).

2026-07-30 — test-writer — J001 path-3 (F-RPRES-R006) no llama a PreservationCheck.verify: el architect documento explicitamente que el check NO cubre R006 (agregar partes cae dentro del alcance declarado, es completitud de contenido nuevo, no preservacion). La aserción es directa sobre el FormEntity recargado (misma clase, mismo estado de `options`, texto propio no vacio).
  why: evita inventar un gate que el contrato no declara; ver decisions.md 2026-07-29 "PreservationCheck TAMPOCO cambia".

2026-07-30 — developer — Algoritmo de parseOnto: por posicion (mismo indice, mismo kind) primero; si el nuevo largo/kind no tiene contraparte en base, se busca la primera parte CLOZE en base (cualquier posicion) como respaldo; si base no tiene ninguna CLOZE, texto queda "" (estado declarado por la clase). TEXT no necesita merge: options siempre null via parser, ya es el estado correcto por F-QSENT-R003. form.kind/incidence/label/name siempre copiados enteros de base.
  why: mecanismo queda deliberadamente fuera de las reglas (R006 no lo fija); position-first+sibling-fallback satisface las 9 aserciones sin pinnear de cual parte exacta se copia.

2026-07-30 — developer — DefaultPreservationCheck/DefaultPreservationRepair comparten un helper package-private `CourseElementFieldDiff` (comparador leaf-por-leaf de QuizTemplateEntity/KnowledgeEntity/FormEntity/SentencePartEntity, con clasificacion ABSENT/EMPTY_PRESENT/VALUE_PRESENT y exencion por `ScopedField(path, elementKind)`), en vez de dos implementaciones independientes.
  why: decisions.md del architect es explicito en que check y repair deben ser "la misma regla hacia adelante y hacia atras" (comparten `CorrectionScope`); con el mismo comparador, Repair literalmente llama `diffQuiz(intacto, actual, scope)` — la MISMA funcion que usa Check para detectar violaciones — y usa la lista resultante para restaurar en vez de solo reportar. Evita que las dos reglas diverjan silenciosamente con el tiempo.

2026-07-30 — developer — `classify()` trata 0.0/0 (numeros) y false (boolean) como ABSENT, no como VALUE_PRESENT.
  why: los 4 tests de F-RPRES-R002 exigen que form.incidence 1.0→0.0 se reporte como "valor propio reemplazado por el default de su tipo" (ABSENT), no como un cambio de valor legitimo; es la unica forma de que `Objects.equals` + `classify` distinga "vacio con valor propio" de "vacio por defecto" para tipos primitivos sin wrapper Optional.

2026-07-30 — developer — DefaultPreservationRepair.looksDegraded(quiz) (heuristica para el caso SIN contraparte historica) solo mira los 4 campos de `form` (kind/incidence/label/name), no todo el quiz.
  why: son los unicos 4 campos que decisions.md documenta como el vector de dano real (2658 quizzes), y son los unicos para los que "ausente" nunca es un estado legitimo por diseno; generalizar la heuristica a cualquier campo (p.ej. topicName o code, legitimamente null en muchos quizzes sanos) haria unrepairable a quizzes nunca tocados.

2026-07-30 — developer — DefaultPreservationRepair aplica la restauracion con setters in-place sobre el mismo grafo de objetos del `course` recibido (QuizTemplateEntity/FormEntity son JavaBeans mutables), no reconstruyendo el arbol inmutable como hace CourseElementLocator.
  why: evita duplicar toda la logica de reconstruccion milestone->topic->knowledge->quiz (que ademas es package-private en el paquete `engine`, no accesible desde `preservationengine`); como `RepairReport.course` en modo inspect debe ser "el mismo objeto de entrada" sin modificar, alcanza con NO llamar los setters en ese modo y sí llamarlos en modo repair.

2026-07-30 — developer — Bloqueante encontrado post-implementacion: `DefaultLemmaAbsenceProposalDeriver` (revision-domain.engine) seguia escribiendo la oracion plana derivada en `title` ADEMAS de `sentences` al construir `afterQuiz` — `newTitle = plainSentences.get(0)` pasado como titulo (linea ~82 vieja) en vez de `beforeQuiz.getTitle()`. Con `DefaultProposalDecisionService.approve()` ahora corriendo `preservationCheck.verify(...)` (F-RPRES-R004/F-LAPS-R014), toda aprobacion de una correccion lexica de lemma-absence violaba preservacion del titulo y era rechazada con PRESERVATION_VIOLATED — pipeline revise→approve muerto en produccion. Fix: preservar `beforeQuiz.getTitle()` sin cambios; eliminada la variable `newTitle` (quedaba sin uso); actualizado el comentario que decia "copy all fields except title, form y translation" (obsoleto). `mvn -o test -pl revision-domain` = 218/218 verde (sin cambios).
  why: DefaultLemmaAbsenceProposalDeriverTest fue corregido en una sesion anterior para assertear `sentences` en vez de `title` (ver entrada de test-writer arriba, 2026-07-30 "cambiado afterQuiz.getTitle() por afterQuiz.getSentences().get(0)"), pero como produccion seguia escribiendo AMBOS campos el test seguia pasando pese al bug — la suite no lo detecta porque DefaultProposalDecisionServiceTest arma propuestas a mano (no usa el deriver real). Ningun test tocado.

2026-07-30 — qa-tester — Cierre del hueco de composicion: 1 handwrittenTest nuevo sobre DefaultProposalDecisionService tagueado F-RPRES-R001 (aprobar una correccion lexica cuyo elementAfter viene del deriver REAL termina en curso guardado, sin violaciones de preservacion). Patch en requirements/2026-07-29.01_.../architectural_patch.yaml (0 adds, 1 mod); guard `--as=qa-tester` lo rechaza por el anidado en packages[engine] — lo aplica el usuario.
  why: el bug de produccion (deriver escribiendo el titulo -> PRESERVATION_VIOLATED en toda aprobacion lexica) vivia en la INTERACCION deriver+guard; las dos piezas tenian tests propios y verdes. FRpresJ003 path-2 gatea R001+R004 pero su implementacion arma el arbol "after" a mano (por visibilidad package-private), asi que tampoco ejercita el deriver real. Un handwrittenTest es la unica via: los paths de journey son autogenerados.
  tag: R001 y no R004 — el propio REQUIREMENT dice, en el detalle de R004, que pisar el titulo con la oracion corregida "no es excepcion a R003: es una violacion de F-RPRES-R001, porque el titulo no es objeto de una correccion de ejercicio". Ademas R001 declara explicitamente que la verificacion es de punta a punta y que ninguna etapa intermedia —"construir la propuesta" incluida— puede descartar informacion: el deriver ES esa etapa. No hizo falta pedir regla nueva a @analyst.
  cableado viable: DefaultQuizSentenceConverter.create() y DefaultPreservationFactory son publicas, y deriver + decision service viven ambos en revision-domain.engine, asi que el test puede armar la cadena real con solo los stores mockeados.
  ojo: DefaultProposalDecisionServiceTest YA tiene un @Tag("F-RPRES-R001"), asi que `sentinel generate` no materializa el stub (bug conocido de hasMatchingTaggedTest).

2026-07-30 — test-writer — El fixture del test de composicion (deriver real + PreservationCheck real) usa exactamente los mismos valores de form/title/instructions que DefaultLemmaAbsenceProposalDeriverTest.buildElementBefore(), envueltos en un CourseEntity completo (root->milestone->topic->knowledge->quiz) construido con el patron no-arg+setters de DefaultPreservationCheckTest.courseOf(), para poder pasarle courseBefore/courseAfterReplace reales a `PreservationCheck.verify`.
  why: reusar el fixture exacto del deriver evita divergencias de interpretacion de la DSL de FEAT-QSENT entre archivos de test; reusar el builder de arbol de DefaultPreservationCheckTest evita adivinar la forma real de CourseEntity/KnowledgeEntity/TopicEntity/MilestoneEntity/RootNodeEntity (todas construidas con constructor vacio + setters, no con el constructor de todos los args).

2026-07-30 — test-writer — El test queda ROJO a proposito: expone que DefaultLemmaAbsenceProposalDeriver sigue llamando `converter.parse(quizSentence)` en vez de `converter.parseOnto(quizSentence, before.form)`, pese a que parseOnto ya esta implementado en course-domain. Diagnostico obtenido con una clase `main` de un solo uso en el mismo paquete (compilada, ejecutada y borrada sin commitear) que llamo al deriver real + `DefaultPreservationFactory().createCheck()` fuera de JUnit para inspeccionar las PreservationViolation reales: form.kind/incidence/label/name pasan a ABSENT y `form.sentenceParts[1].text` tambien.
  why: aislar el diagnostico de la ejecucion normal de JUnit evito reportar un falso "bug de Mockito/JaCoCo" (F5 de AGENTS.md) y confirmo que la falla es de produccion, no del fixture.
