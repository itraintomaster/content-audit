code: FEAT-QICOR
description: |
  Restaura el nombre descriptivo del test de F-QICOR-R004 sobre DefaultCandidateAssessor
  (el nombre corto era solo para aislar por que el generador no emitia su stub; la causa
  no es el nombre — ver fix-log 2026-08-11).
modules:
- name: revision-domain
  packages:
  - name: candidatecriteriaengine
    implementations:
    - name: DefaultCandidateAssessor
      handwrittenTests:
      - name: should evaluate every criterion before deciding
        _change: delete
      - name: "should submit the candidate to the whole catalog and hand back one\
          \ verdict per criterion, instead of stopping at the first failing one"
        traceability:
          feature: FEAT-QICOR
          rule: F-QICOR-R004
