# Progress — FEAT-QICOR

2026-08-04 — analyst — Escrito el REQUIREMENT.md inicial: 8 reglas (R001–R008), 2 journeys de flujo (J001 con 3 desenlaces, J002 con 2), 6 dudas abiertas. `requirement validate` = [OK] (solo warnings de alias del glosario, preexistentes en todo el repo); `feature list` = FEAT-QICOR 8 reglas / 2 journeys.
  why: la feature cierra el corte que F-QINST-R017 dejó explícito ("la corrección automática es alcance futuro"); parte del diagnóstico con violaciones+evidencia que F-QINST-R003 ya registra.

  Estado: FEAT-QICOR es id nuevo — falta `sentinel feature sync` (lo corre el usuario) para que entre en sentinel.yaml#definitions.
  Próximo paso: revisión humana de las 6 dudas (ninguna bloqueante para arquitectura) y después @architect. Las decisiones que cambian cuánto se paga —DOUBT-REINTENTOS, DOUBT-PRESUPUESTO-CORRECCION, DOUBT-COSTO-VERIFICACION— conviene medirlas antes de fijarlas.

2026-08-04 — analyst — Agregada F-QICOR-R009 (revalidación previa del original) y ajustados R006 y R008 para convivir con su desenlace. También: nueva subsección de Contexto "El diagnóstico puede haber envejecido" con la medición de los 1814 ejercicios, 2 bullets de Alcance, 3 dudas nuevas, J001 con el nodo `revalidar_original` + terminal `diagnostico_caido` (4 paths ahora), J002 con el reporte de desenlaces separados. `requirement validate` = [OK]; `feature list` = FEAT-QICOR **9 reglas / 2 journeys**.
  why: hueco real —ver decisions.md—; R005 sólo miraba el corregido y nada revalidaba el original.

  Próximo paso sin cambios: revisión humana de las 9 dudas y después @architect. Sigue faltando `sentinel feature sync` (id nuevo).

2026-08-04 — developer — Implementados los 28 stubs de producción (audit-domain 4, refiner-domain 1 escrito a mano, revision-domain 12, revision-infrastructure 9, audit-infrastructure 1, audit-cli 3) + Main.java wiring completo (candidate criteria catalog, compliance checker, generador+juez de agente, run store/runner/formatter). `mvn -o compile` limpio en los 6 módulos tocados; test suites existentes (revision-domain 256, revision-infrastructure 27, audit-infrastructure 29) verdes, incluyendo handwrittenTests pre-escritos que no había visto hasta correrlos (QuizInstructionReviserTest, DefaultQuizInstructionCorrectionRunnerTest, DefaultCandidateAssessorTest, los 4 CriterionTest).
  why: arquitectura ya aplicada (ARCH-QICOR-001/002/003); trabajo era 100% implementación.

  Próximo paso: revisar y correr `mvn -o test -pl audit-cli` completo (test-compile de audit-cli no verificado al cierre de esta sesión, corriendo en background). Falta wiring de `ReviseInstructionsCmd` como subcomando picocli invocable (ver decisions.md — necesita patch de @architect para externalImplements Callable&lt;Integer&gt;).

2026-08-04 — developer — `mvn -o test -pl audit-cli -am` completo confirmado VERDE (370 tests, incluye los 6 de FQicorJ001/J002) tras el fix de STALE en `DefaultRevisionEngine`. Corrección del coordinador aplicada: `serializeQuiz` ahora delega en `serializeParts` (una sola línea) en vez de duplicar el algoritmo — la salida no cambió (ambos cuerpos eran idénticos carácter por carácter), confirmado con `mvn -o test -pl audit-domain` = 414 tests verde, incluyendo `DefaultQuizInstructionSubjectBuilderTest` (paridad build/buildFromView). Javadocs de `serializeQuiz` y `serializeParts` actualizados para reflejar la delegación real.
  why: la razón de haber promovido el seam era que el render viva en un solo lugar; con la copia, un cambio futuro a `form.sentences` podía tocar uno y no el otro.

  Pendiente: @architect va a aplicar un patch para que `ReviseInstructionsCmd` declare `externalImplements: java.util.concurrent.Callable<Integer>`. Cuando avise, falta registrarlo como subcomando picocli en Main.java (`cmd.addSubcommand("revise-instructions", ...)`, con `@Command`/`@Option` para `--plan`/`--max`/`--course-path`) y verificar `revise-instructions --plan <id> --max N` de punta a punta (journey J002 completo depende de esto para ser alcanzable desde la CLI).

2026-08-04 — developer — ARCH-QICOR-004 aplicado (`ReviseInstructionsCmd` ya declara `externalImplements: Callable<Integer>`). Registrado como subcomando picocli (`cmd.addSubcommand("revise-instructions", ...)` en Main.java, junto a `revise`), con `@Option --plan` (required), `--max` (opcional), `-c/--course-path`. Javadoc de la clase reescrito (ya no dice que no hay entrada de Callable en sentinel.yaml).
  why: sin registro, J002 (la corrida acotada) quedaba escrita pero inalcanzable — la mitad de R008 sin superficie invocable.

  Nota de regeneración: al aplicarse ARCH-QICOR-004, `sentinel generate` volvió a resetear la firma de clase de `QuizInstructionContextResolver` (perdió el `implements CorrectionContextResolver` escrito a mano, aunque conservó los métodos) — reaplicado. Este es el costo aceptado ya documentado (ArchUnit no cubre esa relación); cualquier regeneración futura probablemente lo vuelva a resetear y hay que revisarlo.

  Verificación de punta a punta DE VERDAD (no mock, no unit test): sandbox aislado (`--workdir` + copia descartable de `audit-cli/src/test/resources/fixtures/tiny-course`, nunca la fixture real ni `db/english-course`), juez real vía `claude-cli` (sin credenciales explícitas, funciona en este entorno). `analyze` con un quiz con violación real introducida a propósito (agreement: "reads"→"read") + 2 quizzes con diagnóstico fabricado a mano como "no cumple" en el AuditReport (`scores["quiz-instruction"]` + `diagnoses.quizInstructionDiagnosis`) mientras el contenido real seguía cumpliendo → `plan` generó 3 tareas QUIZ_INSTRUCTION → `revise-instructions --plan <id> --max 2` (LAGEN_MODE=canned) dio en UNA sola corrida real: 1 NO_LOGRADA (criterio nombrado: LENGTH_IN_RANGE, porque el milestone fixture "Basics" no parsea como CefrLevel — comportamiento correcto, no bug) + 1 DIAGNOSTICO_CAIDO (juez real revalida y dice que cumple, tarea marcada STALE y persistida) + 1 NO_INTENTADA nombrada por id (`[task-007]`). Segunda corrida sin `--max` confirmó el fallback al default de config (tope=20). `git status` limpio en `db/` y en la fixture real — nada tocado fuera del sandbox.
  why: PROPUESTA (candidato aprobado) no se pudo demostrar en vivo sin gastar tiempo/costo en generación real vía LLM (canned mode devuelve el original sin cambios a propósito); las otras tres categorías + "no intentada nombrada" quedaron confirmadas con datos reales, no mockeados. FQicorJ001/J002 (mocks controlados) ya cubren el cuarto caso (PROPOSED) en verde.

  `mvn -o test` (suite completa, sin -pl) corriendo en background para el cierre pedido por el coordinador.

2026-08-04 — qa-tester — Diseño de cobertura: 42 handwrittenTests propuestos (sin aplicar) en `architectural_patch.yaml` del requisito. Reparto: R001 2, R002 2, R003 4, R004 14, R005 5, R006 5, R008 6, R009 4. R007 sin handwrittenTest: lo cubren los gates de J001 (aplicar_correccion / rechazo).
  why: R004 son cuatro criterios de aceptación por cinco criterios de no-regresión; el caso (c) —flag léxico preexistente NO bloquea— quedó como test propio en LevelVocabularyCriterion porque es el que evita que la feature vuelva incorregible a la mayoría del curso.

  Bloqueante para @test-writer: `QuizInstructionContextResolver` (refiner-domain) está declarado sin `implements: [CorrectionContextResolver]` y sin requiresInject; el .java generado es una clase vacía. Los 2 tests de R002 no tienen método al que llamar hasta que @architect lo cierre.
  Dudas abiertas registradas para el usuario: cláusula de traducción de R003 sin test; "una tarea caída consume tope" (DOUBT-REVALIDACION-PRESUPUESTO, assumption A) sin regla que lo exija; catálogo activo reducido cuando falta un evaluador, sin regla.

2026-08-04 — test-writer — Implementados 3 de los 4 stubs asignados: `DefaultQuizInstructionSubjectBuilderTest#...criterionOfTheAuditItself` (R005, audit-domain), `LedgerBackedQuizInstructionComplianceCheckerTest#shouldAskTheJudgeAgain...` (R009, audit-domain), y los 3 tests de `AgentCandidateJudgmentEvaluatorTest` (R004, revision-infrastructure). Compilan con JDK 25; los dos de audit-domain corren en VERDE contra producción ya implementada (buildFromView y revalidate ya existen y son correctos); los 3 de revision-infrastructure corren en ROJO esperado (`evaluate()` sigue con `UnsupportedOperationException`, fase de developer pendiente).
  why: R005 se verificó comparando `getContent()` de `build(quizNode)` vs `buildFromView(view)` sobre el MISMO quiz (no "que ambos anden"); R009 se verificó distinguiendo `resolveForced` de `resolve` vía Mockito.verify + payloads distintos (stale vs fresh), no solo el resultado final; R004 verifica NOT_EVALUABLE≠FAILED≠PASSED explícitamente en su propio test, no colapsado en los otros dos.

  NO implementado (en esa pasada): los 2 tests de R002 en `QuizInstructionContextResolverTest` (refiner-domain) — bloqueado, confirmado vía `describeComponent`: `implements: []`, `requiresInject: []`. Sin SendMessage disponible en esta sesión; escalación entregada como texto al orquestador/usuario para reenviar a @architect. Ver decisions.md para el detalle del pedido.

2026-08-04 — test-writer — Desbloqueado por el coordinador: `QuizInstructionContextResolver` ya tiene cuerpo (resolve/resolveWithIndex/supports) escrito A MANO, sin declarar `implements: [CorrectionContextResolver]` en sentinel.yaml — corrección importante al diagnóstico anterior, ver decisions.md. Implementados los 2 tests de R002 en `QuizInstructionContextResolverTest.java` siguiendo el patrón exacto de `SentenceLengthContextResolverTest`/`KnowledgeTitleContextResolverTest` (mismo paquete, helpers de árbol AuditNode locales, `sut.resolve(report, task)`). Los 4 archivos de FEAT-QICOR asignados a este test-writer quedan COMPLETOS: los 2 de R002 corren en VERDE (resolve() ya implementado), los 2 de audit-domain en VERDE, los 3 de revision-infrastructure en ROJO esperado (evaluate() pendiente de @developer). `mvn -pl refiner-domain test` completo también verde (sin regresión).

2026-08-04 — test-writer — Implementados los 17 stubs de `revision-domain/.../candidatecriteriaengine/` (paquete internal): `ScopedEditCriterionTest` (4, R003), `LengthInRangeCriterionTest` (2, R004 criterio 1), `LevelVocabularyCriterionTest` (3, R004 criterio 2), `DistinctnessCriterionTest` (3, R004 criterio 3), `InstructionComplianceCriterionTest` (4, R005), `DefaultCandidateAssessorTest` (3 de R004 + 1 de R006). Los 6 archivos compilan contra los tipos generados reales (verificado con `javac` aislado, ver nota de bloqueo abajo); todo ROJO esperado por `UnsupportedOperationException` (fase developer pendiente).
  why: R004 caso (c) (flag léxico preexistente no bloquea) y R003 sin fragmentos señalados (cualquier cambio reprueba) fueron los dos casos que el pedido marcó como fáciles de invertir; ambos fixtures se armaron explícitamente para eso. `CriterionOutcome.NOT_EVALUABLE` se afirmó con `assertEquals` puntual (nunca colapsado con FAILED) en `InstructionComplianceCriterionTest` y en `DefaultCandidateAssessorTest`. Los fixtures de longitud/vocabulario/distinción usan contenido cue-aware ("(eat)", "(go)") aunque las cues no son objeto directo de ningún assert.

  Bloqueo de compilación DEL MÓDULO detectado y NO mío para arreglar: `revision-domain` main source no compila hoy (`mvn -pl revision-domain compile` falla) — `engine/QuizInstructionReviser.java` importa `com.learney.contentaudit.revisiondomain.quizinstruction.QuizInstructionProposalStrategy`, pero el archivo real está en `com.learney.contentaudit.revisiondomain.QuizInstructionProposalStrategy` (paquete raíz, sin subpaquete `quizinstruction`) — desajuste de paquete, git status lo muestra `??` (untracked, WIP ajeno). También hay un test preexistente roto en `refiner-domain` (`DispatchingCorrectionContextResolverTest` — constructor con 3 args vs 4 requeridos) y otro en `revision-domain/engine/DefaultRevisionEngineFactoryTest` (arg-count de `RevisionEngineConfig`). Ninguno de los tres es de mi lote; verifiqué mis 6 archivos compilando en aislamiento con `javac -sourcepath ... ` contra los `.java` reales (no via `mvn test-compile`, que arrastra los tres archivos rotos de arriba).

2026-08-04 — test-writer — Implementados los 7+8 stubs de `QuizInstructionReviserTest` y `DefaultQuizInstructionCorrectionRunnerTest` (`revision-domain/.../engine/`): R001 2, R006 3, R009 2 (Reviser); R008 6, R006 1, R009 1 (Runner). `mvn -pl revision-domain -am compile` YA compila (el import roto de `QuizInstructionReviser.java` que anotó la sesión anterior fue corregido por @developer entretanto); `QuizInstructionReviser.java` pasó de stub a 239 líneas implementadas. Corrí ambos archivos con `javac` aislado + `junit-platform-launcher` (evita el `DefaultRevisionEngineFactoryTest` roto, ajeno, que sigue bloqueando `mvn test-compile` del módulo entero): los 7 tests de `QuizInstructionReviserTest` corren en VERDE contra el Reviser ya implementado; los 8 de `DefaultQuizInstructionCorrectionRunnerTest` corren en ROJO esperado (`run()` sigue con `UnsupportedOperationException`, 38 líneas, fase developer pendiente).
  why: los cuatro desenlaces (PROPOSED/NOT_CORRECTED/DIAGNOSIS_STALE/NOT_ATTEMPTED) se separaron en tests propios para no repetir el error que R008/R009 previenen; en el runner usé stubbing secuencial de `revisionEngine.revise(...)` (`thenReturn(a, b, c)`) en vez de asumir qué tarea específica cae en qué bucket, para no acoplar el test al orden de iteración interno.

  NO tocado (fuera de los 2 archivos asignados): el roto de `DefaultRevisionEngineFactoryTest` (mismo síntoma que ya loggeó la sesión anterior, sigue sin resolverse) — bloquea `mvn test-compile` a nivel módulo pero no es de mi lote.

2026-08-04 — test-writer — Implementados los cuerpos de los 4 paths de `FQicorJ001JourneyTest` (audit-cli) y los 2 paths de `FQicorJ002JourneyTest` (audit-cli). J001 path-1 = diagnostico_caido tratado como `result: success` (NO fracaso): revalida R009 "ya cumple", ningún candidato se pide, tarea queda `RefinementTaskStatus.STALE`. Paths 2/3/4 pasan por obtener_candidato (gates R004/R005) y terminan en aplicar_correccion / rechazo / sin_correccion. J002 cubre tramo_acotado (tope=2 sobre 3 tareas, `notAttempted` nombra la que quedó afuera) y todas_intentadas (tope=10 sobre 3 tareas con desenlaces MIXTOS: A→PROPOSED, B→NOT_CORRECTED con LENGTH_IN_RANGE nombrado, C→DIAGNOSIS_STALE con `attempts=0`).
  why: `candidateAssessor` y `quizInstructionComplianceChecker` (campos directos de `RevisionEngineConfig`, análogos a `validator`/`contextResolver` en el wiring de FKtlrJ001) se mockean en vez de wirear los 6 `CandidateCriterionEvaluator` reales: su corrección propia (R004/R005/R006) ya la cubren los handwrittenTests dedicados de `DefaultCandidateAssessor` (sesión anterior); el journey verifica el ORDEN/EFECTO de la orquestación (R001, R003, R007, R008, R009), no la medición interna de cada criterio.

  BLOQUEO reconfirmado, pero NO es el mismo de antes: `QuizInstructionContextResolver.java` (refiner-domain) es HOY otra vez una clase vacía (`public class QuizInstructionContextResolver {}`, sin `implements`, sin métodos). Releyendo fix-log.md (entrada del propio 2026-08-04): esto NO es una regresión de `sentinel generate` ni un hueco de sentinel.yaml — el patrón establecido en este proyecto es que `implements CorrectionContextResolver<X>` se escribe A MANO en el .java (nunca en el DSL, ver fix-log línea 15); lo que pasa es que ese paso manual todavía no se hizo para esta clase en particular (es simplemente otra pieza de producción sin implementar, igual que `DefaultCandidateAssessor`/`DefaultQuizInstructionCorrectionRunner`/etc.). NO es un `underspecified_contract` ni requiere escalar a @architect. Para no bloquear J001/J002 con esto (ni depender de una clase todavía sin cuerpo), ambos journeys wirean su propio `CorrectionContextResolver<CorrectionContext>` de prueba (test double, igual que un mock) en vez de instanciar `QuizInstructionContextResolver` — R002 sigue cubierta por sus propios handwrittenTests dedicados (ya implementados en sesión anterior), no por este journey.

  Compilación: `mvn compile -pl audit-cli -am` falla HOY por motivos ajenos a mis 2 archivos — `Main.java:412` (constructor de `DispatchingCorrectionContextResolver` con 3 args vs 4 requeridos, composición root de @developer sin terminar) y `ReviseCmd.java:224` (switch sobre `RevisionOutcomeKind` no exhaustivo, faltan `DIAGNOSIS_NOT_SUSTAINED`/`NO_ACCEPTABLE_CANDIDATE`) — ambos archivos `M` en git status, WIP de otra sesión en paralelo. Verifiqué mis 2 archivos aislados con `javac` directo contra las clases YA compiladas de course-domain/audit-domain/refiner-domain/revision-domain/etc. (`mvn compile` de esos módulos sale limpio) + jars de terceros: compilan sin error ni warning propio. Ambos en ROJO esperado una vez el módulo completo compile (candidateAssessor/complianceChecker/registry/runner siguen con `UnsupportedOperationException` en varios puntos, fase developer pendiente).

2026-08-04 — developer — Dos arreglos de cableado pedidos tras probar FEAT-QICOR E2E por CLI, ambos verificados y verdes:
  (1) `get tasks --diagnosis QUIZ_INSTRUCTION -f json` no traía `correctionContext`. Causa: `GetCmd.needsCorrectionContext()` sólo listaba SENTENCE_LENGTH/LEMMA_ABSENCE/KNOWLEDGE_TITLE_LENGTH — QUIZ_INSTRUCTION faltaba, así que `resolveContext` nunca se llamaba (la clave ni aparecía, no aparecía vacía). El resolver (`QuizInstructionContextResolver.resolve`) ya arma el contexto completo con violations — no era un problema de resolución. Fix: agregada la rama a `needsCorrectionContext`, más render de texto (`printQuizInstructionContextText`, violations con constraint/evidence/explanation numeradas) y de JSON (`DefaultCorrectionContextJsonMapper.buildQuizInstructionContextMap`, violations como lista de objetos {code,constraint,evidence,explanation}).
  (2) Corridas de agente ignoraban `--workdir`. Traceado más allá de `Main.java:367`: el runtime (`DefaultAgentGraphRunner`) resuelve `runsBase`/agent-dir con prioridad config > `inputs["project_root"]` > CWD. quiz-instruction-corrector y knowledge-title-agent SÍ pueblan `project_root` por-llamada con `baseDir` (correcto, no tocado). Pero el juez (`quiz-instruction-validator`, wireado en Main.java:367 y reusado tanto por `analyze` como por la revalidación R009) y el evaluador de juicio de candidato (`candidate-judgment`, vía `DefaultCandidateJudgmentEvaluatorFactory`) NUNCA pueblan `project_root` en sus inputs (los suyos son un set fijo y declarado — el del juez está protegido por el contrato de estabilidad de huella de F-QINST-R009, cinco keys, ninguna de más). Fix: en vez de agregar `project_root` a esos inputs (tocaría el contrato protegido), se fija `agentsBaseDir`/`runsBaseDir` explícitos desde `baseDir` en el `AgentGraphRunnerConfig` de ambos sitios — la config explícita gana la prioridad y el input nunca hace falta. `DefaultCandidateJudgmentEvaluatorFactory` pasó de runner `static` con config vacía a runner de instancia construido con `baseDir` (mismo patrón de constructor con overload que `DefaultQuizInstructionAgentGeneratorFactory`/`DefaultKnowledgeTitleAgentGeneratorFactory`).
  Asimetría preexistente NO tocada (fuera de alcance, ajena a FEAT-QICOR): `DefaultLemmaAbsenceAgentGeneratorFactory` tampoco puebla `project_root` nunca — documentado a propósito en su propio Javadoc como "matching the launcher's pre-consolidation behavior exactly"; ignora `--workdir` igual, pero es precedente de F-LASAG, no de esta feature.
  Test re-alojado (no borrado, no bajada de aserciones): `GetCmdTest#shouldReturnTheQUIZINSTRUCTIONTasksOfThePlan...` (F-QINST-R017) afirmaba con `verify(..., never())` que QUIZ_INSTRUCTION NO tenía resolver — cierto cuando se escribió, falso desde que F-QICOR-R002 le dio uno. Actualizado para stubear report+contexto y verificar que SÍ se resuelve (`times(1)`) para la tarea listada, mientras la tarea filtrada por `--diagnosis` sigue en `never()`; se sumaron dos asserts sobre el texto de la violación en el JSON.
  Verificación: `mvn -o compile`/`test-compile` limpios en audit-cli y revision-infrastructure; `GetCmdTest` 82/82 verde; `revision-infrastructure` 27/27 verde; suite completa `mvn -o test` (sin `-pl`, JAVA_HOME=openjdk-25) = **1572/1572 verde, 0 failures, 0 errors, BUILD SUCCESS** (mismo total que la sesión anterior — no se agregaron ni se perdieron tests).
  why: ambos eran de cableado de composición-root, no de diseño — ningún cambio a sentinel.yaml ni a contratos generados.

2026-08-09 — analyst — Resuelta la contradiccion entre R003 (edicion acotada) y R004 criterio 4 (traduccion fiel) que aparecio en la primera corrida real. Tocados: blockquote y detalle de R003 (tabla con los 2 casos reales, medicion del lote, y criterios de aceptacion (b) y (c) nuevos), criterio 4 de R004 en el blockquote y en la tabla de como se verifica (+ criterio de aceptacion (e)), nueva subseccion de Contexto "La traduccion sigue a la oracion, y tiene un solo juez", texto de la accion del nodo `armar_propuesta` de J001, y nueva DOUBT-TRADUCCION-COLATERAL. `requirement validate` = [OK] (solo los warnings de alias del glosario, preexistentes); `feature list` = FEAT-QICOR 9 reglas / 2 journeys (sin cambios de estructura, ningun path nuevo).
  why: 17 de 19 fallos del primer lote eran la misma regla pidiendo algo imposible — ver decisions.md.

  Pendiente aguas abajo (NO hecho por analyst):
  (1) Produccion: `ScopedEditCriterion.java:47-52` reprueba hoy toda traduccion cambiada que no este "señalada"; debe reprobar solo cuando la oracion (partes, huecos y opciones) quedo identica a la original. Es el unico cambio de produccion que pide esta edicion.
  (2) Cobertura: faltan 2 handwrittenTests de R003 en `ScopedEditCriterionTest` — traduccion que acompaña a una respuesta señalada => PASSED; traduccion que cambia sola => FAILED. Los 4 tests actuales de R003 siguen validos (ninguno toca la traduccion; el hueco ya lo habia anotado @qa-tester como "clausula de traduccion de R003 sin test").
  (3) Sin cambios necesarios en el juez: `.sentinel/agents/candidate-judgment/nodes/judge.md:36-44` ya juzga `candidateQuizSentence` contra `candidateTranslation` y ya dice que conservar la traduccion del original solo esta bien "cuando el cambio no altero el significado" — o sea, ya implementa lo que R004 criterio 4 ahora exige por escrito.

2026-08-09 — qa-tester — Propuestos 3 handwrittenTests de F-QICOR-R003 sobre `ScopedEditCriterion` (revision-domain / paquete `candidatecriteriaengine`) en el `architectural_patch.yaml` del requisito. Patch handwrittenTests-only, SIN aplicar. Son los 2 que dejo anotados @analyst (traduccion que acompaña a una respuesta señalada => PASSED; traduccion que cambia sola con la oracion identica => FAILED) mas uno propio: traduccion que cambia Y ademas reescribe una parte no señalada => FAILED. Los 4 tests ya declarados no se tocan ni se re-declaran.
  why: los 4 existentes dejan la traduccion constante, asi que ninguno recorre el loop de partes con la traduccion movida; si la relajacion se escribe como early-return ("la traduccion queda fuera de esta regla") el cambio colateral pasa y ningun test lo ve.

  OJO: ese `architectural_patch.yaml` contenia el patch consolidado del diseño (25KB, el que decisions.md marca como "NO se aplica"); se reemplazo con `--replace` porque mergear habria producido un patch de QA con contenido arquitectonico, que el guard de apply rechaza. Esta commiteado en fa652110 — despues de aplicar el de QA se recupera con `git checkout`.

2026-08-09 — test-writer — Restaurados los 4 cuerpos de `ScopedEditCriterionTest` (perdidos al mover+regenerar el archivo para destrabar el generador, que no agrega stubs a una clase de test ya existente) desde el backup en scratchpad, tal cual. Implementados los 3 nuevos de la excepcion de traduccion de R003 (los 2 que dejo anotados @analyst + el propio de @qa-tester). `mvn test -pl revision-domain` = **258/259 verde**: los 4 restaurados + 2 de los 3 nuevos ya pasan; el unico rojo es `shouldPassACandidateThatChangedTheAcceptedAnswerAViolationSignalledAndMovedTheSpanishTranslationWithIt` — el caso exacto de los 17/19 fallos reales, rojo porque `ScopedEditCriterion.java` todavia rechaza cualquier traduccion cambiada no señalada.
  why: los otros 2 nuevos (`...LeftEveryPartGapAndAcceptedOptionIdentical...`, `...ChangedTheSpanishTranslationAndAlsoRewroteAPart...`) esperan FAILED, y el bug actual — rechaza TODA traduccion cambiada no señalada, sin mirar si la oracion cambio — ya produce ese mismo veredicto por una razon mas amplia y distinta de la correcta. No estan mal escritos: son candados de regresion para cuando @developer acote la excepcion (family ver fix-log).

  Proximo paso: @developer implementa la excepcion en `ScopedEditCriterion.java` — eximir la traduccion del chequeo de fragmentos señalados, pero SOLO cuando la oracion (partes/huecos/opciones) efectivamente cambio; si la oracion quedo identica, la traduccion tiene que serlo tambien. Los 7 tests de este archivo deberian quedar los 7 en verde con eso, sin que los 6 ya verdes se muevan.

2026-08-09 — developer — Aplicada la excepcion de traduccion en `ScopedEditCriterion.java` (ver fix-log para el detalle del cambio). Unico archivo de produccion tocado, confirmado con `git diff --stat` (23 inserciones / 7 borrados, un solo archivo). `mvn test -pl revision-domain -Dtest=ScopedEditCriterionTest` = **7/7 verde** (el que estaba en rojo, `...MovedTheSpanishTranslationWithIt`, ahora pasa; los 2 candados de regresion siguen en FAILED como deben). `mvn test -pl revision-domain` completo = 259/259. Build de reactor completo (`JAVA_HOME`=openjdk-25, `-Djacoco.skip=true -Dexec.skip=true`, sin `-pl`) = **BUILD SUCCESS, 1603/1603 tests verdes, 0 failures, 0 errors, 0 skipped** (15 modulos con tests, suma verificada por modulo).
  why: cierra el bloqueante que dejo anotado @test-writer el mismo dia — candados de regresion ya escritos, faltaba solo la implementacion.

  Nota: el `git status` de esta sesion mostro ademas varios `pom.xml`, `sentinel.yaml`, `sentinel-report.yaml`, `REQUIREMENT.md`, los 3 archivos de memoria y `ScopedEditCriterionTest.java` como modificados — ninguno tocado por este developer (verificado con `git diff --stat` acotado al archivo propio); son WIP de otras sesiones de equipo (analyst/qa-tester/test-writer/architect) ya reflejado en el arbol de trabajo antes de empezar esta tarea. Sin commitear (no se pidio).

  Proximo paso: ninguno pendiente de esta sesion para FEAT-QICOR; el unico rojo conocido esta resuelto.

2026-08-10 — analyst — Resuelto el segundo bloqueo estructural de R003 (partes vacias del enunciado). Nueva **F-QICOR-R010** con las dos mitades (no bloquea / no se pierde al guardar). Tocados: tramo 1 del encabezado (ahora R001,R002,R003,R010), blockquote+detalle+criterio (d) de R003, parrafo de radio de impacto de R007 (puntero, sin duplicar criterio), nueva subseccion de Contexto "Una parte vacia no es contenido, y por eso no puede bloquear ni perderse", bullet de Alcance, gates de J001 (`armar_propuesta` +R010, `aplicar_correccion` +R010 con accion reescrita a "releido el curso... conserva las partes vacias"), nueva DOUBT-PARTE-VACIA-AL-GUARDAR, y References (FEAT-QSENT nueva + anotacion de FEAT-RPRES ampliada al caso simetrico). `requirement validate` = [OK] (solo warnings de alias del glosario, preexistentes); `feature list` = FEAT-QICOR **10 reglas / 2 journeys**. Sin paths nuevos: los gates son informativos, J001 sigue con 4 paths y J002 con 2.
  why: 192/519 tareas (37%) estructuralmente incorregibles — ver decisions.md. Mediciones propias sobre `db/english-course` (11487 ejercicios) para sostener la decision, no la palabra del reporte: 4015 con parte vacia (35,0%), distribucion de formas completa, 0 partes vacias entre dos partes con texto, 884 ejercicios ya guardados sin la parte vacia, y el par de ejemplos hermanos (`Do I want` con parte vacia / `Do I need` sin ella) que estan a un knowledge de distancia en el curso.

  Pendiente aguas abajo (NO hecho por analyst):
  (1) Cobertura R010 — 3 handwrittenTests nuevos, ninguno de los 7 de `ScopedEditCriterionTest` queda sin sustento (verificado: los 7 usan fixtures con la MISMA cantidad de partes en original y candidato). Detalle en el reporte al coordinador.
  (2) Journey: `aplicar_correccion` (J001 path-3) pasa a tener que afirmar que el ejercicio RELEIDO conserva sus partes vacias — es la unica mitad de R010 que solo se ve de punta a punta.
  (3) Produccion: la comparacion de la edicion acotada no debe contar partes vacias, y el guardado debe conservarlas. Son dos cambios en dos lugares distintos; el segundo es el que nadie habia legislado.

2026-08-10 — qa-tester — Cobertura de F-QICOR-R010: **3 handwrittenTests** propuestos (SIN aplicar) sobre `ScopedEditCriterion` (revision-domain / paquete `candidatecriteriaengine`), los tres con `rule: F-QICOR-R010`. Patch handwrittenTests-only (24 lineas, 921 bytes). Los 7 ya declarados NO se re-declaran (el merge es por nombre de test, medido el 2026-08-09) — post-apply tienen que quedar **10**.
  why: son los 2 que dejo pedidos @analyst (perdio la parte vacia + corrigio lo señalado => PASSED; y el candado contra el early-return permisivo) mas uno propio: perdio la parte vacia Y ademas reescribio una parte no señalada => FAILED. El candado que pidio @analyst se instrumento como "perdio la parte vacia y cambio la traduccion con todas las partes con texto identicas => FAILED": es la unica forma de que ese escenario sea observable EN ESTE criterio (si de verdad no cambia nada mas, el veredicto correcto es PASSED y el "no se propone" de R010(c) lo produce R005/R006, ya cubiertos por sus propias reglas).

  Journey: **NADA que declarar**. F-QICOR-J001 ya tiene `testModule: audit-cli` / `testPackage: com.learney.contentaudit.journeys`, `feature status` da FEAT-QICOR OK, y los gates nuevos de R010 (`armar_propuesta`, `aplicar_correccion`) no abren paths: siguen siendo 4, los 4 generados, implementados y PASSING. La mitad (b) de R010 —releido el curso el ejercicio conserva sus 3 partes— es trabajo de @test-writer sobre el cuerpo del **path-2** ya existente (ver fix-log: NO es el path-3).

  OJO `--replace`: se piso el `architectural_patch.yaml` consolidado del diseño (25 KB, commiteado en fa652110). Backup en el scratchpad de la sesion; despues de aplicar el patch de QA se restaura con `git checkout`.

2026-08-10 — test-writer — Dos archivos. (1) `ScopedEditCriterionTest` (revision-domain): restaurados los 7 cuerpos desde el backup de scratchpad (verbatim, sin tocarlos) + implementados los 3 nuevos de F-QICOR-R010 (dominio "Do I want / Do I need a pen?", el ejemplo real que cita la regla). (2) `FQicorJ001JourneyTest#path2` (audit-cli): extendido — NO se agregaron paths ni tests — con un `elementBefore` propio (parte TEXT vacia al principio, unica para este path) y un `ArgumentCaptor` sobre `elementLocator.replace(...)` que verifica que el quiz entregado al guardado conserva las 4 partes con la vacia en su posicion original.
  Estado: **7/7 restaurados en VERDE**. De los 3 nuevos de R010: **2 en VERDE** (candados de regresion — el bug actual, mas estricto que la regla, ya da FAILED por una razon distinta; mismo patron que la familia de traduccion del 2026-08-09) y **1 en ROJO esperado** (el que espera PASSED — bloqueo real de R010, sin arreglar). `path2` de J001 en **ROJO esperado** (assert `4 == deliveredParts.size()` falla con `3` — la parte vacia se pierde hoy); **paths 1/3/4 y el resto de `audit-cli` (380 tests) en VERDE** (`mvn test -pl audit-cli -am` = 381 tests, 1 failure, la esperada). Ningun archivo de produccion tocado (`ScopedEditCriterion.java` intacto, confirmado sin leerlo).
  why: producción todavía no implementa ninguna mitad de R010 — ambos rojos son exactamente el punto de partida que le toca a @developer.
  Próximo paso: @developer implementa las dos mitades de R010 en producción (edición acotada exceptuando vacías + conservación al guardar). Con eso deberían quedar en verde: el test 8 de `ScopedEditCriterionTest` y `path2` de `FQicorJ001JourneyTest`, sin mover los demás.

2026-08-10 — developer — Implementadas las dos mitades de F-QICOR-R010 (ver fix-log para el detalle de código). Ningún test tocado, ningún archivo fuera de `revision-domain` (`ScopedEditCriterion.java` y `QuizInstructionReviser.java`), ni el parser/serializador de FEAT-QSENT ni el deriver compartido (`DefaultLemmaAbsenceProposalDeriver`, también usado por lemma-absence) tocados.
  Verificado: `ScopedEditCriterionTest` 10/10 verde (el único rojo, test 8, ahora PASSED; los 2 candados de regresión de R010 y los 2 de la familia de traducción del 2026-08-09 siguen FAILED como deben). `revision-domain` completo 262/262. `FQicorJ001JourneyTest` 4/4 (path-2 ahora verde: `elementLocator.replace(...)` recibe las 4 partes, la vacía de vuelta en la posición 0). `audit-cli -am` 381/381 (era 380/381 antes). Reactor completo (`JAVA_HOME`=openjdk-25, `-Djacoco.skip=true -Dexec.skip=true`, sin `-pl`) = **1606/1606 tests verdes, 0 failures, 0 errors, 0 skipped, BUILD SUCCESS** (15 módulos con tests; 1603 previo + 3 tests nuevos de R010 que ya existían sin implementación).
  why: cierra el bloqueante que dejó anotado @test-writer el mismo día — candados de regresión y fixtures ya escritos, faltaba solo la implementación de producción.
  Próximo paso: ninguno pendiente para F-QICOR-R010. Las demás DOUBT-* de FEAT-QICOR siguen abiertas para revisión humana, sin cambios de esta sesión.

2026-08-11 — analyst — Resuelto el tercer bloqueo estructural de la feature: la longitud dejo de rechazar correcciones. Nueva **F-QICOR-R011** (VALIDATED). Tocados: tramo 3 del encabezado de reglas; blockquote de R004 (excepcion del criterio 1) + fila 1 de su tabla de verificacion + parrafo nuevo "El criterio 1 era la excepcion a esa doctrina" + criterios de aceptacion (a) y (d); blockquote de R006 ("criterios que bloquean") + su ejemplo de mensaje —que citaba la longitud y era imposible— + su criterio de aceptacion; parrafo nuevo en R008 ("siguen siendo cuatro desenlaces"); premisa del Contexto "El piso a superar..." (afirmaba que el original ya estaba en rango: falso en el 41% de los casos) + nueva subseccion "La longitud dejo de decidir"; dos bullets de Alcance (medicion+declaracion adentro, corregir la longitud afuera); J001 `obtener_candidato` (gate +R011, los dos `when` reescritos) y `armar_propuesta` (gate +R011, accion con la declaracion); J002 `todas_intentadas` (gate +R011, accion con el recuento); dos dudas nuevas; References (FEAT-RCLA y FEAT-RCSL nuevas, anotacion de FEAT-SLEN ampliada a su rol de red de seguridad).
  Verificacion: `requirement validate` = [OK] (solo los warnings de alias del glosario, preexistentes); `feature list` = FEAT-QICOR **11 reglas / 2 journeys**; `feature status` = FEAT-QICOR OK; `feature sync` = "in sync (2 journeys)", sin cambios en sentinel.yaml. **Sin paths nuevos**: los gates son informativos y los `outcomes` no se tocaron en cantidad — J001 sigue con 4 paths y J002 con 2.
  why: la longitud era el motivo mas frecuente de correccion no lograda (78 de 162) y, medido, el 96% de esos casos partia de un ejercicio que YA estaba fuera de rango — ver decisions.md.

  Datos de la corrida analizada (`.content-audit/quiz-instruction-runs/2026-08-09T22-30-12/run-2026-08-11T07-51-33-943.json` + `plans/plan-2026-08-09T22-30-12.json`), por si hay que rehacer la cuenta: 305 tareas intentadas, 125 PROPOSED / 159 NOT_CORRECTED / 18 DIAGNOSIS_STALE / 3 FAILED; failedCriteria = 78 LENGTH_IN_RANGE, 71 SCOPED_EDIT, 4 SOLVABLE_SAME_KIND, 3 LEVEL_VOCABULARY, 2 INSTRUCTION_COMPLIANCE, 1 DISTINCTNESS; los 78 con attempts=3 (234 consultas); las 125 propuestas TODAS con attempts=1. Interseccion con las tareas SENTENCE_LENGTH de QUIZ del mismo plan: 75/78 de los bloqueados por longitud, 43/125 de los propuestos, 211/519 del universo de tareas de consigna.

  Pendiente aguas abajo (NO hecho por analyst):
  (1) Arquitectura — ESCALACION enviada a @architect. Hoy no existe superficie observable para las dos mitades del "se informa": la propuesta no tiene donde declarar la longitud medida + el rango (solo `rationale`, texto libre) y `QuizInstructionCorrectionRunReport` no tiene el recuento de propuestas fuera de rango. Ademas hay que decidir COMO se expresa un criterio que se mide y no bloquea (hoy `CriterionOutcome` es PASSED/FAILED/NOT_EVALUABLE y `DefaultCandidateAssessor` rechaza ante cualquier no-PASSED).
  (2) Cobertura — 5 archivos de test usan hoy LENGTH_IN_RANGE como el criterio que BLOQUEA; ninguno pierde sustento pero todos necesitan re-fixture a un criterio que si bloquea, y los 2 de `LengthInRangeCriterionTest` necesitan re-tag de R004 a R011. Detalle completo en el reporte al coordinador.
  (3) Produccion: la longitud deja de rechazar; la propuesta declara; la corrida cuenta.

2026-08-11 — analyst — Resuelto el cuarto bloqueo de la feature, y el mas de fondo: **el lazo de reintentos cambia de dueño y el mejor candidato se entrega**. Cuatro reglas nuevas (R012 lazo+champion, R013 orden del mejor, R014 declaracion de lo incumplido, R015 criterios consultables), todas VALIDATED.
  Tocados: TL;DR; encabezado de reglas (ahora **cinco** tramos); R003 (parrafo "Que no exige esta regla" — el reintento ya no es la unica salida); R004 (titulo, blockquote, fila 1 de la tabla, parrafo nuevo "Que queda de este catalogo ahora que ninguno de sus criterios veta", criterios de aceptacion b/c/d, mensaje de error); R005 (blockquote + dos parrafos nuevos "Por que este si y los demas no" / "Consultarlo y verificarlo no se pagan dos veces" + criterio (c)); **R006 reescrita** (titulo, blockquote, todo el detalle, criterios, error); R008 (parrafo de atributos + parrafo nuevo del tope); R011 (blockquote, "Lo que esta regla NO autoriza", tabla nueva "Que quedo de propio…", criterio (f)); Contexto (parrafo de las tres reglas del corazon + **nueva subseccion "El lazo estaba del lado equivocado"** + "El costo"); Alcance (4 bullets); J001 (`obtener_candidato` reescrito, **nodo nuevo `verificar_consigna`**, `armar_propuesta`, `sin_correccion`, encabezado); J002 (`todas_intentadas` + encabezado); DOUBT-SIN-CORRECCION → RESOLVED opcion B; DOUBT-REINTENTOS reformulada; 3 dudas nuevas; References (FEAT-LASAG pasa de contraste a modelo, FEAT-CLEX suma su rol de precedente de R015).
  Verificacion: `requirement validate` = **[OK]** (solo los warnings de alias del glosario, preexistentes); `feature list` = FEAT-QICOR **15 reglas / 2 journeys**; `feature status` = FEAT-QICOR OK (el unico `missing-placement` pendiente es de otra feature); `feature sync` = "in sync (2 journeys)", **sin cambios en sentinel.yaml**. **Sin paths nuevos**: `verificar_consigna` es un nodo lineal (`then:`) y no bifurca — J001 sigue con 4 paths y J002 con 2.
  why: de 162 tareas sin corregir, 157 tenian un candidato producido y tirado; 318 reintentos rescataron cero. Ver decisions.md.

  Datos de la corrida, recontados esta sesion desde `.content-audit/quiz-instruction-runs/2026-08-09T22-30-12/run-2026-08-11T07-51-33-943.json`: 305 attempted = 125 PROPOSED (attempts=1 **las 125**) + 159 NOT_CORRECTED (attempts=3 **las 159**) + 18 DIAGNOSIS_STALE + 3 FAILED (attempts=0, "quizSentence is malformed"). `failedCriteria` trae **exactamente un criterio por tarea**: 78 LENGTH_IN_RANGE, 71 SCOPED_EDIT, 4 SOLVABLE_SAME_KIND, 3 LEVEL_VOCABULARY, 2 INSTRUCTION_COMPLIANCE, 1 DISTINCTNESS. De ahi sale la prediccion de R006: **157 de las 159 pasan a propuesta y quedan 2**.

2026-08-11 — developer — Implementada la produccion de R011-R015 (y propagadas las firmas de R003-R006 que arrastraban). Detalle completo de CADA archivo en fix-log.md; resumen: los 6 archivos que el pedido señalo como rotos quedaron implementados (`LengthInRangeCriterion`, `DefaultCandidateAssessor`, `DefaultCandidateCriteriaFactory`, `DefaultRevisionEngineFactory`, `QuizInstructionReviser`, `QuizInstructionAgentStrategy`, `QuizInstructionAgentGenerator` — son 7, el pedido conto uno de mas o de menos, ver el propio texto), mas 4 piezas nuevas que tambien estaban en `UnsupportedOperationException` (`DefaultCandidateLengthMeter`, `DefaultCandidateRanking`, `DefaultQuizInstructionCandidateAssessor` + su factory, `DefaultQuizInstructionAgentCandidateParser.parseJson()`) y el subcomando completo `AssessCandidateCmd` (texto + JSON, R015). Fixtures migradas (propagacion de firma, sin tocar ninguna asercion): los 4 cuerpos protegidos de `QuizInstructionReviserTest` + los 9 handwrittenTests YA ESCRITOS de R011/R012/R014 que resultaron TAMBIEN estar contra una firma vieja (ver decisions.md — hallazgo no anticipado por el pedido), los paths 2/3 de `FQicorJ001JourneyTest`, y **31+3 sitios en 14 archivos de test ajenos a QICOR** rota por el cambio de `RevisionProposal` (2 campos nuevos) que bloqueaba la compilacion de TODO el reactor — ver fix-log.md para la lista completa y el incidente propio (double-patch por `import`, corregido en la misma sesion).
  Verificacion: reactor completo (`JAVA_HOME`=openjdk-25, `-Djacoco.skip=true -Dexec.skip=true`, sin `-pl`) = **1638 tests, 1634 verdes, 4 errores** — los 4 son `UnsupportedOperationException` en tests que NUNCA se implementaron (3 de R006 en `QuizInstructionReviserTest` + 1 en `DefaultQuizInstructionCorrectionRunnerTest`, ver decisions.md 2026-08-11 qa-tester "Reformulada la cobertura de F-QICOR-R006" — quedaron renombrados/nuevos y ningun test-writer los toco todavia). `sentinel verify` = **VERIFY OK** (los warnings de "sealed interface... permits en paquetes distintos" y "test sin aserciones" son preexistentes, de OTROS modulos, no de esta sesion). `feature status --feature FEAT-QICOR` = **OK**, sin missing-from-gate/missing-placement/orphan. `listModules --with-coverage`: las 15 reglas `[covered]` salvo R007 (por diseño, cubierta por gates de J001, ver decisions.md del 2026-08-04) — el reporte de PATHS de J001/J002 en ese mismo comando muestra 0/4 y 0/2 porque lee `.sentinel/sentinel-report.yaml`, que es de una corrida `mvn verify` anterior a esta sesion (no se re-corrio `mvn verify` con jacoco+mutation por costo/tiempo); confirmado en cambio con `mvn test` real que J001=4/4 y J002=2/2.
  2 ESCALACIONES entregadas (detalle completo con el razonamiento en decisions.md): (1) `QuizInstructionCorrectionContext.planId` no existe — `QuizInstructionAgentGenerator` manda `"planId": ""` al grafo en vez de inventar un valor; `assess.sh` ya maneja ese caso como fallo de infraestructura (inelegible, nunca un veredicto fabricado), asi que el grafo hoy SIEMPRE agota intentos sin champion hasta que se cierre el campo. (2) `DefaultCourseElementLocator` es package-private — `AssessCandidateCmd` esta implementado y probado (2/2 verde con mocks) pero NO wireado en Main.java porque no hay forma de construir un `CourseElementLocator` desde audit-cli; el subcomando `assess-candidate` queda inalcanzable por CLI hasta que se resuelva.
  Sin tocar: el grafo `.sentinel/agents/quiz-instruction-corrector/` (el contrato JSON de `assess.sh`/`emit.sh` coincidio exactamente con lo implementado, no hizo falta avisar ningun cambio); `DefaultQuizInstructionSubjectBuilder.serializeParts`/`serializeQuiz`; sentinel.yaml.
  Proximo paso: (a) escalar a @architect las 2 ESCALACIONES; (b) @test-writer implementa los 4 stubs de R006 que quedaron pendientes (unico hueco de cobertura real); (c) una vez cerrada la escalacion 2, wirear `assess-candidate` en Main.java (falta solo esa linea + el `addSubcommand`, el resto de la wiring — `QuizInstructionCandidateAssessorFactory`, `refinementPlanStore`, `auditReportStore`, `correctionContextResolver`, `courseRepository` — ya esta disponible en el scope de `Main.java`).

2026-08-11 — test-writer — Implementados los 10+2 stubs de R015 sobre las dos clases que el usuario regenero de cero (el generador no agrega stubs a una clase de test ya existente, ver fix-log 2026-08-11 qa-tester): `DefaultQuizInstructionCandidateAssessorTest` (revision-domain/engine) y `AssessCandidateCmdTest` (audit-cli/commands). Restaurados 3 cuerpos desde el backup de scratchpad: los 2 de `DefaultQuizInstructionCandidateAssessorTest` (migracion mecanica del constructor 2→7 args, ninguna asercion tocada) y el de `AssessCandidateCmdTest` ("un veredicto por criterio con su motivo") re-fixtureado de 5 puertos mockeados a 1 solo seam. Nuevos: el test de convergencia (`shouldHandTheCatalog...`, `ArgumentCaptor` con 2 capturas vía `assessTask` y `assess` sobre el MISMO candidato + `assertEquals` entre ambos inputs capturados — nunca contra un input armado a mano, para que sea falsable de verdad si `assessTask` dejara de delegar), el re-alojado desde el comando (`shouldTakeTheOriginalQuizFromTheCourseThroughTheLocator...`, fixture de R010 con la parte vacia), las 6 ramas de resolucion fallida (todas terminan en `CandidateAssessmentUnavailableException`, ninguna asierta el `reason` libre, sí el `taskId`), y el nuevo test del comando (`shouldReportTheConsultationAsFailed...`, exit≠0 y ausencia de los 7 nombres de criterio en el output).
  Verificacion SIN `mvn` (revision-domain no compila hoy por 2 archivos ajenos a mi lote — ver fix-log): `javac -sourcepath` aislado + `junit-platform-launcher`. `DefaultQuizInstructionCandidateAssessorTest`: **10/10 compilan y corren**; **2/10 VERDE** (los restaurados, contra `assess()` que YA esta implementado); **8/10 ROJO esperado**, los 8 caen exactamente en `DefaultQuizInstructionCandidateAssessor.assessTask():104` con `UnsupportedOperationException`, ninguna otra causa (sin bugs de fixture propios). `AssessCandidateCmdTest`: **2/2 compilan** contra un stand-in de scratch (nunca escrito al repo real) que replica el constructor de 1 arg que `sentinel.yaml` declara HOY para `AssessCandidateCmd` — el archivo real de audit-cli sigue con el constructor viejo de 6 args, confirmado por el propio compilador de Maven (no leido); ambos **ROJO esperado** contra el stand-in, mismo patron (arranque limpio, cae solo en el `throw` del stub).
  Sin escalaciones: las 12 pruebas tenian superficie de contrato completa (assessTask/assess, CandidateAssessmentUnavailableException, CandidateAssessmentInput con equals de valor) para expresar lo pedido.
  Proximo paso: @developer cierra `AssessCandidateCmd.java` (colapsar al constructor de 1 arg, delegar en `assessTask`) e implementa `DefaultQuizInstructionCandidateAssessor.assessTask()` (mas los 2 factories de `revision-domain/engine` que siguen en el constructor viejo de 2 args, bloqueando `mvn compile -pl revision-domain` para todo el modulo). Con eso los 10 tests de la primera clase y los 2 de la segunda deberian pasar a verde sin tocar sus cuerpos.

  Pendiente aguas abajo (NO hecho por analyst):
  (1) **Arquitectura — ESCALACION a @architect** (enviada en el reporte al coordinador, esta sesion no tiene canal directo). Cinco huecos: (i) el contrato de correccion pasa de "devolver un candidato" a "devolver el mejor candidato + su evaluacion", y el lazo sale de `DefaultCandidateAssessor`/reviser hacia el grafo `quiz-instruction-corrector`; (ii) `DefaultCandidateAssessor` deja de ser accept/refuse booleano y pasa a producir orden + conjunto de criterios incumplidos (ya estaba escalado por R011 el problema de "criterio que mide y no bloquea": ahora es general); (iii) la propuesta necesita donde declarar criterios incumplidos (hoy solo `rationale`, texto libre) — mismo hueco que la declaracion de longitud de R011; (iv) `QuizInstructionCorrectionRunReport` necesita el recuento de propuestas con criterios sin cumplir (ademas del de fuera de rango); (v) **R015 es capacidad nueva**: hoy no existe subcomando que exponga la evaluacion de criterios sobre un candidato — es lo que permite que el grafo consulte en vez de copiar (precedente exacto: `content-audit tokens` que ya usa `eval_length.sh` de lemma-absence).
  (2) **Cobertura** — 3 tests declarados quedan **sin sustento** y hay que re-declararlos/darlos vuelta: `QuizInstructionReviser#shouldDiscardTheBestCandidateItProducedInsteadOfProposingOneThatFailedACriterion` (afirma exactamente lo contrario de R012), `#shouldStopGeneratingCandidatesOnceTheConfiguredAttemptLimitIsReached...` (afirma que el lazo es del sistema) y `DefaultCandidateAssessor#shouldRefuseACandidateAsSoonAsOneCriterionReturnsAFailingVerdict, whichever criterion it is` (ya no refuta ninguno). Otros dos cambian de dueño sin morir: `DefaultCandidateAssessor#shouldRefuseACandidateWhoseCriterionCouldNotBeEvaluated` → R013 (no evaluable = no cumplido para el orden) y `#shouldCarryInTheAssessmentTheCriterionThatBlocked...` → R014. Los 2 de `LengthInRangeCriterionTest` siguen necesitando el re-tag R004→R011 ya pedido. **Ninguno de los 10 de `ScopedEditCriterionTest` ni de los 4 de `InstructionComplianceCriterionTest` pierde sustento**: los criterios siguen emitiendo el mismo veredicto; lo que cambia es la consecuencia. Faltan tests nuevos para R012 (una sola solicitud por tarea; entrega del champion), R013 (los 7 criterios de aceptacion del orden), R014 (declaracion + recuento) y R015 (la consulta y su paridad con la medicion de la auditoria).
  (3) **Journey**: cambiaron los textos `when` de `obtener_candidato`, asi que al regenerar **cambian los nombres de los metodos path2/path3/path4** de `FQicorJ001JourneyTest` — hay que llevar los cuerpos existentes al nombre nuevo (mismo baile que el backup/restore de `ScopedEditCriterionTest` del 2026-08-09, ver fix-log). Ademas el nodo nuevo `verificar_consigna` pide que esos paths afirmen que la validacion de consigna se ejecuto sobre el ejercicio entregado.
  (4) **Produccion**: el lazo se va al grafo; el assessor ordena en vez de vetar; la propuesta declara; la corrida cuenta; y aparece el subcomando de consulta de criterios.

2026-08-11 — qa-tester — Cobertura del tramo "el lazo cambia de dueño" (R012–R015) + la R011 que estaba sin un solo test directo. **34 handwrittenTests nuevos, 4 re-tags, 4 borrados**, propuestos y APLICADOS (guard `--as=qa-tester` es falso positivo en este repo, ver fix-log) + `sentinel generate`. Reparto nuevo: R011 7, R012 6, R013 8, R014 6, R015 6, R004 1. Post-apply las 15 reglas de FEAT-QICOR quedan `[covered]` salvo R007 (pre-existente: lo cubren los gates de J001, y J001 sigue 0/4 paths).
  Borrados (afirmaban lo contrario del tramo): `QuizInstructionReviser#shouldDiscardTheBestCandidate...` y `#shouldStopGeneratingCandidatesOnceTheConfiguredAttemptLimitIsReached...`; `DefaultCandidateAssessor#shouldRefuseACandidateAsSoonAsOneCriterionReturnsAFailingVerdict...` y —hallazgo propio, no estaba en la lista— `#shouldAcceptACandidateOnlyWhenEveryCriterionOfTheActiveCatalogReturnedAPassingVerdict`, que consulta `accepted`, campo que el patch elimino de `CandidateAssessment`.
  Re-tags: `#shouldRefuseACandidateWhoseCriterionCouldNotBeEvaluated...` R004→R013, `#shouldCarryInTheAssessmentTheCriterionThatBlocked...` R006→R014, y los 2 de `LengthInRangeCriterionTest` R004→R011.
  PENDIENTE para @test-writer: (1) borrar los 4 metodos huerfanos de los tests eliminados —los 2 de `DefaultCandidateAssessorTest` ni siquiera compilan—; (2) corregir a mano los 4 `@Tag` de los re-tags, que el generador no toca; (3) re-correr `sentinel generate` para que salga el unico stub que falto.

2026-08-11 — qa-tester — Cierre de la vuelta anterior. **4 renombres** aplicados sobre los tests cuyo nombre contradecia su regla: en `DefaultCandidateAssessor` "refuse…could not be evaluated"→"count …as unmet when it ranks the candidate" (R013 (f)) y "carry…the criterion that **blocked**"→"carry…every criterion the candidate did not meet with the reason it was reproached" (R014); en `LengthInRangeCriterion` los dos pasan a "turn a measurement that falls outside/lands inside the target range into a failing/passing verdict" (R011), porque el criterio ya no mide — mide `DefaultCandidateLengthMeter`.
  Se borraron los 8 metodos huerfanos de los `.java` (4 en `DefaultCandidateAssessorTest`, 2 en `LengthInRangeCriterionTest`, 2 en `QuizInstructionReviserTest`) — ninguno compilaba contra el contrato nuevo. Estado final verificado: **14 implementations, 71 handwrittenTests declarados, 71 stubs, 0 huerfanos, 0 `@Tag` desalineado**. Sobrevivieron intactos los 10 cuerpos de `ScopedEditCriterion`, los 5 del reviser y los 8 del runner.

2026-08-11 — test-writer — Implementados 4 stubs de R012/R015 en 4 archivos (lote acotado, en paralelo con otros test-writers): `AssessCandidateCmdTest` (audit-cli, 2 de R015), `QuizInstructionAgentGeneratorTest` (revision-infrastructure, 1 de R012), `DefaultQuizInstructionAgentCandidateParserTest` (revision-infrastructure, 1 de R012), `LevelVocabularyCriterionTest` (revision-domain, el 4to stub de R015; los otros 3 de R004 no se tocaron).
  Verificado con `javac` aislado (sourcepath multi-modulo, cp filtrado de jars `.m2` viejos de `com/learney/contentaudit`) + `junit-platform-launcher` 1.10.2 standalone (no hay `junit-platform-console-standalone` en el repo local): **3 de 4 compilan y CORREN limpio** — `LevelVocabularyCriterionTest` 4/4 VERDE (los 3 viejos + el nuevo, contra `LevelVocabularyCriterion` ya implementado); `AssessCandidateCmdTest` 2/2 ROJO esperado (`UnsupportedOperationException` en `AssessCandidateCmd.assessCandidate:45`, stub limpio); `DefaultQuizInstructionAgentCandidateParserTest` 1/1 ROJO esperado (compara `attempts` esperado=2 vs devuelto=0 — el parser stub no lee el artifact, no revienta con excepcion rara, o sea que el mecanismo de fixture es correcto). El 4to, `QuizInstructionAgentGeneratorTest`, **NO compila** — ver fix-log, bloqueante de produccion nuevo, ajeno a los "18 errores en 5 archivos" que ya traia la sesion (ese conteo es solo de `revision-domain`; este es un 6to archivo, en `revision-infrastructure`).
  why: R015 en `AssessCandidateCmdTest` se verifico con dos angulos distintos a proposito (uno cuenta los 7 criterios del catalogo completos en el output, el otro con un `ArgumentCaptor` prueba que el `original` que llega al assessor es el objeto EXACTO que devuelve `elementLocator`, no uno reconstruido — fixture con el ejemplo real de R010, "Do I want / Do I need a pen?", con 3 partes reales vs 2 que daria parsear el DSL). R012 en `QuizInstructionAgentGeneratorTest` se verifico contra la clave literal `"maxAttempts"` del mapa de inputs (no una busqueda generica de substring), porque `agent.yaml` del grafo la declara como dependencia exacta — ver fix-log. R012 en `DefaultQuizInstructionAgentCandidateParserTest` uso el mismo patron de `ArtifactPointer` + archivo temporal real que `QuizInstructionAgentJudgeTest` (quiz-instruction-infrastructure, ya verde), porque `RunState.data()` no alcanza: `correctionCandidate` es un artifact respaldado en archivo (`emit.md`'s `completion.produces`), no un valor en memoria. R015(b) en `LevelVocabularyCriterionTest` uso un nivel B1 (no el A1 de la constante de clase compartida) para que la paridad "misma medicion que la auditoria" sea falsable de verdad — con A1 el test pasaria igual aunque el criterio lo hubiera hardcodeado.
  Ningun archivo de produccion tocado ni leido (ni siquiera el generico `AssessCandidateCmd.java`/`QuizInstructionAgentGenerator.java`/`DefaultQuizInstructionAgentCandidateParser.java`) — el unico contacto con produccion fue el TEXTO de 2 lineas que el propio compilador de `javac` mostro como error al intentar compilar `QuizInstructionAgentGeneratorTest`, nunca una lectura directa.
  Sin escalaciones de tipo `underspecified_contract`/`inconsistent_traceability`: las 4 reglas tenian superficie de contrato completa (interfaces + modelos) para expresar lo que pedian.

2026-08-11 — test-writer — Implementados los **18 stubs** de `revision-domain/.../candidatecriteriaengine/` de mi lote (en paralelo con otros test-writers sobre otras clases): `DefaultCandidateRankingTest` (7, R013), `DefaultCandidateAssessorTest` (6, R004/R011/R013/R014 — fixtures reconstruidas desde cero contra el constructor nuevo `DefaultCandidateAssessor(evaluatorsByCriterion, lengthMeter, ranking)`, que habia perdido las suyas), `DefaultCandidateLengthMeterTest` (3, R011/R015), `LengthInRangeCriterionTest` (2, R011 — fixtures reconstruidas contra el constructor nuevo `LengthInRangeCriterion(lengthMeter)`, tambien perdidas). Los dos tests que verifican la decision del usuario en `DefaultCandidateRankingTest` (`shouldRankAnOutofrangeCandidateThatStaysSolvableAboveAnInrangeOneThatDoesNot` y `shouldKeepTheIncumbentChampionWhenTheChallengerRanksEqual...`) llevan comentario explicito citando la decision ("un ejercicio bien escrito fuera de rango vale mas que uno mal escrito dentro") y el mecanismo de campeon (`better(incumbent, challenger)`).
  Verificacion SIN tocar ni leer produccion (que hoy no compila — confirmado con `mvn compile -pl revision-domain -am`, 18 errores de firma en los 5 archivos ya anotados por el coordinador — `DefaultCandidateAssessor.java`, `DefaultCandidateCriteriaFactory.java`, `DefaultRevisionEngineFactory.java`, `QuizInstructionReviser.java`, `QuizInstructionAgentStrategy.java`): scratch tree aislado (descartado, no commiteado) con (a) los `.java` reales de `candidatecriteria/` + `CourseElementSnapshot.java` sin modificar, (b) 4 stubs PROPIOS de los SUT que replican exacto el constructor+metodos que `describeComponent` declara (no el estado actual, roto, de los `.java` reales), (c) mis 4 archivos de test. Compilado con `javac` contra el classpath de `mvn dependency:build-classpath -pl revision-domain` + corrido con un launcher minimo de `junit-platform-launcher` 1.10.2 (jar suelto de `.m2`). Resultado: **18/18 compilan, construyen su fixture sin excepcion, y fallan exactamente en la llamada al SUT con `UnsupportedOperationException`** — el rojo esperado de esta fase.
  why: `CandidateAssessment.satisfiedCriteria`/`unmetCriteria` de R013 EXCLUYEN tanto `LENGTH_IN_RANGE` (explicito en el propio campo) como `INSTRUCTION_COMPLIANCE` (implicito — es el gate de `eligible`, R004 dice expreso "no pertenece a este catalogo") — el catalogo que cuenta tiene **5** miembros, no 6 ni 7; ver fix-log para el detalle y por que importa para cualquier fixture futura de `CandidateAssessment`.
  Sin escalaciones: las 5 reglas (R004/R011/R013/R014/R015) tenian superficie de contrato completa para expresar lo que pedian — `rankKey`/`better`/`precedence` en `CandidateRanking`, `satisfiedCriteria`/`unmetCriteria`/`eligible`/`realChange`/`lengthMeasurement` en `CandidateAssessment`, `measured`/`inRange` en `CandidateLengthMeasurement`.
  Proximo paso: ninguno pendiente de mi lote. @developer implementa los 4 SUT (constructores ya declarados en sentinel.yaml); los 18 deberian pasar de rojo a verde sin tocarlos.

2026-08-11 — test-writer — Implementados los 9 stubs de R011/R012/R014 en `QuizInstructionReviserTest`, los 4 de R011/R014 en `DefaultQuizInstructionCorrectionRunnerTest` y los 2 de R015 en `DefaultQuizInstructionCandidateAssessorTest` (revision-domain, paquete `engine`). Los 5 cuerpos ya escritos del reviser y los 8 del runner quedan intactos, sin tocar. Verificacion aislada (javac contra una copia PARCHEADA en scratch de `QuizInstructionReviser.java`, nunca escrita al repo real, mas los .java reales de produccion sin tocar para todo lo demas): mis 15 metodos compilan limpio contra el contrato actual — confirmado ademas corriendo los 6 que sí compilan hoy contra produccion real (runner + candidate assessor) con `junit-platform-launcher`: los 2 de `DefaultQuizInstructionCandidateAssessor` caen en `UnsupportedOperationException` (rojo esperado, stub intacto); 3 de los 4 del runner caen por aserto contra el valor real (`unmetCriteria`/`proposedOutOfLengthRange` hoy hardcodeados en `null`/`0`/`false` en `DefaultQuizInstructionCorrectionRunner.run()`, rojo esperado); el 4to (`shouldNeverNameTheLengthAmongTheFailedCriteriaOfACorrectionItReportsAsNotAchieved`) ya pasa porque se apoya en el mecanismo `parseFailedCriteria` ya existente y mi fixture nunca menciona longitud.
  why: ver decisions.md — el hallazgo de deriva de contrato en `QuizInstructionReviser.java` fue el bloqueante central de esta sesion.

  BLOQUEANTE encontrado, NO mio para arreglar (instruccion explicita de dejar los 5+8 protegidos intactos): el archivo REAL de produccion `QuizInstructionReviser.java` ya tiene el constructor NUEVO de **7 args** (`strategyRegistry, deriver, assessor, complianceChecker, subjectViewFactory, config, candidateAssessor`) — el campo viejo `assessor: CandidateAssessor` NO se borro (sentinel generate hizo merge ADITIVO sobre una clase con cuerpo ya escrito a mano: agrego `candidateAssessor` al final sin quitar `assessor`), mientras que `describeComponent` reporta `requiresInject` con solo 6 entradas (sin `assessor`) — quien confie solo en `describeComponent` aqui escribiria un constructor de 6 args que tampoco compila. Los 5 cuerpos ya escritos del reviser usan el constructor VIEJO de 6 args (sin `candidateAssessor`) y `new CandidateAssessment(true, allPassed())` (constructor viejo de 2 args; el campo `accepted` ya no existe, `CandidateAssessment` es ahora un record de 7 campos: verdicts/eligible/realChange/satisfiedCriteria/unmetCriteria/lengthMeasurement/rankKey) y `strategy.propose(task, context, List, int)` (4 args; la interfaz real ya es `propose(RefinementTask, QuizInstructionCorrectionContext, int): QuizInstructionBestCandidate`, 3 args). Aislado con javac: de 12 errores de compilacion restantes, los 12 caen EXACTAMENTE en esas 5 lineas de instanciacion viejas — cero en mis 9 metodos nuevos.
  Ademas: el test protegido `shouldLeaveNoProposalAndReportTheFailureNamingAtLeastOneFailedCriterionWhenTheAttemptsWereExhaustedWithoutAnAcceptableCandidate` (R006) usa LENGTH_IN_RANGE como el UNICO criterio que reprueba y espera `NoAcceptableCandidateException` — eso ya no es cierto bajo R011 (la longitud sola nunca bloquea; ese escenario exacto es ahora mi propio test `shouldProposeACorrectionWhoseOnlyFailingCriterionIsThatItsLengthFellOutsideTheTargetRangeOfItsLevel` de R011, y espera justo lo contrario: que SI se proponga). No lo toque (protegido), queda anotado para quien re-escriba esos 5 cuerpos.

  Proximo paso: alguien re-escribe los 5 cuerpos viejos de `QuizInstructionReviserTest` contra el constructor de 7 args + `CandidateAssessment` de 7 args + `QuizInstructionProposalStrategy.propose(task, context, int): QuizInstructionBestCandidate`, y cambia el criterio que reprueba en el test protegido de R006 (ya no puede ser solo LENGTH_IN_RANGE). @developer reescribe el cuerpo real de `QuizInstructionReviser.propose()` (hoy con el lazo viejo de 3 intentos externos, `strategy.propose(..., previousVerdicts, attempt)` y `assessment.isAccepted()`, ninguno de los dos existe ya), `DefaultQuizInstructionCorrectionRunner.run()` (hoy hardcodea `unmetCriteria=null`, `outOfLengthRange=false`, `proposedWithUnmetCriteria=0`, `proposedOutOfLengthRange=0`) y `DefaultQuizInstructionCandidateAssessor.assess()` (stub puro) contra los 15 tests (9+4+2) que quedan en rojo por el motivo correcto.

2026-08-11 — qa-tester — Reformulada la cobertura de **F-QICOR-R006** (la reescribio el tramo de R012 y sus 2 tests quedaron afirmando el desenlace contrario). Borrados y reemplazados: `QuizInstructionReviser#shouldLeaveNoProposal...WhenTheAttemptsWereExhausted...` → 3 tests nuevos (ningun candidato cumplio su consigna / ninguno fue un cambio real / nunca nombra la longitud entre los criterios reprobados); `DefaultQuizInstructionCorrectionRunner#shouldReportATaskWhoseEveryCandidateFailed...` → `...WhereNoCandidateTurnedOutToBeDeliverable...`. R006 pasa de 2 a 4 tests. Estado: 18 implementations, **80 handwrittenTests, 0 stubs faltantes, 0 huerfanos, 0 `@Tag` desalineado**.
  Auditados los otros 4 cuerpos protegidos del reviser: los 2 de R001 y los 2 de R009 **conservan sustento** (solo no compilan por firmas). Ojo el de R009 "carry on with the correction": su claim sigue vigente pero verifica `strategy.propose(task, context, List.of(), 1)`, la firma del lazo externo — al reescribirlo tiene que verificar `propose(task, context, maxAttempts)`.
  PENDIENTE ajeno (@test-writer): `FQicorJ001JourneyTest` path-4 y `FQicorJ002JourneyTest` implementan el desenlace sin correccion con fixture de longitud. El flujo en REQUIREMENT.md ya es correcto ("sin nombrar nunca la longitud"); lo que hay que cambiar es el fixture. Menor: el detail de `DefaultQuizInstructionCorrectionRunnerTest:218` cita LENGTH_IN_RANGE como causa de un NOT_CORRECTED.

2026-08-11 — test-writer — Resuelto el pendiente de arriba, los 3 fixtures. `FQicorJ001JourneyTest#path4` y `FQicorJ002JourneyTest#path2` (el candidato "B") dejaron de montar un fallo de longitud como causa del desenlace sin correccion: ahora el `CandidateAssessment` mockeado reprueba `INSTRUCTION_COMPLIANCE` (eligible=false, realChange=true, satisfiedCriteria=5, unmetCriteria=[compliance failure]) — el unico camino que R006/R012/R013(c) dejan vivo — y ADEMAS queda fuera de rango de longitud (`CandidateLengthMeasurement` con inRange=false) a proposito, para que el assert "la longitud nunca se nombra" sea una prueba genuina y no vacua (mismo patron que el test ya-migrado `QuizInstructionReviser#shouldNeverNameTheLengthAmongTheUnmetCriteriaOfTheProposal...`, R014). Las aserciones se dieron vuelta: donde antes esperaban `LENGTH_IN_RANGE`, ahora `assertTrue(...contains(INSTRUCTION_COMPLIANCE))` + `assertFalse(...contains(LENGTH_IN_RANGE))`. `DefaultQuizInstructionCorrectionRunnerTest.java:218` (el test de los 4 desenlaces, R008): un solo cambio de string, `[LENGTH_IN_RANGE]` → `[INSTRUCTION_COMPLIANCE]` en el `detail` no asertado, sin tocar nada de lo que el test verifica.
  NO tocados (instruccion explicita del pedido, "los otros paths tienen que seguir intactos"): `FQicorJ001JourneyTest#path2`/`#path3` siguen con el constructor VIEJO de 2 args de `CandidateAssessment` y el de 3 args de `QuizInstructionGeneratorResponse` (staleness PREEXISTENTE de la migracion de R011-R015, NO introducida ni agravada por esta sesion, y NO acotada al fixture de longitud que pedia este ticket) — confirmado con `javac` aislado: compilando los 2 archivos de journey juntos salen exactamente 4 errores, los 4 en esas dos lineas de path-2/path-3 de J001, cero en path-4 y cero en todo J002. Sigue pendiente para otra sesion (alcance mayor: reescribir esos 2 cuerpos contra el modelo de 7 campos, incluido decidir `rankKey`/`satisfiedCriteria` para esos candidatos — no es "arreglar el mismo bug", es la migracion completa que ya se hizo en `QuizInstructionReviserTest`).
  Verificacion (sin tocar ni leer produccion real): `mvn compile` de los 10 modulos upstream (course/audit/refiner/evaluation-ledger-domain + 6 infra) sale LIMPIO — confirma que el listado de "6 archivos rotos" es exacto y exhaustivo (los 5 de revision-domain: `DefaultCandidateAssessor`, `DefaultCandidateCriteriaFactory`, `DefaultRevisionEngineFactory`, `QuizInstructionReviser`, `QuizInstructionAgentStrategy`; el 6to en revision-infrastructure no se toco en esta sesion). Para los 2 journeys: `javac` aislado (sourcepath real + 2 stubs propios, SOLO en scratch, de `DefaultRevisionEngineFactory`/`QuizInstructionAgentStrategy` con las firmas exactas de `describeComponent`, cuerpos `throw UnsupportedOperationException`) — los dos archivos reales compilan tal cual quedaron (J002 100% limpio, J001 con los 4 errores preexistentes de arriba, cero en path-4). Con una copia DESCARTABLE de J001 (path-2/3 parcheados SOLO en scratch, nunca en el repo) corrieron los 4 paths de punta a punta: los 4 llegan limpios hasta el mismo punto (el stub de `DefaultRevisionEngineFactory.create()`), sin ninguna excepcion de fixture propia. `DefaultQuizInstructionCorrectionRunnerTest` corrido completo con `junit-platform-launcher`: 8/12 verde (incluido el test que toque, `shouldReportTheFourTaskOutcomesInFourSeparateCounts...`), 4 rojo esperado (1 stub sin implementar + 3 por valores todavia hardcodeados en `DefaultQuizInstructionCorrectionRunner.run()`, ninguno de los 3 tocado por esta sesion).
  Sin escalaciones: las 15 reglas de FEAT-QICOR ya tenian superficie de contrato completa para expresar el desenlace correcto; ningun rule/path resulto sin sentido al releerlo.

2026-08-11 — qa-tester — Cobertura del cambio de diseño que devolvio la resolucion
  (plan / tarea / contexto / original) desde `AssessCandidateCmd` al seam. **9
  handwrittenTests nuevos + 1 borrado**, propuestos y APLICADOS (sin `--as=qa-tester`:
  falso positivo sobre `packages[]` en este repo) + `sentinel generate`. Todos R015.
  Reparto: `DefaultQuizInstructionCandidateAssessor` 2 → **10** (1 convergencia
  assessTask↔assess, 1 re-alojado desde el comando, 6 de resolucion fallida + los 2 que
  ya tenia); `AssessCandidateCmd` 2 → **2** (uno borrado, uno nuevo). Post-apply
  verificado campo por campo contra sentinel.yaml; R015 pasa de 6 a 14 tests.
  DOS CASOS que la lista del architect no traia y que si son ramas propias: (a) el
  ANALISIS del plan ya no se puede cargar — `auditReportStore.load()` es un `Optional`
  aparte, y `prune` existe, asi que un plan vivo con su analisis borrado es real; (b) la
  tarea consultada NO es de incumplimiento de consigna — el resolver despacha por
  `diagnosisKind` y devuelve un contexto de OTRO tipo (no vacio), asi que el riesgo no es
  `Optional.empty()` sino `ClassCastException` o, peor, seguir con un contexto ajeno.
  Ninguno de los 4 que si listo colapsa: plan-inexistente y tarea-ausente son ramas
  distintas del MISMO puerto y se rompen distinto.
  NO se propuso nada sobre `DefaultQuizInstructionCandidateAssessorFactory` (0 tests): que
  sustituya el locator por defecto cuando el config lo trae nulo no tiene regla que lo
  constriña — es invariante de seam de factory. Queda como DUDA para @analyst.

  PENDIENTE aguas abajo:
  (1) @test-writer — los 9 stubs NO se materializaron (hueco del generador, ver fix-log).
      Hay que hacer el backup/mover/regenerar/restaurar sobre las dos clases, y de paso
      re-fixturear: los 2 cuerpos del assessor ya no compilan (constructor 2 args → 7) y
      el que sobrevive en el comando pasa de stubear 5 puertos a stubear el seam.
  (2) @developer — `AssessCandidateCmd.java` sigue con el constructor viejo de 6 args;
      `DefaultQuizInstructionCandidateAssessor.assessTask()` es `UnsupportedOperationException`.

2026-08-11 — test-writer — Implementados los 4 stubs pendientes de F-QICOR-R006: 3 en `QuizInstructionReviserTest` (ningun candidato cumplio su consigna / el candidato no fue un cambio real / nunca nombra la longitud entre lo reprobado) + 1 en `DefaultQuizInstructionCorrectionRunnerTest` (tarea reportada NOT_CORRECTED nombrando criterios, `proposalId` null). Los 2 primeros nuevos del reviser pasan por `NoAcceptableCandidateException` vía `reviser.propose(...)`: el de "consigna" fija `candidateAssessor.assess(...)` con `eligible=false` por `INSTRUCTION_COMPLIANCE` FAILED (`unmetCriteria` no vacio, R006(a)); el de "cambio real" aisla la OTRA mitad de la condicion de R006/R010 — `realChange=false` con el resto del catalogo en `allPassed()` y el candidato con texto literalmente igual a `context.getQuizSentence()/getTranslation()` (verificado con `verify(candidateAssessor).assess(eq(...), eq(candidate.getQuizSentence()), ...)`) — para que la aserción tenga sustento propio y no repita el primer test. El tercero combina `INSTRUCTION_COMPLIANCE` FAILED con longitud fuera de rango a proposito (mismo patron que el test ya-verde de R014 en el mismo archivo) para que "nunca nombra la longitud" no sea trivial.
  Verificacion: NO via `mvn test` (bloqueado por algo ajeno, ver bloqueante abajo) sino via compilacion+ejecucion AISLADAS (javac -sourcepath + junit-platform-launcher, mismo patron de fix-log 2026-08-04/2026-08-11) contra la produccion REAL sin tocarla: `DefaultQuizInstructionCorrectionRunnerTest` **12/12 VERDE tal cual esta hoy en el repo** (mi stub no necesito ningun ajuste de fixture ajeno). `QuizInstructionReviserTest` **16/16 VERDE** (13 preexistentes + mis 3) pero solo lo pude correr sobre una COPIA de scratch con `buildContext()` parcheado (17mo arg, `"plan-qicor-1"`) — el archivo REAL del repo hoy no compila por el nuevo campo `planId`, algo previo a mi sesion y ajeno a mis 3 cuerpos (ver abajo). NO se toco el archivo real ni ningun archivo de produccion.
  BLOQUEANTE de `mvn compile -pl revision-domain`, NO mio y YA DOCUMENTADO (no es hallazgo nuevo — ver fix-log 2026-08-11 lineas ~149-173 y decisions.md ~182-207): la resolucion de R015 (`assessTask`, ESCALACION 1+2 del developer) le agrego `planId` a `QuizInstructionCorrectionContext` (16→17 args del constructor canonico) y expandio `DefaultQuizInstructionCandidateAssessor` de 2 a 7 args; la propagacion quedo a medio hacer — `DefaultQuizInstructionCandidateAssessorFactory.java` y `DefaultRevisionEngineFactory.java` (`revision-domain/engine`, ambos `?? `/`M` en git status, sin commitear) siguen llamando al constructor VIEJO de 2 args y no compilan, lo que bloquea `mvn compile -pl revision-domain` para TODO el modulo — no solo mis 2 archivos. Es exactamente el pendiente que qa-tester ya anoto para @developer ese mismo dia. Tambien hay 8 call-sites de `new QuizInstructionCorrectionContext(` en 5 modulos (grep propio) que van a necesitar el 17mo arg cuando se propague, `buildContext()` de `QuizInstructionReviserTest` es uno de ellos.
  Proximo paso: cuando @developer cierre la propagacion de `assessTask` (los 2 factories + los 8 call-sites de `QuizInstructionCorrectionContext`, incluido `buildContext()` de este archivo con `"plan-qicor-1"` o el valor que corresponda), `mvn test -pl revision-domain` deberia dar los 4 stubs de R006 en verde sin tocar sus cuerpos — ya verificados correctos contra la produccion real.

2026-08-11 — developer — CIERRE de F-QICOR-R015 y su cableado. Los cinco pedidos del coordinador, cerrados: (1) `planId` estampado en `DefaultRevisionEngine.revise()` junto a `setSourceAuditId`, mismo bloque; propagado a los 7 call-sites de fixture que faltaban (`QuizInstructionReviserTest`, `QuizInstructionAgentGeneratorTest`, `GetCmdTest`, `FQicorJ001/J002JourneyTest` — solo el argumento nuevo, ninguna aserción tocada; `DefaultQuizInstructionCandidateAssessorTest` ya venía migrado). (2) `DefaultQuizInstructionCandidateAssessor.assessTask()` implementado: reproduce los pasos 1-3 de `revise()` (plan→tarea→auditReport→context→original) y delega en `assess()` sin armar un `CandidateAssessmentInput` paralelo; las 6 ramas de fallo (plan inexistente, tarea fuera del plan, análisis no cargable, contexto no resoluble, quiz no encontrado, tarea de otro `DiagnosisKind`) terminan en `CandidateAssessmentUnavailableException`, nunca un veredicto. (3) `DefaultQuizInstructionCandidateAssessorFactory.create(config)` reescrito (tenía la firma vieja de 2 args, no compilaba contra la interfaz) — construye `DefaultCourseElementLocator` cuando `config.elementLocator()` es null, calcado de `DefaultRevisionEngineFactory` (no se abrió el locator). De paso, `DefaultRevisionEngineFactory` tenía el mismo defecto (`new DefaultQuizInstructionCandidateAssessor(2 args)` contra un constructor de 7) — corregido reordenando la resolución de `elementLocator` antes de construir el assessor. (4) `AssessCandidateCmd` colapsado al constructor de 1 arg (`QuizInstructionCandidateAssessor`), delega todo en `assessTask`; wireado en `Main.java` como subcomando `assess-candidate` (mismos 7 colaboradores de `revisionEngineConfig`, `elementLocator=null`). (5) `QuizInstructionAgentGenerator.buildInputs()` aplana `context.getPlanId()`, retirado `planIdOf(...)` provisorio.
  Hallazgo propio no pedido explícitamente: `assessTask()` resuelve un context FRESCO vía `contextResolver.resolve(...)`, y el resolver real (`QuizInstructionContextResolver`) SIEMPRE deja `sourceAuditId`/`planId` en null (mismo motivo que en `DefaultRevisionEngine`) — sin estamparlos ahí también, `CandidateAssessmentInput.sourceAuditId` (que alimenta el criterio de distinción, F-QICOR-R004 criterio 3) habría llegado null en producción real, invisible en la suite porque los mocks de `contextResolver` siempre devuelven contexts ya pre-poblados a mano. Agregado `qic.setSourceAuditId(plan.getSourceAuditId()); qic.setPlanId(planId);` en `assessTask()`, mismo patrón que `DefaultRevisionEngine`.
  Verificación: reactor completo (`JAVA_HOME`=openjdk-25, `-Djacoco.skip=true -Dexec.skip=true`, sin `-pl`) = **1646 tests, 1646 verdes, 0 failures, 0 errors, 0 skipped, BUILD SUCCESS** en los 15 módulos con tests. `sentinel verify` = **VERIFY OK** (únicos warnings: sealed-interface-cross-package y tests-sin-aserciones, ambos preexistentes de otros módulos). `content-audit assess-candidate --help` responde con exit 0, aparece listado en `content-audit --help`, y `assess-candidate` sin argumentos requeridos falla con el mensaje de picocli esperado (confirma que las 6 opciones —`--plan/--task/--candidate-sentence/--candidate-translation/-c/-f`— están wireadas). Contrato de `assess.sh` (última línea de stdout = JSON con `eligible/satisfiedCriteria/unmetCriteria/rankKey/lengthMeasurement`) verificado leyendo el script — no tocado, no hizo falta.
  Sin escalaciones: las dos escalaciones que dejó pendiente el developer anterior (planId faltante, `DefaultCourseElementLocator` package-private) ya estaban resueltas en `sentinel.yaml`/arquitectura al empezar esta sesión — solo faltaba la implementación.
  Próximo paso: ninguno pendiente para F-QICOR-R015. Sigue sin commitear (no se pidió).

2026-08-11 — analyst — Agregada **F-QICOR-R016** (sentido en el mundo real, bloqueante) y armonizado el resto del catalogo. FEAT-QICOR pasa de 15 a **16 reglas**; los 2 journeys y sus paths NO cambian.
  Tocadas ademas: R004 (catalogo 5→6 criterios, cinco no vetan y el sexto si; fila nueva en la tabla de "como se verifica"; aceptacion (f)), R005 ("unico criterio absoluto" → "uno de los dos"), R006 (tercer motivo de "no lograda" + aceptacion (d) + los dos unicos nombres que puede llevar a solas), R011 (bloque nuevo "La asimetria con el sentido es deliberada" con tabla de 4 filas; aceptacion (a) "2 a 5"→"2 a 6"), R012 (aceptacion (b) + fila de la tabla de lo que el sistema no delega), R013 (tercera condicion para competir + parrafo "por que el sentido tampoco es un escalon del orden" + aceptacion (h)), R014 (los 3 criterios que nunca aparecen en la lista, con sus dos motivos distintos; aceptacion (f)), R015 (la consulta incluye el sentido — es lo que convierte el veto en reintento; aceptacion (a) "los seis"). Contexto: subseccion nueva "Una oracion imposible no es un ejercicio". Alcance: 1 bullet en cada lista (fuera de alcance: buscar imposibles en el curso — no hay analizador y esta feature no lo crea). Journey J001: `when` de las dos ramas de `obtener_candidato` + gate con R016 + linea de cobertura.
  Dudas: **DOUBT-SENTIDO-BLOQUEANTE** RESOLVED (Opcion B, absoluto) y **DOUBT-SENTIDO-PREEXISTENTE** OPEN (que pasa si el original YA afirmaba un imposible — se especifica Opcion A, absoluto sobre el corregido; la medicion que la decide sale gratis del recuento de R006). Actualizadas DOUBT-CRITERIO-ABSOLUTO (los absolutos son 2, la pregunta viva es si resolubilidad/traduccion se suman) y DOUBT-REVERIFICACION (el sentido se toma de la evaluacion recibida — unico absoluto que no se rehace).
  Verificacion: `requirement validate` = **[OK]** (solo los WARN de alias del glosario, preexistentes); `feature sync --dry-run` = "FEAT-QICOR: in sync (2 journeys), No changes needed" — las reglas no se duplican en sentinel.yaml, asi que R016 ya es citable en `traceability` sin sync. NO se toco sentinel.yaml ni ningun otro requisito.
  Proximo paso: @qa-tester necesita cobertura propia de R016 — como minimo (a) el candidato absurdo no se propone aunque cumpla todo lo demas, (b) el candidato con sentido que reprueba edicion acotada SI se propone declarandolo, (e) el sentido nunca figura en `unmetCriteria`. Ojo: R006 gana un tercer motivo (fixtures que hoy montan INSTRUCTION_COMPLIANCE siguen valiendo) y R015 gana un veredicto mas en la consulta.

2026-08-11 — test-writer — Implementados los 3 stubs de F-QICOR-R016 en `QuizInstructionReviserTest` (revision-domain/engine), los unicos que quedaban con `UnsupportedOperationException` en ese archivo: `shouldLeaveNoProposalForACandidateThatDescribesSomethingThatCannotHappenInTheRealWorldEvenThoughItCompliesWithItsInstructionAndSatisfiesEveryOtherCriterion` (R016(a), veto puro), `shouldProposeTheCandidateThatDescribesSomethingThatCanHappenInTheRealWorldEvenThoughItChangedMoreThanTheViolationSignalled` (R016(b), interaccion con R003 sin tocar R003), `shouldNameTheRealworldSenseAmongTheFailedCriteriaItReportsWhenNoCandidateDescribedSomethingThatCouldHappen` (R006(d), desenlace/naming). Dominio real de la regla (`Does she drink meat?`→`milk?`, `task-876`) para los dos primeros; `He ... drive a book` (ejemplo propio de la tabla de R016) para el tercero. Los 16 tests protegidos no se tocaron.
  **Hallazgo: `QuizInstructionReviser.propose()` YA implementa R016 de punta a punta** — no hizo falta ninguna produccion nueva; `eligible=false`+`unmetCriteria` con REAL_WORLD_SENSE fluye igual que INSTRUCTION_COMPLIANCE (mismo mecanismo generico de R012/R013). `mvn test -pl revision-domain -am -Dtest=QuizInstructionReviserTest` = **19/19 VERDE**.
  why: test 1 usa `assertEquals(List.of(REAL_WORLD_SENSE), ex.getFailedCriteria())` (exacto, no `contains`) para probar que ninguna otra cosa fallo — es la forma observable de "satisfies every other criterion"; test 2 verifica que el sentido PASSED deja competir a un candidato que reprueba SCOPED_EDIT (`assertFalse(...anyMatch(REAL_WORLD_SENSE))` + `assertTrue(...contains(scopedEditFailure))`); test 3 usa la asercion mas laxa de los hermanos de R006 (`contains`, no exactEquals) porque R006(d) es agnostico a que mas paso, y `attempts=3` (vs `attempts=1` del test 1) para diferenciar narrativamente el desenlace-por-agotamiento del veto-puro.
  Helper nuevo, sin tocar `allPassed()`/`verdictsWith()` (predatan R016 y las usan los 16 protegidos): `withRealWorldSense(List<CriterionVerdict> base, CriterionVerdict senseVerdict)`, agrega el 8vo veredicto a una lista base.
  `mvn test -pl revision-domain -am` (suite completa) = 309 tests, **5 failures, todas ajenas**: `DefaultCandidateAssessorTest` (4) y `DefaultCandidateCriteriaFactoryTest` (1) — archivos que esta sesion no toco (ya `M`/`??` en git status de otra sesion en paralelo), en rojo esperado porque `DefaultCandidateAssessor`/`DefaultCandidateCriteriaFactory` todavia no tienen su produccion de R016 (a diferencia de `QuizInstructionReviser`, que si). No es mio para arreglar.
  Proximo paso: ninguno pendiente en `QuizInstructionReviserTest`. Los 5 failures ajenos son terreno de @developer.

2026-08-11 — qa-tester — Cobertura de **F-QICOR-R016** y de los cambios que trajo a R006/R011/R013/R014/R015. **11 handwrittenTests nuevos, 0 borrados**, PROPUESTOS (no aplicados) en `requirements/2026-08-04.01_correccion-de-consigna-de-quiz/architectural_patch.yaml` — validacion "0 additions, 4 modifications, 0 conflicts". Reparto por superficie, porque el veto se puede romper por cada una por separado: `DefaultCandidateAssessor` 6 → **12** (2 de R016: el veto con todo lo demas cumplido + `NOT_EVALUABLE` que deja fuera igual que `FAILED`; 1 de R013: el sentido no suma en `satisfiedCriteria`; 1 de R011: la asimetria por sus dos puntas en un solo test; 1 de R014: el sentido aparece en `unmetCriteria` SOLO sobre un no-elegible; 1 de R015: el veredicto viene en la consulta); `DefaultCandidateRanking` 7 → **8** (R013 (h): `drink meat` pierde contra `drink milk` aunque sea el unico que respeto la edicion acotada); `QuizInstructionReviser` 16 → **19** (2 de R016 = las dos filas de la tabla de la regla + 1 de R006 (d) nombrando el sentido en el desenlace); `DefaultCandidateCriteriaFactory` 0 → **1** (R016: la excepcion que NOMBRA el criterio absoluto sin evaluador).
  NO propuesto, y por que: (i) segundo test de `precedence()` — el de R013 ya existente (`DefaultCandidateRankingTest:104`) la fija por `assertEquals` contra una lista de 5, asi que agregar el sentido al orden ya rompe hoy; (ii) nada sobre `AgentCandidateJudgmentEvaluator` — R016 (c) (trivial/descontextualizado no reprueba) y (f) (dos respuestas aceptadas, una imposible) son claims de PROMPT: ese test mockea el `CandidateJudgmentVerdictParser`, asi que cualquier asercion sobre el juicio seria vacua; van a los evals del grafo `candidate-judgment`; (iii) nada nuevo en `AssessCandidateCmd` ni en `DefaultQuizInstructionCorrectionRunner` — sus tests de R015 ("un veredicto por criterio del catalogo") y de R006 ("tarea sin candidato entregable") son motivo-agnosticos: cambia la fixture, no el claim.
  PENDIENTE aguas abajo: (1) `.sentinel/agents/candidate-judgment/agent.yaml` sigue declarando `criterion: FAITHFUL_TRANSLATION | SOLVABLE_SAME_KIND` y `judge.md` no conoce REAL_WORLD_SENSE — sin eso el evaluador del absoluto no existe y la factory va a tirar `AbsoluteCriterionEvaluatorMissingException` en toda corrida; (2) fixtures a actualizar cuando el catalogo pase de 5 a 6 criterios: el test de R015 de `AssessCandidateCmd`, el de R004 de `DefaultCandidateAssessor` ("un veredicto por criterio") y el de R011 del reviser (su candidato tiene que pasar ahora "los criterios 2 a 6").
  DUDAS abiertas para el usuario (ver decisions.md): el tag del test de la factory, y que el juez recibe hoy la forma SERIALIZADA del enunciado y no la lectura con el hueco resuelto que pide R016.

2026-08-11 — test-writer — Implementado el unico stub de `DefaultCandidateCriteriaFactoryTest` (revision-domain/candidatecriteriaengine, clase nueva, sin otros tests): `shouldRefuseToBuildTheCatalogNamingTheRealworldSenseAsTheAbsoluteCriterionLeftWithoutAnEvaluatorInsteadOfJudgingCandidatesAsIfItHadPassed` (F-QICOR-R016). `CandidateCriteriaConfig` con los 10 demas campos mockeados/validos (los 3 nullable documentados en null: siblingSimilarityThreshold/courseSimilarityThreshold/courseWideDistinctness) y SOLO `realWorldSenseEvaluator=null`, para aislar la falla a ese unico campo. Cuerpo: `assertThrows(AbsoluteCriterionEvaluatorMissingException.class, () -> factory.create(config))` + `assertEquals(CorrectionCriterion.REAL_WORLD_SENSE, exception.getCriterion())` — las dos mitades pedidas (falla / nombra el criterio) en un solo test, sin `assertNotNull`.
  Traceabilidad verificada ANTES de implementar (no forzada al tag mas cercano): aunque el TEXTO de R016 no menciona construccion de catalogo, tanto `CandidateCriteriaConfig.realWorldSenseEvaluator` como `AbsoluteCriterionEvaluatorMissingException.criterion` (via `describeComponent`) citan **F-QICOR-R016 explicitamente** como la razon de ser del campo/excepcion ("Un criterio que veta no admite ausencia: si no se lo puede evaluar hay que decirlo, no seguir (F-QICOR-R016)") — es el architect, no el test-writer, quien establecio el vinculo. La "duda" que dejo @qa-tester (ver decisions.md 2026-08-11) es sobre si el CRITERIO DE ACEPTACION deberia agregarse al texto de la regla, no sobre si el comportamiento pertenece a R016.
  Verificado: `mvn compile`/`test-compile -pl revision-domain -am` limpios (el modulo compila entero hoy, sin bloqueantes de sesiones anteriores). `mvn test -pl revision-domain -Dtest=DefaultCandidateCriteriaFactoryTest -Djacoco.skip=true`: compila y corre, **ROJO ESPERADO** — `AssertionFailedError: Expected AbsoluteCriterionEvaluatorMissingException to be thrown, but nothing was thrown` (la fixture arma y llega limpio hasta el assert; la factory de produccion todavia no valida `realWorldSenseEvaluator==null`, fase de @developer pendiente — y ademas, per la nota de @qa-tester arriba, el catalogo de evaluadores del grafo `candidate-judgment` todavia no declara REAL_WORLD_SENSE).
  Sin escalaciones.
  Nota de higiene: este archivo ya supera las ~200 lineas que sugiere sumarizar en `## Archived`; no lo hice en esta sesion (alcance acotado a un solo test + sesiones paralelas activas sobre el mismo requisito) — queda pendiente para quien cierre este tramo.

2026-08-11 — test-writer — Implementado el 8vo stub de `DefaultCandidateRankingTest` (R013 (h), el par real `task-876`): `Does she drink meat?` (satisfiedCriteria=5, eligible=false por `REAL_WORLD_SENSE` FAILED) pierde contra `Does she drink milk?` (satisfiedCriteria=4, eligible=true, unmetCriteria=[SCOPED_EDIT FAILED]) via `ranking.better(...)`. No usa los helpers compartidos `eligibleAssessment`/`ineligibleAssessment` tal cual para el candidato ineligible (ese helper hardcodea `INSTRUCTION_COMPLIANCE`, no `REAL_WORLD_SENSE`) — construye el `CandidateAssessment` inline con el mismo shape (`verdicts=[failed(criterio)]`, `unmetCriteria=List.of()`), sin tocar los 7 tests/helpers preexistentes. `ranking.precedence()` y `satisfiedCriteria` no se tocan (siguen en 5 miembros) — respeta la advertencia de que `REAL_WORLD_SENSE` no entra en ninguna de esas dos superficies. Los 7 tests preexistentes siguen intactos y verdes.
  Verificacion: `revision-domain` no compila hoy por un break AJENO y en progreso de otro agente (`DefaultQuizInstructionCandidateAssessor.java`, `DefaultRevisionEngine.java`, `QuizInstructionCorrectionContext.java` — `setPlanId` no existe; nada que ver con `candidatecriteria`/`candidatecriteriaengine`). Compilado y corrido AISLADO con `javac -sourcepath` + un launcher minimo de `junit-platform-launcher` (ver fix-log) — **8/8 tests verdes**. No se tocó ningun archivo de produccion.

2026-08-11 — test-writer — Implementados los 6 stubs restantes de `DefaultCandidateAssessorTest` (revision-domain/candidatecriteriaengine), todos sobre F-QICOR-R016 y sus reglas vecinas: 2 de R016 (veta cuando REAL_WORLD_SENSE=FAILED pese a cumplir todo lo demas; veta tambien cuando NOT_EVALUABLE, contrastado contra PASSED en el mismo test), 1 de R015 (el veredicto de REAL_WORLD_SENSE viaja en `getVerdicts()` junto al resto del catalogo), 1 de R013 (no suma a `satisfiedCriteria` aun cuando PASSED), 1 de R011 (la asimetria longitud/sentido en UN solo test: fuera de rango compite, sentido reprobado no), 1 de R014 (REAL_WORLD_SENSE aparece en `unmetCriteria` solo del candidato no elegible, nunca del elegible). Los 6 ya implementados no se tocaron. Ningun helper nuevo: reusan `quiz`/`input`/`assessorFor`/`allCatalogCriteriaPassing`/`passed`/`verdict`/`findByCriterion` tal cual (ver fix-log).
  Estado: `mvn test -pl revision-domain -am` (compila limpio hoy, sin el break AJENO que anoto la entrada anterior — ya resuelto al momento de esta sesion) sobre la clase = 12 tests, **8 verdes / 4 rojo esperado** (los 2 de R016 + el de R011 + el de R014 fallan en `assertFalse(...isEligible())` porque `DefaultCandidateAssessor.java` real todavia no gatea `eligible` con REAL_WORLD_SENSE). Los de R015 y R013 salen verdes YA sin el fix — mismo patron "candado de regresion" que R003/R010 (fix-log 2026-08-09/2026-08-10): su aserto no distingue el REAL_WORLD_SENSE=PASSED del comportamiento viejo, asi que van a seguir en verde despues del fix tambien.
  Descubierto pero NO mio (no tocado, confirmado con `git status`): `DefaultCandidateCriteriaFactoryTest.java` (1 rojo) y `DefaultCandidateRankingTest.java` (8vo test, ya verde) son de las dos sesiones de test-writer en paralelo de arriba; `QuizInstructionReviserTest.java` tiene ademas 3 stubs de R016 sin implementar, tambien ajenos. Reactor `revision-domain` completo con `-am`: 309 tests, 5 failures + 3 errors — 4 failures son mias (esperado), el resto es ajeno (1 failure de la factory + 3 errors del reviser).
  Proximo paso: @developer implementa el gate de REAL_WORLD_SENSE en `DefaultCandidateAssessor.eligible` (F-QICOR-R016) — con eso los 4 rojos de esta clase deberian quedar en verde sin mover los 8 ya verdes.

2026-08-11 — developer — Cableado F-QICOR-R016 en el composition root (`Main.java` Step 7c, audit-cli). `CandidateCriteriaConfig.realWorldSenseEvaluator` (11vo campo del record, el `null` final hardcodeado que ya estaba ahi como placeholder) se llena en las 3 ramas: CANNED usa `cannedPassingJudgmentEvaluator(REAL_WORLD_SENSE)` (mismo precedente que FAITHFUL_TRANSLATION/SOLVABLE_SAME_KIND); config LLM resuelta usa un tercer `candidateJudgmentEvaluatorFactory.create(..., REAL_WORLD_SENSE)` (misma factory, mismo indexado); `quizInstructionLagenSetupError != null` usa un evaluador nuevo, `notEvaluableJudgmentEvaluator(criterion, reason)` (helper agregado al final de la clase, mismo molde de clase anonima que `cannedPassingJudgmentEvaluator` — la interfaz tiene 2 metodos abstractos, no puede ser lambda), que siempre devuelve NOT_EVALUABLE con el mismo `deferredMsg` que ya arma esa rama — nunca PASSED por omision, a diferencia de `judgmentEvaluators = List.of()` en esa misma rama, que si deja afuera en silencio a los dos no-vetantes.
  Verificacion: `mvn compile -pl audit-cli -am` limpio. `mvn test -pl audit-cli -am -Dmaven.test.failure.ignore=true` (revision-domain tiene 5 rojos PRE-EXISTENTES que bloquean el reactor con -am; tecnica ya documentada en este mismo archivo 2026-08-10): **audit-cli = 383 tests, 1 failure**, confirmado PRE-EXISTENTE y AJENO: `AssessCandidateCmdTest#shouldReturnOneVerdictPerCriterionOfTheCatalogForTheCandidateConsultedWithItsReasonWhenTheVerdictIsNegative` arma su fixture con solo 7 `CriterionVerdict` (comentario propio del test dice literal "all seven", escrito antes de que `CorrectionCriterion` creciera a 8) mientras su loop de asercion itera `CorrectionCriterion.values()` (8 hoy); `AssessCandidateCmd.java` no fue tocado, no importa `Main`, y su render es agnostico al criterio (imprime lo que venga en `assessment.getVerdicts()`, nunca enumera el enum el mismo) — es exactamente el pendiente que ya habia anotado @qa-tester ("fixtures a actualizar... el test de R015 de AssessCandidateCmd"). No tocado (instruccion explicita del pedido de no tocar tests). Los 5 rojos de `revision-domain` (`DefaultCandidateAssessorTest` x4, `DefaultCandidateCriteriaFactoryTest` x1) tambien pre-existentes y ajenos: es el gate de R016 en `DefaultCandidateAssessor`/`DefaultCandidateCriteriaFactory`, trabajo de otro developer sobre otro modulo, no del composition root.
  why: el pedido distinguia 3 ramas e insistia en que la rama de error de config llenara el slot con NOT_EVALUABLE, nunca con aprobacion silenciosa — era la unica de las tres sin precedente directo ya escrito en el archivo.
  Ningun archivo fuera de `audit-cli/src/main/java/.../commands/Main.java` tocado. Sin commitear (no se pidio).
  Re-verificacion tras el cierre de produccion de la entrada siguiente (sesion en paralelo, `DefaultCandidateAssessor`/`DefaultCandidateCriteriaFactory`): `mvn test -pl audit-cli -am -Dmaven.test.failure.ignore=true` de nuevo = `revision-domain` **309/309 verde** (los 5 rojos de arriba ya no estan), `audit-cli` sigue **383/1**, mismo unico failure de `AssessCandidateCmdTest` (fixture stale, sin relacion con mi cambio). El wiring del composition root sigue compilando y corriendo limpio con la produccion nueva.

2026-08-11 — developer — Cerrado el ultimo pendiente de F-QICOR-R016 del lado de produccion. `DefaultCandidateAssessor.assess()` gatea `eligible` con las 3 condiciones (`instructionCompliant && realWorldSenseCompliant && realChange`), tratando `NOT_EVALUABLE` como no-cumplido igual que `FAILED` (default `false`, mismo patron que `instructionCompliant`); `REAL_WORLD_SENSE` no suma a `satisfiedCriteria` ni entra a `COUNTED_CATALOG` (sigue en sus 5 miembros); solo aparece en `unmetCriteria` cuando el candidato queda no-elegible, por construccion del flujo (sin filtro defensivo, tal como pedia el enunciado). `DefaultCandidateCriteriaFactory.create()` ahora valida `config.getRealWorldSenseEvaluator()==null` -> `AbsoluteCriterionEvaluatorMissingException(REAL_WORLD_SENSE)` fail-fast antes de construir nada mas, y registra el evaluador en el catalogo activo (`evaluators.put(REAL_WORLD_SENSE, realWorldSenseEvaluator)`). `DefaultCandidateRanking` (precedence, 5 renglones) NO se toco — ya estaba conforme, confirmado leyendolo antes de tocar nada.
  Verificacion: `mvn test -pl revision-domain -am -Djacoco.skip=true` = **309 tests, 0 failures, 0 errors, 0 skipped, BUILD SUCCESS** (baseline era 309/5: los 4 de `DefaultCandidateAssessorTest` + el 1 de `DefaultCandidateCriteriaFactoryTest` que los test-writers dejaron en rojo esperado). `mvn compile` corre `sentinel verify` en la fase validate y da "VERIFY OK — all generated files match sentinel.yaml". Los modulos dependientes (course-domain 50, evaluation-ledger-domain 26, audit-domain 421, refiner-domain 138) siguen todos verdes, sin tocarlos.
  Sin escalaciones — el contrato ya estaba completo en sentinel.yaml (`CandidateCriteriaConfig.realWorldSenseEvaluator`, `AbsoluteCriterionEvaluatorMissingException`) desde la sesion del architect; solo faltaba la produccion.
  Proximo paso: ninguno pendiente para F-QICOR-R016. Sigue sin commitear (no se pidio en esta sesion).

2026-08-13 — developer — Pedido de rendimiento (no una regla nueva de FEAT-QICOR): bajar el tiempo de `assess-candidate` en revision-domain sin cambiar conducta observable. Causa: `DefaultCandidateAssessor.assess()` evaluaba los 8 criterios en serie; 3 son jueces LLM (~15s c/u via el grafo `candidate-judgment`) y ponian una tanda de 25 tareas al borde del timeout de 60s del `SubprocessScriptAdapter` del framework (52s medidos, no configurable desde la definicion del agente).
  Fix: `evaluateCatalog()` nuevo en `DefaultCandidateAssessor` — despacha FAITHFUL_TRANSLATION/SOLVABLE_SAME_KIND/REAL_WORLD_SENSE a un `ExecutorService` (hilos daemon, `shutdown()` en `finally`) ANTES de correr los criterios serie en el hilo llamador, y solo al final espera (`Future.get()`) a los jueces — la espera de red se solapa con el trabajo serie en vez de sumarse. INSTRUCTION_COMPLIANCE (tambien juez, pero su evaluador escribe en el ledger `.jsonl` de FEAT-EVCOST) queda deliberadamente fuera del grupo concurrente. Catalogo completo siempre consultado, sin corte (F-QICOR-R004); lista final reconstruida en orden de catalogo desde un `Map` — nunca en orden de llegada (F-QICOR-R015).
  Verificacion: `mvn test -pl revision-domain -am -Djacoco.skip=true` = 309/309 verde, mismo numero que el baseline de la entrada anterior. Corrida real sobre task-864/plan `2026-08-12T13-51-08`: baseline secuencial (stash temporal del cambio + rebuild) 30s vs. version concurrente 19-25s; mismo `rankKey` (`"13011101"`), mismos 8 criterios, mismo `eligible`/`satisfiedCriteria`/`unmetCriteria`/`lengthMeasurement` en las tres corridas — solo vario la prosa (no el outcome) del veredicto LLM de SOLVABLE_SAME_KIND, esperado por no-determinismo del juez.
  Sin escalaciones. Unico archivo tocado: `DefaultCandidateAssessor.java` (cuerpo de `assess()` + 2 helpers privados nuevos + 2 campos `static` nuevos; constructor/campos/`implements`/anotacion `@Generated` intactos — ver fix-log 2026-08-11 sobre la convencion de que el cuerpo de metodo en `candidatecriteriaengine` es del developer). Sin commitear (no se pidio).
  Proximo paso: ninguno pendiente. Si mas adelante se quiere sumar INSTRUCTION_COMPLIANCE al grupo concurrente, `FileSystemEvaluationLedger.append()` ya documenta su propio `append()` como seguro entre corridas concurrentes (channel `APPEND` fresco por llamada) — dato a favor, pero no evaluado a fondo aqui porque no hacia falta para el objetivo.
