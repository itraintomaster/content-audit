# Fix log — FEAT-QICOR

2026-08-04 — developer — ScopedEditCriterion: `fragment.contains(text)` con `text=""` (CLOZE sin texto literal) siempre da true → cualquier hueco parecía "señalado". Fix: `containsOrIsContained` descarta `text` blank antes de comparar.
  why: `ScopedEditCriterionTest#shouldFailACandidateThatAlteredTheAcceptedOptionsOfAGapNoViolationImplicated` fallaba (PASSED en vez de FAILED) hasta el fix.

2026-08-04 — developer — DefaultRevisionEngine: el catch de `DiagnosisNotSustainedException` devolvía el outcome pero NO marcaba la tarea STALE ni guardaba el plan (DOUBT-TAREA-CAIDA opcion B). Agregado el mismo patron que el Step 10 (COMPLETED) pero con STALE.
  why: `FQicorJ001JourneyTest#path1` (audit-cli) verifica `planStore.save(...)` con la tarea en STALE; sin el fix el mock nunca se invoca.

2026-08-04 — developer — DefaultRevisionEngineFactory: agregado el registro de `QuizInstructionReviser` en el mapa `byKind` bajo `DiagnosisKind.QUIZ_INSTRUCTION` (mismo patron que `KnowledgeTitleReviser`), y DispatchingCorrectionContextResolver + QuizInstructionContextResolver conectados en Main.java. Sin esto las tareas QUIZ_INSTRUCTION caían al bypass de identidad (F-REVBYP-R004) — exactamente el hueco que la feature cierra.
  why: ninguno de los dos estaba en la lista explícita de 30 stubs, pero sin ellos ningún candidato se genera nunca; confirmado leyendo DefaultRevisionEngineFactory.java existente antes de tocarlo.

2026-08-04 — developer — Cada `sentinel generate` (incluido el de ARCH-QICOR-004, que no tocaba refiner-domain para nada) resetea la firma de clase de `QuizInstructionContextResolver` a `public class QuizInstructionContextResolver {` — pierde `implements CorrectionContextResolver` aunque conserva los métodos ya escritos. Fix: volver a agregar `implements CorrectionContextResolver` a mano después de cada regeneración. Se detecta por el error de compilación "method does not override or implement a method from a supertype" en ese archivo puntual.
  why: la relación no está declarada en sentinel.yaml (no puede estarlo, ver DispatchingCorrectionContextResolverTest/decisions — el puerto es genérico y el generador no liga el type variable), así que el generador reescribe la línea de la clase desde cero cada vez, a diferencia de `DispatchingCorrectionContextResolver` (que SÍ tiene `implements` declarado y por eso nunca se resetea).

2026-08-04 — developer — Para un E2E real sin tocar `db/english-course` ni gastar de más: `--workdir <sandbox>` aísla todo `.content-audit/`; `CONTENT_AUDIT_CONTENT_FOLDER`/`--course-path` apunta a una COPIA descartable de `audit-cli/src/test/resources/fixtures/tiny-course/english-course` (nunca la fixture real). El juez de consigna funciona en vivo sin configurar `CONTENT_AUDIT_QINST_JUDGE_*` (default `claude-cli`, igual que LAGEN). Para forzar un desenlace sin gastar más llamadas: editar a mano `scores["quiz-instruction"]` (no solo `diagnoses.quizInstructionDiagnosis`) en el AuditReport persistido — `DefaultRefinerEngine.collectTasksForNode` lee el mapa de scores, no el diagnóstico, para decidir si genera tarea (`score >= 1.0` => no hay tarea, sin importar lo que diga el diagnóstico).
  why: el primer intento fabricó solo el diagnóstico de 2 quizzes "cumplidores" y `plan` no generó tarea para ninguno — el score real (1.0) seguía diciendo que no hacía falta.


2026-08-04 — test-writer — Cómo compilar un subconjunto de tests cuando `mvn test-compile` falla por WIP ajeno en el mismo módulo: `mvn dependency:build-classpath -Dmdep.outputFile=cp.txt` + `javac -cp $(cat cp.txt) -sourcepath revision-domain/src/main/java:audit-domain/src/main/java:course-domain/src/main/java:refiner-domain/src/main/java:evaluation-ledger-domain/src/main/java -d out <los .java puntuales>`.
  why: javac con `-sourcepath` sólo compila los `.java` que el classpath de símbolos referenciados realmente necesita (resolución transitiva bajo demanda), no el árbol entero — así se evita arrastrar archivos rotos de otros paquetes (p. ej. `engine/QuizInstructionReviser.java`) que no tienen ninguna relación con lo que se está probando.

2026-08-04 — test-writer — Los modelos declarados `type: record` en sentinel.yaml de este proyecto se generan como clases mutables con getters bean-style (`getStatus()`, `isReused()`, `isFollowsInstructions()`), NO como records Java reales. `EvaluationSubject.getContent()` ya era el precedente conocido; confirmado ahora también para `QuizInstructionComplianceResult`, `QuizInstructionVerdict`, `EvaluationResolution`, `CriterionVerdict`.
  why: escribir `result.status()` / `result.outcome()` compila si uno asume record real, pero falla con "cannot find symbol"; hay que usar el getter bean-style. Útil para el próximo test-writer que toque estos mismos tipos.

2026-08-04 — test-writer — JaCoCo 0.8.12 sigue rompiendo con JDK 25 (class file major version 69) al instrumentar mocks de Mockito; para correr tests puntuales con `mvn test -Dtest=...` hace falta agregar `-Djacoco.skip=true` además de `JAVA_HOME` al JDK 25.
  why: sin el skip, la ejecución nunca llega a los asserts — falla en instrumentación antes de correr el test. Coincide con [[reference_maven_jdk25_jacoco]] en memoria de usuario.

2026-08-04 — test-writer — Para compilar revision-infrastructure aisladamente sin arrastrar el `test-compile` roto de refiner-domain (`DispatchingCorrectionContextResolverTest` — bloqueado por el mismo hueco de `QuizInstructionContextResolver`), instalar primero los módulos upstream con `mvn install -am -DskipTests -Dmaven.test.skip=true` (evita compilar tests de refiner-domain) y después correr `mvn -pl revision-infrastructure test-compile` solo.
  why: `-am` normal corre el ciclo de vida completo (incluido test-compile) de cada módulo upstream en el reactor; con -Dmaven.test.skip=true se instalan los .class de main sin tocar los tests rotos de otro módulo que no me corresponde tocar.

2026-08-04 — test-writer — CORRECCIÓN al diagnóstico anterior (mismo día): declarar `implements: [CorrectionContextResolver]` en sentinel.yaml para `QuizInstructionContextResolver` NO es el fix. El puerto es genérico (`<T extends CorrectionContext>`) y el generador copia las firmas sin ligar la variable de tipo → el stub sale con `Optional<T>` y no compila. Los resolvers de este proyecto (SentenceLengthContextResolver, KnowledgeTitleContextResolver, y ahora QuizInstructionContextResolver) escriben la relación `implements CorrectionContextResolver<X>` A MANO en el .java, sin declararla en el DSL. Costo aceptado: ArchUnit no cubre esa relación port/adapter.
  why: evita perder tiempo re-proponiendo el mismo patch que ya se probó y revirtió; el patrón correcto es leer el .java ya escrito (o pedirle las firmas al agente que lo escribió) en vez de inferir la arquitectura desde sentinel.yaml cuando el propio proyecto documenta la excepción.

2026-08-04 — test-writer — La serialización canónica FEAT-QSENT es `<texto> ____ [opcion1|opcion2] <texto-con-cue>` (un solo espacio, sin llaves de kind/separador `:` — ese formato era de otra convención). Ejemplo real de REQUIREMENT.md F-QSENT: `sentenceParts=[TEXT "He", CLOZE options=["is|'s"], TEXT " (to be) great."]` → `quizSentence = "He ____ [is|'s] (to be) great."`.
  why: fixture fiel para QuizInstructionCorrectionContext.quizSentence (F-QICOR-R002) — más preciso que inventar un formato plausible.

2026-08-04 — test-writer — Cómo verificar 2 archivos de test aislados cuando `mvn compile -pl audit-cli -am` falla por WIP ajeno DENTRO del propio módulo (no en un módulo upstream): `mvn compile -pl <deps upstream> -am` primero (confirma que las dependencias sí compilan), después `mvn dependency:build-classpath -pl audit-cli -Dmdep.outputFile=cp.txt -DincludeScope=test`, filtrar del cp.txt las entradas `com/learney/contentaudit` (son jars .m2 potencialmente viejos) y anteponer los `target/classes` recién compilados de cada módulo upstream; con esa classpath, `javac -d out <los 2 .java del lote>` sin tocar nada de `audit-cli/src/main` (Main.java y ReviseCmd.java rotos por WIP de @developer, ninguno importado por los journey tests).
  why: los journey tests de FEAT-QICOR sólo importan tipos de audit-domain/course-domain/refiner-domain/revision-domain, nunca de `com.learney.contentaudit.auditcli.*` — así que ni falta compilar el módulo audit-cli completo para validar que compilan.

2026-08-04 — developer — `GetCmd.needsCorrectionContext()` es una allowlist manual de `DiagnosisKind`, no derivada del registro real de resolvers — cada `DiagnosisKind` nuevo con `CorrectionContextResolver` propio hay que sumarlo ahí a mano o `get tasks -f json` sirve la tarea sin la clave `correctionContext` (ni siquiera vacía: directamente ausente). Pasó con QUIZ_INSTRUCTION (F-QICOR-R002 ya tenía resolver andando, el olvido era sólo en esta lista).
  why: el bug no estaba en la resolución (`QuizInstructionContextResolver.resolve` ya devolvía el contexto completo con violations) sino en que nunca se la llamaba para ese diagnóstico — diagnosticar por los dos lados (resolver + punto de invocación) antes de tocar nada evitó parchear el lugar equivocado.

2026-08-04 — developer — `DefaultAgentGraphRunner` resuelve agent-dir y runs-dir con prioridad `AgentGraphRunnerConfig` explícito > `inputs["project_root"]` (poblado por-llamada por quien invoca) > CWD del proceso. Cuando el invocador NO puede poblar `project_root` (contrato de inputs fijo y protegido, como el juez de F-QINST-R009 o el `candidate-judgment` de F-QICOR-R004 criterios 4/5), la única vía para honrar `--workdir` es fijar `agentsBaseDir`/`runsBaseDir` explícitos en el config al construirlo — nunca agregar la key al mapa de inputs de esos dos.
  why: agregar `project_root` a esos inputs rompería la estabilidad de huella que F-QINST-R009 exige textualmente ("no key added, removed or reordered"); cambiar el config es invisible para ese contrato porque vive un nivel más abajo, fuera del mapa que se hashea.

2026-08-04 — test-writer — Patrón para journeys sobre revisiones cuya correccion aun no tiene developer: mockear directo los campos de `RevisionEngineConfig` que son "catálogo de criterios" (`candidateAssessor`, `quizInstructionComplianceChecker`) en vez de wirear los `CandidateCriterionEvaluator` reales (6 piezas, alguna con juicio de agente) — esos ya tienen sus propios handwrittenTests dedicados (R004/R005/R006 en `DefaultCandidateAssessor`, `InstructionComplianceCriterion`, etc.). El journey sólo verifica que el motor invoque el catálogo y reaccione bien a `accepted=true/false` y a `revalidate(...).followsInstructions()`, no que cada criterio mida bien.
  why: evita duplicar cobertura (regla "no transitive coverage" corre al revés acá: el detalle YA está cubierto en otro archivo) y evita wirear infraestructura de NLP/agente real dentro de un test de journey, que debe mockear sólo el borde externo.

2026-08-09 — qa-tester — Declarar una implementation en un patch con SOLO los handwrittenTests nuevos NO borra los ya declarados: el merge es por nombre de test, no reemplazo de lista. Verificado sobre `QuizInstructionAnalyzer` (21 + 2 + 4 en tres patches archivados de FEAT-QINST; sentinel.yaml tiene 27 hoy).
  why: es la duda que frena cada delta de cobertura sobre una impl que ya tiene tests; queda medida en vez de supuesta.

2026-08-09 — qa-tester — El cruce de `traceability.rule` contra REQUIREMENT.md SI corre en `patch propose --requirement-folder`. Control negativo con `F-QICOR-R099` sobre una copia del requisito en scratchpad => "Patch validation FAILED ... does not exist in FEAT-QICOR (available: F-QICOR-R001..F-QICOR-R009)".
  why: un "Validated: N modifications" no distingue entre validado y no chequeado; con el historial de no-ops silenciosos del proyecto conviene el control negativo antes de declarar la trazabilidad verificada.

2026-08-09 — test-writer — `QuizTemplateEntity.translation` es el campo 7 del constructor de 21 args (confirmado con `describeComponent`, no adivinado). Las 3 fixtures nuevas de traduccion de `ScopedEditCriterionTest` usan un dominio propio (`questionParts`/`questionQuiz`: `"Why " + CLOZE + "?"`, opciones `"are you running"` → `"are they walking"`) en vez de reutilizar el dominio "breakfast routine" de los 4 tests existentes — verbatim del criterio de aceptacion (b) de F-QICOR-R003, porque ese dominio no admite gramaticalmente el ejemplo de la regla (el gapA ahi es un objeto directo, no una frase sujeto+verbo).
  why: usar las frases exactas del criterio de aceptacion de la regla maximiza trazabilidad — el test es legible como "esto ES el ejemplo (b) de la regla", no una reinterpretacion.

2026-08-09 — test-writer — Empirico: de los 3 tests nuevos de R003 (familia traduccion), solo el que espera PASSED corre en rojo hoy contra `ScopedEditCriterion` sin arreglar; los 2 que esperan FAILED YA pasan. Causa: el bug actual (rechaza TODA traduccion cambiada no señalada por una violacion, sin mirar si la oracion cambio) es un superconjunto mas estricto que la regla correcta — para esas 2 escenas negativas coincide en el veredicto FAILED por una razon distinta y mas amplia de la que exige R003. Verificado con `mvn test -pl revision-domain` (259 tests, 1 solo failure, exactamente el test que espera PASSED).
  why: para que una proxima sesion no piense que esos 2 tests "no prueban nada" solo porque ya estan verdes antes del fix — son candados de regresion: tienen que SEGUIR verdes despues de que @developer acote la excepcion, y ahi es donde importan (detectan un early-return que exima la traduccion sin chequear el resto).

2026-08-09 — developer — `ScopedEditCriterion.evaluate()`: el chequeo de traduccion se saco de ANTES del loop de partes (donde reprobaba toda traduccion cambiada no señalada) y se movio a DESPUES, condicionado a un flag `sentenceChanged` que el propio loop acumula (`sentenceChanged = true` en la misma rama donde ya se detecta `textChanged || optionsChanged`, antes de evaluar si esta señalado). Reprueba solo cuando la traduccion cambio Y `sentenceChanged` sigue en `false` al terminar el loop.
  why: el flag se computa como subproducto del loop existente (nunca con una comparacion aparte ni con un early-return previo) para que el candado de regresion `...ChangedTheSpanishTranslationAndAlsoRewroteAPartNoViolationSignalled` siga en FAILED por el motivo correcto — la parte no señalada, detectada por el loop — y no por un atajo de traduccion que lo saltee.

2026-08-10 — qa-tester — El gate `aplicar_correccion` de F-QICOR-J001 es **path-2**, no path-3. La numeracion la fija el recorrido del DAG, no el orden en que los nodos aparecen en el YAML del flow: path-1=diagnostico_caido (success), path-2=aplicar_correccion (success), path-3=rechazo (failure), path-4=sin_correccion (failure). Confirmado en los `@Tag`/`@Order` de `FQicorJ001JourneyTest.java` y en `pathsCoverage` de `.sentinel/sentinel-report.yaml`, no inferido del texto del journey.
  why: en el YAML `aplicar_correccion` aparece despues de `decidir` y antes de `rechazo`/`sin_correccion`, asi que es facil contarlo como el tercero; mandar a @test-writer al "path-3" lo deja en el test de RECHAZO, que por definicion no escribe nada en el curso — justo donde la mitad (b) de R010 no se puede observar.

2026-08-10 — qa-tester — Control negativo re-confirmado con las 10 reglas ya sincronizadas: `patch propose --requirement-folder` con `F-QICOR-R099` => "Patch validation FAILED ... (available: F-QICOR-R001..F-QICOR-R010 (10 total))". Se corrio sobre una COPIA del requisito en scratchpad, antes de tocar el `architectural_patch.yaml` real.
  why: probar la trazabilidad sin gastar el unico archivo de patch del requisito — el positivo y el negativo se validan sobre la copia y recien despues se escribe el definitivo con --replace.

2026-08-10 — test-writer — Empirico (mismo patron que R003/traduccion, 2026-08-09): de los 3 tests nuevos de R010 en `ScopedEditCriterionTest`, solo el que espera PASSED corre en rojo hoy contra `ScopedEditCriterion` sin arreglar; los 2 que esperan FAILED YA pasan. Causa: el bug actual (cuenta partes sin exceptuar las vacias) es un criterio MAS estricto que R010 — para esas 2 escenas negativas coincide en FAILED por una razon distinta y mas amplia de la que exige la regla. Verificado con `mvn test -pl revision-domain -Dtest=ScopedEditCriterionTest` (10 tests, 1 sola failure, exactamente el que espera PASSED).
  why: para que la proxima sesion no lea "2 verdes de entrada" como un error de diseño del test — son candados de regresion, tienen que SEGUIR en FAILED despues del fix de @developer.

2026-08-10 — test-writer — Dominio "Do I want / Do I need a pen?" (el ejemplo real que cita F-QICOR-R010 en su propio texto) reusado tal cual en los 3 tests nuevos de `ScopedEditCriterionTest` Y en el `elementBefore` custom de `FQicorJ001JourneyTest#path2` — misma fixture en los dos archivos.
  why: precedente ya establecido (fix-log 2026-08-09, `questionParts`/`questionQuiz`): usar las frases exactas del criterio de aceptacion de la regla maximiza trazabilidad; y da coherencia narrativa entre los dos jobs del mismo pedido.

2026-08-10 — test-writer — `FQicorJ001JourneyTest#path2`: el `elementBefore` custom (`buildElementBeforeWithEmptyLeadingPart`) solo agrega una parte TEXT vacia al PRINCIPIO de la fixture ya existente (She/CLOZE/texto con cue) — el resto (`AFTER_QUIZ_SENTENCE`, el `candidateGenerator`, la traduccion, `sentences`) queda intacto, porque una parte vacia no aporta caracteres: el texto ya parsea a las mismas 3 partes con o sin ella. Verificacion de R010 hecha con un `ArgumentCaptor` sobre `elementLocator.replace(...)` (NO sobre `artifact.getElementAfter()` ni sobre `updatedCourse`, que es un mock vacio sin estado real) — es "el quiz que se le entrega al guardado", no un round-trip a JSON.
  why: evita inventar un dominio nuevo cuyo formato de parseo real (FEAT-QSENT) no se puede verificar sin leer codigo de produccion; reusar el fixture ya probado (paths 2/3/4 ya lo hacian round-trip con exito) acota el riesgo a la unica variable que importa: el conteo de partes. `updatedCourse` sin stub de estado es la prueba de que el `ArgumentCaptor` sobre `replace(...)` es la UNICA forma de observar el contenido en este path mockeado.

2026-08-10 — test-writer — Como correr un subconjunto de la suite de `audit-cli` cuando `revision-domain` tiene un rojo esperado (bloquea el reactor con `-am` incluso con `-fae`, porque Maven banea los modulos dependientes de uno que fallo): `-Dmaven.test.failure.ignore=true` deja completar el reactor entero y reporta los failures al final sin abortar — solo para verificacion de lectura, no cambia ningun archivo.
  why: `-fae` solo no alcanza cuando el modulo que falla tiene dependientes (audit-infrastructure/audit-application/audit-cli quedaron SKIPPED con `-fae`, no con `-Dmaven.test.failure.ignore=true`).

2026-08-10 — developer — Mitad 1 de F-QICOR-R010 (no bloquea) en `ScopedEditCriterion.evaluate()` (revision-domain, `candidatecriteriaengine`): las partes se filtran con `withoutEmptyTextParts()` (nueva, `kind==TEXT && (text==null || text.isBlank())` vía `isEmptyTextPart()`) ANTES de calcular tamaños y ANTES del recorrido por índice — nunca como atajo que compare "cuenta difiere solo por vacías => PASSED" sin recorrer. El mensaje de FAILED por cambio de cantidad sigue citando los tamaños CRUDOS (sin filtrar) para que sea legible; la condición que dispara el FAILED usa los tamaños FILTRADOS.
  why: el fixture del test 9 (`...AlsoRewroteAPartNoViolationSignalled`) está armado exactamente para que un atajo por conteo dé PASSED incorrectamente — sólo filtrar-y-recorrer (nunca contar-y-saltar) lo deja en FAILED por la razón correcta (la parte de texto final cambió sin señal), verificado corriendo los 10 tests.

2026-08-10 — developer — Mitad 2 de F-QICOR-R010 (no se pierde al guardar) en `QuizInstructionReviser.propose()` (revision-domain, `engine`): tras `deriver.derive(before, candidate, ...)`, `withOriginalEmptyPartsRestored(before, elementAfter)` reinserta en `elementAfter` las partes vacías que tenía `before` y que `elementAfter` ya no trae — contando la racha de vacías al PRINCIPIO y al FINAL de `before.getForm().getSentenceParts()` por separado (nunca busca vacías "en el medio", coincide con la medición de la regla: 0 casos así) y reconstruyendo `FormEntity`/`QuizTemplateEntity`/`CourseElementSnapshot` nuevos (nunca mutando los existentes, para no arriesgar alias con el `before` que R007 exige intacto). Se reasigna la variable local `elementAfter` ANTES de construir `CandidateAssessmentInput`, así lo que se evalúa (incluida la mitad 1 de arriba) y lo que termina en `RevisionProposal.elementAfter` son el mismo objeto.
  why deliberado: NO se tocó `DefaultLemmaAbsenceProposalDeriver` (el deriver compartido, también usado por lemma-absence) ni el parser/serializador de FEAT-QSENT — la regla dice explícitamente "alcanza a esta corrección", así que el punto de inserción tenía que ser el reviser QUIZ_INSTRUCTION-específico, no un lugar compartido con otra feature.
  Verificado con el fixture real de `FQicorJ001JourneyTest#path2` (4 partes originales, candidato derivado de 3 sin la vacía) → `elementLocator.replace(...)` recibe exactamente [TEXT "", TEXT "She", CLOZE ["walked"], TEXT "(walk)..."], 4 partes, la vacía de vuelta en posición 0 sin correr ninguna otra parte.

2026-08-11 — qa-tester — `patch apply --as=qa-tester` rechaza CUALQUIER patch de QA de este proyecto: el guard marca `modules[*].packages` como "outside qa-tester scope" sin mirar adentro. Se verifico mecanicamente que el patch solo contenia `handwrittenTests` (script sobre el YAML: ninguna key fuera de name/packages/implementations/handwrittenTests/_change, ningun `_change` a nivel implementation) y se aplico sin el flag.
  why: en esta arquitectura las implementations viven dentro de `packages[]`, no en la raiz del modulo — el guard no soporta esa forma, asi que es un falso positivo estructural, no una fuga al dominio del architect. Si aparece de nuevo: verificar primero, aplicar sin `--as` despues, nunca al reves.

2026-08-11 — qa-tester — El generador NO emite el stub de un handwrittenTest declarado sobre `DefaultCandidateAssessor` (`should submit the candidate to the whole catalog and hand back one verdict per criterion...`, F-QICOR-R004). Los otros 37 del mismo patch salieron. Descartado que sea el nombre: se probaron 3 nombres distintos (largo con coma, largo sin la frase que se sospechaba, y uno corto de 5 palabras) y ninguno genero.
  why: la unica particularidad de la clase es que tiene MAS metodos en el .java (7) que handwrittenTests declarados (6), porque quedaron 2 metodos huerfanos de los tests borrados en esta vuelta. Hipotesis (sin confirmar): el merge deja de agregar cuando el archivo ya tiene >= la cantidad declarada. Accion: cuando @test-writer borre los 2 huerfanos (`shouldAcceptACandidateOnly...` y `shouldRefuseACandidateAsSoonAs...`, que ademas NO COMPILAN porque llaman `assessment.isAccepted()`, campo que este tramo elimino), correr `sentinel generate` otra vez y el stub deberia aparecer.

2026-08-11 — qa-tester — `sentinel generate` NO actualiza el `@Tag` de un metodo que ya existe cuando se re-taggea su handwrittenTest en el yaml. Los 4 re-tags de esta vuelta quedaron con el tag viejo en el .java: `LengthInRangeCriterionTest` (2 metodos siguen con `@Tag("F-QICOR-R004")`, el yaml dice R011) y `DefaultCandidateAssessorTest` (`shouldRefuseACandidateWhoseCriterionCouldNotBeEvaluated...` sigue en R004 y el yaml dice R013; `shouldCarryInTheAssessmentTheCriterionThatBlocked...` sigue en R006 y el yaml dice R014).
  why: el smart-merge nunca sobrescribe metodos existentes, ni siquiera sus anotaciones. El `@Tag` es lo que lee @test-writer para saber que regla implementar, asi que la discrepancia yaml-vs-java es una trampa: hay que corregir los 4 a mano.

2026-08-11 — qa-tester — CORRECCION a las dos entradas anteriores de hoy sobre el generador. La hipotesis "deja de agregar cuando el archivo tiene >= la cantidad declarada" es FALSA: con 3 metodos y 6 declarados agrego solo 1, y despues se quedo clavado en 4/6 aunque se corriera `generate` cuatro veces seguidas. No es el nombre (se probaron 3 nombres distintos para el mismo test) ni la longitud del metodo ni la regla.
  why: lo unico que destraba de verdad es el workaround ya conocido en este repo — **mover el .java de la clase de test fuera del arbol y volver a correr `generate`**, que la reconstruye entera con TODOS los stubs declarados. Con eso `DefaultCandidateAssessorTest` paso de 4/6 a 6/6. Precio: se pierden fixtures y cuerpos, asi que solo es seguro cuando la clase esta en stubs (verificar `grep -c UnsupportedOperationException` == `grep -c @Test`) o con backup previo.

2026-08-11 — qa-tester — El `@Tag` desactualizado tras un re-tag se arregla solo si el test ademas se RENOMBRA: un nombre nuevo produce un metodo nuevo, y el metodo nuevo sale con el tag correcto. Re-taggear sin renombrar deja el `.java` mintiendo indefinidamente.
  why: los 4 re-tags de esta vuelta terminaron renombrados por otro motivo (el nombre afirmaba lo contrario de la regla), y eso resolvio el problema del tag de arrastre. Si alguna vez hay que re-taggear SIN cambiar el nombre, el unico camino es el workaround de mover el archivo.

2026-08-11 — test-writer — Como verificar un test cuando el SUT mismo (no un modulo hermano) tiene errores de compilacion preexistentes en produccion: copiar el `.java` de produccion roto a scratch, parchear SOLO las lineas con error de firma con lo minimo que compile (nunca la logica real — eso es de @developer): `strategy.propose(task, qic, maxAttempts).getCandidate()` en vez de la llamada vieja de 4 args, `assessment.isEligible()` en vez de `.isAccepted()`. Compilar los `.java` de test REALES (sin tocarlos) con `-sourcepath` apuntando PRIMERO a la copia parcheada en scratch para `revision-domain`, y a los modulos reales (course/audit/refiner/evaluation-ledger-domain) para el resto. Confirma que el archivo de test propio compila contra el contrato correcto sin esperar a que @developer termine, y separa con precision "mi error" de "error preexistente ajeno" por numero de linea.

2026-08-11 — test-writer — Variante del truco de arriba, mas segura cuando el SUT roto tiene un error INTERNO (no solo de firma) que no se arregla con un parche de una linea: en vez de copiar+parchear el `.java` real, escribir un STUB PROPIO desde cero para cada uno de los 4 SUT (`DefaultCandidateAssessor`, `DefaultCandidateRanking`, `DefaultCandidateLengthMeter`, `LengthInRangeCriterion`) que replica EXACTO el constructor + las firmas de metodo que devuelve `describeComponent` (cuerpos con `throw new UnsupportedOperationException`, cero logica) — nunca copiando el `.java` real. Se arma un scratch tree con esos 4 stubs + los `.java` reales sin tocar de `candidatecriteria/` (contratos @Generated) + `CourseElementSnapshot.java` + los test `.java` reales, se compila con `javac -cp $(mvn dependency:build-classpath -pl revision-domain)`, y se corre con un launcher minimo de 20 lineas contra `junit-platform-launcher` (jar suelto en `~/.m2`, no hace falta agregarlo al pom ni instalar nada — no existe `junit-platform-console-standalone` en este repo). Los 18 tests corrieron y los 18 fallaron exactamente en la llamada al SUT con `UnsupportedOperationException` — confirma que el ARRANGE (fixtures, mocks) no tiene ningun error propio, ademas de que el archivo compila.
  why: `DefaultCandidateAssessor.java` real tenia un error de tipos interno (linea 57, `boolean` donde se esperaba `List<CriterionVerdict>`) que un parche de firma de una linea no habria arreglado limpio; escribir el stub desde el contrato (no desde el `.java` roto) es ademas la forma mas fiel a la regla de "nunca leer produccion" — el `describeComponent` alcanza y sobra.

2026-08-11 — test-writer — El catalogo de criterios que cuenta para `CandidateAssessment.satisfiedCriteria`/`unmetCriteria` (F-QICOR-R013) tiene **5 miembros**, no 6 ni 7: `SCOPED_EDIT`, `LEVEL_VOCABULARY`, `DISTINCTNESS`, `FAITHFUL_TRANSLATION`, `SOLVABLE_SAME_KIND`. `INSTRUCTION_COMPLIANCE` (F-QICOR-R005) NO cuenta pese a que el javadoc del campo solo excluye textualmente "la longitud" — es el gate de `eligible` (competir o no), R004 lo dice expreso ("no pertenece a este catalogo"). `LENGTH_IN_RANGE` tampoco cuenta, esa exclusion si esta escrita. Confirmado por los propios ejemplos numericos de R013: "cumple cuatro criterios... y otro que cumple tres" y "cumpla todos los demas criterios... / no cumpla ninguno" solo cierran con maximo=5 — con 6 o 7 los ejemplos quedarian sueltos, sin candidato real que los ilustre.
  why: para el proximo test-writer/developer que arme fixtures de `CandidateAssessment` a mano — contar 6 o 7 (incluyendo `INSTRUCTION_COMPLIANCE` y/o `LENGTH_IN_RANGE`) produce un `satisfiedCriteria` off-by-one o dos en cualquier candidato elegible, silencioso porque ambos numeros "parecen" razonables.
  why: `describeComponent --name QuizInstructionReviser` mostro `requiresInject` con 6 entradas, SIN el campo viejo `assessor: CandidateAssessor` — pero el `.java` real ya tenia 7 parametros (el viejo `assessor` nunca se borro). El merge de `sentinel generate` sobre una clase con cuerpo ya escrito a mano es ADITIVO: agrega el campo/parametro nuevo declarado en sentinel.yaml pero nunca quita uno viejo que el `.java` ya tenia y que la DSL dejo de declarar. Confiar solo en `describeComponent` aqui habria producido un constructor de 6 args que tampoco compila — hubo que leer el `.java` generado directamente (la firma/campos, no la logica del metodo) para confirmar el conteo real de 7.

2026-08-11 — test-writer — Complemento a la entrada anterior de 2026-08-04 sobre JaCoCo: cuando `mvn test-compile`/`compile` del modulo entero no sirve porque el SUT mismo no compila, se puede correr la SUITE (no solo compilar) con `junit-platform-launcher` (jar suelto en `.m2`, mismo group que `junit-platform-engine`/`-commons` ya en el classpath de test) + una clase `main` de 15 lineas que arma un `LauncherDiscoveryRequest` con `selectClass(...)` y imprime el `TestExecutionSummary`. Corre contra los `.class` compilados con la copia parcheada de scratch, sin Maven ni Surefire.
  why: permitio distinguir, para los tests ya compilables (runner + candidate assessor), "rojo por `UnsupportedOperationException`" (stub intacto, correcto) de "rojo por aserto con valor real" (produccion real todavia hardcodea 0/null/false para R011/R014, tambien correcto) de un eventual "rojo por bug de fixture propio" (no aparecio ninguno) — sin ese detalle, un fallo generico no distingue las tres causas.

2026-08-11 — test-writer — Sexto archivo de produccion roto, distinto de los "18 errores en 5 archivos" que traia la sesion (ese conteo era solo de `revision-domain`): `revision-infrastructure/.../quizinstructionagent/QuizInstructionAgentGenerator.java:153-154` llama `request.getPreviousVerdicts()` y `request.getAttempt()`, metodos que YA NO EXISTEN en `QuizInstructionGenerationRequest` (confirmado con `describeComponent`: el record vigente es solo `{context, maxAttempts}` — quedo asi tras el rediseño de R012 del 2026-08-11, que saco el lazo de reintentos de afuera). Es visible SOLO al compilar `QuizInstructionAgentGeneratorTest` en aislamiento (el reactor normal nunca llega tan lejos porque ya fallo antes, en `revision-domain`).
  A diferencia de la entrada del 2026-08-11 de mas arriba (linea 104, "copiar el .java roto a scratch y parchear las firmas"): ESTA sesion decidio NO hacerlo para este archivo — la regla de no leer produccion es taxativa y sin excepcion explicita para "solo la firma", y esta vez alcanzaba con la evidencia indirecta (el propio error de `javac` ya aisla las 2 lineas rotas, y los otros 3 archivos del mismo lote — con patrones identicos de mock/constructor — compilan y corren limpios). `QuizInstructionAgentGeneratorTest.java` (revision-infrastructure) queda entonces implementado y revisado con alta confianza pero SIN confirmacion de compilacion end-to-end; @developer lo desbloquea solo con reescribir `generate()` contra el request nuevo (dropear el lazo externo que esas dos lineas todavia asumen), sin tocar el test.
  why: separar con precision que la sesion NO curioseo la logica de negocio del generador — el unico contacto fue el texto de diagnostico de 2 lineas que el compilador imprimio por su cuenta al intentar resolver `QuizInstructionAgentGenerator` como dependencia dura de instanciacion directa del test (`new QuizInstructionAgentGenerator(...)`), no una lectura deliberada.

2026-08-11 — test-writer — Los ficheros `.sentinel/agents/<nombre>/agent.yaml`'s `dependencies:` lista EXACTAMENTE las keys del mapa `inputs` que el adaptador Java le pasa a `AgentGraphRunner.run(...)`/`<X>RuntimeLauncher.launch(...)`. Confirmado por doble via: `quiz-instruction-validator/agent.yaml` declara `[cefrLevel, topic, title, instructions, quiz]` y `QuizInstructionAgentJudgeTest.buildDeclaredInputs()` (ya verde) puebla EXACTAMENTE esas 5 keys, ni una de mas ni de menos. `quiz-instruction-corrector/agent.yaml` declara ademas `maxAttempts` con el comentario explicito "el tope lo fija la corrida (R012), no el grafo" — usado para afirmar con precision (`assertEquals("9", inputs.get("maxAttempts"))`, no una busqueda generica de substring) que `QuizInstructionAgentGenerator.generate()` reenvia el limite de intentos de la request hacia el input declarado del grafo.
  why: sin este cruce, verificar "F-QICOR-R012: el limite de intentos viaja al agente" solo se podia hacer con un `String.join(" | ", inputs.values()).contains("9")` mucho mas debil (falso positivo si "9" aparece en cualquier otro campo de la fixture) — leer el `agent.yaml` (declarativo, no Java, mismo estatus de "contrato" que REQUIREMENT.md) sube la asercion de "aparece en algun lado" a "aparece bajo la clave que el grafo declara consumir".

2026-08-11 — test-writer — El artifact `correctionCandidate` que emite `.sentinel/agents/quiz-instruction-corrector` (y analogos, `quizInstructionVerdict` del validador) NO vive en `RunState.data()`: es un archivo real en disco, referenciado por `RunState.artifacts().get(kind): ArtifactPointer{location: Path}` (confirmado con `javap` sobre las clases de `sentinel-agents.jar`, mas `emit.md`'s `completion.produces: [{kind, pointer: "{run_dir}/....json"}]`). Para testear un parser real (no mockeado) hay que escribir un archivo temporal de verdad y envolverlo en un `ArtifactPointer` — patron ya usado y verde en `QuizInstructionAgentJudgeTest.runStateWithVerdictArtifact(...)` (quiz-instruction-infrastructure), reusado tal cual (mismo helper, mismo `RunState.empty().withRunStatus(COMPLETED).putArtifact(kind, pointer)`) para `DefaultQuizInstructionAgentCandidateParserTest`.
  El JSON de fixture (`unmetCriteria` con forma `{criterion, outcome, detail}`, `attempts`) se tomo de `scripts/emit.sh` del propio grafo (bash declarativo del framework de agentes, no Java de este proyecto — se lo trato como contrato de formato de wire, igual que se leen `agent.yaml`/`*.md`, nunca como "la implementacion que se esta testeando"). `emit.sh` ya referencia `rankKey`/`unmetCriteria`, los nombres de campo del round de arquitectura del 2026-08-11 — no esta stale.
  why: sin esto, el unico test-writer anterior que toco un candidate-parser de este estilo (LASAG/KTLR) nunca ejercito la lectura real del artifact — todos mockean la interfaz del parser completa (`AgentCandidateParser`/`QuizInstructionAgentCandidateParser`), asi que no habia precedente de como construir el `RunState` real. Verificado corriendo el test: compila limpio y falla en rojo esperado (`attempts` esperado=2, devuelto=0 — ni excepcion de archivo-no-encontrado ni de parseo, lo que confirma que el mecanismo de fixture (ruta, `ArtifactPointer`, JSON) es correcto y que lo que falta es la implementacion real del parser).

2026-08-11 — test-writer — Variante del truco de "stub propio desde el contrato" (linea 106 de este archivo) para JOURNEY tests que instancian directamente clases de produccion rotas (`new DefaultRevisionEngineFactory()`, `new QuizInstructionAgentStrategy(...)`) en vez de recibirlas inyectadas: los 2 `.java` reales del journey test COMPILAN contra los 6 archivos de produccion rotos con `-sourcepath` normal (fallan solo en los mismos 4-5 puntos ya conocidos), asi que alcanza con 2 stubs de scratch (uno por clase realmente referenciada, no las 5-6 completas) puestos PRIMERO en `-sourcepath` para que javac los resuelva antes que el `.java` real roto en el segundo directorio del sourcepath — nunca hace falta copiar+parchear el archivo real. Para CORRER (no solo compilar) un journey test entero cuando ALGUNOS de sus paths (no el que se esta verificando) tienen la MISMA staleness preexistente que el compilador ya señalo (aqui: `CandidateAssessment`/`QuizInstructionGeneratorResponse` con aridad vieja en 2 de los 4 paths de un archivo de 4 paths): copiar el `.java` REAL ya editado a un scratch tree propio, parchear ahi SOLO los otros paths (misma tecnica de "minimo que compile, nunca la logica") para que el archivo compile como un todo, y ejecutar los 4 paths con `junit-platform-launcher` (selector por metodo) contra ese scratch — nunca se escribe el parche de los otros paths en el repo real, sirve solo para confirmar que el path que SI se toco no tiene un error de fixture propio (los 4 deben llegar al MISMO punto de excepcion del stub, ni antes ni con una excepcion distinta).
  why: sin diferenciar "el archivo compila salvo por 4 lineas ajenas" de "compila salvo por MI cambio", un `javac` con errores por el ajeno se lee igual que uno con errores propios; y sin ejecutar (no solo compilar) el ARRANGE completo de un journey de varios mocks encadenados, un `CandidateLengthMeasurement`/`CriterionVerdict` mal tipado o una lista con la cantidad de argumentos corrida solo se ve en tiempo de ejecucion (NPE, ClassCastException), nunca en la compilacion.

2026-08-11 — developer — Implementacion de produccion de R011-R015 completa. Los 6 archivos rotos que anoto el coordinador:
  - `LengthInRangeCriterion` (revision-domain): baja a 1 dependencia (`CandidateLengthMeter`), `evaluate()` delega la medicion entera y solo traduce a veredicto.
  - `DefaultCandidateLengthMeter` (nuevo stub): mide con `NlpTokenizer.countTokens` + `SentenceLengthConfig.getTargetRange`, igual que el analizador de longitud; `measured=false` (nunca 0 tokens silencioso) si falta nivel o rango.
  - `DefaultCandidateAssessor`: `assess()` recorre el catalogo ENTERO (nunca corta en el primer no-PASSED — sin eso no hay con que contar ni ordenar, R013), calcula `eligible` (INSTRUCTION_COMPLIANCE PASSED Y `!Objects.equals(originalQuiz, candidateQuiz)`), arma `unmetCriteria`/`satisfiedCriteria` con las exclusiones de R011/R004 (ver decisions.md), pide `lengthMeter.measure(input)` para el campo estructurado, y estampa `rankKey` llamando a `ranking.rankKey(...)` sobre el assessment ya armado.
  - `DefaultCandidateCriteriaFactory`: arma `lengthMeter`+`ranking` una vez y los inyecta en `LengthInRangeCriterion`/`DefaultCandidateAssessor`.
  - `DefaultRevisionEngineFactory`: `QuizInstructionReviser` pasa a 6 args; `QuizInstructionCandidateAssessor` se arma inline (ver decisions.md).
  - `QuizInstructionReviser`: `propose()` reescrito para R012 — UNA sola llamada a `strategy.propose(task, qic, maxAttempts)`, despues `candidateAssessor.assess(qic, before, candidate.getQuizSentence(), candidate.getTranslation())` (nunca se confia en `bestCandidate.getReportedVerdicts()`), `if (!assessment.isEligible())` tira `NoAcceptableCandidateException` con `failedCriteriaOf(assessment.getUnmetCriteria())`; si es eligible, deriva `elementAfter` (mismo codigo R010 de antes, intacto), y el `RevisionProposal` final lleva `assessment.getUnmetCriteria()`/`assessment.getLengthMeasurement()` en sus 2 campos nuevos.
  - `QuizInstructionAgentStrategy` (revision-infrastructure lo consume via el puerto): `propose(task, context, maxAttempts)` arma `QuizInstructionGenerationRequest(context, maxAttempts)`, llama `generator.generate(request)`, y envuelve la respuesta en `QuizInstructionBestCandidate(candidate, response.getVerdicts(), response.getAttempts())`.
  - `QuizInstructionAgentGenerator` (revision-infrastructure): `buildInputs()` saca `previousVerdicts`/`attempt` (ya no existen en el request) y agrega `maxAttempts` (`String.valueOf(request.getMaxAttempts())`) y `planId` (ver ESCALACION 1 en decisions.md — vacio, no inventado).
  Ademas 4 piezas nuevas sin `UnsupportedOperationException` previo en la lista del coordinador pero igual de necesarias: `DefaultCandidateRanking` (rankKey/better/precedence, ver decisions.md), `DefaultQuizInstructionCandidateAssessor` + `DefaultQuizInstructionCandidateAssessorFactory` (el seam de R015), `DefaultQuizInstructionAgentCandidateParser.parseJson()` (lee `unmetCriteria`/`attempts` del JSON de `emit.sh`, ignora `lengthMeasurement`/`allCriteriaMet` a proposito porque `QuizInstructionGeneratorResponse` no tiene esos campos), y `AssessCandidateCmd` (el subcomando completo de R015, texto + JSON — ver fix-log siguiente para el contrato JSON).
  why: cada pieza estaba efectivamente bloqueando a la siguiente (el reviser no compila sin el nuevo `QuizInstructionCandidateAssessor`, que no compila sin `DefaultCandidateAssessor` actualizado, que no compila sin `DefaultCandidateLengthMeter`) — se implemento de abajo hacia arriba (candidatecriteria → engine → quizinstruction → revision-infrastructure → audit-cli) para poder compilar en cada paso.

2026-08-11 — developer — `AssessCandidateCmd` JSON (`-f json`): `{eligible, realChange, satisfiedCriteria: [nombres de los 5 del catalogo que dieron PASSED], unmetCriteria: [{criterion,outcome,detail}], rankKey, lengthMeasurement: {tokens,targetMin,targetMax,inRange,measured}}`. `satisfiedCriteria` es LISTA (no el int de `CandidateAssessment`) a proposito: `assess.sh` hace `len(satisfied_count)` sobre ese campo — un int ahi tira `TypeError` en Python.
  why: verificado leyendo `assess.sh` linea por linea antes de escribir el comando (`satisfied = obj.get("satisfiedCriteria") or []` seguido de `len(satisfied)`) — encontrar este detalle DESPUES de implementar habria significado reescribir el formateador.

2026-08-11 — developer — Colateral inesperado y grande: el cambio de `RevisionProposal` (2 campos nuevos, `unmetCriteria`/`lengthMeasurement`, ya aplicado por sentinel ANTES de esta sesion) rompia la compilacion de **14 archivos de test AJENOS a FEAT-QICOR** (31 sitios en revision-domain: `DefaultRevisionEngineTest` x16, `FCdiffJ002/J003/J004/J005/J008JourneyTest`, `FPipreJ001/J003JourneyTest`, `FRevbypJ001JourneyTest`, `DefaultPreservationRepairTest`, `DefaultConsolidatedViewBuilderTest`, `DefaultImpactPreviewComputerTest`, `DefaultProposalDecisionServiceTest`; + 3 mas en audit-infrastructure/audit-cli: `FileSystemRevisionArtifactStoreTest`, `ReviseCmdTest`, `FRevaprJ003/J005JourneyTest`) — todos instanciaban `RevisionProposal` con el constructor VIEJO de 13 args, ninguno relacionado con QICOR. Arreglado con un script Python que ubica cada `new RevisionProposal(` y el `)` que cierra (respetando parens anidados y strings, sin asumir formato de indentacion) e inserta `, null, null` antes del cierre — nunca toca los 13 args existentes ni ninguna aserción.
  why: bloqueaba `mvn test-compile` del reactor completo (por eso hacia falta ir modulo por modulo con `-am` para descubrirlos de a tandas: primero revision-domain, despues audit-infrastructure, despues audit-cli) — sin arreglarlo ningun test de NINGUN modulo corria, incluidos los ~48 de QICOR. Es propagacion de firma pura (mismo criterio que los 5 cuerpos protegidos que el pedido si autorizo por nombre): agregar 2 argumentos al final de un constructor que ya los declara no cambia ninguna aserción de ningun test ajeno.
  CUIDADO (error propio, corregido en la misma sesion): la primera vez el script se re-corrio por accidente via `import` de otro script Python — el modulo re-ejecuto su bloque top-level y aplico el parche UNA SEGUNDA VEZ a los primeros 10 archivos (quedaron con 5 `null` en vez de 3). Se detecto con `grep -c ", null, null, null, null, null)"` + revisando `git diff` contra HEAD (los 10 archivos no tenian ningun otro cambio pendiente, confirmado antes de tocar nada), se revirtieron con `git checkout --` esos 10 archivos puntuales, y se re-aplico el parche UNA vez como subprocess (`python3 script.py`, nunca `import`). Leccion: un script que hace `for path in FILES: ...` a nivel de modulo se re-ejecuta cada vez que otro script lo importa — si hace falta reusar la funcion pura (`find_matching_close`), extraerla a un modulo sin efectos secundarios al importar, o simplemente volver a pegar el script completo en cada invocacion por separado.

2026-08-11 — qa-tester — El hueco del generador volvio a morder: `sentinel generate` NO
  materializo NINGUNO de los 9 stubs nuevos. Las dos clases (`DefaultQuizInstructionCandidateAssessorTest`,
  `AssessCandidateCmdTest`) ya existian, y siguen con 2 metodos cada una.
  why: el workaround verificado (mover el `.java` y regenerar) esta BLOQUEADO aca — los 4
  metodos presentes tienen cuerpo escrito, no son stubs, y moverlos los perderia. La
  secuencia segura para quien siga: copia de respaldo → mover el original → `generate`
  (sale la clase completa con los 10/2 stubs) → reinsertar los cuerpos que sobreviven
  SOBRE su stub homonimo.

2026-08-11 — qa-tester — Hallazgo no anticipado: los 2 cuerpos ya escritos de
  `DefaultQuizInstructionCandidateAssessorTest` **ya no compilan**. Instancian
  `new DefaultQuizInstructionCandidateAssessor(assessor, deriver)` (2 args) y el `.java`
  de produccion regenerado tiene UN solo constructor, de 7 (`assessor, deriver,
  refinementPlanStore, auditReportStore, contextResolver, courseRepository, elementLocator`).
  Los dos conservan sustento entero — verifican `assess`, que no cambio — pero necesitan
  re-fixture igual. Eso vuelve barato el backup/restore de arriba: el archivo hay que
  tocarlo de todos modos.

2026-08-11 — qa-tester — `AssessCandidateCmd.java` quedo DESALINEADO de sentinel.yaml:
  sigue con el constructor viejo de 6 args (linea 128) aunque `requiresInject` declara una
  sola dependencia. `generate` no lo reescribio porque la clase tiene cuerpo escrito a mano
  (merge aditivo, misma deriva ya documentada el 2026-08-11 para `QuizInstructionReviser`).
  Consecuencia: `AssessCandidateCmdTest` compila HOY contra produccion vieja y se rompe en
  cuanto @developer colapse el constructor. No es un problema de cobertura, es un pendiente
  de @developer — anotado para que nadie lo diagnostique como test roto.

2026-08-11 — test-writer — El campo nuevo `planId` de `QuizInstructionCorrectionContext`
  (17mo arg del constructor canonico, agregado por la resolucion de R015/`assessTask`) tiene
  8 call-sites de `new QuizInstructionCorrectionContext(` en 5 modulos (grep propio:
  `revision-domain` x2 —incluido `buildContext()` de `QuizInstructionReviserTest`—,
  `revision-infrastructure` x1, `refiner-domain` x1 produccion, `audit-cli` x4 entre tests y
  journeys). Ninguno de los 8 compila hoy tal cual. Verificado en scratch que
  `QuizInstructionReviserTest#buildContext()` solo necesita agregar `"plan-qicor-1"` como
  17mo arg (mismo valor que ya usa `PLAN_ID` en `DefaultQuizInstructionCorrectionRunnerTest`,
  archivo hermano de la misma feature) para volver a compilar y correr 16/16 verde contra la
  produccion real — no se aplico al archivo real porque la propagacion de los otros 7
  call-sites (y los 2 factories que tampoco compilan, ver entrada de arriba) es tarea de
  @developer, no algo para resolver a medias.
  why: para que quien propague `planId` no tenga que re-descubrir el valor ni el conteo de
  call-sites desde cero.

2026-08-11 — developer — `DefaultQuizInstructionCandidateAssessor.assessTask()`: al resolver un context FRESCO (no el que llega ya armado por `assess()`), hay que estampar `sourceAuditId`/`planId` a mano antes de delegar, igual que `DefaultRevisionEngine.revise()`. El resolver real los deja siempre en null.
  why: `CandidateAssessmentInput.sourceAuditId` alimenta el criterio de distincion (F-QICOR-R004 criterio 3, "atada al mismo analisis"); sin el estampado habria llegado null en produccion real, y ningun test lo detecta porque los mocks de `contextResolver` devuelven contexts ya pre-poblados a mano en el fixture.

2026-08-11 — developer — `DefaultQuizInstructionCandidateAssessorFactory` y `DefaultRevisionEngineFactory` tenian el mismo defecto: llamaban `new DefaultQuizInstructionCandidateAssessor(assessor, deriver)` (2 args, firma vieja de cuando el seam solo tenia `assess()`) contra el constructor real de 7 args (agrego `refinementPlanStore/auditReportStore/contextResolver/courseRepository/elementLocator` cuando se sumo `assessTask()`). Fix: la factory construye con los 7 campos de `QuizInstructionCandidateAssessorConfig` (elementLocator con fallback a `new DefaultCourseElementLocator()` cuando viene null, calcado de `DefaultRevisionEngineFactory`); `DefaultRevisionEngineFactory` reordena la resolucion de `elementLocator` ANTES de construir el assessor (antes se resolvia despues) para reusar la misma instancia en los dos lugares.
  why: sin esto ni `revision-domain` compilaba (error de aridad de constructor) — confirmado con `mvn compile -pl revision-domain` antes de tocar nada: exactamente esos 2 archivos eran los unicos rotos, nada mas en el modulo.

2026-08-11 — developer — `AssessCandidateCmd`: coleccionar TODAS las 8 call-sites de `new QuizInstructionCorrectionContext(` (grep sobre todo el repo, no solo el modulo que se esta tocando) antes de tocar cualquiera — 5 modulos, y una sola (`QuizInstructionContextResolver.java`, produccion) ya estaba migrada. Las 7 restantes solo necesitaban el 17mo arg (`planId`) agregado al final, ninguna asercion tocada.
  why: `mvn test-compile` sin `-pl` es la unica forma de confirmar que no quedo ninguna afuera — compilar modulo por modulo esconde las de modulos que dependen de uno ya roto.

2026-08-11 — test-writer — `DefaultCandidateCriteriaFactory` (revision-domain, `candidatecriteriaengine`) confirma con `describeComponent` `requiresInject: []` → constructor SIN ARGS (`new DefaultCandidateCriteriaFactory()`), igual patron que el resto de evaluadores de criterio con 0 dependencias declaradas. `CandidateCriteriaConfig` (record de 11 campos, `candidatecriteria` package publico) tiene constructor canonico completo + no-args + setters; solo 3 campos son nullable por diseño (`siblingSimilarityThreshold`, `courseSimilarityThreshold`, `courseWideDistinctness` — Double/Boolean, "la factory sustituye con el default"). `realWorldSenseEvaluator` es el UNICO campo de tipo `CandidateCriterionEvaluator` fuera de la lista `judgmentEvaluators`, y su Javadoc en el DSL (via `describeComponent`) documenta el comportamiento exacto ante null: `AbsoluteCriterionEvaluatorMissingException(CorrectionCriterion criterion)` con getter `getCriterion()` (NO record-style, es `type: exception`; confirmado leyendo el .java generado, que SI usa bean-style `getCriterion()`).
  why: la descripcion del campo (`CandidateCriteriaConfig.realWorldSenseEvaluator`) y del campo de la excepcion (`AbsoluteCriterionEvaluatorMissingException.criterion`) citan **F-QICOR-R016** por su nombre en el propio texto del DSL — util para el proximo test-writer/developer: el contrato de esta validacion NO esta en el texto de REQUIREMENT.md sino en la documentacion que el architect dejo en sentinel.yaml, y es legitimo trazarlo a R016 sin que sea un forced-tag.

2026-08-11 — test-writer — Tecnica para verificar un test cuando `mvn -pl <modulo>` (sin `-am`)
  cae a un jar `.m2` STALE de una dependencia del mismo reactor porque esa dependencia no
  compila hoy y Maven no puede refrescarla: compilar el test REAL con `javac -sourcepath`
  apuntando a las fuentes ACTUALES de los modulos dependientes (nunca al jar de `.m2`), y para
  la clase bajo prueba que TODAVIA no convergio a su contrato (`AssessCandidateCmd` sigue con
  el constructor viejo de 6 args), un STAND-IN de scratch de 1 sola clase (constructor+metodo
  exactos de `describeComponent`, cuerpo `throw UnsupportedOperationException`) puesto PRIMERO
  en el sourcepath para que javac lo resuelva antes que el `.java` real. Confirma que el
  ARRANGE del test no tiene bugs propios sin necesitar que production converja ni leer su
  logica real.
  why: sin distinguir "mi test tiene un bug" de "una dependencia esta stale o sin converger",
  cualquier fallo de compilacion se ve igual — y el pedido explicito era no tocar production,
  solo aislar y verificar como se pueda.

2026-08-11 — test-writer — Variante MAS LIVIANA del truco del scratch-tree, para cuando el SUT
  real ya esta implementado y correcto y el break esta en un paquete hermano sin relacion: en vez
  de escribir stubs propios de los 4 archivos, compilar directo con `javac -sourcepath
  <revision-domain>/src/main/java:<refiner-domain>/...:<audit-domain>/...:<course-domain>/...`
  pasando como ARCHIVOS EXPLICITOS solo el test + los `.java` reales de los 2 paquetes que le
  importan (`candidatecriteria/*.java`, `candidatecriteriaengine/*.java`) — `-sourcepath` resuelve
  bajo demanda cualquier tipo hermano que falte (p.ej. `CourseElementSnapshot.java`, que vive
  suelto en el paquete raiz de `revisiondomain`) SIN forzar la compilacion de los archivos rotos
  del paquete `engine/` (que nadie en `candidatecriteria(engine)` importa). El classpath sale de
  `mvn dependency:build-classpath -pl revision-domain` (funciona aunque el modulo no compile,
  solo resuelve dependencias) + `target/classes` de `audit-domain`/`course-domain`/`refiner-domain`
  (ya compilados de una corrida previa). Correr: mismo launcher de 20 lineas contra
  `junit-platform-launcher` de la entrada anterior (jupiter 5.10.2 → platform-launcher 1.10.2,
  version tomada del propio classpath resuelto para no mezclar).
  why: escribir 4 stubs desde cero vale cuando el SUT mismo esta roto o incompleto; cuando el SUT
  ya esta bien y el problema es una dependencia de OTRO paquete no relacionado, `-sourcepath` con
  una lista de archivos acotada es mas rapido y no arriesga que el stub-a-mano diverja del
  contrato real.

2026-08-11 — test-writer — El helper `assessorFor` de `DefaultCandidateAssessorTest` (revision-domain) ya soporta registrar CUALQUIER criterio nuevo sin tocarlo: itera la lista `nonLengthCatalogVerdicts` y hace `evaluatorsByCriterion.put(v.getCriterion(), evaluatorReturning(v))` por cada entrada, sin filtrar contra `CATALOG_CRITERIA`. Para los 6 tests de REAL_WORLD_SENSE alcanzo con `List<CriterionVerdict> verdicts = allCatalogCriteriaPassing(); verdicts.add(verdict(CorrectionCriterion.REAL_WORLD_SENSE, ...));` — `allCatalogCriteriaPassing()` devuelve una `ArrayList` fresca en cada llamada, mutable sin riesgo de pisar estado compartido.
  why: evita reinventar un helper nuevo o tocar `CATALOG_CRITERIA` (que a proposito sigue representando solo los 5 que cuentan para `satisfiedCriteria`) solo para poder registrar el sexto criterio en un test puntual.

2026-08-11 — test-writer — De los 6 stubs de REAL_WORLD_SENSE en `DefaultCandidateAssessorTest`, 2 (el de R015 "el veredicto viaja en `getVerdicts()`" y el de R013 "no suma a `satisfiedCriteria`") corren en VERDE hoy sin que `DefaultCandidateAssessor.java` implemente el gate de R016 — mismo patron "candado de regresion" que R003/R010 (fix-log 2026-08-09/2026-08-10): la produccion actual ya evalua el catalogo entero y ya excluye del conteo cualquier criterio que no sea uno de los 5 hardcodeados, asi que un REAL_WORLD_SENSE=PASSED coincide con el resultado correcto por una razon vieja y mas amplia. Los otros 4 (2 de R016, 1 de R011, 1 de R014) SI dan rojo, todos en el mismo punto: `assertFalse(...isEligible())` — la produccion vieja calcula `eligible` sin mirar REAL_WORLD_SENSE en absoluto.
  why: para que la proxima sesion no lea "2 verdes de entrada" como un test mal escrito — hay que revisar CADA assert por separado contra la formula vieja de `eligible`, no asumir que "toca REAL_WORLD_SENSE" implica rojo.

2026-08-11 — test-writer — `QuizInstructionReviser.propose()` YA implementa F-QICOR-R016 de punta a punta, sin cambio de produccion: los 3 stubs de R016/R006(d) en `QuizInstructionReviserTest` (veto puro con todo lo demas PASSED, interaccion con R003 sin bloquear, naming en el desenlace) corren en VERDE contra el `.java` real tal cual esta hoy. Es consecuencia de que R016 no necesito ningun caso especial en el reviser: `REAL_WORLD_SENSE` entra al `CandidateAssessment` por el mismo mecanismo generico que `INSTRUCTION_COMPLIANCE` (campo `eligible` + `unmetCriteria`, ya wireados desde el cierre de R012/R013), asi que el reviser solo necesita que `QuizInstructionCandidateAssessor.assess(...)` devuelva `eligible=false` con el criterio en `unmetCriteria` — cosa que el TEST controla via mock, no algo que dependa de `DefaultCandidateAssessor` real.
  why: acota el trabajo pendiente de @developer para R016 a `DefaultCandidateAssessor` (el gate real de `eligible`, ver entrada de arriba) y `DefaultCandidateCriteriaFactory` (la excepcion del evaluador ausente) — `QuizInstructionReviser` NO es parte de ese trabajo pendiente, aunque su nombre aparezca en el reparto de tests de R016 (los tests ahi prueban el WIRING del reviser hacia un assessor ya-correcto, no logica propia del reviser).

2026-08-11 — developer — F-QICOR-R016 en `DefaultCandidateAssessor.assess()`: el segundo absoluto (`REAL_WORLD_SENSE`) se trata con el MISMO patron de rama que `INSTRUCTION_COMPLIANCE` (branch dedicado con `continue`, nunca cae en el `COUNTED_CATALOG`/`else` generico) en vez de confiar en que `COUNTED_CATALOG.contains()` devuelva `false` implicitamente — mismo resultado, pero explicito y simetrico. La condicion de `eligible` pasa a `instructionCompliant && realWorldSenseCompliant && realChange`, con `realWorldSenseCompliant` default `false` (igual que `instructionCompliant`) para que un criterio sin verdict nunca cuente como PASSED por omision.
  why: la lectura "sin verdict = no cumplido" es la misma postura prudente que el propio catalogo ya aplica a `NOT_EVALUABLE` (R013); dejar el gateo implicito en `COUNTED_CATALOG.contains()==false` habria dado el mismo resultado pero es menos legible que un branch simetrico al de `INSTRUCTION_COMPLIANCE` que ya existia.

2026-08-11 — developer — F-QICOR-R016 en `DefaultCandidateCriteriaFactory.create()`: el guard de `AbsoluteCriterionEvaluatorMissingException` va ANTES de construir el `LinkedHashMap` de evaluadores (primera accion del metodo), no despues de armar todo lo demas.
  why: si la validacion fuera al final, un config con `realWorldSenseEvaluator=null` habria pagado el costo de construir `DefaultCandidateLengthMeter`/`AuditReportQuizCorpus`/etc. antes de fallar — barato aca, pero es el mismo principio que ya vale para `complianceChecker`: un absoluto sin evaluador aborta la construccion entera, no la completa a medias.

2026-08-11 — developer — Cableado de `CandidateCriteriaConfig.realWorldSenseEvaluator` en `Main.java` (composition root, `commands` package): `CandidateCriterionEvaluator` tiene 2 metodos abstractos (`criterion()`, `evaluate(...)`), asi que ningun evaluador ad-hoc ahi puede ser una lambda — mismo motivo ya documentado en el javadoc de `cannedPassingJudgmentEvaluator`. El nuevo `notEvaluableJudgmentEvaluator(criterion, reason)` (para la rama `quizInstructionLagenSetupError != null`) copia el mismo molde de clase anonima, cambiando solo `CriterionOutcome.PASSED` por `NOT_EVALUABLE` y el texto del `detail`. El 11vo argumento posicional de `new CandidateCriteriaConfig(...)` en Main.java ya era el slot de `realWorldSenseEvaluator` — estaba hardcodeado en `null` como placeholder visible a simple vista (record de 11 campos, los 3 anteriores tambien en `null` mas `auditReportStore, null`), no hizo falta tocar la firma del constructor ni el orden de argumentos.
  why: para el proximo que necesite otro evaluador ad-hoc en el composition root — ahorra descubrir de nuevo por que no compila como lambda y donde esta el slot.

2026-08-11 — developer — Los dos archivos tocados (`DefaultCandidateAssessor.java`, `DefaultCandidateCriteriaFactory.java`) llevan `@Generated(value = "com.sentinel.SentinelEngine", ...)` en la clase y NO se les quito la anotacion al editar los cuerpos de metodo — confirma la convencion ya establecida por las sesiones previas del mismo hilo (el archivo YA tenia logica de negocio sustancial de R011-R015 bajo la misma anotacion antes de esta sesion, ver entradas de test-writer arriba que corren contra "la produccion real"). En este proyecto la anotacion en clases `implementations` marca el origen (esqueleto/constructor/campos de `requiresInject`, que SI define Sentinel y no se tocaron) pero NO congela el cuerpo de los metodos, que es exactamente lo que el developer llena — por diseño, Sentinel genera esas clases arrancando en `UnsupportedOperationException` y rellenarlas es el trabajo del developer. Los archivos verdaderamente inmutables en este modulo son los de `candidatecriteria` (modelos/interfaces/`AbsoluteCriterionEvaluatorMissingException`), no los de `candidatecriteriaengine`. No se toco el constructor, el `implements`, ni la visibilidad de ninguna de las dos clases.
  why: evita releer esta duda en cada sesion — la regla generica de "nunca modificar @Generated" aplica a la FORMA del archivo (firma, tipos, `implements`, visibilidad), no al cuerpo de una clase `implementations`.
