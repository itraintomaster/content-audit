# Fix log — FEAT-QICOR

2026-08-04 — developer — ScopedEditCriterion: `fragment.contains(text)` con `text=""` (CLOZE sin texto literal) siempre da true → cualquier hueco parecía "señalado". Fix: `containsOrIsContained` descarta `text` blank antes de comparar.
  why: `ScopedEditCriterionTest#shouldFailACandidateThatAlteredTheAcceptedOptionsOfAGapNoViolationImplicated` fallaba (PASSED en vez de FAILED) hasta el fix.

2026-08-04 — developer — DefaultRevisionEngine: el catch de `DiagnosisNotSustainedException` devolvía el outcome pero NO marcaba la tarea STALE ni guardaba el plan (DOUBT-TAREA-CAIDA opcion B). Agregado el mismo patron que el Step 10 (COMPLETED) pero con STALE.
  why: `FQicorJ001JourneyTest#path1` (audit-cli) verifica `planStore.save(...)` con la tarea en STALE; sin el fix el mock nunca se invoca.

2026-08-04 — developer — DefaultRevisionEngineFactory: agregado el registro de `QuizInstructionReviser` en el mapa `byKind` bajo `DiagnosisKind.QUIZ_INSTRUCTION` (mismo patron que `KnowledgeTitleReviser`), y DispatchingCorrectionContextResolver + QuizInstructionContextResolver conectados en Main.java. Sin esto las tareas QUIZ_INSTRUCTION caían al bypass de identidad (F-REVBYP-R004) — exactamente el hueco que la feature cierra.
  why: ninguno de los dos estaba en la lista explícita de 30 stubs, pero sin ellos ningún candidato se genera nunca; confirmado leyendo DefaultRevisionEngineFactory.java existente antes de tocarlo.

2026-08-04 — developer — Cada `sentinel generate` (incluido el de ARCH-QICOR-004, que no tocaba refiner-domain para nada) resetea la firma de clase de `QuizInstructionContextResolver` a `public class QuizInstructionContextResolver {` — pierde `implements CorrectionContextResolver` aunque conserva los métodos ya escritos. Fix: volver a agregar `implements CorrectionContextResolver` a mano después de cada regeneración. Se detecta por el error de compilación "method does not override or implement a method from a supertype" en ese archivo puntual.
  why: la relación no está declarada en sentinel.yaml (no puede estarlo, ver DispatchingCorrectionContextResolverTest/decisions — el puerto es genérico y el generador no liga el type variable), así que el generador reescribe la línea de la clase desde cero cada vez, a diferencia de `DispatchingCorrectionContextResolver` (que SÍ tiene `implements` declarado y por eso nunca se resetea).

2026-08-04 — developer — Para un E2E real sin tocar `db/english-course` ni gastar de más: `--workdir <sandbox>` aísla todo `.content-audit/`; `CONTENT_AUDIT_CONTENT_FOLDER`/`--course-path` apunta a una COPIA descartable de `audit-cli/src/test/resources/fixtures/tiny-course/english-course` (nunca la fixture real). El juez de consigna funciona en vivo sin configurar `CONTENT_AUDIT_QINST_JUDGE_*` (default `claude-cli`, igual que LAGEN). Para forzar un desenlace sin gastar más llamadas: editar a mano `scores["quiz-instruction"]` (no solo `diagnoses.quizInstructionDiagnosis`) en el AuditReport persistido — `DefaultRefinerEngine.collectTasksForNode` lee el mapa de scores, no el diagnóstico, para decidir si genera tarea (`score >= 1.0` => no hay tarea, sin importar lo que diga el diagnóstico).
  why: el primer intento fabricó solo el diagnóstico de 2 quizzes "cumplidores" y `plan` no generó tarea para ninguno — el score real (1.0) seguía diciendo que no hacía falta.


2026-08-04 — test-writer — Cómo compilar un subconjunto de tests cuando `mvn test-compile` falla por WIP ajeno en el mismo módulo: `mvn dependency:build-classpath -Dmdep.outputFile=cp.txt` + `javac -cp $(cat cp.txt) -sourcepath revision-domain/src/main/java:audit-domain/src/main/java:course-domain/src/main/java:refiner-domain/src/main/java:evaluation-ledger-domain/src/main/java -d out <los .java puntuales>`.
  why: javac con `-sourcepath` sólo compila los `.java` que el classpath de símbolos referenciados realmente necesita (resolución transitiva bajo demanda), no el árbol entero — así se evita arrastrar archivos rotos de otros paquetes (p. ej. `engine/QuizInstructionReviser.java`) que no tienen ninguna relación con lo que se está probando.

2026-08-04 — test-writer — Los modelos declarados `type: record` en sentinel.yaml de este proyecto se generan como clases mutables con getters bean-style (`getStatus()`, `isReused()`, `isFollowsInstructions()`), NO como records Java reales. `EvaluationSubject.getContent()` ya era el precedente conocido; confirmado ahora también para `QuizInstructionComplianceResult`, `QuizInstructionVerdict`, `EvaluationResolution`, `CriterionVerdict`.
  why: escribir `result.status()` / `result.outcome()` compila si uno asume record real, pero falla con "cannot find symbol"; hay que usar el getter bean-style. Útil para el próximo test-writer que toque estos mismos tipos.

2026-08-04 — test-writer — JaCoCo 0.8.12 sigue rompiendo con JDK 25 (class file major version 69) al instrumentar mocks de Mockito; para correr tests puntuales con `mvn test -Dtest=...` hace falta agregar `-Djacoco.skip=true` además de `JAVA_HOME` al JDK 25.
  why: sin el skip, la ejecución nunca llega a los asserts — falla en instrumentación antes de correr el test. Coincide con [[reference_maven_jdk25_jacoco]] en memoria de usuario.

2026-08-04 — test-writer — Para compilar revision-infrastructure aisladamente sin arrastrar el `test-compile` roto de refiner-domain (`DispatchingCorrectionContextResolverTest` — bloqueado por el mismo hueco de `QuizInstructionContextResolver`), instalar primero los módulos upstream con `mvn install -am -DskipTests -Dmaven.test.skip=true` (evita compilar tests de refiner-domain) y después correr `mvn -pl revision-infrastructure test-compile` solo.
  why: `-am` normal corre el ciclo de vida completo (incluido test-compile) de cada módulo upstream en el reactor; con -Dmaven.test.skip=true se instalan los .class de main sin tocar los tests rotos de otro módulo que no me corresponde tocar.

2026-08-04 — test-writer — CORRECCIÓN al diagnóstico anterior (mismo día): declarar `implements: [CorrectionContextResolver]` en sentinel.yaml para `QuizInstructionContextResolver` NO es el fix. El puerto es genérico (`<T extends CorrectionContext>`) y el generador copia las firmas sin ligar la variable de tipo → el stub sale con `Optional<T>` y no compila. Los resolvers de este proyecto (SentenceLengthContextResolver, KnowledgeTitleContextResolver, y ahora QuizInstructionContextResolver) escriben la relación `implements CorrectionContextResolver<X>` A MANO en el .java, sin declararla en el DSL. Costo aceptado: ArchUnit no cubre esa relación port/adapter.
  why: evita perder tiempo re-proponiendo el mismo patch que ya se probó y revirtió; el patrón correcto es leer el .java ya escrito (o pedirle las firmas al agente que lo escribió) en vez de inferir la arquitectura desde sentinel.yaml cuando el propio proyecto documenta la excepción.

2026-08-04 — test-writer — La serialización canónica FEAT-QSENT es `<texto> ____ [opcion1|opcion2] <texto-con-cue>` (un solo espacio, sin llaves de kind/separador `:` — ese formato era de otra convención). Ejemplo real de REQUIREMENT.md F-QSENT: `sentenceParts=[TEXT "He", CLOZE options=["is|'s"], TEXT " (to be) great."]` → `quizSentence = "He ____ [is|'s] (to be) great."`.
  why: fixture fiel para QuizInstructionCorrectionContext.quizSentence (F-QICOR-R002) — más preciso que inventar un formato plausible.

2026-08-04 — test-writer — Cómo verificar 2 archivos de test aislados cuando `mvn compile -pl audit-cli -am` falla por WIP ajeno DENTRO del propio módulo (no en un módulo upstream): `mvn compile -pl <deps upstream> -am` primero (confirma que las dependencias sí compilan), después `mvn dependency:build-classpath -pl audit-cli -Dmdep.outputFile=cp.txt -DincludeScope=test`, filtrar del cp.txt las entradas `com/learney/contentaudit` (son jars .m2 potencialmente viejos) y anteponer los `target/classes` recién compilados de cada módulo upstream; con esa classpath, `javac -d out <los 2 .java del lote>` sin tocar nada de `audit-cli/src/main` (Main.java y ReviseCmd.java rotos por WIP de @developer, ninguno importado por los journey tests).
  why: los journey tests de FEAT-QICOR sólo importan tipos de audit-domain/course-domain/refiner-domain/revision-domain, nunca de `com.learney.contentaudit.auditcli.*` — así que ni falta compilar el módulo audit-cli completo para validar que compilan.

2026-08-04 — developer — `GetCmd.needsCorrectionContext()` es una allowlist manual de `DiagnosisKind`, no derivada del registro real de resolvers — cada `DiagnosisKind` nuevo con `CorrectionContextResolver` propio hay que sumarlo ahí a mano o `get tasks -f json` sirve la tarea sin la clave `correctionContext` (ni siquiera vacía: directamente ausente). Pasó con QUIZ_INSTRUCTION (F-QICOR-R002 ya tenía resolver andando, el olvido era sólo en esta lista).
  why: el bug no estaba en la resolución (`QuizInstructionContextResolver.resolve` ya devolvía el contexto completo con violations) sino en que nunca se la llamaba para ese diagnóstico — diagnosticar por los dos lados (resolver + punto de invocación) antes de tocar nada evitó parchear el lugar equivocado.

2026-08-04 — developer — `DefaultAgentGraphRunner` resuelve agent-dir y runs-dir con prioridad `AgentGraphRunnerConfig` explícito > `inputs["project_root"]` (poblado por-llamada por quien invoca) > CWD del proceso. Cuando el invocador NO puede poblar `project_root` (contrato de inputs fijo y protegido, como el juez de F-QINST-R009 o el `candidate-judgment` de F-QICOR-R004 criterios 4/5), la única vía para honrar `--workdir` es fijar `agentsBaseDir`/`runsBaseDir` explícitos en el config al construirlo — nunca agregar la key al mapa de inputs de esos dos.
  why: agregar `project_root` a esos inputs rompería la estabilidad de huella que F-QINST-R009 exige textualmente ("no key added, removed or reordered"); cambiar el config es invisible para ese contrato porque vive un nivel más abajo, fuera del mapa que se hashea.

2026-08-04 — test-writer — Patrón para journeys sobre revisiones cuya correccion aun no tiene developer: mockear directo los campos de `RevisionEngineConfig` que son "catálogo de criterios" (`candidateAssessor`, `quizInstructionComplianceChecker`) en vez de wirear los `CandidateCriterionEvaluator` reales (6 piezas, alguna con juicio de agente) — esos ya tienen sus propios handwrittenTests dedicados (R004/R005/R006 en `DefaultCandidateAssessor`, `InstructionComplianceCriterion`, etc.). El journey sólo verifica que el motor invoque el catálogo y reaccione bien a `accepted=true/false` y a `revalidate(...).followsInstructions()`, no que cada criterio mida bien.
  why: evita duplicar cobertura (regla "no transitive coverage" corre al revés acá: el detalle YA está cubierto en otro archivo) y evita wirear infraestructura de NLP/agente real dentro de un test de journey, que debe mockear sólo el borde externo.

2026-08-09 — qa-tester — Declarar una implementation en un patch con SOLO los handwrittenTests nuevos NO borra los ya declarados: el merge es por nombre de test, no reemplazo de lista. Verificado sobre `QuizInstructionAnalyzer` (21 + 2 + 4 en tres patches archivados de FEAT-QINST; sentinel.yaml tiene 27 hoy).
  why: es la duda que frena cada delta de cobertura sobre una impl que ya tiene tests; queda medida en vez de supuesta.

2026-08-09 — qa-tester — El cruce de `traceability.rule` contra REQUIREMENT.md SI corre en `patch propose --requirement-folder`. Control negativo con `F-QICOR-R099` sobre una copia del requisito en scratchpad => "Patch validation FAILED ... does not exist in FEAT-QICOR (available: F-QICOR-R001..F-QICOR-R009)".
  why: un "Validated: N modifications" no distingue entre validado y no chequeado; con el historial de no-ops silenciosos del proyecto conviene el control negativo antes de declarar la trazabilidad verificada.

2026-08-09 — test-writer — `QuizTemplateEntity.translation` es el campo 7 del constructor de 21 args (confirmado con `describeComponent`, no adivinado). Las 3 fixtures nuevas de traduccion de `ScopedEditCriterionTest` usan un dominio propio (`questionParts`/`questionQuiz`: `"Why " + CLOZE + "?"`, opciones `"are you running"` → `"are they walking"`) en vez de reutilizar el dominio "breakfast routine" de los 4 tests existentes — verbatim del criterio de aceptacion (b) de F-QICOR-R003, porque ese dominio no admite gramaticalmente el ejemplo de la regla (el gapA ahi es un objeto directo, no una frase sujeto+verbo).
  why: usar las frases exactas del criterio de aceptacion de la regla maximiza trazabilidad — el test es legible como "esto ES el ejemplo (b) de la regla", no una reinterpretacion.

2026-08-09 — test-writer — Empirico: de los 3 tests nuevos de R003 (familia traduccion), solo el que espera PASSED corre en rojo hoy contra `ScopedEditCriterion` sin arreglar; los 2 que esperan FAILED YA pasan. Causa: el bug actual (rechaza TODA traduccion cambiada no señalada por una violacion, sin mirar si la oracion cambio) es un superconjunto mas estricto que la regla correcta — para esas 2 escenas negativas coincide en el veredicto FAILED por una razon distinta y mas amplia de la que exige R003. Verificado con `mvn test -pl revision-domain` (259 tests, 1 solo failure, exactamente el test que espera PASSED).
  why: para que una proxima sesion no piense que esos 2 tests "no prueban nada" solo porque ya estan verdes antes del fix — son candados de regresion: tienen que SEGUIR verdes despues de que @developer acote la excepcion, y ahi es donde importan (detectan un early-return que exima la traduccion sin chequear el resto).

2026-08-09 — developer — `ScopedEditCriterion.evaluate()`: el chequeo de traduccion se saco de ANTES del loop de partes (donde reprobaba toda traduccion cambiada no señalada) y se movio a DESPUES, condicionado a un flag `sentenceChanged` que el propio loop acumula (`sentenceChanged = true` en la misma rama donde ya se detecta `textChanged || optionsChanged`, antes de evaluar si esta señalado). Reprueba solo cuando la traduccion cambio Y `sentenceChanged` sigue en `false` al terminar el loop.
  why: el flag se computa como subproducto del loop existente (nunca con una comparacion aparte ni con un early-return previo) para que el candado de regresion `...ChangedTheSpanishTranslationAndAlsoRewroteAPartNoViolationSignalled` siga en FAILED por el motivo correcto — la parte no señalada, detectada por el loop — y no por un atajo de traduccion que lo saltee.

2026-08-10 — qa-tester — El gate `aplicar_correccion` de F-QICOR-J001 es **path-2**, no path-3. La numeracion la fija el recorrido del DAG, no el orden en que los nodos aparecen en el YAML del flow: path-1=diagnostico_caido (success), path-2=aplicar_correccion (success), path-3=rechazo (failure), path-4=sin_correccion (failure). Confirmado en los `@Tag`/`@Order` de `FQicorJ001JourneyTest.java` y en `pathsCoverage` de `.sentinel/sentinel-report.yaml`, no inferido del texto del journey.
  why: en el YAML `aplicar_correccion` aparece despues de `decidir` y antes de `rechazo`/`sin_correccion`, asi que es facil contarlo como el tercero; mandar a @test-writer al "path-3" lo deja en el test de RECHAZO, que por definicion no escribe nada en el curso — justo donde la mitad (b) de R010 no se puede observar.

2026-08-10 — qa-tester — Control negativo re-confirmado con las 10 reglas ya sincronizadas: `patch propose --requirement-folder` con `F-QICOR-R099` => "Patch validation FAILED ... (available: F-QICOR-R001..F-QICOR-R010 (10 total))". Se corrio sobre una COPIA del requisito en scratchpad, antes de tocar el `architectural_patch.yaml` real.
  why: probar la trazabilidad sin gastar el unico archivo de patch del requisito — el positivo y el negativo se validan sobre la copia y recien despues se escribe el definitivo con --replace.

2026-08-10 — test-writer — Empirico (mismo patron que R003/traduccion, 2026-08-09): de los 3 tests nuevos de R010 en `ScopedEditCriterionTest`, solo el que espera PASSED corre en rojo hoy contra `ScopedEditCriterion` sin arreglar; los 2 que esperan FAILED YA pasan. Causa: el bug actual (cuenta partes sin exceptuar las vacias) es un criterio MAS estricto que R010 — para esas 2 escenas negativas coincide en FAILED por una razon distinta y mas amplia de la que exige la regla. Verificado con `mvn test -pl revision-domain -Dtest=ScopedEditCriterionTest` (10 tests, 1 sola failure, exactamente el que espera PASSED).
  why: para que la proxima sesion no lea "2 verdes de entrada" como un error de diseño del test — son candados de regresion, tienen que SEGUIR en FAILED despues del fix de @developer.

2026-08-10 — test-writer — Dominio "Do I want / Do I need a pen?" (el ejemplo real que cita F-QICOR-R010 en su propio texto) reusado tal cual en los 3 tests nuevos de `ScopedEditCriterionTest` Y en el `elementBefore` custom de `FQicorJ001JourneyTest#path2` — misma fixture en los dos archivos.
  why: precedente ya establecido (fix-log 2026-08-09, `questionParts`/`questionQuiz`): usar las frases exactas del criterio de aceptacion de la regla maximiza trazabilidad; y da coherencia narrativa entre los dos jobs del mismo pedido.

2026-08-10 — test-writer — `FQicorJ001JourneyTest#path2`: el `elementBefore` custom (`buildElementBeforeWithEmptyLeadingPart`) solo agrega una parte TEXT vacia al PRINCIPIO de la fixture ya existente (She/CLOZE/texto con cue) — el resto (`AFTER_QUIZ_SENTENCE`, el `candidateGenerator`, la traduccion, `sentences`) queda intacto, porque una parte vacia no aporta caracteres: el texto ya parsea a las mismas 3 partes con o sin ella. Verificacion de R010 hecha con un `ArgumentCaptor` sobre `elementLocator.replace(...)` (NO sobre `artifact.getElementAfter()` ni sobre `updatedCourse`, que es un mock vacio sin estado real) — es "el quiz que se le entrega al guardado", no un round-trip a JSON.
  why: evita inventar un dominio nuevo cuyo formato de parseo real (FEAT-QSENT) no se puede verificar sin leer codigo de produccion; reusar el fixture ya probado (paths 2/3/4 ya lo hacian round-trip con exito) acota el riesgo a la unica variable que importa: el conteo de partes. `updatedCourse` sin stub de estado es la prueba de que el `ArgumentCaptor` sobre `replace(...)` es la UNICA forma de observar el contenido en este path mockeado.

2026-08-10 — test-writer — Como correr un subconjunto de la suite de `audit-cli` cuando `revision-domain` tiene un rojo esperado (bloquea el reactor con `-am` incluso con `-fae`, porque Maven banea los modulos dependientes de uno que fallo): `-Dmaven.test.failure.ignore=true` deja completar el reactor entero y reporta los failures al final sin abortar — solo para verificacion de lectura, no cambia ningun archivo.
  why: `-fae` solo no alcanza cuando el modulo que falla tiene dependientes (audit-infrastructure/audit-application/audit-cli quedaron SKIPPED con `-fae`, no con `-Dmaven.test.failure.ignore=true`).

2026-08-10 — developer — Mitad 1 de F-QICOR-R010 (no bloquea) en `ScopedEditCriterion.evaluate()` (revision-domain, `candidatecriteriaengine`): las partes se filtran con `withoutEmptyTextParts()` (nueva, `kind==TEXT && (text==null || text.isBlank())` vía `isEmptyTextPart()`) ANTES de calcular tamaños y ANTES del recorrido por índice — nunca como atajo que compare "cuenta difiere solo por vacías => PASSED" sin recorrer. El mensaje de FAILED por cambio de cantidad sigue citando los tamaños CRUDOS (sin filtrar) para que sea legible; la condición que dispara el FAILED usa los tamaños FILTRADOS.
  why: el fixture del test 9 (`...AlsoRewroteAPartNoViolationSignalled`) está armado exactamente para que un atajo por conteo dé PASSED incorrectamente — sólo filtrar-y-recorrer (nunca contar-y-saltar) lo deja en FAILED por la razón correcta (la parte de texto final cambió sin señal), verificado corriendo los 10 tests.

2026-08-10 — developer — Mitad 2 de F-QICOR-R010 (no se pierde al guardar) en `QuizInstructionReviser.propose()` (revision-domain, `engine`): tras `deriver.derive(before, candidate, ...)`, `withOriginalEmptyPartsRestored(before, elementAfter)` reinserta en `elementAfter` las partes vacías que tenía `before` y que `elementAfter` ya no trae — contando la racha de vacías al PRINCIPIO y al FINAL de `before.getForm().getSentenceParts()` por separado (nunca busca vacías "en el medio", coincide con la medición de la regla: 0 casos así) y reconstruyendo `FormEntity`/`QuizTemplateEntity`/`CourseElementSnapshot` nuevos (nunca mutando los existentes, para no arriesgar alias con el `before` que R007 exige intacto). Se reasigna la variable local `elementAfter` ANTES de construir `CandidateAssessmentInput`, así lo que se evalúa (incluida la mitad 1 de arriba) y lo que termina en `RevisionProposal.elementAfter` son el mismo objeto.
  why deliberado: NO se tocó `DefaultLemmaAbsenceProposalDeriver` (el deriver compartido, también usado por lemma-absence) ni el parser/serializador de FEAT-QSENT — la regla dice explícitamente "alcanza a esta corrección", así que el punto de inserción tenía que ser el reviser QUIZ_INSTRUCTION-específico, no un lugar compartido con otra feature.
  Verificado con el fixture real de `FQicorJ001JourneyTest#path2` (4 partes originales, candidato derivado de 3 sin la vacía) → `elementLocator.replace(...)` recibe exactamente [TEXT "", TEXT "She", CLOZE ["walked"], TEXT "(walk)..."], 4 partes, la vacía de vuelta en posición 0 sin correr ninguna otra parte.
