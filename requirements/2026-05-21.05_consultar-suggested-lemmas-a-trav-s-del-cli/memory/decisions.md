# Decisions — FEAT-CSLATDC

2026-05-21 — qa-tester — R002 ("uso del verbo existente get"): tiene superficie observable en GetCmd (despacho del resource "suggested-lemmas" por la rama de `get` y no por un nuevo command). Se propuso handwrittenTest en GetCmd.
  why: aunque el sealed interface ya está cerrado a `GetCommand`, el routing del string "suggested-lemmas" sí es observable en runtime y diferenciable de "introducir un nuevo verbo CLI" (que sería otro command class).

2026-05-21 — qa-tester — R013 ("filtros no soportados como isIrregular"): NO se propuso handwrittenTest.
  why: el TECH_SPEC declara satisfacción estructural via record cerrado `SuggestedLemmasFilter` (sólo 3 campos). El residual semántico para `SuggestedLemmaQueryRejectedException` requeriría inventar un caso de validación adicional (ej. limit negativo), que no está cubierto por la letra de R013 — la regla habla específicamente de filtros como `isIrregular`. Pasar un campo inexistente es imposible en Java. Reformular o aceptar cobertura estructural es decisión del analista/usuario.

2026-05-21 — qa-tester — R015 ("instrucciones funcionales del agente revisor"): NO se propuso handwrittenTest.
  why: TECH_SPEC y el architect coinciden en que es responsabilidad de prompt template / documentación, sin superficie observable en código. Aplica memoria del usuario "Untestable rules go to analyst, not ArchUnit": preferir retiro/reformulación por @analyst antes que inventar test reflexivo.

2026-05-21 — analyst — Retirados R013 (filtros no soportados / isIrregular) y R015 (instrucciones al agente revisor) del REQUIREMENT por decisión funcional del usuario; eliminado J003 (era el único journey con gate exclusivo en R013) y eliminada DOUBT-IRREGULAR-FILTER. Limpieza en Scope (bullet IN "Instruir al agente revisor"; bullet OUT "filtro isIrregular"). IDs de reglas restantes NO renumerados — identidad estable. Validación CLI: [OK].
  why: R013 queda cubierta estructuralmente por el record cerrado SuggestedLemmasFilter; R015 es prompt/documentación, sin superficie observable. Sin reglas que validar, J003 quedaba sin gate observable.

2026-06-23 — analyst — Agregada nueva regla F-CSLATDC-R015 (la consulta dinámica opera sobre el MISMO análisis fuente del que se derivó el contexto de la task) + journey F-CSLATDC-J005. Resuelve fix de consistencia: la consulta `get suggested-lemmas` resolvía su análisis contra el más reciente/un análisis fijo, ignorando el análisis fuente de la task; cuando la revisión apunta a un análisis proyectado (`--audit`, FEAT-PLANEF/FEAT-CDIFF), reintroducía lemas ya consumidos ("butter"). El id R015 se REUTILIZA (el R015 retirado en 2026-05-21 era otra regla); identidad estable porque el anterior fue eliminado del REQUIREMENT y nunca tuvo handwrittenTest. DOUBT-PROJECTED-CONTEXT pasa a RESUELTA PARCIALMENTE. `feature sync` aplicado: +J005 (id-only, testModule/testPackage pendientes → @qa-tester). `requirement validate`: [OK].
  why: el dueño natural es FEAT-CSLATDC (dueña de la consulta dinámica); FEAT-INCEXP y FEAT-LAGAG sólo la consumen, FEAT-REVCTX es la pieza simétrica sobre `revise`, no sobre la consulta.

2026-06-23 — architect — F-CSLATDC-R015: SI requiere cambio de contrato (no es solo rewiring). Opcion B elegida. Patch propuesto (FEAT-CSLATDC, 4 mods, 0 conflicts):
  (1) SuggestedLemmaQuerySessionFactory: bindForTask(RefinementTask) → bindForTask(String sourceAuditId, RefinementTask). El RefinementTask NO transporta sourceAuditId/planId, por eso el factory caia en loadLatest() (bug). La identidad del analisis ahora la pasa quien la conoce.
  (2) DefaultSuggestedLemmaQuerySessionFactory: se ELIMINA la inyeccion de RefinementPlanStore (solo existia para loadLatest()); resuelve con auditReportStore.load(sourceAuditId).
  (3) LemmaAbsenceCorrectionContext: +campo sourceAuditId (String) para que el camino revise (generator recibe solo el context) propague el analisis fuente al seam. NO se agrego planId (carrier liviano).
  Descartadas: A (meter AuditReport en el carrier, infla el DTO que cruza a JSON en revise --correction-context), C (overload no destructivo, deja vivo bindForTask(task) con loadLatest()).
  Implementacion (de @developer, NO contrato): GetCmd.handleSuggestedLemmas debe resolver el plan dueño de la task honrando --audit/seleccion activa en vez de loadLatest(); LemmaAbsenceContextResolver debe estampar sourceAuditId.
  why: dueño natural FEAT-CSLATDC (dueña de la consulta dinamica). El cambio mínimo de contrato que cierra R015 simetricamente entre CLI y revise.

2026-06-23 — qa-tester — Cobertura R015 + ubicación J005. Decisión: J005 placement (audit-cli/com.learney.contentaudit.journeys, igual que J001/J002/J004 — mismo entry point GetCommand.get) MÁS un handwrittenTest DIRECTO de R015 en DefaultSuggestedLemmaQuerySessionFactory (package lemmasuggestion). Nombre: "should bind the query session to the task's source analysis so consumed lemmas do not reappear even when a newer analysis still exposes them".
  why: R015 es critical y su mecanismo vive aislado en el factory seam (bindForTask(String sourceAuditId, RefinementTask) → auditReportStore.load(sourceAuditId), ya NO loadLatest()). J005 cubre R015 a nivel integración pero su harness GetCmd es package-private + reflection (J001/J002 fallaban por reflection access). Para regla critical con seam aislado claro, no me conformo con cobertura sólo vía journey frágil: agrego test unitario sharp del contrato "mismo análisis fuente, no el más reciente". Traceability honesta (no forzada) — el test verifica literalmente la letra de R015. Patch tests-only requirement-scoped: 0 add, 2 mod, 0 conflicts.
