# Progress

2026-04-28 — analyst — drafted FEAT-RCLALEN as a slice over FEAT-RCLA adding tokenCount/targetMin/targetMax/delta/lengthDirection from SentenceLengthDiagnosis on the same quiz node.
  why: enriches the LEMMA_ABSENCE prompt without re-defining R001..R009 of F-RCLA.

2026-05-04 — analyst — migrated REQUIREMENT.md to current template (TL;DR Que/Por que, rules grouped A/B with anchor + blockquote + details, Alcance section with explicit limitations, annotated References citing rules). No functional content changed; IDs, severities, validation states and Doubts preserved. `sentinel requirement validate` passes.
  why: align with the template used in FEAT-PIPRE / FEAT-LAGEN for scan-first readability and consistent cross-feature citation.

2026-05-04 — analyst — reformulated F-RCLALEN-R004 from internal-state language ("nulos en el registro") to observable-boundary language: lengthDirection==UNKNOWN is the sole discriminator, and the four numeric values are not exposed to the consumer (omitted from JSON via R005, absent from the LLM prompt, replaced by `(unavailable)` in the CLI line via R006). No other rule or journey edited; J001 `marcar_unknown` and R005/R006 already aligned. Validator OK.
  why: handoff from architect after Sentinel generator bug 2026-05-04-01 forces UNKNOWN-as-discriminator; aligns rule with project convention that REQUIREMENT.md describes function at the system boundary, decoupled from internal representation.

2026-05-04 — qa-tester — proposed handwrittenTests coverage for FEAT-RCLALEN R001..R006 (24 tests across LemmaAbsenceContextResolver, DefaultLemmaAbsencePromptBuilder, GetCmd) and journey placement (testModule=audit-cli, testPackage=com.learney.contentaudit.journeys) for F-RCLALEN-J001/J002. Validator OK: 5 modifications, 0 conflicts. architectural_patch.yaml regenerated.
  why: every R001..R006 needs a directly tagged test (no transitive coverage); resolver carries R001..R004 (model+invariant), prompt builder gets R001/R004 (length section presence/suppression), GetCmd gets R005 (JSON) and R006 (text). Journey paths covered by auto-generated FRclalenJ001/J002JourneyTest in audit-cli/journeys.

2026-05-04 — test-writer — implemented 10 stubs in GetCmdTest.java (5 for R005 JSON, 5 for R006 text). All use the 15-param LemmaAbsenceCorrectionContext constructor with LengthDirection enum. Added LengthDirection import. No pre-existing errors introduced. Compile check: zero new errors in lines > 2967.
  why: pre-existing project has unresolved FEAT-PIPRE scaffold errors (ImpactPreviewStore, SentenceLengthAnalyzer constructor) that prevent full test-compile; new tests are isolated from those issues.

2026-05-05 — test-writer — implemented FRclalenJ002JourneyTest#path1_success (KEEP_SAME scenario). Test compiles and runs; fails on tokenCount assertion with expected:<6> but was:<0> — production code (LemmaAbsenceContextResolver) not yet reading SentenceLengthDiagnosis.
  why: correct TDD state; test expresses R001 (tokenCount/targetMin/targetMax/delta populated), R002 (KEEP_SAME when delta==0), R003 (same quiz node), R005 (JSON fields present), R006 (CLI line "6 tokens (target: 5-8, delta: 0, direction: KEEP_SAME)"). Fix: mvn install audit-domain needed before running test (stale jar had 5-arg MisplacedLemma constructor).

2026-05-05 — test-writer — implemented 12 stubs in LemmaAbsenceContextResolverTest.java for F-RCLALEN-R001 (4 tests), R002 (4 tests), R003 (2 tests), R004 (2 tests). All 37 tests in the class pass. Added helper buildQuizDiagnosesWithPlacementAndLength and import for SentenceLengthDiagnosis. Required mvn install -pl audit-domain to sync stale m2 jar (MisplacedLemma.cocaRank changed from int to Integer in workspace but jar was stale).
  why: TDD; resolver does not yet populate the 5 new fields; tests fail on production side (UNKNOWN where SHORTEN/LENGTHEN/KEEP_SAME expected, 0 where positive tokenCount expected). Correct pre-implementation state.

2026-05-05 — test-writer — implemented 2 stubs in DefaultLemmaAbsencePromptBuilderTest.java: R001 (length section present when direction known, scenario: 10 tokens / 5-8 range / delta=+2 / SHORTEN) and R004 (length section absent when UNKNOWN). Added LengthDirection import. Existing 13 FEAT-LAGEN tests unaffected. Required mvn install -pl refiner-domain (twice) to sync installed jar with source that includes compat 10-arg constructor + new fields.
  why: TDD state — R001 fails (prompt builder not yet emitting length section), R004 passes (no length section emitted at all, which is correct when direction UNKNOWN). 14/15 tests pass.

2026-05-05 — test-writer — implemented FRclalenJ001JourneyTest (4 path stubs). path1=SHORTEN(delta>0), path2=KEEP_SAME(delta==0), path3=LENGTHEN(delta<0), path4=UNKNOWN(no SentenceLengthDiagnosis). All use real LemmaAbsenceContextResolver + real audit tree; no GetCmd (package-private, causes NoClassDefFoundError from com.learney.contentaudit.journeys). Test compiles; paths 1-3 pass, path4 fails on cefrLevel==null (correct TDD failure: F-RCLALEN-R004 requires non-length fields populated normally; implementation has bug in UNKNOWN branch).
  why: GetCmd is package-private final class — cannot be referenced from com.learney.contentaudit.journeys even via reflection due to ImpactPreviewStore ClassNotFoundException at getDeclaredConstructor time (FEAT-PIPRE not fully scaffolded). Journey tests follow FRclaJ001/FRcslJ001 pattern: resolver-only, no CLI.

2026-05-05 — developer — verified DefaultLemmaAbsencePromptBuilder production implementation (already in place from prior session). buildUserPrompt emits length section when lengthDirection != UNKNOWN (R001) and suppresses it when UNKNOWN (R004). All 79 revision-infrastructure tests pass (15/15 in DefaultLemmaAbsencePromptBuilderTest, 13 pre-existing FEAT-LAGEN + 2 new FEAT-RCLALEN). No changes needed.
  why: implementation was already committed; mvn install -pl refiner-domain run to ensure jar is current before test run.

2026-05-05 — developer — verified LemmaAbsenceContextResolver: all 37 tests pass (including 12 new F-RCLALEN-R001..R004 stubs). Implementation at lines 165-191 was already complete from a prior session. No changes made. Journey J001/J002 tests cannot run because audit-cli main sources fail to compile (FEAT-PIPRE scaffolding gaps — pre-existing). Inconsistency in progress.md (entry says "All 37 pass" then "tests fail on production side") explained: test-writer entries 2 and 3 were written in the same session but in sequence — entry 2 noted the TDD failure (before entry 3's resolver changes), then the implementation was committed and the note became outdated. Actual state: resolver fully implemented and all 37 resolver tests pass green.
  why: user asked for implementation of LemmaAbsenceContextResolver + investigation of J001 paths 1-3 pre-pass inconsistency.

2026-05-05 — developer — implemented R005 (JSON) and R006 (text) formatters in GetCmd.java for FEAT-RCLALEN. Also restored pre-existing PIPRE scaffold (6-arg constructor, setters, printImpactPreviewView, DefaultFormatterRegistry field/constructor, record-style accessors on ImpactPreviewView/LevelImpactView) that were lost by sentinel regeneration. 78 RCLALEN tests pass (73 GetCmdTest + 4 FRclalenJ001 + 1 FRclalenJ002). Remaining 29 audit-cli failures are pre-existing (FClirv journey test ordering, FRevapr errors, FLaps errors, env-var tests).
  why: git stash pop conflict during development reverted GetCmd.java to committed version (which lacked PIPRE scaffold), requiring manual restoration of PIPRE working-tree changes.
