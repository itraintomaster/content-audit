# Fix Log

Fixes that worked, with a short why. Future agents hitting the same
symptom read this before trying new approaches. Newest entries on top.

<!-- entries below -->
2026-07-21 — developer — R042 casi rompe el test baseline de R041 "shouldKeepTheAutoComputedTagForSingleWordEntriesWhoseEVPCategoryIsPhrase" (solo "let's", sin "let" palabra): mi primer diseno clasificaba TODAS las entradas categoria "phrase"/"phrasal verb" (monopalabra Y multipalabra) como techo-solo-rebaja, lo que habria devuelto Optional.empty() para ese test (nada que rebajar, "never invent"). Fix: el techo de R042 aplica SOLO a entradas `phrasal` (word.contains(" ")); las monopalabra "phrase" siguen su camino normal de entrada de palabra (ya correcto desde R040/R041, ver el hallazgo de test-writer de mas abajo) y no se tocan.
  why: el enunciado de R042 dice "gobierna phrasal Y monopalabra phrase de forma UNIFORME como frases-que-solo-bajan", pero el mecanismo YA vigente (merge-min de claves duplicadas) logra ese mismo resultado para el caso monopalabra sin necesitar el techo nuevo -- diversificar tambien las monopalabra al techo es redundante y ademas rompe el caso "sin par de palabra real" que el test baseline SI espera que resuelva (R041, no R042: para monopalabra "phrase" el fallback siempre fue directo a spacyPosTag, nunca goberno por "nunca inventar").
2026-07-21 — test-writer — R042-3 (fixture "let" verbo B1 + "let's" A2 categoria "phrase") pasa ya hoy sin implementar R042: verificado empiricamente (invirtiendo el orden de insercion de las dos entradas en el catalogo fixture, resultado A2 estable en ambos ordenes) que NO es un last-wins accidental sino un merge min() ya existente para claves (lema,pos) duplicadas, combinado con el fallback de R041 que ya resuelve la categoria "phrase" monopalabra al spacyPosTag (VERB) -- por eso "let's" ya participa hoy como entrada VERB ordinaria y el min(B1,A2)=A2 sale gratis. Distinto es el caso multipalabra (R042-1/2, fixture "live"): las entradas con espacio se excluyen enteramente por R005 hoy, no se mergean, por eso esas SI fallan pre-implementacion. Reportado como hallazgo, test no tocado.
  why: mismo patron de riesgo que el fix-log de R040 (test que pasa por coincidencia de fixture); aqui la verificacion de orden confirmo que es comportamiento real y no coincidencia, asi que no se penalizo con inversion de niveles.
2026-07-21 — test-writer — R042-3 (fixture "let" verbo B1 + "let's" A2 categoria "phrase") pasa ya hoy sin implementar R042: verificado empiricamente (invirtiendo el orden de insercion de las dos entradas en el catalogo fixture, resultado A2 estable en ambos ordenes) que NO es un last-wins accidental sino un merge min() ya existente para claves (lema,pos) duplicadas, combinado con el fallback de R041 que ya resuelve la categoria "phrase" monopalabra al spacyPosTag (VERB) -- por eso "let's" ya participa hoy como entrada VERB ordinaria y el min(B1,A2)=A2 sale gratis. Distinto es el caso multipalabra (R042-1/2, fixture "live"): las entradas con espacio se excluyen enteramente por R005 hoy, no se mergean, por eso esas SI fallan pre-implementacion. Reportado como hallazgo, test no tocado.
  why: mismo patron de riesgo que el fix-log de R040 (test que pasa por coincidencia de fixture); aqui la verificacion de orden confirmo que es comportamiento real y no coincidencia, asi que no se penalizo con inversion de niveles.
2026-07-21 — developer — R040's literal word!=lemma exclusion inicialmente rompia el test de R041 "let's"/"let" (evpPos="phrase"): la contraccion tiene word!=lemma pero no es una forma derivada/compuesta en el sentido de R040. Fix: `derivedForm` excluye ademas las entradas cuya evpPartOfSpeech sea "phrase"/"phrasal verb" (case-insensitive), espejando el carve-out que R041 ya declara para esas categorias. Con eso los 12/12 tests quedan verdes sin tocar la fixture.
  why: sin este carve-out, cualquier entrada monopalabra de categoria "phrase" (contraccion/idiom con apostrofe) caeria en R040 aunque el Doubt[DOUBT-PHRASE-MONOPALABRA] la deja explicitamente diferida.
2026-07-21 — test-writer — R040 test de fallback por lema necesito invertir los niveles fixture (derivada mas baja que base) para que el test falle pre-implementacion; con derivada B1 > base A2 el min() actual coincidia por casualidad con el resultado correcto y el test pasaba en falso (no probaba R040 en absoluto)
  why: al testear un fallback tipo minimo(), la forma excluida debe tener el nivel MAS BAJO para que su inclusion cambie el resultado observable
2026-05-09 — test-writer — J001 usa @ExtendWith(MockitoExtension) + InjectMocks sobre LemmaByLevelAbsenceAnalyzer; J002–J005 son carriers in-memory sin el analyzer
  why: J001 es pipeline completo (full-audit), J002–J005 son journeys de "consulta de datos" donde basta construir los modelos y validar sus propiedades
