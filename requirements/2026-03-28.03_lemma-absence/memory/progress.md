# Progress

Current state, last action, next step. Newest entries on top.

<!-- entries below -->
2026-07-22 — developer — Reparada compilacion de tests preexistente-rota en audit-infrastructure (FileSystemAuditReportStoreTest, 2 llamadas a LemmaPlacementDiagnosis(int,List) -> +2 args (0, List.of())) y audit-cli (25+ llamadas viejas de LemmaPlacementDiagnosis/MisplacedLemma/MisplacedLemmaContext/LemmaAbsenceCorrectionContext en GetCmdTest y ~15 *JourneyTest, arrastradas por la evolucion de modelos de F-LABS-R033/R034/R038). `mvn test-compile -pl audit-infrastructure,audit-cli -Dexec.skip=true` BUILD SUCCESS. NOTA: FileSystemAuditReportStoreTest sigue con `_change: delete` PENDIENTE en sentinel.yaml (linea ~8399) — solo repare su compilacion, no decidi ni ejecute el delete (fuera de mi alcance como developer).
  why: el compile-break bloqueaba toda la suite de audit-infrastructure/audit-cli desde que R033/R034/R038 agregaron campos a los 4 modelos; el fix es mecanico (args nuevos al final), no cambia intencion de ningun test.
2026-07-22 — developer — implementado F-LABS-R045 en LemmaByLevelAbsenceAnalyzer.scoreQuiz (audit-domain, pkg labs): cuando el lema literal agota los dos pasos de R036 (lookupLevel exacto y lookupLevelByLemma ambos vacios), se reintentan AMBOS pasos con el lema normalizado a minusculas (Locale.ROOT), solo si `lowerLemma != literalLemma` (evita reintento redundante cuando el lema ya es todo minusculas). Si el reintento resuelve, `lp` (el par LemmaAndPos usado para el MisplacedLemma y las consultas de cocaRank/semanticCategory) se reasigna a la clave minuscula; `observedPos` no cambia (sigue siendo el del token). Si tampoco resuelve, la rama fuera-de-catalogo sigue usando `token.getLemma()` (literal) sin tocar, correcto porque ahi no hubo resolucion. mvn test -Dtest=LemmaByLevelAbsenceAnalyzerTest -Djacoco.skip=true: 134/134 verdes (el TDD-red de "Sheep" ahora pasa; los guards "Sophie" y "Bank" siguen verdes sin cambio). mvn test -pl audit-domain -Djacoco.skip=true: 354/354 verdes, BUILD SUCCESS. Nota tooling: en este entorno JDK25, jacoco-prepare-agent rompe la instrumentacion (major version 68, bug preexistente documentado) — hace falta agregar `-Djacoco.skip=true` a los mvn test de este modulo, `-Dexec.skip=true` solo no alcanza.
  why: el repliegue debe ser transparente para el resto del pipeline (rebajas/puentes R040-R044 ya operan sobre el nivel resuelto sin cambios, porque siguen viendo un Optional<CefrLevel> normal); mantener `lp` coherente con la clave minuscula evita que cocaRank/semanticCategory se consulten con una clave (mayuscula) que nunca existio en el catalogo.
2026-07-22 — test-writer — implementados los 3 handwrittenTests stubs de F-LABS-R045 en LemmaByLevelAbsenceAnalyzerTest (audit-domain, pkg labs): (1) "Sheep"/NOUN rank 6081 en A1 con lookupLevel("sheep",NOUN)=A1 stubbed -> espera score 1.0 (repliegue a minusculas); (2) "Sophie"/NOUN (mal etiquetada, no PROPN) sin stub bajo ninguna forma -> espera OOC banda fuerte 0.3, score 0.7; (3) guard "Bank"/NOUN con lookupLevel("Bank",NOUN)=A1 y lookupLevel("bank",NOUN)=B2 stubbed distinto -> espera que gane el literal (score 1.0) y verify(never) sobre la clave minuscula. mvn test -Dtest=LemmaByLevelAbsenceAnalyzerTest: 134/134 (131 preexistentes intactos), 1 failure esperado (test 1, TDD: produccion aun no reintenta en minusculas); tests 2 y 3 verdes ya hoy tal como se preveia (guardas sin cambio de comportamiento). NEXT: @developer implementa el repliegue a minusculas en LemmaByLevelAbsenceAnalyzer (los dos pasos de R036 reintentados con lemma.toLowerCase() cuando la forma literal no resuelve).
  why: test 1 fuerza una diferencia observable real (rank 6081 penaliza fuerte en A1 via OOC vs no-penalidad via catalogo) para no ser un falso-verde por coincidencia de banda; test 3 usa niveles A1/B2 deliberadamente distintos entre la clave literal y la minuscula para probar que "el literal manda" y no solo que ambas resuelven igual.
2026-07-22 — analyst — agregada R045 (major, AUTO_VALIDATED) al Grupo D: repliegue del emparejamiento por lema del token normalizado a minusculas. Cuando el lema literal no existe en el catalogo (tras agotar los 2 pasos de R036), se reintenta con el lema en minusculas antes de declarar fuera-de-catalogo. Solo-repliegue (literal manda), nunca inventa nivel (si no existe en ninguna forma -> R034), no rescata nombres propios (sophie no esta en catalogo). Evidencia audit 2026-07-22T04-50-18: Sheep x2 + Clean x2 rescatables (A1), Sophie x2 correctamente NO rescatada. Cross-refs: header Grupo D, R036 (paso fuera-de-catalogo), R034 (condicion de entrada). Validacion [OK]. Journeys NO tocados. NEXT: @qa-tester disena handwrittenTest dedicado para R045 (superficie del emparejamiento token->catalogo; verificar donde vive la normalizacion — analyzer o catalog); luego @developer implementa el lowercase-fallback.
  why: cada comportamiento en regla propia y testeable (no cobertura transitiva por J006); el fix es del lado del TOKEN (lema con caja de inicio de oracion), distinto de R040/R041 que son del lado de las entradas del catalogo.
2026-07-21 — developer — implementado el refinamiento de R040 en FileSystemEvpCatalog.loadCatalog (vocabulary-infrastructure): `derivedForm` ya no usa `!word.equals(lemma)` literal; ahora delega en `isInflectionalVariant(word, lemma)` (nuevo helper privado), que compara case-insensitive y tolera que `word` sea `lemma+"s"` o `lemma+"es"` (nunca al reves). `derivedForm = !phrasal && !evpPhraseCategory && word != null && !isInflectionalVariant(word, lemma)`. FileSystemEvpCatalogTest: 23/23 verdes (21 preexistentes + 2 nuevos de variantes ya escritos por test-writer: clothes/clothe plural y DVD/dvd caja). Suite ampliada `audit-domain,refiner-domain,revision-domain,audit-application` (`-Dexec.skip=true -DskipPitest=true`): 97/97 verdes, BUILD SUCCESS. Sin trabajo pendiente para esta iteracion de R040; feature lista para revision/commit del usuario.
  why: el criterio refinado solo necesitaba cambiar el discriminante de "derivedForm" (de comparacion literal a tolerancia flexiva/caja); el resto del pipeline (post-pasada de techos R042/R043, puente R044) es agnostico a como se calcula ese flag.
2026-07-21 — test-writer — implementados los 2 handwrittenTests stubs finales de R040 refinada en FileSystemEvpCatalogTest (vocabulary-infrastructure): (a) plural regular "clothes"/lemma "clothe" A1 NOUN -> lookupLevel + lookupLevelByLemma ambos deben resolver A1; (b) variante de caja "DVD"/lemma "dvd" A1 NOUN -> lookupLevel debe resolver A1. Compila limpio (mvn test-compile -am). mvn test FileSystemEvpCatalogTest: 23 tests, 2 failures por asercion (esperado en TDD: la produccion (`derivedForm` en FileSystemEvpCatalog) aun compara word!=lemma de forma literal case-sensitive, no tolera plural regular ni caja). Los 21 tests preexistentes (incluidos los 3 R040 de derivadas genuinas next-door/working/working-class) siguen verdes, sin tocar. NEXT: @developer ajusta `derivedForm` en FileSystemEvpCatalog.loadCatalog para el criterio flexivo/caja (case-insensitive == lema, o difiere solo por sufijo de plural regular "-s"/"-es").
  why: cada cara de la regla refinada (excluir derivadas genuinas vs re-admitir variantes flexivas/caja) necesita su propio test explicito; los 3 tests de derivadas genuinas no cubren la cara re-admitida.
2026-07-21 — qa-tester — R040 refinada: propuestos 2 handwrittenTests nuevos sobre FileSystemEvpCatalog (vocabulary-infrastructure/evp) para el criterio flexivo/caja: (a) variante plural regular clothes/clothe A1 -> lookupLevel(clothe,NOUN)=A1; (b) variante de caja DVD/dvd A1 -> lookupLevel(dvd,NOUN)=A1. Patch handwrittenTests-only, requirement-scoped, validado OK (1 modification, 0 conflicts), PENDIENTE apply + sentinel generate. Los 3 tests R040 existentes (fixtures next-door/working/working-class) siguen VALIDOS sin tocar: son derivadas genuinas, siguen excluidas. Guard (c) working/work NO necesita test nuevo: ya cubierto por el test existente 'return empty from exact lookupLevel when the only entry for the pair is a derived form whose headword differs from the lemma'.
  why: la refinacion solo AGREGA la cara re-admitida (variantes flexivas/caja); la cara excluyente (derivadas genuinas) ya estaba cubierta.
2026-07-21 — analyst — refinada R040 en REQUIREMENT.md: criterio literal -> tolerante a variantes flexivas/ortograficas (case-insensitive == lema, o difiere solo por plural regular "-s"/"-es"). Re-admite clothes/clothe, maths/math, DVD/dvd, etc.; sigue excluyendo working/next-door/planning/open-minded. Evidencia de regresion (52 ocurrencias fuera-de-catalogo) registrada en el cuerpo de la regla. Cross-refs ajustadas en R005 y R036. Validacion [OK]. Journeys NO tocados. NEXT: @developer cambia la comparacion literal de `derivedForm` en FileSystemEvpCatalog por el criterio flexivo/caja; @qa-tester revisa cobertura de tests de R040 (fixture clothes/clothe re-admitida vs working/work excluida).
  why: la comparacion literal barria variantes flexivas que no son formas derivadas; proposito de R040 intacto, solo se afina el discriminante.
2026-07-21 — developer — implementadas R042/R043/R044 en FileSystemEvpCatalog.loadCatalog (vocabulary-infrastructure), via una post-pasada `applyCeilingsAndBridge()` al final de loadCatalog (independiente del orden de filas del JSON). R042: las entradas phrasal (multipalabra) ya NO se excluyen del todo; ahora aportan un techo asimetrico (`phraseCeilingByPos`/`phraseCeilingByLemma`, merge-min) que se aplica con `computeIfPresent` sobre `levelByLemma`/`lowestLevelByLemmaOnly` -- nunca inventa (si el lema no tiene entrada de palabra alcanzable, computeIfPresent no hace nada). Las entradas monopalabra categoria "phrase"/"phrasal verb" (ej. "let's") NO se tocaron: siguen su camino normal de entrada de palabra ya existente desde R040/R041 (el merge-min de claves duplicadas ya les daba el comportamiento correcto, confirmado por el hallazgo de test-writer en fix-log; agregarlas al mecanismo de techo habria roto el test baseline de R041 "let's" solo, que depende de que esa entrada SI fije nivel cuando es la unica). R043: nuevo `nonContentCeilingByLemma` (merge-min por lema para entradas cuyo posTag resuelto esta en NON_CONTENT_POS={DET,PRON,ADP,CCONJ,INTJ,NUM,AUX}); en la post-pasada rebaja, via computeIfPresent, todas las categorias de contenido ya registradas para el mismo lema (`posTagsByLemma`). El fallback por lema no necesito cambios: ya agregaba estas entradas al merge-min existente de `lowestLevelByLemmaOnly`. R044: bucle final sobre lemas con entradas registradas que, si tiene AMBAS claves (lema,NOUN) y (lema,ADV) en `levelByLemma` (ya rebajadas por R042/R043), las iguala al minimo de las dos; no toca ningun otro par. Resultado: `FileSystemEvpCatalogTest` 21/21 verdes (12 baseline + 9 nuevos, sin retocar tests). Suite ampliada `audit-domain,refiner-domain,revision-domain,audit-application` (`-Dexec.skip=true -DskipPitest=true`): 97/97 verdes, BUILD SUCCESS (los stack traces de JaCoCo "Unsupported class file major version 68" son el problema JDK25-vs-JaCoCo preexistente y documentado, no fallas de test). NEXT: sin trabajo pendiente para esta iteracion de R042/R043/R044; feature lista para revision/commit del usuario.
  why: separar el mecanismo en techo-por-pos (R042 exacto) + techo-por-lema (R042 fallback + R043 exacto, ya cubierto naturalmente para el fallback) + puente NOUN-ADV (R044) evita depender del orden del JSON y preserva exactamente el comportamiento ya-verde de R042-3 ("let"/"let's") documentado por test-writer como hallazgo, no regresion.
2026-07-21 — test-writer — implementados los 9 handwrittenTests stubs de R042 (4), R043 (2), R044 (3) en FileSystemEvpCatalogTest (vocabulary-infrastructure). Compila limpio (mvn test-compile -am). mvn test: 21 tests, 5 failures por asercion (esperado en TDD, produccion aun no implementa R042/R043/R044): las 2 de R042 con B1->A1 (match exacto y fallback por lema, fixture "live"), la de R043 rebaja (fixture "much"), y las 2 de R044 puente NOUN-ADV (fixture "tonight"/"home"). Los 4 restantes (R042-3 "let"/"let's", R042-4 "give" never-invent, R043-2 "snow" guard, R044-3 "late" guard) pasan ya hoy: los 3 guard-tests pasan porque describen el comportamiento SIN cambios (nada que rebajar); R042-3 pasa por una razon distinta (ver fix-log): la entrada monopalabra "phrase" ya se resuelve hoy via el fallback de R041 a spacyPosTag, y el merge de claves (lema,pos) duplicadas ya toma el minimo (verificado empiricamente invirtiendo el orden de insercion, resultado estable) -- no es un bug de fixture, es hallazgo reportado al usuario. Los 12 tests baseline preexistentes siguen verdes (12+9=21, 16 pass/5 fail). NEXT: @developer implementa R042/R043/R044 en FileSystemEvpCatalog; el desarrollador puede confirmar si el mecanismo de merge min() que ya cubre R042-3 es reutilizable para las demas rebajas (R042-1/2, R043, R044).
  why: cada regla necesita verificacion explicita propia; se documenta el hallazgo del test ya-verde para que developer no lo interprete como regresion cuando implemente R042.
2026-07-21 — analyst — iteracion 2 escrita en REQUIREMENT.md: R042 (frases solo bajan), R043 (categorias no-de-contenido solo bajan), R044 (puente NOUN-ADV, ASSUMPTION); cross-refs en header Grupo A (R039-R044), R005, R017, R036, R041; DOUBT-PHRASE-MONOPALABRA -> RESOLVED; DOUBT-NOUN-ADV-BRIDGE nueva (OPEN). Validacion [OK]. NEXT: usuario decide DOUBT-NOUN-ADV-BRIDGE (opcion A propuesta). Luego @qa-tester disena handwrittenTests dedicados para R042/R043/R044 (patron R040/R041, superficie FileSystemEvpCatalog en vocabulary-infrastructure) -> nueva ronda patch + sentinel generate.
  why: R042/R043 firmes; R044 pendiente de confirmacion; cada regla necesita test explicito propio (no cobertura transitiva por J006).
2026-07-21 — developer — implementadas R040 y R041 en FileSystemEvpCatalog.loadCatalog (vocabulary-infrastructure). R041: nueva `resolveUniversalPos(evpPartOfSpeech, spacyPosTag)` traduce la categoria EVP declarada al tagset universal (noun/verb/adjective/adverb/preposition/pronoun/determiner/conjunction/exclamation/number/modal verb -> NOUN/VERB/ADJ/ADV/ADP/PRON/DET/CCONJ/INTJ/NUM/AUX); vacio o "phrase"/"phrasal verb" -> fallback a spacyPosTag (comportamiento actual). R040: nuevo flag `derivedForm` (word!=lemma literal, case-sensitive, y no phrasal) excluye la entrada de `expectedByLevel`, `levelByLemma` y `lowestLevelByLemmaOnly`; sigue alimentando `cocaRanks`/`semanticCategories` igual que las phrasal. 12/12 tests de FileSystemEvpCatalogTest verdes. Suite completa: verde hasta revision-domain inclusive (course-domain, audit-domain 351, refiner-domain, course-infrastructure, nlp-infrastructure, vocabulary-infrastructure 16, revision-domain 191); audit-infrastructure/revision-infrastructure/audit-application/audit-cli no corren por un error de COMPILACION preexistente y AJENO en `FileSystemAuditReportStoreTest` (constructor de `LemmaPlacementDiagnosis` no coincide, 2 argumentos vs 4) — no tocado, fuera de mi modulo y fuera del alcance de R040/R041.
  why: la firma de LemmaPlacementDiagnosis pertenece a otra area de la feature (Grupo D, R017-R020/R033-R039) y no fue modificada por mi; corresponde a analyst/architect/test-writer resolver ese mismatch, no al developer de vocabulary-infrastructure.
2026-07-21 — test-writer — implementados los 6 handwrittenTests stubs de R040 (3) y R041 (3) en FileSystemEvpCatalogTest (vocabulary-infrastructure). Agregado overload entry(word,lemma,cefrLevel,evpPartOfSpeech,spacyPos,frequencyRank) sin tocar el entry() existente. Compila limpio (mvn test-compile). mvn test: 12 tests, 4 failures — las 3 de R040 + la primera de R041 (match EVP declarado) fallan por assertion (produccion aun no implementa R040/R041, esperado en TDD); las otras 2 de R041 (fallback a tag calculado sin categoria EVP, y carve-out "phrase" monopalabra) pasan porque la regla las declara explicitamente "comportamiento actual, sin cambios". Los 6 tests baseline preexistentes siguen verdes. NEXT: @developer implementa R040/R041 en FileSystemEvpCatalog.
  why: 2 de los 6 nuevos tests verifican casos que R041 marca como sin-cambio; verde ya en este punto es la conducta correcta, no un bug de fixture.
2026-07-21 — analyst — agregadas R040 y R041 al REQUIREMENT.md (Grupo A, tras R005) + Doubt[DOUBT-PHRASE-MONOPALABRA]; cross-refs en R005/R017/R036; header Grupo A -> R039-R041. Validacion [OK]. Journeys NO tocados. NEXT: @qa-tester debe disenar handwrittenTests dedicados para R040 y R041 (patron R033-R039); requiere nueva ronda de patch + sentinel generate.
  why: cada regla necesita test explicito propio; no forzar traceabilidad en J006.
2026-07-16 — qa-tester — propuesto patch de test design C1/C2+fuera-de-catalogo: 38 handwrittenTests nuevos (R017-R020, R033-R039), 4 deletes de re-baseline, placement J006 (audit-domain / auditdomain.labs). Escrito en architectural_patch.yaml, PENDIENTE de apply por el usuario + sentinel generate.
  why: R039/R034/R020 invierten afirmaciones de 4 tests existentes; el generate estaba bloqueado por J006 sin testModule
2026-05-09 — test-writer — implementados 17 path bodies en FLabsJ001–J005JourneyTest; todos pasan (BUILD SUCCESS)
  why: stubs tiraban UnsupportedOperationException bloqueando el build de audit-domain

2026-07-21 — qa-tester — propuestos 6 handwrittenTests para R040 (3) y R041 (3) sobre FileSystemEvpCatalog (vocabulary-infrastructure, package evp). Patch handwrittenTests-only, requirement-scoped, validado OK (1 modification). Escrito en architectural_patch.yaml, PENDIENTE apply + sentinel generate. Fixtures de R041 requeriran variante entry() con evpPartOfSpeech no-vacio.
  why: R040/R041 estaban [uncovered]; cada regla necesita test explicito propio.
2026-07-21 — qa-tester — propuestos 9 handwrittenTests para R042 (4), R043 (2), R044 (3) sobre FileSystemEvpCatalog (vocabulary-infrastructure, package evp). R042: rebaja en lookupLevel exacto + rebaja en lookupLevelByLemma (live A1-frase vs B1-verbo), monopalabra "phrase" (let's A2 -> let A2), never-invent (frase sola -> ambas vacias). R043: rebaja pos-contenido por entrada no-de-contenido (much DET A1 rebaja ADV B1 -> A1) + guard sin entrada no-de-contenido (snow VERB A2 intacto). R044: puente NOUN->min (tonight A1), puente ADV->min (home A1), guard ADJ-ADV no puentea (late ADV A2 intacto). Patch handwrittenTests-only, requirement-scoped, validado OK (1 modification, 0 conflicts). Escrito en architectural_patch.yaml, PENDIENTE apply por el usuario + sentinel generate. Journeys NO tocados (superficie ya definida, sin placement nuevo).
  why: R042/R043/R044 estaban [uncovered]; cada regla necesita test explicito propio; fixtures reutilizan overload entry(word,lemma,cefr,evpPos,spacyPos,rank) ya introducido para R041.

2026-07-22 — qa-tester — propuestos 3 handwrittenTests F-LABS-R045 sobre LemmaByLevelAbsenceAnalyzer (audit-domain, pkg labs). Superficie = analyzer (lado emparejamiento token->catalogo), consistente con la R036 "should treat word as out of catalog only when the lemma has no entry under any pos" que tambien vive ahi; FileSystemEvpCatalog solo tiene tests de query (lookupLevelByLemma), no la decision de penalizacion. Tests: (1) Sheep->sheep A1 no penaliza OOC; (2) ni literal ni minuscula (Sophie) -> queda OOC; (3) guard literal manda, sin normalizacion. Patch propuesto (no aplicado) en architectural_patch.yaml. NEXT: user aplica; luego @test-writer implementa.
  why: R045 habla del emparejamiento (lado token), no de las entradas del catalogo.
