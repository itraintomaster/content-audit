package com.learney.contentaudit.auditcli;

import com.learney.contentaudit.auditapplication.AnalyzerRegistry;
import com.learney.contentaudit.auditapplication.AuditRunner;
import com.learney.contentaudit.auditdomain.AuditReport;
import com.learney.contentaudit.auditdomain.NodeScores;
import java.lang.RuntimeException;
import java.util.List;
import java.util.Map;
import javax.annotation.processing.Generated;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.mockito.junit.jupiter.MockitoExtension;

@Generated(
    value = "com.sentinel.SentinelEngine",
    comments = "Generated by Sentinel"
)
@ExtendWith(MockitoExtension.class)
public class DefaultAuditCliSentinelTest {
  @Mock
  private AuditRunner auditRunner;

  @Mock
  private FormatterRegistry formatterRegistry;

  @Mock
  private ReportViewModelTransformer viewModelTransformer;

  @Mock
  private RawReportFormatter rawReportFormatter;

  @Mock
  private AnalyzerRegistry analyzerRegistry;

  @Mock
  private AnalyzerStatsTransformer analyzerStatsTransformer;

  @InjectMocks
  private DefaultAuditCli sut;

  @Test
  @DisplayName("Given valid args with course path, when run is called, then returns exit code 0")
  @Tag("F-CLI")
  public void givenValidArgsWithCoursePathWhenRunIsCalledThenReturnsExitCode0() {
    NodeScores emptyScores = new NodeScores(Map.of());
    AuditReport report = new AuditReport(0.85, emptyScores, List.of());
    ReportFormatter formatter = Mockito.mock(ReportFormatter.class);
    Mockito.when(auditRunner.runAudit(Mockito.any())).thenReturn(report);
    Mockito.when(formatterRegistry.getFormatter(Mockito.eq("text"))).thenReturn(formatter);
    int result = sut.run("/path/to/course");
    Assertions.assertEquals(0, result);
  }

  @Test
  @DisplayName("Given no args provided, when run is called, then returns non-zero exit code")
  @Tag("F-CLI")
  public void givenNoArgsProvidedWhenRunIsCalledThenReturnsNonzeroExitCode() {
    int result = sut.run();
    Assertions.assertEquals(1, result);
  }

  @Test
  @DisplayName("Given auditRunner throws RuntimeException, when run is called, then returns non-zero exit code")
  @Tag("F-CLI")
  public void givenAuditRunnerThrowsRuntimeExceptionWhenRunIsCalledThenReturnsNonzeroExitCode() {
    ReportFormatter formatter = Mockito.mock(ReportFormatter.class);
    Mockito.lenient().when(auditRunner.runAudit(Mockito.any())).thenThrow(new RuntimeException("Failed to load course"));
    Mockito.lenient().when(formatterRegistry.getFormatter(Mockito.eq("text"))).thenReturn(formatter);
    int result = sut.run("/path/to/course");
    Assertions.assertEquals(1, result);
  }

  @Test
  @DisplayName("Given valid args with --format json, when run is called, then json formatter is looked up and returns 0")
  @Tag("F-CLI")
  public void givenValidArgsWithFormatJsonWhenRunIsCalledThenJsonFormatterIsLookedUpAndReturns0() {
    NodeScores emptyScores = new NodeScores(Map.of());
    AuditReport report = new AuditReport(0.75, emptyScores, List.of());
    ReportFormatter jsonFormatter = Mockito.mock(ReportFormatter.class);
    Mockito.when(auditRunner.runAudit(Mockito.any())).thenReturn(report);
    Mockito.when(formatterRegistry.getFormatter(Mockito.eq("json"))).thenReturn(jsonFormatter);
    int result = sut.run("/path/to/course", "--format", "json");
    Assertions.assertEquals(0, result);
    Mockito.verify(formatterRegistry, Mockito.times(1)).getFormatter(Mockito.any());
  }

  @Test
  @DisplayName("Given valid args without --format, when run is called, then text formatter is used by default and returns 0")
  @Tag("F-CLI")
  public void givenValidArgsWithoutFormatWhenRunIsCalledThenTextFormatterIsUsedByDefaultAndReturns0(
      ) {
    NodeScores emptyScores = new NodeScores(Map.of());
    AuditReport report = new AuditReport(0.9, emptyScores, List.of());
    ReportFormatter textFormatter = Mockito.mock(ReportFormatter.class);
    Mockito.when(auditRunner.runAudit(Mockito.any())).thenReturn(report);
    Mockito.when(formatterRegistry.getFormatter(Mockito.eq("text"))).thenReturn(textFormatter);
    int result = sut.run("/path/to/course");
    Assertions.assertEquals(0, result);
    Mockito.verify(formatterRegistry, Mockito.times(1)).getFormatter(Mockito.any());
  }

  @Test
  @DisplayName("Given valid args, when run is called, then auditRunner runAudit is invoked with course path")
  @Tag("F-CLI")
  public void givenValidArgsWhenRunIsCalledThenAuditRunnerRunAuditIsInvokedWithCoursePath() {
    NodeScores emptyScores = new NodeScores(Map.of());
    AuditReport report = new AuditReport(0.5, emptyScores, List.of());
    ReportFormatter formatter = Mockito.mock(ReportFormatter.class);
    Mockito.when(auditRunner.runAudit(Mockito.any())).thenReturn(report);
    Mockito.when(formatterRegistry.getFormatter(Mockito.eq("text"))).thenReturn(formatter);
    int result = sut.run("/path/to/course");
    Assertions.assertEquals(0, result);
    Mockito.verify(auditRunner, Mockito.times(1)).runAudit(Mockito.any());
  }

  @Test
  @DisplayName("Given an unsupported format value, when run is called, then returns non-zero exit code")
  @Tag("F-CLI")
  public void givenAnUnsupportedFormatValueWhenRunIsCalledThenReturnsNonzeroExitCode() {
    NodeScores emptyScores = new NodeScores(Map.of());
    AuditReport report = new AuditReport(0.7, emptyScores, List.of());
    Mockito.when(auditRunner.runAudit(Mockito.any())).thenReturn(report);
    Mockito.when(formatterRegistry.getFormatter(Mockito.eq("xml"))).thenReturn(null);
    int result = sut.run("/path/to/course", "--format", "xml");
    Assertions.assertEquals(1, result);
  }

  @Test
  @DisplayName("Given valid args and low audit scores, when run is called, then returns 0 regardless of score values")
  @Tag("F-CLI")
  public void givenValidArgsAndLowAuditScoresWhenRunIsCalledThenReturns0RegardlessOfScoreValues() {
    NodeScores emptyScores = new NodeScores(Map.of());
    AuditReport lowScoreReport = new AuditReport(0.1, emptyScores, List.of());
    ReportFormatter formatter = Mockito.mock(ReportFormatter.class);
    Mockito.when(auditRunner.runAudit(Mockito.any())).thenReturn(lowScoreReport);
    Mockito.when(formatterRegistry.getFormatter(Mockito.eq("text"))).thenReturn(formatter);
    int result = sut.run("/path/to/course");
    Assertions.assertEquals(0, result);
  }
}
