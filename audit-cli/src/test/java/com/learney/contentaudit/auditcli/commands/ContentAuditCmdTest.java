package com.learney.contentaudit.auditcli.commands;

import java.io.ByteArrayOutputStream;
import java.io.PrintStream;
import java.io.PrintWriter;
import javax.annotation.processing.Generated;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import picocli.CommandLine;

import static org.junit.jupiter.api.Assertions.assertNotEquals;
import static org.junit.jupiter.api.Assertions.assertTrue;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
public class ContentAuditCmdTest {

    /**
     * Builds a CommandLine with only ContentAuditCmd at the root — no 'refiner' or 'analyzer'
     * subcommands registered. Captures the error stream so assertions can inspect the message.
     */
    private static CommandLine buildBareCommandLine(ByteArrayOutputStream errCapture) {
        CommandLine cmd = new CommandLine(new ContentAuditCmd());
        cmd.setErr(new PrintWriter(errCapture, true));
        return cmd;
    }

    @Test
    @DisplayName("should fail with picocli 'unknown subcommand' when invoked with the removed 'refiner' subcommand")
    @Tag("FEAT-CLIRV")
    @Tag("F-CLIRV-R020")
    public void shouldFailWithPicocliUnknownSubcommandWhenInvokedWithTheRemovedRefinerSubcommand() {
        // Arrange — bare CommandLine with no 'refiner' subcommand registered (R020: no shims)
        ByteArrayOutputStream errCapture = new ByteArrayOutputStream();
        CommandLine commandLine = buildBareCommandLine(errCapture);

        // Act — invoke 'refiner plan' as an operator who has not yet learned the new grammar
        int exitCode = commandLine.execute("refiner", "plan");

        // Assert — R020: the CLI parser must reject 'refiner' with a non-zero exit code
        assertNotEquals(0, exitCode,
                "Invoking the removed 'refiner' subcommand must produce a non-zero exit code");

        String errOutput = errCapture.toString();
        assertTrue(
                errOutput.contains("Unmatched") || errOutput.contains("Unknown") || errOutput.contains("refiner"),
                "Error output must mention the unrecognized argument; got: " + errOutput);
    }

    @Test
    @DisplayName("should fail with picocli 'unknown subcommand' when invoked with the removed 'analyzer' subcommand")
    @Tag("FEAT-CLIRV")
    @Tag("F-CLIRV-R020")
    public void shouldFailWithPicocliUnknownSubcommandWhenInvokedWithTheRemovedAnalyzerSubcommand(
            ) {
        // Arrange — bare CommandLine with no 'analyzer' subcommand registered (R020: no shims)
        ByteArrayOutputStream errCapture = new ByteArrayOutputStream();
        CommandLine commandLine = buildBareCommandLine(errCapture);

        // Act — invoke 'analyzer list' as an operator using the old grammar
        int exitCode = commandLine.execute("analyzer", "list");

        // Assert — R020: the CLI parser must reject 'analyzer' with a non-zero exit code
        assertNotEquals(0, exitCode,
                "Invoking the removed 'analyzer' subcommand must produce a non-zero exit code");

        String errOutput = errCapture.toString();
        assertTrue(
                errOutput.contains("Unmatched") || errOutput.contains("Unknown") || errOutput.contains("analyzer"),
                "Error output must mention the unrecognized argument; got: " + errOutput);
    }
}
