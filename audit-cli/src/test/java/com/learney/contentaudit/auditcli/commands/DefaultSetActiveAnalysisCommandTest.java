package com.learney.contentaudit.auditcli.commands;

import com.learney.contentaudit.auditdomain.ActiveAnalysisSelection;
import com.learney.contentaudit.auditdomain.ActiveAnalysisSelectionStore;
import javax.annotation.processing.Generated;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.ArgumentCaptor;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.never;
import static org.mockito.Mockito.verify;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
@ExtendWith(MockitoExtension.class)
public class DefaultSetActiveAnalysisCommandTest {

    @Mock
    private ActiveAnalysisSelectionStore activeAnalysisSelectionStore;

    @Test
    @DisplayName("Dada una invocacion setActive(auditId, planId) con valores no nulos, cuando setActive corre, entonces ActiveAnalysisSelectionStore.write se invoca con un ActiveAnalysisSelection que tiene esos auditId y planId")
    @Tag("FEAT-CDIFF")
    @Tag("F-CDIFF-R002")
    public void dadaUnaInvocacionSetActiveauditIdPlanIdConValoresNoNulosCuandoSetActiveCorreEntoncesActiveAnalysisSelectionStorewriteSeInvocaConUnActiveAnalysisSelectionQueTieneEsosAuditIdYPlanId() {
        DefaultSetActiveAnalysisCommand command = new DefaultSetActiveAnalysisCommand(activeAnalysisSelectionStore);

        command.setActive("audit-001", "plan-001");

        ArgumentCaptor<ActiveAnalysisSelection> captor = ArgumentCaptor.forClass(ActiveAnalysisSelection.class);
        verify(activeAnalysisSelectionStore).write(captor.capture());
        assertEquals("audit-001", captor.getValue().getAuditId());
        assertEquals("plan-001", captor.getValue().getPlanId());
        verify(activeAnalysisSelectionStore, never()).clear();
    }

    @Test
    @DisplayName("Dada una invocacion setActive(null, null), cuando setActive corre, entonces ActiveAnalysisSelectionStore.clear se invoca y write nunca se invoca")
    @Tag("FEAT-CDIFF")
    @Tag("F-CDIFF-R002")
    public void dadaUnaInvocacionSetActivenullNullCuandoSetActiveCorreEntoncesActiveAnalysisSelectionStoreclearSeInvocaYWriteNuncaSeInvoca() {
        DefaultSetActiveAnalysisCommand command = new DefaultSetActiveAnalysisCommand(activeAnalysisSelectionStore);

        command.setActive(null, null);

        verify(activeAnalysisSelectionStore).clear();
        verify(activeAnalysisSelectionStore, never()).write(org.mockito.ArgumentMatchers.any());
    }

    @Test
    @DisplayName("Dada cualquier invocacion de setActive (con o sin valores), entonces no se invoca save sobre AuditReportStore, RefinementPlanStore, RevisionArtifactStore ni sobre el repositorio del curso")
    @Tag("FEAT-CDIFF")
    @Tag("F-CDIFF-R002")
    public void dadaCualquierInvocacionDeSetActiveConOSinValoresEntoncesNoSeInvocaSaveSobreAuditReportStoreRefinementPlanStoreRevisionArtifactStoreNiSobreElRepositorioDelCurso() {
        // DefaultSetActiveAnalysisCommand only holds ActiveAnalysisSelectionStore — no AuditReportStore,
        // RefinementPlanStore, RevisionArtifactStore, or CourseRepository is injected. The observable
        // contract: setActive with any arguments only touches the activeAnalysisSelectionStore (write or clear)
        // and no other system artifact.
        DefaultSetActiveAnalysisCommand command = new DefaultSetActiveAnalysisCommand(activeAnalysisSelectionStore);

        command.setActive("audit-abc", "plan-xyz");

        // Only activeAnalysisSelectionStore.write was called — no other store interaction possible
        // given the declared constructor. This verifies F-CDIFF-R002 non-destructiveness at wiring level.
        verify(activeAnalysisSelectionStore).write(org.mockito.ArgumentMatchers.any());
    }
}
