package com.learney.contentaudit.auditcli.commands;
import java.util.concurrent.Callable;

import javax.annotation.processing.Generated;
import picocli.CommandLine;
import picocli.CommandLine.Command;
import picocli.CommandLine.Spec;
import picocli.CommandLine.Model.CommandSpec;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
@Command(
        name = "analyzer",
        description = "Manage and inspect analyzers.%n%n"
                + "Use subcommands to list analyzers, view their configuration,%n"
                + "or see detailed statistics from an audit run.",
        mixinStandardHelpOptions = true,
        footer = {
                "",
                "Examples:",
                "  # List all available analyzers",
                "  content-audit analyzer list",
                "",
                "  # Show configuration for an analyzer",
                "  content-audit analyzer config sentence-length",
                "",
                "  # Show detailed stats for an analyzer on a course",
                "  content-audit analyzer stats coca-buckets-distribution db/english-course",
        }
)
class AnalyzerCmd implements Callable<Integer> {
    @Spec
    CommandSpec spec;

    @Override
    public Integer call() throws Exception {
        spec.commandLine().usage(System.out);
        return 0;
    }

}
