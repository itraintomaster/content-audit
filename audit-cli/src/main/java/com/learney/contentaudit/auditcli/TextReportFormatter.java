package com.learney.contentaudit.auditcli;

import com.learney.contentaudit.auditdomain.AuditReport;
import com.learney.contentaudit.auditdomain.MilestoneNode;
import java.util.List;
import javax.annotation.processing.Generated;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
public class TextReportFormatter implements ReportFormatter {
    @Override
    public String format(AuditReport report) {
        StringBuilder sb = new StringBuilder();
        sb.append("=== Audit Report ===\n");
        sb.append(String.format("Overall Score: %.2f%%\n", report.getOverallScore() * 100));
        sb.append("\n--- Milestones ---\n");

        List<MilestoneNode> milestones = report.getMilestones();
        if (milestones == null || milestones.isEmpty()) {
            sb.append("No milestones found.\n");
        } else {
            for (int i = 0; i < milestones.size(); i++) {
                MilestoneNode m = milestones.get(i);
                String id = m.getMilestoneId() != null ? m.getMilestoneId() : String.valueOf(i);
                sb.append(String.format("  Milestone [%s]: ", id));
                if (m.getScores() != null && m.getScores().getScores() != null) {
                    sb.append(m.getScores().getScores().toString());
                } else {
                    sb.append("no scores");
                }
                sb.append("\n");
            }
        }
        return sb.toString();
    }
}
