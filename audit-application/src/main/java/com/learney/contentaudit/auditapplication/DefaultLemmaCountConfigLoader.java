package com.learney.contentaudit.auditapplication;

import com.learney.contentaudit.auditdomain.LemmaCountConfig;
import javax.annotation.processing.Generated;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
public class DefaultLemmaCountConfigLoader implements LemmaCountConfigLoader {
    @Override
    public LemmaCountConfig load(String rawThreshold) {
        if (rawThreshold == null) {
            return new DefaultLemmaCountConfig(4);
        }
        String trimmed = rawThreshold.trim();
        if (trimmed.isEmpty()) {
            throw new IllegalArgumentException("El umbral de lemma-count no puede ser una cadena vacía");
        }
        if (trimmed.contains(".")) {
            throw new IllegalArgumentException(
                    "El umbral de lemma-count debe ser un entero, pero se recibió un decimal: '" + rawThreshold + "'");
        }
        int parsed;
        try {
            parsed = Integer.parseInt(trimmed);
        } catch (NumberFormatException e) {
            throw new IllegalArgumentException(
                    "El umbral de lemma-count no es un entero válido: '" + rawThreshold + "'", e);
        }
        if (parsed <= 0) {
            throw new IllegalArgumentException(
                    "El umbral de lemma-count debe ser mayor que 0, pero se recibió: " + parsed);
        }
        return new DefaultLemmaCountConfig(parsed);
    }
}
