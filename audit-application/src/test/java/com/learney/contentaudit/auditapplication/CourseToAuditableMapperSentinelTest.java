package com.learney.contentaudit.auditapplication;

import com.learney.contentaudit.auditdomain.NlpToken;
import com.learney.contentaudit.auditdomain.NlpTokenizer;
import com.learney.contentaudit.coursedomain.CourseEntity;
import com.learney.contentaudit.coursedomain.NodeKind;
import com.learney.contentaudit.coursedomain.RootNodeEntity;
import java.lang.RuntimeException;
import java.lang.String;
import java.util.HashMap;
import java.util.List;
import java.util.Map;
import javax.annotation.processing.Generated;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.mockito.junit.jupiter.MockitoExtension;

@Generated(
    value = "com.sentinel.SentinelEngine",
    comments = "Generated by Sentinel"
)
@ExtendWith(MockitoExtension.class)
public class CourseToAuditableMapperSentinelTest {
  @Mock
  private NlpTokenizer nlpTokenizer;

  @InjectMocks
  private CourseToAuditableMapper sut;

  @Test
  @DisplayName("Given a course with quizzes, when map is called, then analyzeTokensBatch is invoked and returns an AuditableCourse")
  @Tag("F-NLP")
  public void givenACourseWithQuizzesWhenMapIsCalledThenAnalyzeTokensBatchIsInvokedAndReturnsAnAuditableCourse(
      ) {
    CourseEntity course = new CourseEntity("507f1f77bcf86cd799439011", "English Course", List.of("k1"), null, "english-course");
    Map<String, List<NlpToken>> batchResult = new HashMap<>();
    Mockito.when(nlpTokenizer.analyzeTokensBatch(Mockito.any())).thenReturn(batchResult);
    sut.map(course);
    Mockito.verify(nlpTokenizer, Mockito.times(1)).analyzeTokensBatch(Mockito.any());
  }

  @Test
  @DisplayName("Given a course with no milestones, when map is called, then returns an AuditableCourse without error")
  @Tag("F-NLP")
  public void givenACourseWithNoMilestonesWhenMapIsCalledThenReturnsAnAuditableCourseWithoutError(
      ) {
    RootNodeEntity emptyRoot = new RootNodeEntity("507f1f77bcf86cd799439099", "root", NodeKind.ROOT, null, List.of(), List.of());
    CourseEntity emptyCourse = new CourseEntity("507f1f77bcf86cd799439012", "Empty Course", List.of(), emptyRoot, "empty-course");
    Assertions.assertDoesNotThrow(() -> sut.map(emptyCourse));
  }

  @Test
  @DisplayName("Given nlpTokenizer throws exception during batch processing, when map is called, then exception propagates")
  @Tag("F-NLP")
  public void givenNlpTokenizerThrowsExceptionDuringBatchProcessingWhenMapIsCalledThenExceptionPropagates(
      ) {
    CourseEntity course = new CourseEntity("507f1f77bcf86cd799439013", "Problem Course", List.of("k1"), null, "problem-course");
    Mockito.lenient().when(nlpTokenizer.analyzeTokensBatch(Mockito.any())).thenThrow(new RuntimeException("NLP processing failed"));
    Assertions.assertThrows(RuntimeException.class, () -> sut.map(course));
  }
}
