package com.learney.contentaudit.auditapplication;

import javax.annotation.processing.Generated;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
public class DefaultLemmaRecurrenceConfigTest {
    @Test
    @DisplayName("should expose top subExposed and overExposed values that satisfy 0 less than overExposed strictly less than subExposed and top greater than zero")
    @Tag("FEAT-LREC")
    @Tag("F-LREC-R014")
    public void shouldExposeTopSubExposedAndOverExposedValuesThatSatisfy0LessThanOverExposedStrictlyLessThanSubExposedAndTopGreaterThanZero(
            ) {
        // R014: 0 < overExposed < subExposed, top > 0
        DefaultLemmaRecurrenceConfig config = new DefaultLemmaRecurrenceConfig();

        int top = config.getTop();
        double overExposed = config.getOverExposedThreshold();
        double subExposed = config.getSubExposedThreshold();

        Assertions.assertTrue(top > 0, "R014: top must be positive, got: " + top);
        Assertions.assertTrue(overExposed > 0, "R014: overExposed must be > 0, got: " + overExposed);
        Assertions.assertTrue(overExposed < subExposed,
                "R014: overExposed (" + overExposed + ") must be strictly less than subExposed (" + subExposed + ")");
    }
}
