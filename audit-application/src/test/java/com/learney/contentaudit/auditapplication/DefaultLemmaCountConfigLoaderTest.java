package com.learney.contentaudit.auditapplication;

import static org.junit.jupiter.api.Assertions.*;

import com.learney.contentaudit.auditdomain.LemmaCountConfig;
import javax.annotation.processing.Generated;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

@Generated(
        value = "com.sentinel.SentinelEngine",
        comments = "Generated by Sentinel"
)
public class DefaultLemmaCountConfigLoaderTest {

    private DefaultLemmaCountConfigLoader loader;

    @BeforeEach
    void setUp() {
        loader = new DefaultLemmaCountConfigLoader();
    }

    @Test
    @DisplayName("should return a config with threshold 4 when raw threshold is null")
    @Tag("FEAT-LCOUNT")
    @Tag("F-LCOUNT-R007")
    public void shouldReturnAConfigWithThreshold4WhenRawThresholdIsNull() {
        // R007: campo ausente (null) → default 4, carga OK
        LemmaCountConfig config = loader.load(null);

        assertNotNull(config, "La config no debe ser null cuando el umbral no está configurado (R007)");
        assertEquals(4, config.getThreshold(),
                "El umbral por defecto debe ser 4 cuando el campo N no está configurado (R007)");
    }

    @Test
    @DisplayName("should reject zero as threshold value with IllegalArgumentException")
    @Tag("FEAT-LCOUNT")
    @Tag("F-LCOUNT-R008")
    public void shouldRejectZeroAsThresholdValueWithIllegalArgumentException() {
        // R008: N=0 no es entero positivo → falla al cargar
        assertThrows(IllegalArgumentException.class,
                () -> loader.load("0"),
                "N=0 debe provocar IllegalArgumentException al cargar la configuración (R008)");
    }

    @Test
    @DisplayName("should reject a negative integer as threshold value with IllegalArgumentException")
    @Tag("FEAT-LCOUNT")
    @Tag("F-LCOUNT-R008")
    public void shouldRejectANegativeIntegerAsThresholdValueWithIllegalArgumentException() {
        // R008: N=-3 no es entero positivo → falla al cargar
        assertThrows(IllegalArgumentException.class,
                () -> loader.load("-3"),
                "N=-3 debe provocar IllegalArgumentException al cargar la configuración (R008)");
    }

    @Test
    @DisplayName("should reject a decimal number as threshold value with IllegalArgumentException")
    @Tag("FEAT-LCOUNT")
    @Tag("F-LCOUNT-R008")
    public void shouldRejectADecimalNumberAsThresholdValueWithIllegalArgumentException() {
        // R008: N="1.5" no es parseable como entero → falla al cargar
        assertThrows(IllegalArgumentException.class,
                () -> loader.load("1.5"),
                "N='1.5' debe provocar IllegalArgumentException al cargar la configuración (R008)");
    }

    @Test
    @DisplayName("should reject a non-numeric string as threshold value with IllegalArgumentException")
    @Tag("FEAT-LCOUNT")
    @Tag("F-LCOUNT-R008")
    public void shouldRejectANonnumericStringAsThresholdValueWithIllegalArgumentException() {
        // R008: N="abc" no es parseable → falla al cargar
        assertThrows(IllegalArgumentException.class,
                () -> loader.load("abc"),
                "N='abc' debe provocar IllegalArgumentException al cargar la configuración (R008)");
    }

    @Test
    @DisplayName("should reject an empty string as threshold value with IllegalArgumentException")
    @Tag("FEAT-LCOUNT")
    @Tag("F-LCOUNT-R008")
    public void shouldRejectAnEmptyStringAsThresholdValueWithIllegalArgumentException() {
        // R008: N="" (campo presente pero vacío) → falla al cargar
        assertThrows(IllegalArgumentException.class,
                () -> loader.load(""),
                "N='' (vacío) debe provocar IllegalArgumentException al cargar la configuración (R008)");
    }
}
